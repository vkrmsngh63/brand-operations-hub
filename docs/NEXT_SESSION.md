# Next session

**Written:** 2026-05-28 (`session_2026-05-28_p46-workstream-2-session-4-captured-reviews-ui` — end-of-session handoff after **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 4 ✅ DONE-AT-CODE-LEVEL 2026-05-28 on `workflow-2-competition-scraping`** — fourth build session of the P-46 Workstream 2 implementation arc (Session 4 of 3-5 estimated per §C.2); landed the Captured Reviews UI end-to-end via a NEW `CapturedReviewsSection` + `CapturedReviewCard` + `CapturedReviewAddModal` (with a 1-5 star-rating picker widget) + per-item Analysis box below each review + Overall Reviews Analysis box at the bottom of the section (via the existing Session 3 `OverallAnalysisBox` component with `kind='overallAnalyses'` `category='reviews'`) + 4 fully-implemented route handlers behind the Workstream 1 501-stub surface (POST list + GET list at the collection path + PATCH + DELETE at the per-record path) + an architectural-move fix relocating the per-record review route from W1's deeper `urls/[urlId]/reviews/[reviewId]` path to the shallow `reviews/[reviewId]` path matching the text/[textId] / images/[imageId] / videos/[videoId] precedent for the other 3 capture types. Build commit `82d390a` — 9 files +2350/-69 on `workflow-2-competition-scraping`; NOT pushed to main — Session 4 is a build session, not a deploy session. **Closes (a.74) RECOMMENDED-NEXT = P-46 Workstream 2 Session 4.** **Opens (a.75) RECOMMENDED-NEXT = P-46 Workstream 2 Session 5** on `workflow-2-competition-scraping` — recommended scope is the URL-level structural fields (Type / Description-1 / Description-2 / Price + Scraping Status toggle + remove Sizes/Options UI per §C.2) since that's the last §C.2 sub-scope remaining for Workstream 2. After Session 5, Workstream 2 is complete and the next session can be the Workstream 2 deploy session (Phase-4 deploy under Rule 9 gate; schema-change-in-flight flag flips back to NO at that deploy).

---

## What we did this session (in plain terms)

This was the **fourth build session of Workstream 2** — the URL detail page redesign. Session 1 (2026-05-25) shipped the TipTap shared rich-text editor wrapper + the first per-item Analysis text box on Captured Text + the route-handler half for the simplest capture type, and locked the card-list layout precedent via a Rule 14f forced-picker. Session 2 (2026-05-26) applied that locked precedent to Captured Image + Captured Video. Session 3 (2026-05-27) completed the "Analysis surface" arc across all capture levels via the new URL-level + per-category Overall Analysis boxes. Today's Session 4 **landed Captured Reviews as a first-class capture type** — the last greenfield capture type that didn't yet have UI.

What landed today, in plain terms:

- **Captured Reviews now has a UI on the URL detail page.** Before today, Reviews was the only capture type without UI — Workstream 1 had landed the `CapturedReview` Prisma model + 501-stubbed routes back on 2026-05-24 but nothing rendered yet. Today's session fills that gap end-to-end: a new card-list section between the Captured Videos gallery and the page-bottom Overall Competitor Analysis box; one card per review with a star-rating display (read-only stars rendered ★ filled / ☆ unfilled) + reviewer name + review date + body + tags + a per-item Analysis editor below each card.
- **A manual-add modal lets the director enter reviews by hand on vklf.com.** Mirrors the existing Captured Text manual-add modal but with a 1-5 star-rating picker widget (5 buttons rendered as ☆ / ★ with click-to-select + "Pick 1–5" / "N of 5" inline label). Body textarea + reviewer-name text input + native HTML date input + tags input. Save button is disabled until both star rating AND body are populated. Per Q1's deferral, there's no extension Reviews capture in v1; the manual-add modal is the only entry path for reviews on vklf.com.
- **An Overall Reviews Analysis box rounds out the section.** Sits at the bottom of the new Captured Reviews section, consuming the existing Session 3 `OverallAnalysisBox` component with `category='reviews'`. This is exactly the slot Session 3 designed for — the 4th category in the per-category Overall Analysis bag (text / image / video / reviews); Session 3 reserved it; Session 4 fills it.
- **4 fully-implemented route handlers replace W1's 501 stubs.** Workstream 1 had scaffolded the CapturedReview CRUD as 501-stubbed shells; today's session fleshed them out: POST list + GET list at the collection path (`urls/[urlId]/reviews`); PATCH + DELETE at the per-record path (`reviews/[reviewId]` — see architectural-move note below). All 4 handlers live behind dependency-injection seams (the same pattern as the older Captured Text P-31 handlers), which means the per-handler validation logic gets its own node:test coverage without needing to mock Next.js.
- **An architectural-move fix relocates the per-record review route.** Workstream 1 originally scaffolded the per-record route at the nested path `urls/[urlId]/reviews/[reviewId]/route.ts`, but the precedent set by the other 3 capture types' per-record routes is the SHALLOW path (`text/[textId]` / `images/[imageId]` / `videos/[videoId]`) — review IDs are globally unique within the workflow so the deeper path adds no security and the shallower path keeps the per-record PATCH/DELETE surface uniform across all 4 capture types. Today's session deleted W1's nested stub + created the new shallow route. The W1 nested stub had no production traffic + no consumer so the move is a zero-migration-cost rename. The collection route stays at the deeper path because parent-URL ownership lookup naturally lives there (matching the other 3 capture types' collection-route precedent).
- **32 new node:test cases land** (17 for the new collection handler + 15 for the new per-record handler). All 5 verification checks GREEN at the new baselines (root tsc clean / extension tsc clean / 558 extension tests UNCHANGED / **670 server tests (+32 from baseline 638)** / **61 routes UNCHANGED** — deleted W1's nested stub + added the new shallow per-record route = net 0; the collection 501→DI-shim is a path already counted).
- **No Rule 14f sub-pickers fired during execution.** Director picked Option A (Captured Reviews UI) at the session-start scope picker between 2 §C.2-aligned candidates (Option B was URL-level structural fields, now Session 5's scope). Inside the chosen scope, every sub-decision was a default-to-recommendation skip (the architectural move from W1's nested per-record path to the shallow precedent path was a clear "most thorough/reliable" choice; star-rating widget was a direct application of the modal precedent; sort-key set was a direct application of the Captured Text sort precedent; DI-seam extraction for per-record handlers was the most-thorough choice given the trust-boundary validation).

No schema changes today (Workstream 1 already added the `CapturedReview` Prisma model + the `overallAnalyses.reviews` JSON slot; today just consumed them). No new npm dependencies (TipTap landed in Session 1; today reused it).

**Schema-change-in-flight flag STAYS YES** — carrying from Workstream 1's `prisma db push` 2026-05-24. Stays YES through Workstream 2 Session 5 + any Workstream 3 sessions until the first deploy session ships the schema-aware code to vklf.com.

**Session 4 came in cleanly within the 3-5 sessions estimate for Workstream 2.** Four consecutive in-scope Workstream 2 sessions (1, 2, 3, 4) confirm the §C.2 plan is well-specced + the three extraction Patterns (PerItemAnalysisBox + OverallAnalysisBox + per-record handler DI-seam) cover the extraction-shape spectrum cleanly. A new reusable Pattern was memorialized in the design doc — **"Per-record handler DI-seam precedent extension"** — pairs with Session 1's "PerItemAnalysisBox extraction" + Session 3's "OverallAnalysisBox extraction" Patterns to cover the full set of extraction shapes Workstream 2 has empirically discovered.

A second reusable Pattern was memorialized in CORRECTIONS_LOG §Entry 2026-05-28 — **"When a foundation workstream scaffolds a 501-stub at a divergent path, surface the divergence at next-workstream session start + execute the path move within-session"** — informational; not a slip-of-W1; captures the right resolution time (the next-workstream session consuming the stub) at zero-migration cost (the 501-stub had no production traffic).

**Workstream 2 is now ~85% complete** (Sessions 1-4 of 3-5 done). Session 5 covers the last §C.2 sub-scope: the URL-level structural fields (Type / Description-1 / Description-2 / Price + Scraping Status toggle + remove Sizes/Options UI). After Session 5, Workstream 2 is complete and the next session can be the Workstream 2 deploy session.

## What we'll do next session (in plain terms)

Next session is **P-46 Workstream 2 Session 5.** Scope is the URL-level structural fields per §C.2 — the last §C.2 sub-scope remaining for Workstream 2:

- **New URL-level structural fields.** Type / Description-1 / Description-2 / Price (4 new text fields at the top of the URL box) + Scraping Status toggle (Incomplete / Complete; bidirectional mirror of the Competition Data table's Status column) + remove Sizes/Options UI (hide-UI-keep-data per Q6). Touches the existing `urls/[urlId]` PATCH route allowlist (extend for 4 new text fields + 1 enum) + render new field group at top of URL box in `UrlDetailContent.tsx` + hide Sizes/Options UI + wire Status column bidirectional mirror. The 4 new text fields slot into the same allowlist Session 3 + Session 4 just extended; the Status column bidirectional mirror is the trickiest piece since it touches both the URL detail page (toggle render) + the Competition Data table page (Status column rendering).

Likely 6-10 new node:test cases for the chosen scope (bringing src/lib to roughly 676-680). No schema changes (Workstream 1 already covered all of P-46's schema). No new npm dependencies. No deploy this session — Workstream 2 Session 5 is the last build session of Workstream 2; after Session 5, the next session can be the Workstream 2 deploy session.

## What's still left on the total roadmap (in plain terms)

Major W#2 items as of session-end 2026-05-28:

- **P-46 Workstream 1 SCHEMA ✅ DONE-AT-CODE-LEVEL 2026-05-24.** Needs its own deploy session later (likely bundled with Workstream 2's deploy).
- **P-46 Workstream 2 (URL detail page redesign) — IN PROGRESS — ~85% complete.** Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-25 (TipTap wrapper + per-item Analysis on Captured Text + card-list precedent). Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-26 (same shape applied to Captured Image + Captured Video). Session 3 ✅ DONE-AT-CODE-LEVEL 2026-05-27 (URL-level + per-category Overall Analysis boxes completing the Analysis surface arc). Session 4 ✅ DONE-AT-CODE-LEVEL 2026-05-28 (this session; Captured Reviews UI end-to-end + W1 path-divergence fix + new handler DI-seam precedent). **Session 5 NEXT** = URL-level structural fields. After Session 5, Workstream 2 is complete and the next session can be the Workstream 2 deploy session to vklf.com.
- **P-46 Workstream 3 (Competition Data table redesign).** ~3-4 sessions. ~12 new columns + click-to-edit on every cell + resizable column widths + drag-to-reorder rows + per-column show/hide toggles + adjustable font size + horizontal checkbox bar at top + cross-device-synced user preferences via `UserTablePreferences` model from Workstream 1.
- **P-46 Workstream 4 (Comprehensive Competitor Analysis page).** ~2-3 sessions. NEW page hosting per-Project TipTap rich-text doc (one per Project) with hyperlinks back to URL detail pages + edit-mode toggle + back-button. Consumes the same `RichTextEditor` wrapper Session 1 built (using the `variant='full'` branch).
- **P-46 Workstream 5 (Extension form additions + manual Reviews entry).** ~1-2 sessions. Add Type / Description-1 / Description-2 / Price fields to the extension URL save form + vklf.com-side manual Reviews entry form. Deploy session ends this workstream + closes the P-46 arc.
- **P-47 Shadow DOM refactor (LOW; AFTER P-46).** ~2-3 sessions. Replaces the 80-event-listener band-aid from P-45 Build #2's Issue 2 fix with proper Shadow DOM isolation. LOW priority since band-aid works empirically.
- **P-26 below-fold scroll capture (LOW).** ~1-2 sessions. Current two-captures workaround works fine. May be reduced in urgency by Workstream 2's vklf.com-side image upload affordances.
- **P-27 Bug #9 (Amazon hover-preview deeper-walk) + Bug #15 (Ebay native-controls quirk) — DEFERRED LOW.** May obsolete with P-46 redesign which restructures the URL detail page surface they live in.
- **W#2 graduation** after P-46 + P-47 + P-26 ship. Then W#3-W#14 (twelve more workflows on the roadmap; none started yet).
- **Optional offline step (any time, NOT blocking P-46):** raise the Supabase Global File Size Limit to enable bucket-level 100 MB cap on `competition-scraping-videos`.

---

**For:** the next Claude Code session — **P-46 Workstream 2 Session 5** (estimated ~2-3 hours: pre-build doc reads ~30 min + session-start Rule 14f operational picker confirming the URL-level structural fields scope ~5 min + code work ~60-90 min + node:test coverage ~20 min + /scoreboard verification + end-of-session doc-batch ~30 min). Per Rule 23 Change Impact Audit: **ADDITIVE + UI-only** (extends the existing `urls/[urlId]` PATCH allowlist for 4 new text fields + 1 enum; adds a new field group at the top of the URL box; hides the Sizes/Options UI section; wires the Status column bidirectional mirror; consumes Workstream 1's existing schema columns; no schema changes; no new npm dependencies). No data risk (existing rows render with empty new-field values per §A.11 "no data backfill needed"; the Sizes/Options data stays in the database per Q6 "hide-UI-keep-data"). Zero downstream W#1 / W#3 cross-tool impact. **Schema-change-in-flight flag enters YES** (carrying from Workstream 1's `prisma db push`); **stays YES** through Workstream 2 Session 5 + any Workstream 3 sessions until the first deploy session ships the schema-aware code to vklf.com. **Rule 9 triggers planned this session: ZERO** — no schema changes; no main push; pure code session. **ONE push planned** — end-of-session doc-batch + build commit to `origin/workflow-2-competition-scraping` (no main push since this is a build session, not a deploy session).

---

## Status of today's session

**W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 4 ✅ DONE-AT-CODE-LEVEL 2026-05-28 on `workflow-2-competition-scraping`** — fourth build session of the P-46 Workstream 2 implementation arc; landed the Captured Reviews UI end-to-end via a NEW `CapturedReviewsSection` + `CapturedReviewCard` + `CapturedReviewAddModal` (with a 1-5 star-rating picker widget) + per-item Analysis box below each review + Overall Reviews Analysis box at the bottom of the section (via the existing Session 3 `OverallAnalysisBox` component with `category='reviews'`) + 4 fully-implemented route handlers behind the Workstream 1 501-stub surface + an architectural-move fix relocating the per-record review route from W1's deeper nested path to the shallow precedent path matching the other 3 capture types. Build commit `82d390a` — 9 files +2350/-69 on `workflow-2-competition-scraping`; NOT pushed to main since Session 4 is a build session.

**Session shape (build session — Captured Reviews UI + 2 handler DI-seam extractions + 1 architectural-move path-relocation):**

- Pre-build reads at session start (read `docs/COMPETITION_DATA_V2_DESIGN.md` §C.2 + §A.11 + §B 2026-05-24 + §B 2026-05-25 + §B 2026-05-26 + §B 2026-05-27 + `prisma/schema.prisma` empirical state for `CapturedReview` + W1 501-stub shape at `urls/[urlId]/reviews/route.ts` + `urls/[urlId]/reviews/[reviewId]/route.ts` + existing `url-text.ts` P-31 precedent + existing `text/[textId]/route.ts` precedent + `src/lib/shared-types/competition-scraping.ts`).
- **Rule 14f forced-picker at session start** between 2 §C.2-aligned candidates — director picked Option A (Captured Reviews UI; recommended).
- **W1 path-divergence finding caught at pre-build read step** + architectural move executed within-session (delete W1 nested per-record stub; create new shallow per-record route; collection route stays at deeper path).
- NEW `src/lib/competition-scraping/handlers/url-reviews.ts` (~295 LOC DI seam for POST + GET collection handlers following the `url-text.ts` P-31 pattern) + NEW `src/lib/competition-scraping/handlers/url-reviews.test.ts` (~408 LOC; 17 new node:test cases).
- NEW `src/lib/competition-scraping/handlers/reviews-by-id.ts` (~290 LOC DI seam for per-record PATCH + DELETE; sets a NEW PRECEDENT) + NEW `src/lib/competition-scraping/handlers/reviews-by-id.test.ts` (~350 LOC; 15 new node:test cases).
- MODIFIED `urls/[urlId]/reviews/route.ts` (W1 501-stub → thin shim ~82 LOC); NEW `reviews/[reviewId]/route.ts` (~80 LOC at SHALLOW PRECEDENT PATH); DELETED W1's `urls/[urlId]/reviews/[reviewId]/route.ts`.
- NEW `CapturedReviewAddModal.tsx` (~479 LOC; 1-5 star-rating picker widget + body + reviewer + date + tags; idempotent on clientId).
- MODIFIED `UrlDetailContent.tsx` (+398 LOC; threads CapturedReview types through; adds reviewsSlot state + parallel-fetches block now 6 reads; renders `<CapturedReviewsSection>` + NEW helpers `CapturedReviewsSection` / `CapturedReviewCard` / `StarRatingDisplay` / `CapturedReviewSortControl`).
- /scoreboard verification: all 5 checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **670 src/lib +32 from baseline 638** / **61 routes UNCHANGED**); Check 6 Playwright SKIPPED per non-deploy-session convention.
- End-of-session doc-batch covers the 8-doc bundle (ROADMAP + CHAT_REGISTRY + DOCUMENT_MANIFEST + CORRECTIONS_LOG with new §Entry 2026-05-28 + HANDOFF_PROTOCOL + CLAUDE_CODE_STARTER + this NEXT_SESSION + the new §B 2026-05-28 entry on COMPETITION_DATA_V2_DESIGN.md).
- ONE push at end-of-session to `origin/workflow-2-competition-scraping` (no main push since this is a build session; no Rule 9 trigger at the push since the file deletion of W1's nested per-record stub is a path-relocation within a feature branch + replaced by the new shallow route, NOT a Rule 9 destructive op since no production data is touched and no main branch is impacted).

**§4 Step 1c forced-picker NOT FIRED** — Session 5 task unambiguous per the §C.2 Workstream 2 plan (structural fields are the only §C.2 sub-scope left after Sessions 1-4 covered the Analysis surface arc + Reviews); the pointer file describes the scope.

**ZERO new DEFERRED items at session end (Rule 26)** — all 9 in-session tasks completed.

**ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 2 Session 4 closing §Entry** capturing the W1 path-divergence finding + the architectural-move resolution executed within this session + the new reusable Pattern memorialized: "when a foundation workstream scaffolds a 501-stub at a path that diverges from the precedent shape of the sibling capture types, surface the divergence at the next-workstream session start + execute the path move + commit + announce in the build commit; treat W1 path divergences as informational findings to fix not blockers." Pairs with the design doc §B 2026-05-28 reusable Pattern "Per-record handler DI-seam precedent extension".

**NINETEENTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → today). The 3 plain-terms sections above continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive.

---

## Branch

**`workflow-2-competition-scraping`** — entered at start of next session; P-46 Workstream 2 Session 5 build session lands here (code commits stay on workflow-2 per the established pattern; deploy to main happens later, after Session 5 closes Workstream 2 + the Workstream 2 deploy session ships the schema-aware code to vklf.com). The `./resume` script (or `./resume-workflow 2`) will switch you to `workflow-2-competition-scraping`. Verify with `git branch --show-current` immediately after `./resume`; should be on `workflow-2-competition-scraping`. If you're on `main`, STOP and surface to director.

**Expected branch state on entry:** `workflow-2-competition-scraping` exactly even with `origin/workflow-2-competition-scraping` at the doc-batch commit landing after this NEXT_SESSION.md is written. `workflow-2-competition-scraping` will be 11 commits ahead of `main` at `ee8c79d` — the 2026-05-23 design-session doc-batch `d364063` + the 2026-05-24 Workstream 1 build commit `caad82a` + the 2026-05-24 doc-batch `fb19314` + the 2026-05-25 Workstream 2 Session 1 build commit `b6e43fe` + the 2026-05-25 doc-batch `9f555d0` + the 2026-05-26 Workstream 2 Session 2 build commit `9747f63` + the 2026-05-26 doc-batch `070a7ee` + the 2026-05-27 Workstream 2 Session 3 build commit `4773b62` + the 2026-05-27 doc-batch `64084ae` + today's build commit `82d390a` + today's end-of-session doc-batch (the commit that lands when the parent pushes the bundle); main doesn't move this session. After the next Workstream 2 Session 5 build commit + doc-batch, workflow-2 will be 13 commits ahead of main; will NOT be ff-merged to main since Session 5 is a build session, not a deploy session.

---

## Launch prompt

Read `docs/CLAUDE_CODE_STARTER.md` and follow every rule in it. **As Step 7b says (NEW 2026-05-21), produce the plain-terms summary of what this session will do BEFORE I give go-ahead.** Today's task:

**W#2 polish P-46 Workstream 2 Session 5, on `workflow-2-competition-scraping`.** Closes **(a.75) RECOMMENDED-NEXT**. This is Session 5 of Workstream 2 (the URL detail page redesign — Workstream 2 reached the ~85% mark at Session 4). Session 5's scope is the last §C.2 sub-scope remaining for Workstream 2:

- **New URL-level structural fields.** Type / Description-1 / Description-2 / Price (4 new text fields at the top of the URL box) + Scraping Status toggle (Incomplete / Complete; bidirectional mirror of the Competition Data table's Status column) + remove Sizes/Options UI (hide-UI-keep-data per Q6). Touches the existing `urls/[urlId]` PATCH route allowlist (extend for 4 new text fields + 1 enum) + render new field group at top of URL box in `UrlDetailContent.tsx` + hide Sizes/Options UI section + wire Status column bidirectional mirror. The 4 new text fields slot into the same allowlist Sessions 3-4 extended; the Status column bidirectional mirror is the trickiest piece since it touches both the URL detail page (toggle render) + the Competition Data table page (Status column rendering — but Q8's locked decision says both reads/writes go through `CompetitorUrl.scrapingStatus` enum so the mirror is at the column level rather than the route level; should be simple).

CODE session — no schema changes (Workstream 1 already covered all of P-46's schema including the `type` + `description1` + `description2` + `price` + `scrapingStatus` enum columns); no new npm dependencies (TipTap landed in Session 1); no deploy session — code stays on `workflow-2-competition-scraping`; main doesn't move this session.

Verify branch state with `git branch --show-current` before any doc reads — should be `workflow-2-competition-scraping`. If you're on `main`, STOP and surface to director.

**Per HANDOFF_PROTOCOL Rule 21 + Rule 22 — Pre-build read list:**

- `docs/CLAUDE_CODE_STARTER.md` (mandatory start-of-session; Step 7b plain-terms summary REQUIRED before any heavy reads or coding).
- `docs/ROADMAP.md` lines 1-30 (header) + the P-46 polish-backlog entry (the Workstream sub-status grid showing WS#2 Sessions 1-4 ✅ DONE-AT-CODE-LEVEL + Session 5 NEXT — the binding input for today's scope).
- **`docs/COMPETITION_DATA_V2_DESIGN.md`** with focus on **§C.2 Workstream 2 implementation outline** (the binding spec for what Workstream 2 builds + what Session 5 covers) + **§A.11 (schema additions including the new `CompetitorUrl` structural fields — all already deployed at code level via Workstream 1; all available to consume in this session's scope)** + **§A.6 (Sizes/Options hide-UI-keep-data decision) + §A.7 (Competition Score number-input-only decision) + §A.8 (Status column bidirectional mirror via `CompetitorUrl.scrapingStatus` enum)** + **§B 2026-05-24 (Workstream 1 closing entry — the schema state Session 5 reads against) + §B 2026-05-25 + §B 2026-05-26 + §B 2026-05-27 + §B 2026-05-28 (Workstream 2 Sessions 1-4 closing entries — the established render + handler precedents)**. This doc is the canonical source of truth.
- `prisma/schema.prisma` — verify the empirical schema state for the model + columns Session 5 reads: `CompetitorUrl.type` + `description1` + `description2` + `price` + `scrapingStatus` enum — all already deployed at code level via Workstream 1.
- `src/lib/shared-types/competition-scraping.ts` — the wire-type surface Workstream 1 extended; Session 5 reads the new `CompetitorUrl` structural fields on the wire types.
- `src/app/api/projects/[projectId]/competition-scraping/urls/[urlId]/route.ts` — the existing PATCH route Sessions 3 + 4 extended; Session 5 extends it again for the 4 new text fields + 1 enum. The same allowlist-extension fix-shape that Sessions 3-4 used applies here.
- `src/app/projects/[projectId]/competition-scraping/url/[urlId]/components/UrlDetailContent.tsx` — Sessions 1-4 rewrote `CapturedTextSubsection` + `CapturedImagesGallery` + `CapturedVideosGallery` to vertical card lists with Analysis editors + added `CapturedReviewsSection`; today extends the page with the new URL-level structural-fields group at the top of the URL box + hides the Sizes/Options section + wires the Scraping Status toggle. Read the existing URL-box render structure (lines ~700-760 per the design doc P-46 entry's pointer) to understand the insertion + removal points.
- The existing Competition Data table page (`src/app/projects/[projectId]/competition-scraping/page.tsx` + `components/CompetitionScrapingViewer.tsx` + `UrlTable.tsx` + `ColumnFilters.tsx`) — Session 5 wires the Status column bidirectional mirror; the table's Status column needs to read from `CompetitorUrl.scrapingStatus` enum (the same column the URL detail page's toggle writes to). Per Q8 the mirror is at the column level; should be a few-line read change.
- `docs/COMPETITION_SCRAPING_DESIGN.md` §B 2026-05-23 (the cross-reference pointer entry to the P-46 design doc — informational only).
- `docs/HANDOFF_PROTOCOL.md` Rule 14f (forced-picker may fire mid-session if any fix-shape ambiguity surfaces) + Rule 21 + Rule 22 (pre-build read list) + Rule 23 (Change Impact Audit — ADDITIVE + UI-only; safe; no new dependencies) + Rule 26 (DEFERRED items registry) + Rule 30 (Session bookends) + §4 Step 4b extended template.

**Task shape (P-46 Workstream 2 Session 5):**

1. **Plain-terms session-start summary per Rule 30** BEFORE any heavy reads or coding. Cover: what we'll do in the session (extend `urls/[urlId]` PATCH allowlist for the 4 new text fields + 1 enum + render new field group at top of URL box in `UrlDetailContent.tsx` + hide Sizes/Options UI section + wire Scraping Status toggle + Competition Data table page Status column bidirectional mirror + node:test coverage + /scoreboard verification + end-of-session doc-batch), the ZERO Rule 9 gates planned (no schema; no main push), the schema-change-in-flight flag state (stays YES from Workstream 1's migration), the no-deploy shape (code stays on workflow-2; main doesn't move this session).

2. **Pre-build reads** — execute the pre-build read list above. ~30 minutes. Surface any drift between §C.2's locked plan + Sessions 1-4's empirical landing.

3. **(Operational) Rule 14f forced-picker confirming the URL-level structural fields scope.** The picker is operational courtesy rather than a real choice (Session 5's scope is the only §C.2 sub-scope remaining). Per `feedback_default_to_recommendation.md`, default to the recommended scope. Mid-session sub-pickers may fire if any fix-shape ambiguity surfaces (e.g., field-ordering in the URL box header; whether the Sizes/Options UI removal is hide-via-conditional or hide-via-delete — likely conditional per Q6 hide-UI-keep-data + reversibility).

4. **Execute the URL-level structural fields scope.** ~60-90 minutes. Extend `urls/[urlId]/route.ts` PATCH allowlist for the 4 new text fields (`type` / `description1` / `description2` / `price`) + 1 enum (`scrapingStatus`). Render new field group at top of URL box in `UrlDetailContent.tsx` (one row per field — labeled inputs; debounced save matching the existing field-edit precedent if there is one, or simple on-blur save if not). Hide the existing Sizes/Options UI section per Q6 (conditional render that defaults to hidden; the underlying `CompetitorSize` table data stays in the database). Add the Scraping Status toggle at the top of the URL box (Incomplete / Complete enum write; UI affordance is a 2-option toggle). Wire the Competition Data table page Status column to read from `CompetitorUrl.scrapingStatus` enum (the same column the URL detail page's toggle writes to — per Q8 the mirror is at the column level so this is a few-line read change in the table's row-rendering code).

5. **Add node:test cases** for the new allowlist-validation logic. Likely 6-10 new cases covering the 4 new text fields' allowlist validation + the 1 new enum's allowlist validation + the bidirectional Status mirror integrity (i.e., a PATCH that writes `scrapingStatus: 'COMPLETE'` from the URL detail page makes the table page render the row with Status=Complete; not via UI test but via the wire-shape contract — the same data column drives both views). Bring src/lib node:test count from 670 to ~676-680.

6. **`/scoreboard` verification** — Check 1 root tsc clean / Check 2 extension tsc clean / Check 3 extension `npm test` 558/558 (no change — extension untouched) / Check 4 src/lib node:test 670 + 6-10 new cases / Check 5 `npm run build` 61 routes UNCHANGED (no new routes; only extending the existing allowlist) / Check 6 Playwright SKIPPED per non-deploy-session convention. All GREEN at new baselines.

7. **End-of-session doc-batch** covers ROADMAP (header bump + P-46 entry annotated with Workstream 2 Session 5 ✅ DONE-AT-CODE-LEVEL; closes (a.75) + opens (a.76) for Workstream 2 deploy session OR a continuation session if Session 5 surfaces unexpected scope) + CHAT_REGISTRY (header bump — 142nd Claude Code session) + DOCUMENT_MANIFEST (header bump) + CORRECTIONS_LOG (header bump only — likely zero new §Entries unless a process slip occurs) + NEXT_SESSION.md (rewritten for P-46 Workstream 2 deploy session) + HANDOFF_PROTOCOL (header bump only — no new rules expected) + CLAUDE_CODE_STARTER (header bump only) + `docs/COMPETITION_DATA_V2_DESIGN.md` (NEW §B entry capturing Session 5's empirical landing — closes the Workstream 2 implementation arc). **ONE push** to `origin/workflow-2-competition-scraping` (no main push since this is a build session; no ping-pong sync since main doesn't move).

**Per `feedback_recommendation_style.md` (most thorough/reliable) + `feedback_default_to_recommendation.md`:** for any layout ambiguity within the chosen scope (e.g., field-ordering in the URL box header; whether the Sizes/Options UI removal is hide-via-conditional or hide-via-delete; whether the Scraping Status toggle is a 2-button group or a single-checkbox), surface 2-4 plausible options + the recommended option + the rationale; default to the recommendation if director defers. Per `feedback_default_to_recommendation.md`, skip the picker if the question is asking permission to proceed on a path the director would default-approve (e.g., "should the 4 new text field allowlist extensions reuse the same fix-shape Sessions 3-4 used?" — yes per layout-precedent consistency; skip the picker).

**Schema-change-in-flight flag:** enters **YES** (carrying from Workstream 1's `prisma db push` 2026-05-24). **Stays YES** through Workstream 2 Session 5 + any Workstream 3 sessions until the first deploy session ships the schema-aware code to vklf.com.

---

## Pre-session notes (offline steps for director between sessions)

**Required offline step BEFORE the next P-46 Workstream 2 Session 5:** none. All infrastructure landed in prior sessions (TipTap dependencies in Session 1; schema in Workstream 1; the `OverallAnalysisBox` + `PerItemAnalysisBox` components consumable in Session 5 if any new structural field warrants an Analysis sibling — likely not since structural fields are simple text inputs + an enum toggle, not rich-text editors).

**Standing optional offline step (NOT blocking P-46 Workstream 2 — standing carry-over):** raise the Supabase Global File Size Limit to enable the bucket-level 100 MB cap on `competition-scraping-videos`. Step-by-step (unchanged from prior handoffs):

1. Open the Supabase dashboard at https://app.supabase.com → sign in if needed → pick the PLOS project.
2. **Storage** tab → **Settings** sub-tab.
3. **Global File Size Limit** → raise to **≥ 100 MB**.
4. Save.
5. EITHER re-run `node scripts/create-competition-scraping-videos-bucket.mjs` to update the bucket's `fileSizeLimit` (the script is idempotent), OR edit the bucket directly via the Supabase Storage UI → bucket settings → file size limit → set to 100 MB.

Not blocking P-46 Workstream 2 at all — can happen any time. Director-independent.

**Optional offline reading for director:** `docs/COMPETITION_DATA_V2_DESIGN.md` §C.2 Workstream 2 implementation outline (~2-minute skim) + §B 2026-05-28 (today's Workstream 2 Session 4 closing entry — the Captured Reviews UI end-to-end + the W1 path-divergence finding + the new "Per-record handler DI-seam precedent extension" Pattern; ~3-minute skim) + §A.6 + §A.7 + §A.8 (the locked decisions Session 5 consumes: Sizes/Options hide-UI-keep-data + Competition Score number-input-only + Status column bidirectional mirror). Worth scanning before the next session if director wants context for Session 5's structural-fields scope.

**Pre-build setup (informational — Claude will handle in-session):** the Workstream 2 Session 5 session doesn't sideload the extension or run any real-Chrome verification; the only "setup" is reading the existing `UrlDetailContent.tsx` URL-box render section + the existing `urls/[urlId]/route.ts` PATCH allowlist + the existing Competition Data table page Status-column code before extending — no director involvement.

---

## Destructive-operation safety check for next session

**Rule 8 triggers planned this session: ZERO** — no destructive git operations planned.

**Rule 9 triggers planned this session: ZERO** — no schema changes (Workstream 1 already covered all of P-46's schema including the new structural columns Session 5 reads); no `git push origin main`; no `git reset --hard`; no `git push --force`; no `git branch -D`; no `rm -rf`; no SQL DELETE/DROP/TRUNCATE planned.

**NO Rule 29 (container-level destructive op) triggers planned** this session. NO Codespaces rebuild planned. Claude's memory directory + `.codespace-backup/memory/` mirror both remain intact. Critical files safe. **Layer-3b mirror-staleness canary active since 2026-05-22-f.** If at session-start the canary emits a 🚨 alert (file count differs, presence differs, or any per-file size differs between source and mirror), STOP and investigate before any build work.

---

## Why this pointer was written this way (debug aid)

Today's session was P-46 Workstream 2 Session 4 (Captured Reviews UI end-to-end + W1 path-divergence fix + handler DI-seam extraction precedent). The session ran cleanly: ONE Rule 14f forced-picker fired at session start (director picked Option A; recommended) + ZERO Rule 14f sub-pickers fired during execution (every sub-decision was a default-to-recommendation skip per `feedback_default_to_recommendation.md`) + 9 files changed in build commit `82d390a` + 32 new node:test cases + /scoreboard 5/5 GREEN at new baselines. The session came in cleanly within the 3-5 sessions estimate for Workstream 2 mirroring Sessions 1-3's clean landings — four consecutive in-scope sessions confirm the §C.2 plan is well-specced + the three extraction Patterns (PerItemAnalysisBox + OverallAnalysisBox + per-record handler DI-seam) cover the extraction-shape spectrum cleanly.

The natural next-session task per §C.2 Workstream 2 implementation outline is **Workstream 2 Session 5** — the URL-level structural fields scope is the last §C.2 sub-scope remaining for Workstream 2:

- **(Recommended)** New URL-level structural fields. Type / Description-1 / Description-2 / Price + Scraping Status toggle + remove Sizes/Options UI. Touches the existing `urls/[urlId]` PATCH allowlist + the URL detail page render + the Competition Data table page Status-column read. Likely 6-10 new node:test cases. Recommended because it's the only §C.2 sub-scope left for Workstream 2 (Sessions 1-3 covered the Analysis surface arc; Session 4 covered Reviews; Session 5 covers structural fields).

The Rule 14f forced-picker at Session 5 start is operational courtesy rather than a real choice; the recommendation is locked + the only candidate; the picker is there so director can confirm or shift scope.

The shape of the Workstream 2 Session 5 session is **pure code session with ZERO Rule 9 gates** — no schema changes (Workstream 1 covered all of P-46's schema); no new npm dependencies (TipTap landed in Session 1; OverallAnalysisBox + PerItemAnalysisBox both consumable but likely not needed since structural fields are simple text inputs + an enum toggle); the chosen scope ships UI + a route-handler extension; node:test coverage; /scoreboard GREEN at new baselines; doc-batch + ONE push to workflow-2 at end. No main push, no ff-merge, no Vercel auto-redeploy, no ping-pong sync — Workstream 2 Session 5 is a build session, not a deploy session.

**After Workstream 2 Session 5 lands, Workstream 2 is complete and the next session can be the Workstream 2 deploy session** (Phase-4 deploy under Rule 9 gate; ff-merge of workflow-2-competition-scraping to main; Vercel auto-redeploy; ping-pong sync; schema-change-in-flight flag flips back to NO at deploy completion; director real-Chrome verification of all the Workstream 2 affordances landing live on vklf.com).

**Alternate next-session candidates if director shifts priorities at session start (after Workstream 2 Session 4 + before Workstream 2 Session 5):**

- **Defer Workstream 2 Session 5 + ship the Workstream 2 deploy session early (skip the URL-level structural fields).** NOT recommended — the structural fields are the last §C.2 sub-scope remaining for Workstream 2 + they're small (extend allowlist + render new field group + hide Sizes/Options + wire Status mirror). Shipping the deploy without them would leave §C.2 partially complete + force a Session 5 build session after the deploy that re-touches the same URL detail page surfaces. Better to finish Workstream 2 fully before deploying.
- **Defer Workstream 2 Session 5 + skip ahead to Workstream 3 (Competition Data table redesign).** NOT recommended — same reason as above; finish Workstream 2 fully (Session 5 + deploy) before starting Workstream 3. The Status column bidirectional mirror Session 5 wires is also a natural pre-cursor to Workstream 3's table-rewrite scope.
- **Defer Workstream 2 Session 5 + start P-47 Shadow DOM refactor.** ~2-3 sessions. NOT recommended — P-47 is LOW priority (band-aid works empirically) AND sequencing-wise the design doc explicitly noted Shadow DOM should wait until P-46 implementation lands.
- **Defer Workstream 2 Session 5 + start P-26 below-fold scroll capture.** ~1-2 sessions. NOT recommended — P-46 is the big-scope item in flight; P-26 is much smaller + the workaround works.
- **Raise Supabase Global File Size Limit (standing optional offline step).** Director's offline dashboard step — see Pre-session notes above. Not a Claude session task; can happen any time. Director-independent.
- **A polish-detour ahead of Workstream 2 Session 5 if director wants pre-flight infrastructure work.** No obvious candidate this time — the operational substrate is hardened across 3 layers (P-42 backup-memory hook + P-43 absolute-paths discipline + P-44 wxt build/zip wrappers); Workstream 2 Sessions 1-4's landings were all clean so there's no new substrate work surfaced. If director picks this path, surface the open polish landscape as a Rule 14f forced-picker.

Check `ROADMAP.md` for the canonical state. Check `docs/COMPETITION_DATA_V2_DESIGN.md` §C.2 + §A.6 + §A.7 + §A.8 + §B 2026-05-24 + §B 2026-05-25 + §B 2026-05-26 + §B 2026-05-27 + §B 2026-05-28 for Workstream 2's binding scope + the prior 5 sessions' empirical landing notes.
