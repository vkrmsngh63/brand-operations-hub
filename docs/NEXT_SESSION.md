# Next session

**Written:** 2026-05-27 (`session_2026-05-27_p46-workstream-2-session-3-url-level-and-per-category-overall-analysis-boxes` — end-of-session handoff after **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 3 ✅ DONE-AT-CODE-LEVEL 2026-05-27 on `workflow-2-competition-scraping`** — third build session of the P-46 Workstream 2 implementation arc (Session 3 of 3-5 estimated); completed the "Analysis surface" arc across all capture levels via a NEW `OverallAnalysisBox` parallel component shipping URL-level Overall Competitor Analysis + per-category Overall Analysis boxes (one per capture category — Text / Image / Video) at the bottom of each capture subsection + one URL-level box at the bottom of the URL detail page + extended the existing `urls/[urlId]` PATCH route allowlist for the two new fields with a NEW `isValidOverallAnalysesBag` strict-shape trust-boundary guard + bag-merge over replace at the PATCH so saving one category doesn't wipe sibling categories. Build commit `4773b62` — 5 files +452/-1 on `workflow-2-competition-scraping`; NOT pushed to main — Session 3 is a build session, not a deploy session. **Closes (a.73) RECOMMENDED-NEXT = P-46 Workstream 2 Session 3.** **Opens (a.74) RECOMMENDED-NEXT = P-46 Workstream 2 Session 4** on `workflow-2-competition-scraping` — multiple §C.2-aligned candidates remain (recommended: Captured Reviews UI — new `CapturedReviewCard` matching Sessions 1-3's card-list precedent + manual-add modal + `CapturedReview` CRUD route flesh-out from Workstream 1 501-stubs; alt: new URL-level Type/Description/Price fields + Scraping Status toggle + remove Sizes/Options UI). Director picks at Session 4 start.

---

## What we did this session (in plain terms)

This was the **third build session of Workstream 2** — the URL detail page redesign. Session 1 (2026-05-25) shipped the TipTap shared rich-text editor wrapper + the first per-item Analysis text box on Captured Text + the route-handler half for the simplest capture type, and locked the card-list layout precedent via a Rule 14f forced-picker. Session 2 (2026-05-26) applied that locked precedent to Captured Image + Captured Video. Today's Session 3 completed the **"Analysis surface" arc** — every capture level now has its rich-text Analysis editor.

What landed today, in plain terms:

- **The URL detail page now has THREE new tiers of Analysis text boxes.** Sessions 1-2 shipped the per-item layer (one rich-text editor per individual captured Text / Image / Video item). Today added the per-category layer (one editor at the bottom of each capture section summarizing all items in that category — so one editor at the bottom of the Captured Text section synthesizing all text captures, one at the bottom of Captured Images synthesizing all image captures, one at the bottom of Captured Videos synthesizing all video captures) PLUS the URL-level layer (one editor at the very bottom of the URL detail page synthesizing the whole URL's competitive intelligence).
- **All four new editors use the same Session 1 RichTextEditor wrapper.** Same toolbar (Bold / Italic / bullet list / numbered list / link). Same on-blur save behavior. Same Saving… / ✓ Saved / Save failed indicators. The director will recognize the editing experience from Sessions 1-2.
- **A new `OverallAnalysisBox` component was extracted.** It's a parallel sibling to Session 1's `PerItemAnalysisBox` — same save-lifecycle shape, but a different wire body shape because the Overall Analysis fields are URL-level (not per-row). A `field` discriminator prop tells the component which URL-level field it's editing — `overallCompetitorAnalysis` (the URL-wide one) or `overallAnalyses.text` / `.image` / `.video` / `.reviews` (the per-category ones). One component covers all 4 categories + the URL-level box.
- **The server-side `urls/[urlId]` PATCH endpoint was extended** to accept the two new fields. The trickiest part: when the per-category boxes save (one category at a time), the server has to MERGE the incoming partial bag onto the existing row's bag — otherwise saving the Captured Text Overall Analysis would wipe the Captured Image one. Bag-merge over replace; well-tested.
- **A new trust-boundary guard `isValidOverallAnalysesBag` was added** that strict-rejects unknown category keys (so a typo like `txet` instead of `text` fails at the boundary rather than silently writing through to the database) and requires each known-category value pass the existing `isValidAnalysisPayload` check. 10 new automated unit tests pin down its behavior.
- **No Rule 14f sub-pickers fired during execution.** Director picked Option A (URL-level + per-category Overall Analysis) at the session-start scope picker between 3 §C.2-aligned candidates (Option B = Captured Reviews UI; Option C = URL-level structural fields). Inside the chosen scope, every sub-decision was a default-to-recommendation skip (bag-merge over replace at the PATCH was the only safe choice; strict unknown-key rejection in the guard is the most-thorough option; parallel component over overloading the Session 1 component is the cleaner-callsite choice).
- **All five verification checks GREEN** at the new baselines (root tsc clean / extension tsc clean / 558 extension tests UNCHANGED / **638 server tests (+10 from baseline 628)** / **61 routes UNCHANGED** — no new routes; only extended the existing URL-level PATCH allowlist).

No schema changes today (Workstream 1 already added the `overallCompetitorAnalysis` and `overallAnalyses` columns; today just consumed them). No new npm dependencies (TipTap landed in Session 1; today reused it).

**Schema-change-in-flight flag STAYS YES** — carrying from Workstream 1's `prisma db push` 2026-05-24. Stays YES through subsequent Workstream 2 + Workstream 3 sessions until the first deploy session ships the schema-aware code to vklf.com.

**Session 3 came in cleanly within the 3-5 sessions estimate for Workstream 2.** Three consecutive in-scope sessions (1, 2, 3) confirm the §C.2 plan + the Session 1 extraction Pattern were both well-specced. The new `OverallAnalysisBox` is a second reusable Pattern memorialization (parallel component for related-but-distinct edit affordances) alongside Session 1's `PerItemAnalysisBox` Pattern (reuse-via-props for sibling capture types). Together they cover the spectrum from "same wire contract, different apiUrl" to "different wire contract, same save-lifecycle."

**Workstream 2 is now ~70% complete** (Sessions 1-3 of 3-5 done). Sessions 4-5 cover the remaining §C.2 work: Captured Reviews UI + new URL-level structural fields (Type / Description / Price) + Scraping Status toggle + remove Sizes/Options UI + any vklf.com-side upload/edit/delete affordances.

## What we'll do next session (in plain terms)

Next session is **P-46 Workstream 2 Session 4.** Two §C.2-aligned candidates remain; director picks at Session 4 start:

- **(Recommended) Captured Reviews UI.** A new card component matching Sessions 1-3's card-list shape + a manual-add modal so the director can enter reviews by hand on vklf.com (no extension review-capture in v1 per Q1's deferral) + flesh out the `CapturedReview` CRUD route handlers that Workstream 1 scaffolded as 501 stubs. Greenfield surface; slots into the same card-list precedent. Recommended because §C.2 originally sequenced this as Session 3 of Workstream 2 — natural next §C.2 step after the Analysis surface arc completion. Reviews is the only capture type that doesn't yet have UI; landing this fills the last gap before structural fields.
- **(Alt) New URL-level structural fields.** Type / Description-1 / Description-2 / Price (4 new text fields at the top of the URL box) + Scraping Status toggle (Incomplete / Complete; bidirectional mirror of the Competition Data table's Status column) + remove Sizes/Options UI (hide-UI-keep-data per Q6). Smaller individual changes but touches more existing surfaces.

Likely 6-10 new node:test cases for the chosen scope (bringing src/lib to roughly 644-648). No schema changes (Workstream 1 already covered all of P-46's schema). No new npm dependencies. No deploy this session — Workstream 2 spans 3-5 sessions total before its deploy.

## What's still left on the total roadmap (in plain terms)

Major W#2 items as of session-end 2026-05-27:

- **P-46 Workstream 1 SCHEMA ✅ DONE-AT-CODE-LEVEL 2026-05-24.** Needs its own deploy session later.
- **P-46 Workstream 2 (URL detail page redesign) — IN PROGRESS — ~70% complete.** Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-25 (TipTap wrapper + per-item Analysis on Captured Text + card-list precedent). Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-26 (same shape applied to Captured Image + Captured Video). Session 3 ✅ DONE-AT-CODE-LEVEL 2026-05-27 (this session; URL-level + per-category Overall Analysis boxes completing the Analysis surface arc). **Session 4 NEXT (recommended)** = Captured Reviews UI. Session 5 covers whatever Session 4 doesn't pick + any vklf.com-side upload/edit/delete affordances. Workstream 2 deploys to vklf.com after Sessions 4-5 complete.
- **P-46 Workstream 3 (Competition Data table redesign).** ~3-4 sessions. ~12 new columns + click-to-edit on every cell + resizable column widths + drag-to-reorder rows + per-column show/hide toggles + adjustable font size + horizontal checkbox bar at top + cross-device-synced user preferences via `UserTablePreferences` model from Workstream 1.
- **P-46 Workstream 4 (Comprehensive Competitor Analysis page).** ~2-3 sessions. NEW page hosting per-Project TipTap rich-text doc (one per Project) with hyperlinks back to URL detail pages + edit-mode toggle + back-button. Consumes the same `RichTextEditor` wrapper Session 1 built (using the `variant='full'` branch).
- **P-46 Workstream 5 (Extension form additions + manual Reviews entry).** ~1-2 sessions. Add Type / Description-1 / Description-2 / Price fields to the extension URL save form + vklf.com-side manual Reviews entry form. Deploy session ends this workstream + closes the P-46 arc.
- **P-47 Shadow DOM refactor (LOW; AFTER P-46).** ~2-3 sessions. Replaces the 80-event-listener band-aid from P-45 Build #2's Issue 2 fix with proper Shadow DOM isolation. LOW priority since band-aid works empirically.
- **P-26 below-fold scroll capture (LOW).** ~1-2 sessions. Current two-captures workaround works fine. May be reduced in urgency by Workstream 2's vklf.com-side image upload affordances.
- **P-27 Bug #9 (Amazon hover-preview deeper-walk) + Bug #15 (Ebay native-controls quirk) — DEFERRED LOW.** May obsolete with P-46 redesign which restructures the URL detail page surface they live in.
- **W#2 graduation** after P-46 + P-47 + P-26 ship. Then W#3-W#14 (twelve more workflows on the roadmap; none started yet).
- **Optional offline step (any time, NOT blocking P-46):** raise the Supabase Global File Size Limit to enable bucket-level 100 MB cap on `competition-scraping-videos`.

---

**For:** the next Claude Code session — **P-46 Workstream 2 Session 4** (estimated ~2-3 hours: pre-build doc reads ~30 min + session-start Rule 14f forced-picker for Session 4's scope ~10 min + the chosen scope's code work ~60-90 min + node:test coverage ~20 min + /scoreboard verification + end-of-session doc-batch ~30 min). Per Rule 23 Change Impact Audit: **ADDITIVE + UI-only** (extends the rich-text + card-list pattern Sessions 1-3 established to the next §C.2-aligned scope; consumes Workstream 1's existing schema columns; no schema changes; no new npm dependencies — TipTap landed in Session 1). No data risk (existing rows render with empty Analysis text boxes per §A.11 "no data backfill needed"). Zero downstream W#1 / W#3 cross-tool impact. **Schema-change-in-flight flag enters YES** (carrying from Workstream 1's `prisma db push`); **stays YES** through Workstream 2 + Workstream 3 sessions until the first deploy session ships the schema-aware code to vklf.com. **Rule 9 triggers planned this session: ZERO** — no schema changes; no main push; pure code session. **ONE push planned** — end-of-session doc-batch + build commit to `origin/workflow-2-competition-scraping` (no main push since this is a build session, not a deploy session).

---

## Status of today's session

**W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 3 ✅ DONE-AT-CODE-LEVEL 2026-05-27 on `workflow-2-competition-scraping`** — third build session of the P-46 Workstream 2 implementation arc; completed the "Analysis surface" arc across all capture levels via a NEW `OverallAnalysisBox` parallel component shipping URL-level Overall Competitor Analysis + per-category Overall Analysis boxes at the bottom of each capture subsection + URL-level box at the bottom of the URL detail page + extended the existing `urls/[urlId]` PATCH allowlist for the two new fields with a new `isValidOverallAnalysesBag` strict-shape trust-boundary guard + bag-merge over replace at the PATCH. Build commit `4773b62` — 5 files +452/-1 on `workflow-2-competition-scraping`; NOT pushed to main since Session 3 is a build session.

**Session shape (build session — completes the Analysis surface arc + 1 route-handler half via allowlist extension):**

- Pre-build reads at session start (read `docs/COMPETITION_DATA_V2_DESIGN.md` §C.2 + §A.5 + §A.11 + §B 2026-05-24 + §B 2026-05-25 + §B 2026-05-26 + `prisma/schema.prisma` empirical state for `CompetitorUrl.overallCompetitorAnalysis` + `CompetitorUrl.overallAnalyses` columns + `src/lib/shared-types/competition-scraping.ts` + Session 1's `RichTextEditor.tsx` + `PerItemAnalysisBox.tsx` + `tiptap-helpers.ts` + existing `urls/[urlId]/route.ts`).
- **Rule 14f forced-picker at session start** between 3 §C.2-aligned candidates — director picked Option A (URL-level + per-category Overall Analysis; recommended because consumes already-shipped infrastructure + completes Analysis surface arc before structural fields).
- NEW `OverallAnalysisBox.tsx` (~200 LOC) — parallel sibling to `PerItemAnalysisBox.tsx`; same save-lifecycle (Saving… / ✓ Saved / Save failed indicators; generation-counter stale-response handling) but different wire body shape via a `field` discriminator prop (`{ kind: 'overallCompetitorAnalysis' }` emits `{ overallCompetitorAnalysis: <doc> }`; `{ kind: 'overallAnalyses', category: 'text'|'image'|'video'|'reviews' }` emits `{ overallAnalyses: { [category]: <doc> } }`).
- NEW `isValidOverallAnalysesBag` guard in `tiptap-helpers.ts` (+37 LOC); strict-rejects non-objects/null/arrays/primitives + strict-rejects unknown category keys (catches typos at the boundary) + requires each known-category value pass `isValidAnalysisPayload`; new `OVERALL_ANALYSES_CATEGORIES` constant + `OverallAnalysesCategory` type export.
- Extended `urls/[urlId]/route.ts` PATCH allowlist (+65 LOC) for `overallCompetitorAnalysis` (validated via existing `isValidAnalysisPayload`) + `overallAnalyses` (validated via new `isValidOverallAnalysesBag`); the `overallAnalyses` field MERGES the incoming partial bag onto the existing row's bag (reads `existingRow.overallAnalyses` first, then spreads incoming on top) so per-category boxes saving only their own slot don't wipe sibling categories.
- `UrlDetailContent.tsx` (+65/-1) — threads `overallAnalysisInitial` prop through to `CapturedTextSubsection` + `CapturedImagesGallery` + `CapturedVideosGallery`; each subsection renders an `OverallAnalysisBox` at the bottom (category text/image/video); page-level `OverallAnalysisBox` for `overallCompetitorAnalysis` renders after `CapturedVideosGallery` at page bottom.
- Added 10 new node:test cases for `isValidOverallAnalysesBag` in `tiptap-helpers.test.ts` (+88 LOC); test count 628 → 638 (+10).
- /scoreboard verification: all 5 checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **638 src/lib +10 from baseline 628** / **61 routes UNCHANGED** — no new routes; only allowlist extension); Check 6 Playwright SKIPPED per non-deploy-session convention.
- End-of-session doc-batch covers the 8-doc bundle (ROADMAP + CHAT_REGISTRY + DOCUMENT_MANIFEST + CORRECTIONS_LOG + HANDOFF_PROTOCOL + CLAUDE_CODE_STARTER + this NEXT_SESSION + the new §B 2026-05-27 entry on COMPETITION_DATA_V2_DESIGN.md).
- ONE push at end-of-session to `origin/workflow-2-competition-scraping` (no main push since this is a build session; no Rule 9 trigger at the push since `feedback_approval_scope_per_decision_unit.md` scopes Rule 9 gates to schema-migration / main-push / destructive-op decision units, not feature-branch doc-batch pushes).

**§4 Step 1c forced-picker NOT FIRED** — Session 4 task candidates ALL align with §C.2 Workstream 2 plan; the director-pick choice between them is a scope-ordering preference (which §C.2 sub-scope lands first), not an unknown next-session task. The pointer file enumerates the candidates so director can choose at Session 4 start; pre-build reads will identify which candidate's surfaces are cleanest to touch first.

**ZERO new DEFERRED items at session end (Rule 26)** — all 6 in-session tasks completed.

**NO new CORRECTIONS_LOG §Entry this session** — no fix-shape ambiguity; no estimate slip; no process slip; Session 3 landed cleanly within the 3-5-session Workstream 2 estimate. (The P-43 cwd-leak class re-reproduced ONCE during /scoreboard Check 5 — extension build ran instead of Next.js build because cwd had drifted; caught + recovered immediately; LOW informational; captured in design doc §B 2026-05-27 NOT promoted to CORRECTIONS_LOG.)

**EIGHTEENTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → today). The 3 plain-terms sections above continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive.

---

## Branch

**`workflow-2-competition-scraping`** — entered at start of next session; P-46 Workstream 2 Session 4 build session lands here (code commits stay on workflow-2 per the established pattern; deploy to main happens later, likely after Workstream 2 + possibly Workstream 3 lands enough UI to demo the schema's reach). The `./resume` script (or `./resume-workflow 2`) will switch you to `workflow-2-competition-scraping`. Verify with `git branch --show-current` immediately after `./resume`; should be on `workflow-2-competition-scraping`. If you're on `main`, STOP and surface to director.

**Expected branch state on entry:** `workflow-2-competition-scraping` exactly even with `origin/workflow-2-competition-scraping` at the doc-batch commit landing after this NEXT_SESSION.md is written. `workflow-2-competition-scraping` will be 8 commits ahead of `main` at `ee8c79d` — the 2026-05-23 design-session doc-batch `d364063` + the 2026-05-24 Workstream 1 build commit `caad82a` + the 2026-05-24 doc-batch `fb19314` + the 2026-05-25 Workstream 2 Session 1 build commit `b6e43fe` + the 2026-05-25 doc-batch `9f555d0` + the 2026-05-26 Workstream 2 Session 2 build commit `9747f63` + the 2026-05-26 doc-batch `070a7ee` + today's build commit `4773b62` + today's end-of-session doc-batch (the commit that lands when the parent pushes the bundle); main doesn't move this session. After the next Workstream 2 Session 4 build commit + doc-batch, workflow-2 will be 10 commits ahead of main; will NOT be ff-merged to main since Session 4 is a build session, not a deploy session.

---

## Launch prompt

Read `docs/CLAUDE_CODE_STARTER.md` and follow every rule in it. **As Step 7b says (NEW 2026-05-21), produce the plain-terms summary of what this session will do BEFORE I give go-ahead.** Today's task:

**W#2 polish P-46 Workstream 2 Session 4, on `workflow-2-competition-scraping`.** Closes **(a.74) RECOMMENDED-NEXT**. This is Session 4 of Workstream 2 (the URL detail page redesign — the densest user-visible improvement of the entire P-46 arc; Workstream 2 reached the ~70% mark at Session 3). Session 4's scope is one of two §C.2-aligned candidates — director picks at session start via Rule 14f forced-picker between:

- **(Recommended) Captured Reviews UI** — new `CapturedReviewCard` matching Sessions 1-3's card-list shape + manual-add modal so the director can enter reviews by hand on vklf.com (no extension Reviews capture in v1 per Q1's deferral) + flesh out the `CapturedReview` CRUD route handlers Workstream 1 scaffolded as 501 stubs. Greenfield card type slots into the same card-list precedent. Recommended because §C.2 originally sequenced this as Session 3 of Workstream 2 — natural next §C.2 step after the Analysis surface arc completion. Reviews is the only capture type that doesn't yet have UI; landing this fills the last gap before structural fields. Likely scope: ~3 new components (`CapturedReviewCard` + `CapturedReviewsSection` + manual-add modal) + 4 new route handlers (POST list / GET list / PATCH single / DELETE single) + ~10-15 new node:test cases for the route logic + render integration in `UrlDetailContent.tsx`.
- **(Alt) New URL-level structural fields** — Type / Description-1 / Description-2 / Price (4 new text fields at the top of the URL box) + Scraping Status toggle (Incomplete / Complete; bidirectional mirror of the Competition Data table's Status column) + remove Sizes/Options UI (hide-UI-keep-data per Q6). Smaller individual changes but touches more existing surfaces (URL box layout shift + Status column wiring in the table page + Sizes/Options removal). Likely scope: extend `urls/[urlId]` PATCH allowlist for 4 new text fields + 1 enum (Scraping Status) + render new field group at top of URL box in `UrlDetailContent.tsx` + hide Sizes/Options UI + wire Status column bidirectional mirror + ~6-10 new node:test cases.

CODE session — no schema changes (Workstream 1 already covered all of P-46's schema); no new npm dependencies (TipTap landed in Session 1); no deploy session — code stays on `workflow-2-competition-scraping`; main doesn't move this session.

Verify branch state with `git branch --show-current` before any doc reads — should be `workflow-2-competition-scraping`. If you're on `main`, STOP and surface to director.

**Per HANDOFF_PROTOCOL Rule 21 + Rule 22 — Pre-build read list:**

- `docs/CLAUDE_CODE_STARTER.md` (mandatory start-of-session; Step 7b plain-terms summary REQUIRED before any heavy reads or coding).
- `docs/ROADMAP.md` lines 1-30 (header) + the P-46 polish-backlog entry (the Workstream sub-status grid showing WS#2 Sessions 1-3 ✅ DONE-AT-CODE-LEVEL + Session 4 NEXT — the binding input for today's scope).
- **`docs/COMPETITION_DATA_V2_DESIGN.md`** with focus on **§C.2 Workstream 2 implementation outline** (the binding spec for what Workstream 2 builds + what Sessions 4-5 cover) + **§A.5 (TipTap library + per-item Analysis pattern)** + **§A.11 (schema additions including the `CapturedReview` model + the new `CompetitorUrl` structural fields — both already deployed at code level via Workstream 1; both available to consume in either Session 4 candidate's scope)** + **§B 2026-05-24 (Workstream 1 closing entry — the schema state Session 4 reads against)** + **§B 2026-05-25 (Workstream 2 Session 1 closing entry — the card-layout precedent + the `PerItemAnalysisBox` extraction Pattern)** + **§B 2026-05-26 (Workstream 2 Session 2 closing entry — the "card-list pattern propagates cleanly" empirical observation)** + **§B 2026-05-27 (Workstream 2 Session 3 closing entry — the Analysis surface arc completion + the new `OverallAnalysisBox` extraction Pattern)**. This doc is the canonical source of truth.
- `prisma/schema.prisma` — verify the empirical schema state for the model + columns the chosen Session 4 candidate reads. For Option A (Captured Reviews): the `CapturedReview` Prisma model (fields: id / clientId / competitorUrlId / starRating / body / reviewerName / reviewDate / capturedAt / analysis JSON) — already deployed at code level via Workstream 1. For Option C (URL-level structural fields): `CompetitorUrl.type` + `description1` + `description2` + `price` + `scrapingStatus` enum — all already deployed at code level via Workstream 1.
- `src/lib/shared-types/competition-scraping.ts` — the wire-type surface Workstream 1 extended (Session 4 reads either the `CapturedReview` wire type or the new `CompetitorUrl` structural fields on the wire types, depending on chosen scope).
- For Option A: the `CapturedReview` CRUD route stubs Workstream 1 scaffolded as 501s — read the existing 501-stub shape before fleshing out the implementation. The 501-stub paths are likely: `src/app/api/projects/[projectId]/competition-scraping/urls/[urlId]/reviews/route.ts` (POST list / GET list) + `src/app/api/projects/[projectId]/competition-scraping/urls/[urlId]/reviews/[reviewId]/route.ts` (PATCH / DELETE single).
- For Option C: the existing `urls/[urlId]/route.ts` PATCH route Session 3 just extended — the new structural fields slot into the same allowlist; the Status column wiring is bidirectional via the existing scrapingStatus enum.
- `src/app/projects/[projectId]/competition-scraping/url/[urlId]/components/UrlDetailContent.tsx` — Sessions 1-3 rewrote `CapturedTextSubsection` + `CapturedImagesGallery` + `CapturedVideosGallery` to vertical card lists with per-item + per-category + URL-level Overall Analysis boxes; today extends the page with either (recommended) the new Captured Reviews section + manual-add modal; OR (alt) the new URL-level structural fields at the top of the URL box. Read the existing page structure to understand the insertion points.
- For Option A: read Sessions 1-3's existing card components (`CapturedTextCard` / `CapturedImageCard` / `CapturedVideoCard`) as the precedent shape for the new `CapturedReviewCard`.
- `docs/COMPETITION_SCRAPING_DESIGN.md` §B 2026-05-23 (the cross-reference pointer entry to the P-46 design doc — informational only).
- `docs/HANDOFF_PROTOCOL.md` Rule 14f (forced-picker REQUIRED at session start to pick between the 2 Session 4 candidates + may fire again mid-session if any fix-shape ambiguity surfaces within the chosen scope) + Rule 21 + Rule 22 (pre-build read list) + Rule 23 (Change Impact Audit — ADDITIVE + UI-only; safe; no new dependencies) + Rule 26 (DEFERRED items registry) + Rule 30 (Session bookends) + §4 Step 4b extended template.

**Task shape (P-46 Workstream 2 Session 4):**

1. **Plain-terms session-start summary per Rule 30** BEFORE any heavy reads or coding. Cover: what we'll do in the session (Rule 14f forced-picker to pick Session 4's scope from the 2 candidates + execute the chosen scope's code work + node:test coverage + /scoreboard verification + end-of-session doc-batch), the ZERO Rule 9 gates planned (no schema; no main push), the schema-change-in-flight flag state (stays YES from Workstream 1's migration), the no-deploy shape (code stays on workflow-2; main doesn't move this session).

2. **Pre-build reads** — execute the pre-build read list above. ~30 minutes. Surface any drift between §C.2's locked plan + Sessions 1-3's empirical landing.

3. **Rule 14f forced-picker — pick Session 4's scope from the 2 candidates.** Surface the 2 options with the rationale shape captured in §B 2026-05-27 of the design doc + recommend Option A (Captured Reviews UI) because §C.2 originally sequenced this as Session 3 of Workstream 2 + because Reviews is the last capture type missing UI; landing this fills the last greenfield gap before structural fields. Per `feedback_default_to_recommendation.md`, default to Option A if director defers. The chosen scope shape locks the next set of tasks.

4. **Execute the chosen scope's code work.** ~60-90 minutes. For Option A: new `CapturedReviewCard` matching Sessions 1-3's card-list shape (pill for starRating + trash button + reviewer/date row + body row + per-item Analysis box) + new `CapturedReviewsSection` parent + new manual-add modal triggered by an "Add review" button at the top of the section + flesh out the 4 route handlers Workstream 1 scaffolded as 501-stubs (POST list / GET list / PATCH single / DELETE single) + render integration in `UrlDetailContent.tsx` + Overall Reviews Analysis box at the bottom of the new section via the existing `OverallAnalysisBox` component with `kind='overallAnalyses'` `category='reviews'`. For Option C: see the candidate shape in the design doc §C.2.

5. **Add node:test cases** for the new route handler logic. Likely 10-15 new cases for Option A (happy path for each of POST/GET/PATCH/DELETE + invalid payload rejection + 404 for missing review + 404 for missing URL + auth checks + analysis field validation via `isValidAnalysisPayload`). Likely 6-10 new cases for Option C (new structural-field allowlist validation + bidirectional Status mirror integrity). Bring src/lib node:test count from 638 to ~644-650.

6. **`/scoreboard` verification** — Check 1 root tsc clean / Check 2 extension tsc clean / Check 3 extension `npm test` 558/558 (no change — extension untouched) / Check 4 src/lib node:test 638 + 6-15 new cases / Check 5 `npm run build` 61 routes (+2 for Option A's new CapturedReview routes if treated as new endpoints; UNCHANGED if treated as fleshing-out existing route shells; UNCHANGED for Option C since only allowlist extensions) / Check 6 Playwright SKIPPED per non-deploy-session convention. All GREEN at new baselines.

7. **End-of-session doc-batch** covers ROADMAP (header bump + P-46 entry annotated with Workstream 2 Session 4 progress; closes (a.74) + opens (a.75) for Workstream 2 Session 5 OR begins Workstream 2 deploy session if Session 4 is the last scope) + CHAT_REGISTRY (header bump — 141st Claude Code session) + DOCUMENT_MANIFEST (header bump) + CORRECTIONS_LOG (header bump only — likely zero new §Entries unless a process slip occurs) + NEXT_SESSION.md (rewritten for P-46 Workstream 2 Session 5 OR deploy session) + HANDOFF_PROTOCOL (header bump only — no new rules expected) + CLAUDE_CODE_STARTER (header bump only) + `docs/COMPETITION_DATA_V2_DESIGN.md` (NEW §B entry capturing Session 4's empirical landing). **ONE push** to `origin/workflow-2-competition-scraping` (no main push since this is a build session; no ping-pong sync since main doesn't move).

**Per `feedback_recommendation_style.md` (most thorough/reliable) + `feedback_default_to_recommendation.md`:** for the Session 4 scope-pick at session start, recommend Option A explicitly + default to it if director defers. For any layout ambiguity within the chosen scope (e.g., Captured Review card heading format; manual-add modal field order; star-rating widget type), surface 2-4 plausible options + the recommended option + the rationale; default to the recommendation if director defers. Per `feedback_default_to_recommendation.md`, skip the picker if the question is asking permission to proceed on a path the director would default-approve (e.g., "should the new Captured Reviews section use the same `OverallAnalysisBox` Session 3 just shipped for per-category Overall Analysis?" — yes per layout-precedent consistency; skip the picker).

**Schema-change-in-flight flag:** enters **YES** (carrying from Workstream 1's `prisma db push` 2026-05-24). **Stays YES** through subsequent Workstream 2 sessions + Workstream 3 sessions until the first deploy session ships the schema-aware code to vklf.com.

---

## Pre-session notes (offline steps for director between sessions)

**Required offline step BEFORE the next P-46 Workstream 2 Session 4:** none. All infrastructure landed in prior sessions (TipTap dependencies in Session 1; schema in Workstream 1; the `OverallAnalysisBox` component just shipped in Session 3 is consumable in Session 4 for the Overall Reviews Analysis box without any new infrastructure).

**Standing optional offline step (NOT blocking P-46 Workstream 2 — standing carry-over):** raise the Supabase Global File Size Limit to enable the bucket-level 100 MB cap on `competition-scraping-videos`. Step-by-step (unchanged from prior handoffs):

1. Open the Supabase dashboard at https://app.supabase.com → sign in if needed → pick the PLOS project.
2. **Storage** tab → **Settings** sub-tab.
3. **Global File Size Limit** → raise to **≥ 100 MB**.
4. Save.
5. EITHER re-run `node scripts/create-competition-scraping-videos-bucket.mjs` to update the bucket's `fileSizeLimit` (the script is idempotent), OR edit the bucket directly via the Supabase Storage UI → bucket settings → file size limit → set to 100 MB.

Not blocking P-46 Workstream 2 at all — can happen any time. Director-independent.

**Optional offline reading for director:** `docs/COMPETITION_DATA_V2_DESIGN.md` §C.2 Workstream 2 implementation outline (~2-minute skim) + §B 2026-05-27 (today's Workstream 2 Session 3 closing entry — the Analysis surface arc completion + the new `OverallAnalysisBox` extraction Pattern; ~3-minute skim). Worth scanning before the next session if director wants context for which §C.2 candidate to pick at Session 4 start.

**Pre-build setup (informational — Claude will handle in-session):** the Workstream 2 Session 4 session doesn't sideload the extension or run any real-Chrome verification; the only "setup" is reading the chosen candidate's surfaces in `UrlDetailContent.tsx` + the existing `CapturedReview` route stubs OR the existing `urls/[urlId]/route.ts` before extending — no director involvement.

---

## Destructive-operation safety check for next session

**Rule 8 triggers planned this session: ZERO** — no destructive git operations planned.

**Rule 9 triggers planned this session: ZERO** — no schema changes (Workstream 1 already covered all of P-46's schema including the `CapturedReview` model + the new structural columns either Session 4 candidate reads); no `git push origin main`; no `git reset --hard`; no `git push --force`; no `git branch -D`; no `rm -rf`; no SQL DELETE/DROP/TRUNCATE planned.

**NO Rule 29 (container-level destructive op) triggers planned** this session. NO Codespaces rebuild planned. Claude's memory directory + `.codespace-backup/memory/` mirror both remain intact. Critical files safe. **Layer-3b mirror-staleness canary active since 2026-05-22-f.** If at session-start the canary emits a 🚨 alert (file count differs, presence differs, or any per-file size differs between source and mirror), STOP and investigate before any build work.

---

## Why this pointer was written this way (debug aid)

Today's session was P-46 Workstream 2 Session 3 (URL-level Overall Competitor Analysis + per-category Overall Analysis boxes — completing the "Analysis surface" arc across all capture levels). The session ran cleanly: ONE Rule 14f forced-picker fired at session start (director picked Option A; recommended) + ZERO Rule 14f sub-pickers fired during execution (every sub-decision was a default-to-recommendation skip per `feedback_default_to_recommendation.md`) + 5 files changed in build commit `4773b62` + 10 new node:test cases for `isValidOverallAnalysesBag` + /scoreboard 5/5 GREEN at new baselines. The session came in cleanly within the 3-5 sessions estimate for Workstream 2 mirroring Sessions 1-2's clean landings — three consecutive in-scope sessions confirm the §C.2 plan is well-specced.

The natural next-session task per §C.2 Workstream 2 implementation outline is **Workstream 2 Session 4** — two §C.2-aligned candidates remain with no single forced-pick:

- **(Recommended) Captured Reviews UI.** Greenfield surface; slots into the same card-list precedent Sessions 1-3 established. Recommended because §C.2 originally sequenced this as Session 3 of Workstream 2 — natural next §C.2 step after the Analysis surface arc completion. Reviews is the only capture type that doesn't yet have UI; landing this fills the last greenfield gap before structural fields.
- **(Alt) New URL-level structural fields.** Type / Description-1 / Description-2 / Price + Scraping Status toggle + remove Sizes/Options UI. Smaller individual changes; touches more existing surfaces.

The Rule 14f forced-picker at Session 4 start gives director the choice; Claude's recommendation is Option A because it's the last greenfield capture type + completes the Captures + Analysis cross product cleanly before Session 5 picks up the structural fields (which slot in as smaller in-place edits regardless of when they land).

The shape of the Workstream 2 Session 4 session is **pure code session with ZERO Rule 9 gates** — no schema changes (Workstream 1 covered all of P-46's schema); no new npm dependencies (TipTap landed in Session 1; `OverallAnalysisBox` landed in Session 3 and is consumable for the Overall Reviews Analysis box); the chosen scope ships UI + route-handler extensions (or new route implementations behind already-scaffolded stubs); node:test coverage; /scoreboard GREEN at new baselines; doc-batch + ONE push to workflow-2 at end. No main push, no ff-merge, no Vercel auto-redeploy, no ping-pong sync — Workstream 2 Session 4 is a build session, not a deploy session.

**Alternate next-session candidates if director shifts priorities at session start (after Workstream 2 Session 3 + before Workstream 2 Session 4):**

- **Defer Workstream 2 Session 4 + skip ahead to Workstream 3 (Competition Data table redesign).** NOT recommended — Workstream 2 is now ~70% complete (Sessions 1-3 ✅ DONE-AT-CODE-LEVEL but Sessions 4-5 pending); skipping ahead to Workstream 3 leaves Workstream 2 incomplete + introduces table-redesign work parallel to the URL-detail-page redesign that risks scope-collision. Better to finish Workstream 2 fully before starting Workstream 3.
- **Defer Workstream 2 Session 4 + start P-47 Shadow DOM refactor.** ~2-3 sessions. NOT recommended — P-47 is LOW priority (band-aid works empirically) AND sequencing-wise the design doc explicitly noted Shadow DOM should wait until P-46 implementation lands.
- **Defer Workstream 2 Session 4 + start P-26 below-fold scroll capture.** ~1-2 sessions. NOT recommended — P-46 is the big-scope item in flight; P-26 is much smaller + the workaround works; jumping to P-26 doesn't advance the W#2 graduation path.
- **Defer Workstream 2 Session 4 + start P-27 Bug #9 + Bug #15 (remaining deferred captured-videos polish leftovers).** NOT recommended — both bugs are LOW priority and may obsolete entirely with P-46's redesign.
- **Raise Supabase Global File Size Limit (standing optional offline step).** Director's offline dashboard step — see Pre-session notes above. Not a Claude session task; can happen any time. Director-independent.
- **A polish-detour ahead of Workstream 2 Session 4 if director wants pre-flight infrastructure work.** No obvious candidate this time — the operational substrate is hardened across 3 layers (P-42 backup-memory hook + P-43 absolute-paths discipline + P-44 wxt build/zip wrappers); Workstream 2 Sessions 1-3's landings were all clean so there's no new substrate work surfaced. (The P-43 cwd-leak class re-reproduced once during today's /scoreboard but caught + recovered fast; LOW informational; no new substrate work warranted.) If director picks this path, surface the open polish landscape as a Rule 14f forced-picker.

Check `ROADMAP.md` for the canonical state. Check `docs/COMPETITION_DATA_V2_DESIGN.md` §C.2 + §B 2026-05-24 + §B 2026-05-25 + §B 2026-05-26 + §B 2026-05-27 for Workstream 2's binding scope + the prior 4 sessions' empirical landing notes.
