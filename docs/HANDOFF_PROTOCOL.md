# HANDOFF PROTOCOL
## The rules Claude must follow at the start, during, and end of EVERY chat

**Last updated:** 2026-05-29 (Header bump only this session — no new rules drafted; no Rule 31 sub-section changes; no Step 7b mechanic changes. This session (`session_2026-05-29_p49-w5-reviews-analysis-table-fix-session-a-shipped-with-4-bundled-ff-cycles` — **W#2 polish P-49 W5 Reviews Analysis Table Fix Session A ✅ DEPLOYED-AND-VERIFIED 2026-05-29 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — the first of the 3-session corrective-fix plan locked 2026-05-28-b — initial build commit `8708343` + FOUR bundled fix-forward commits (FF1 `0b21c09` 8 redirects + FF2 `31c54a0` 5 redirects + FF3 `12c042c` 4 redirects + FF4 `3fbe12e` 1 redirect = 18 total redirects across 4 FFs in one Phase 4 verification day — NEW RECORD for the Same-day Phase 4 multi-redirect bundling Pattern beating the prior 3-FF max). Director Phase 4 verbatim PASS verdict on FF4: "everything passed". Q3 schema gap (`CapturedReview.title` column missing — title silently dropped at orchestrator's `saveReview` adapter) DISCOVERED mid-Fix-Session-A + DEFERRED to Fix Session B per director-approved Rule 14f picker — preserves Fix Session A's NO-schema-change scope intact while still capturing the work for Fix Session B's now-YES schema-change-in-flight entry state.**) — DEPLOY session: 5 Rule 9 deploy gates fired across initial build + FF1 + FF2 + FF3 + FF4. One-hundred-and-seventy-third Claude Code session. Closes (a.107) RECOMMENDED-NEXT = P-49 W5 Reviews Analysis Table Fix Session A ✅ DEPLOYED-AND-VERIFIED 2026-05-29 with 4 bundled fix-forward cycles; opens (a.108) RECOMMENDED-NEXT = **P-49 W5 Reviews Analysis Table Fix Session B** on `workflow-2-competition-scraping` per `docs/polish-item-specs/P-49-W5-S2-S3-competitor-reviews-analysis.md` §3 "Fix Session B" sub-section (with Q3 schema gap carry-over). **SEVEN Rule 14f forced-pickers fired this session — 7/7 = 100% Yes-to-Recommended** (1 Q10 resolution + 1 Q3 schema-gap deferral + 5 Rule 9 deploy gates); running cumulative 96/99 = 97.0% Yes-to-Recommended. **FIVE Rule 9 deploy gates fired this session.** **Schema-change-in-flight flag STAYS NO entire session.** Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **950 src/lib** / **67 routes**); Check 6 SKIPPED per Rule 27. Post-FF4 /scoreboard 5/5 GREEN at NEW LOCKED baseline: src/lib `node:test` = **984/984** (+34 net cumulative) / `npm run build` = **68 routes** (+1 NEW GET /review-analysis route in FF2) / extension `npm test` = **910/910 UNCHANGED**. **NEW baselines locked: src/lib `node:test` = 984/984 + `npm run build` = 68 routes + extension = 910/910 UNCHANGED.** ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-29 capturing FOUR observations: (1) NEW RECORD for the Same-day Phase 4 multi-redirect bundling Pattern; (2) Q3 schema-gap discovery during build is a NEW positive pattern — director-approved Rule 14f deferral preserved Fix Session A's NO-schema-change scope intact; (3) NEW reusable PATTERN memorialized: Cell-level click handlers + state-aware text affordance Pattern; (4) P-43 cwd-leak running tally ~31-33+. NEW §B 2026-05-29 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (FOURTEENTH build/deploy-session §B entry per Rule 18; SIXTH W5 entry). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED. **2 polish-item-specs MODIFIED:** `docs/polish-item-specs/P-49-W5-S2-S3-competitor-reviews-analysis.md` + `docs/polish-item-specs/P-49-W5-reviews-phase-2-master-spec.md`. NEW source-tree files shipped to production: NEW handler `src/lib/competition-scraping/handlers/review-analysis-list.ts` (FF2) + NEW shared component `src/app/projects/[projectId]/competition-scraping/components/ColumnResizeHandle.tsx` (FF1; extracted from `UrlTable.tsx`). **ZERO DEFERRED items at session end (Rule 26)** — Q3 carry-over captured into spec doc §3 + §4 not into TaskList. **~14-16 pushes total this session.** No memory files added or modified this session. Branch state at end-of-session: both `workflow-2-competition-scraping` and `origin/main` at SHA `3fbe12e` plus the end-of-session doc-batch SHA after the standard 3-push pattern. **FIFTY-FIRST end-of-session run under the Rule 30 + §4 Step 4b template.** Rule 31's mechanical read-guarantee + audit-shipped-state mandate sub-sections (added 2026-05-28-b) STAYED in place this session — auto-loaded spec docs at session start; audit of shipped state vs verbatim §1 confirmed 12 divergences still applied; Q10 resolved at session-start planning per the spec doc §4 directive.)

**Last updated in session:** session_2026-05-29_p49-w5-reviews-analysis-table-fix-session-a-shipped-with-4-bundled-ff-cycles (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-28-b (**NEW Rule 31 sub-sections drafted this session — "Mechanical read-guarantee (NEW 2026-05-28-b)" + "Audit-shipped-state mandate for in-flight workstreams (NEW 2026-05-28-b)" — both added inline under existing Rule 31 (no new top-level rule number; these are Rule 31 extensions). The mechanical-layer extension is enforced via an updated SessionStart hook (`.claude/hooks/inject-next-session-pointer.sh`) that auto-detects `P-NN` token references in the resume-flow pointer content + emits a "🔵 RULE 31 MANDATORY READ — POLISH-ITEM SPEC DOCS" block listing every matching `docs/polish-item-specs/P-NN-*.md` file. Tested working: for today's pointer content referencing P-43/P-46/P-49/P-50/P-51, the hook emits all 5 matching files (`P-49-W5-reviews-phase-2-master-spec.md` + `P-49-W5-S2-S3-competitor-reviews-analysis.md` + `P-49-W5-S4-category-page.md` + `P-49-W5-S5-type-page.md` + `P-51-comprehensive-analysis-ai-summary.md`). The audit-shipped-state mandate extension codifies a new Claude obligation at session start: when an in-flight workstream has sister surfaces already shipped (e.g., Reviews Analysis Table page + Category page + Type page are all P-49 W5), Claude MUST audit every shipped sister surface against the master verbatim spec — not just the surface the launch prompt is steering toward. Today's incident is the trigger: even with Rule 31 established yesterday, session-start scoping narrowly followed the launch-prompt's Category-page focus without independently auditing the shipped Reviews Analysis Table page on the same workstream against the verbatim spec. The new sub-section memorializes that prevention rule.**) This session (`session_2026-05-28-b_p49-w5-reviews-phase-2-master-spec-backfill-and-page-2-divergence-fix-plan` — **W#2 polish P-49 W5 Reviews Phase 2 master-spec-backfill + page-2 (Competitor Reviews Analysis Table) divergence-discovery + 3-session corrective-fix plan locked — PURE-PLANNING + DOC-ONLY SESSION on `workflow-2-competition-scraping` — NO code, NO builds, NO deploys this session**) — pure-planning + doc-batch only session: ZERO Rule 9 deploy gates fired; ZERO code changes; ZERO schema changes; ZERO new routes. One-hundred-and-seventy-second Claude Code session. Closes (a.106) RECOMMENDED-NEXT = P-49 W5 corrective rebuild Block 1 planning resume — REDIRECTED to Reviews Analysis Table master-spec-backfill + page-2 divergence-discovery + 3-session corrective-fix plan locked. Opens (a.107) RECOMMENDED-NEXT = **P-49 W5 Reviews Analysis Table Fix Session A** on `workflow-2-competition-scraping` per `docs/polish-item-specs/P-49-W5-S2-S3-competitor-reviews-analysis.md` §3 "Fix Session A" sub-section. **THREE Rule 14f forced-pickers fired this session — 8/8 = 100% Yes-to-Recommended** (1 session-shape picker + 2 AskUserQuestion clarifying-question batches comprising 4 + 3 = 7 questions); running cumulative 89/92 = 96.7% Yes-to-Recommended. **ZERO Rule 9 deploy gates fired this session.** **Schema-change-in-flight flag STAYS NO entire session.** /scoreboard NOT RUN this session — pure-doc work; Check 6 SKIPPED per Rule 27 standing precedent for pure-doc sessions; baselines UNCHANGED from W5 Session 4: root tsc clean / extension tsc clean / extension `npm test` = **910/910 UNCHANGED** / src/lib `node:test` = **950/950 UNCHANGED** / `npm run build` = **67 routes UNCHANGED**. **NEW baselines locked: UNCHANGED from W5 Session 4.** ONE NEW HIGH-importance CORRECTIONS_LOG §Entry 2026-05-28-b capturing the meta-pattern (Rule 14a Read-It-Back gap at session-start scoping for in-flight workstreams with sister surfaces; prevention via NEW Rule 31 "Audit-shipped-state mandate") + 12-divergence finding on the shipped Reviews Analysis Table page + 2 NEW reusable PATTERNS memorialized. NEW §B 2026-05-28-b entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (THIRTEENTH build/deploy-session §B entry per Rule 18). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED this session. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED this session. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. **2 NEW spec docs in `docs/polish-item-specs/`** (master + Reviews Analysis Table backfilled) + MODIFIED 2 existing spec docs (Category §5 + Type §5). NEW Rule 31 sub-sections added inline to this protocol. **ZERO DEFERRED items at session end (Rule 26)** — 7 in-session tasks; all 7 completed cleanly. **3 pushes total this session** (standard 3-push pattern). No memory files added or modified this session. Branch state at end-of-session: both `workflow-2-competition-scraping` and `origin/main` at SHA `958ccf8` (yesterday's revert commit) plus the end-of-session doc-batch SHA after the standard 3-push pattern fires. **FIFTIETH end-of-session run under the Rule 30 + §4 Step 4b template.** Rule 31's mechanical read-guarantee — the hook auto-detection of `P-NN` references — means future sessions don't have to rely on Claude remembering to read the spec docs; the SessionStart hook emits the mandatory-read block listing every matching `docs/polish-item-specs/P-NN-*.md` file based on which polish items appear in the NEXT_SESSION pointer content.)

**Last updated in session:** session_2026-05-28-b_p49-w5-reviews-phase-2-master-spec-backfill-and-page-2-divergence-fix-plan (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-28 (**NEW Rule 31 drafted this session — Polish-item spec capture (verbatim director instructions checked into `docs/polish-item-specs/<item-id>-<slug>.md` per the standardized 5-section structure); added inline at line 936.** This session (`session_2026-05-28_p49-w5-session-4-scope-misread-rollback-and-corrective-planning` — **W#2 polish P-49 W5 Session 4 — SCOPE-MISREAD ROLLBACK + CORRECTIVE-PLANNING SESSION on `workflow-2-competition-scraping` — wrong-spec build commit `5fa1f53` (8 files +2705/-54 — single combined "By Category-Type" page + card-style grouping + browser-side execution + 1 AI flow + NEW P-51 ROADMAP skeleton) shipped to vklf.com via `workflow-2-competition-scraping` → `main` and was HARD-REVERTED mid-Phase-4 via revert commit `958ccf8` (8 files +54/-2705) — director's actual spec required TWO separate pages (one Category + one Type) + flat 13-column tables + first-row-carries-label grouping + per-batch endpoint architecture + 4 AI flows + click-to-edit + drag-to-reorder + Excel export + write-back to URL detail; net live-site change = ZERO since the revert returned the site to the W5 Session 3 deploy state. NEW Rule 31 added inline to this protocol at line 936 — Polish-item spec capture: verbatim director instructions are checked into the repo at `docs/polish-item-specs/<item-id>-<slug>.md` per a standardized 5-section structure (§1 verbatim append-only / §2 joint-discussion append-only / §3 consolidated source-of-truth / §4 open questions / §5 cross-references); Rule 31 is the structural backstop for `feedback_plan_output_shape_before_building.md` — even if plan-output-shape gets shortchanged in a future session, the spec doc still carries the verbatim intent for recovery. Director's verbatim 2026-05-28 standing directive (Rule 31 source quotation): *"I want us to make a note of this huge disparity in what I had instructed and what you developed and put something in the documentation that will prevent such a thing from happening in the future in such a way that whenever I add new items in any workflow and provide instructions, you should put my original instructions in some part of the repo so that when working on the programming at any later point, you can and will access those instructions again as well. Any adjustments we make as part of our discussions should be added to that document as well."* Claude backfilled THREE NEW spec docs as the first artifact of Rule 31 — `docs/polish-item-specs/P-49-W5-S4-category-page.md` (25 KB; Category page §1 verbatim + §2 Block-0 architecture decisions + §3 consolidated spec rolled-up) + `docs/polish-item-specs/P-49-W5-S5-type-page.md` (15 KB; Type page; structurally mirrors Category) + `docs/polish-item-specs/P-51-comprehensive-analysis-ai-summary.md` (7 KB; SKELETON PLACEHOLDER per director's "Q&A at start of P-51 session" directive)**) — DEPLOY-AND-REVERT session: 2 Rule 9 deploy gates fired (build deploy + revert deploy). One-hundred-and-seventy-first Claude Code session. Closes (a.105) RECOMMENDED-NEXT = P-49 W5 Session 4 wrong-spec build SHIPPED + HARD-REVERTED; opens (a.106) RECOMMENDED-NEXT = **P-49 W5 corrective rebuild — Block 1 planning resume (answer the 6 open questions) + (likely) Session 1 Category page scaffold per `docs/polish-item-specs/P-49-W5-S4-category-page.md` §3** on `workflow-2-competition-scraping`. **THREE Rule 14f forced-pickers fired this session — 3/3 = 100% Yes-to-Recommended** (session-start scope picker + Rule 9 deploy gate picker + post-Phase-4-discovery rollback picker); running cumulative 82/85 = 96.5% Yes-to-Recommended. **TWO Rule 9 deploy gates fired this session** (build deploy of `5fa1f53` + hard-revert deploy of `958ccf8`). **Schema-change-in-flight flag STAYS NO entire session.** Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **950 src/lib** / **67 routes**); mid-session (post-build `5fa1f53`) **968 src/lib (+18)** / **68 routes (+1 — new by-category-type page)**; post-revert /scoreboard 5/5 GREEN at LOCKED baselines UNCHANGED from W5 Session 3 exit (since the build was hard-reverted): root tsc clean / extension tsc clean / extension `npm test` = **910/910 UNCHANGED** / src/lib `node:test` = **950/950** / `npm run build` = **67 routes**; Check 6 Playwright SKIPPED per Rule 27. **NEW baselines locked: UNCHANGED from W5 Session 3 exit.** ONE NEW HIGH-importance CORRECTIONS_LOG §Entry 2026-05-28 capturing the scope-misread incident (with the divergence-table reproduced verbatim) + root cause (RE-VIOLATION of `feedback_plan_output_shape_before_building.md`) + wrong-spec shipped + reverted artifacts + NEW Rule 31 + 3 backfilled spec docs + 5-session corrective rebuild plan + 6 open Block 1 questions paused mid-way + 3 sub-observations. NEW §B 2026-05-28 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (TWELFTH build/deploy-session §B entry per Rule 18). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED this session. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED this session. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. NEW directory `docs/polish-item-specs/` with 3 NEW spec docs. **ZERO DEFERRED items at session end (Rule 26)** — 7 in-session tasks; all 7 completed cleanly. **8 pushes total this session** (1 pre-deploy + 1 deploy + 1 ping-pong + 1 revert + 1 ping-pong + 3 doc-batch). No memory files added or modified this session (Rule 31 + the spec-doc mechanism replace the need for a new memory file — the structural backstop lives in the protocol, not in a feedback memory). Branch state at end-of-session: both `workflow-2-competition-scraping` and `origin/main` at SHA `958ccf8` (the revert commit) plus the end-of-session doc-batch SHA after the standard 3-push pattern fires. **FORTY-NINTH end-of-session run under the Rule 30 + §4 Step 4b template.** Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged this session; the ONE new rule (Rule 31) adds a new mandate but does not change existing Step 7b mechanics — Rule 31's session-start enforcement happens via the existing Step 7b plain-terms-summary mechanism (the §3 read happens at the same point).)
**Previously updated in session:** session_2026-05-28_p49-w5-session-4-scope-misread-rollback-and-corrective-planning (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-27-c (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-27-c_p49-w5-session-3-per-competitor-deploy-and-2-ff-cycles` — **W#2 polish P-49 W5 Session 3 — Per-Competitor Comprehensive (bulleted) flow ✅ DEPLOYED-AND-VERIFIED 2026-05-27-c end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — first of 3 remaining aggregation flows from §B 2026-05-27 design lock — initial build commit `b9d232e` (10 files +1481/-45) + 2 fix-forward commits (FF#1 `1cd6e3b` bundling 4 Phase 4 redirects — v1→v2 critique-only prompt with 4 fixed theme headings + NEW global Summarize-all button + NEW `GlobalCompetitorSummarizeModal.tsx` 606 LOC + Table 2 button rename + Edit affordance with NEW PATCH endpoint + NEW `review-analysis-update.ts` handler + wire-shape extension carrying `analysisId` / FF#2 `7f19aca` bundling 2 Phase 4 redirects — v2→v3 theme-emergent prompt + 9 example emergent themes + PROMPT_VERSION bump v2→v3 + explicit "✅ AI Review Summarizing job complete" banner in both modals) all ff-merged to main under 3 Rule 9 deploy gates within ONE Phase 4 verification day**) — DEPLOY session: 3 Rule 9 deploy gates fired. One-hundred-and-seventieth Claude Code session. Closes (a.104) RECOMMENDED-NEXT = P-49 W5 Session 3 Per-Competitor flow ✅ DEPLOYED-AND-VERIFIED 2026-05-27-c; opens (a.105) RECOMMENDED-NEXT = **P-49 W5 Session 4 — Per-Category Comprehensive (bulleted) flow + NEW "By Category-Type" page (Table 3 scaffold)** on `workflow-2-competition-scraping`. **EIGHT Rule 14f forced-pickers fired this session — 8/8 = 100% Yes-to-Recommended** (variant scope + build order + which flow first + bullet structure + length range + input strategy + deploy-now + end-session); running cumulative 78/81 = 96.3% Yes-to-Recommended. **THREE Rule 9 deploy gates fired this session** (initial build deploy + FF#1 bundled-4-redirect deploy + FF#2 bundled-2-redirect deploy); per `feedback_approval_scope_per_decision_unit.md`, the session-start deploy-or-continue picker approved the whole Phase 4 redirect cycle. **Schema-change-in-flight flag STAYS NO entire session.** Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **922 src/lib** / **66 routes**); Check 6 Playwright SKIPPED per Rule 27. Post-FF#2 /scoreboard 5/5 GREEN at NEW LOCKED baseline: src/lib `node:test` = **950/950** (+28 cumulative from 922 entry); `npm run build` = **67 routes** (+1 from 66 — new PATCH endpoint); extension `npm test` = **910/910 UNCHANGED**. **NEW baselines locked: src/lib `node:test` = 950/950 + `npm run build` = 67 routes + extension = 910/910 UNCHANGED.** ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-27-c capturing THREE NEW reusable Patterns ("Same-day Phase 4 multi-redirect bundling Pattern" + "Edit affordance for cached AI output Pattern" + "Test stub level-discriminator filtering Pattern") + P-43 cwd-leak reproduction running tally ~26+. NEW §B 2026-05-27-c entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (ELEVENTH build/deploy-session §B entry per Rule 18). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED this session. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED this session. **ZERO DEFERRED items at session end (Rule 26).** **6 pushes total this session** (3 deploy + 3 ping-pong pairs). No memory files added or modified this session. Branch state at end-of-session: both `workflow-2-competition-scraping` and `origin/main` at SHA `7f19aca` (the FF#2 deploy push completion) plus the end-of-session doc-batch SHA after the standard 3-push pattern fires. **FORTY-EIGHTH end-of-session run under the Rule 30 + §4 Step 4b template.** Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Previously updated in session:** session_2026-05-27-c_p49-w5-session-3-per-competitor-deploy-and-2-ff-cycles (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-27-b (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-27-b_p49-w5-session-2-deploy-and-catchup-doc-batch` — **RETROACTIVE catch-up doc-batch for an unrecorded same-day Claude session (the 168th session) that ran W#2 polish P-49 W5 Session 2 per-batch endpoint scaffold + Per-Review Summarize end-to-end ✅ DEPLOYED-AND-VERIFIED 2026-05-27 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — initial build commit `60609f6` (12 files +2975/-1267) + 3 fix-forward commits (FF#1 `ecf292d` Table 2 response-shape unwrap / FF#2 `d713712` in-modal View prompts panel for AI transparency / FF#3 `cd6478b` v2 bulleted-critical prompt + cache-key version bump + Table 2 whiteSpace pre-wrap) all ff-merged to main under 4 Rule 9 deploy gates within ONE Phase 4 verification day**) — catch-up doc-batch session: the prior Claude session ended without running the canonical end-of-session §4 Step 1 doc-batch protocol; this catch-up session executes the missing doc-batch + push only; ZERO code changes this session. One-hundred-and-sixty-ninth Claude Code session (this catch-up doc-batch); the prior unrecorded W5 Session 2 + 3 FF deploy session was the one-hundred-and-sixty-eighth. Closes (a.103) RECOMMENDED-NEXT = P-49 W5 Session 2 ✅ DEPLOYED-AND-VERIFIED 2026-05-27; opens (a.104) RECOMMENDED-NEXT = **P-49 W5 Session 3 — rewrite remaining 6 of 7 flow builders into `prompts.ts` + start Tables 3 + 4 (By Category + By Type) + add their AI run buttons + modals + browser batch loops + first live e2e runs on those 6 flows** on `workflow-2-competition-scraping`. **TWO Rule 14f forced-pickers fired in THIS catch-up doc-batch session — 2/2 = 100% Yes-to-Recommended on the pickers answered** (initial "catch-up vs skip vs other" picker + §4 Step 1c next-task picker for W5 Session 3). **For the underlying W5 Session 2 work that shipped this morning, calibration data is unrecoverable** (no record of which pickers fired in that unrecorded session) — flagged as a process-gap signal in CORRECTIONS_LOG §Entry 2026-05-27-b. **FOUR Rule 9 deploy gates fired in the unrecorded W5 Session 2 session** (initial deploy + FF#1 deploy + FF#2 deploy + FF#3 deploy); ZERO Rule 9 deploy gates fired in this catch-up doc-batch session itself (doc batch + push only). **Schema-change-in-flight flag transitions:** entry of the unrecorded W5 Session 2 session = YES; FLIPPED YES → NO at the W5 Session 2 initial deploy push completion (canonical schema-change-ships-to-production transition); STAYS NO through the 3 FFs; final state at the catch-up doc-batch entry = NO. Just-run /scoreboard 5/5 GREEN (post-FF#3 working tree at catch-up doc-batch entry): root tsc clean / extension tsc clean / extension `npm test` = **910/910 UNCHANGED** / src/lib `node:test` = **922/922** (+16 net from W5 Session 1.5's 906 entry) / `npm run build` = **66 routes** (+1 new Table 2 page; new run-batch route replaced deleted run route net 0); Check 6 Playwright SKIPPED per Rule 27. **NEW baselines locked: src/lib `node:test` = 922/922 + `npm run build` = 66 routes + extension = 910/910 UNCHANGED.** **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-27-b** capturing the process-gap meta-pattern (prior session ended without running the §4 Step 1 doc-batch protocol — captured as a positive learning opportunity not a top-tier slip) + the actual W5 Session 2 build + 3 FF outcomes summarized from commit messages + THREE NEW reusable Patterns (v1 → v2 prompt-version-bump + cache-key-versioning + in-modal View prompts panel UI transparency + Rule 14f scope-narrowing) + P-43 cwd-leak reproduction during /scoreboard run today (running tally now ~24+) + operational notes on UNCHANGED Group B docs. NEW §B 2026-05-27-b entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (TENTH build/deploy-session §B entry per Rule 18; covers BOTH the prior unrecorded session's W5 Session 2 build + deploy + 3 FF cascade AND this catch-up doc-batch; supersedes §A.13 prompt content + the §B 2026-05-27 line-1474 reference to "7 v1 prompts locked during planning" — the v2 bulleted-critical format shipped today replaces both). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED this session (W5 is PLOS-side only). `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED this session. **ZERO DEFERRED items at session end (Rule 26).** **ONE push planned at end of this catch-up doc-batch session** per `feedback_approval_scope_per_decision_unit.md` — workflow-branch push carrying the doc-batch commit only; parent Claude will then run /deploy to ff-merge the doc-batch to main. **No fresh extension zips this session** (doc-batch only). Branch state at end-of-session: `workflow-2-competition-scraping` = `cd6478b` (matches `main`) + the new doc-batch commit = 1 commit ahead of main pending the parent's /deploy. **FORTY-SEVENTH end-of-session run under the Rule 30 + §4 Step 4b template.** Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Previously updated in session:** session_2026-05-27-b_p49-w5-session-2-deploy-and-catchup-doc-batch (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-27 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-27_p49-w5-session-1.5-reviews-phase-3-design-lock` — **W#2 polish P-49 Reviews Phase 3 design lock + schema enum extension + shared ExecutionModeSelect component SHIPPED at code level via build commit `252e1dd` on `workflow-2-competition-scraping` — W5 Session 1.5 director-driven planning conversation locked the 3-table + 7-flow + browser-first execution + 7 v1 prompts + 4-option toggle expansion across ~20 Rule 14f forced-pickers (19/19 = 100% Yes-to-Recommended on pickers answered + 2 free-text redirects); per-batch server endpoint scaffold + `prompts.ts` rewrite + Table 2 page + first live end-to-end run DEFERRED to next W5 session (a.103) in a deliberately narrow code-mechanics scope cut** — pure BUILD session; NO production deploy; ZERO Rule 9 deploy gates fired this session; build commit stays on workflow branch until next W5 deploy session. Build commit `252e1dd` (6 files +181/-10) carrying MODIFY `prisma/schema.prisma` (`ReviewAnalysisLevel` enum extended with PER_REVIEW + PER_CATEGORY) + MODIFY `src/lib/shared-types/competition-scraping.ts` (union widened) + MODIFY `src/lib/competition-scraping/handlers/review-analysis-run.ts` (row type widened) + NEW `src/lib/workflow-components/execution-mode.ts` + NEW `src/lib/workflow-components/execution-mode-select.tsx` + NEW `src/lib/workflow-components/execution-mode.test.ts` (7 node:test cases). One-hundred-and-sixty-seventh Claude Code session. Closes (a.102) RECOMMENDED-NEXT = P-49 W5 Session 1.5 director-driven output-shape planning ✅ DONE 2026-05-27; opens (a.103) RECOMMENDED-NEXT = **P-49 W5 Session 2 — per-batch server endpoint scaffold + delete shipped per-product two-sweep handler + rewrite `prompts.ts` with 7 flow-specific builders + start Table 2 page + Per-Review Summarize button + modal + browser batch loop + first live end-to-end run** on `workflow-2-competition-scraping`. **~20 Rule 14f forced-pickers fired this session — 19/19 = 100% Yes-to-Recommended on pickers answered + 2 free-text redirects** (positive calibration data points for Rule 14f's free-text escape-hatch design: Round 2 row-structure picker REDIRECTED dropping Stars + Reviews Summary columns from Tables 3 + 4 + Round 3 cost-caps picker REDIRECTED removing all cost caps for transparency-only). **ZERO Rule 9 deploy gates fired this session** (pure BUILD session). **Schema-change-in-flight flag FLIPPED NO → YES mid-session at `npx prisma db push` completion** (PER_REVIEW + PER_CATEGORY enum values added to `ReviewAnalysisLevel`; additive; zero data loss); STAYS YES through end of session (until next W5 deploy session ff-merges to main + Vercel auto-redeploys). Pre-build /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **899 src/lib** / **65 routes**); Check 6 Playwright SKIPPED per Rule 27. Post-build /scoreboard 5/5 GREEN at new src/lib baseline: src/lib `node:test` = **906/906** (+7 — exact match with 7 new cases in execution-mode.test.ts); npm run build = **65 routes UNCHANGED**; extension `npm test` = **910/910 UNCHANGED**; Check 6 SKIPPED per Rule 27. **NEW baseline locked: src/lib `node:test` = 906/906** (+7 from 899 entry baseline). **ONE NEW HIGH-importance CORRECTIONS_LOG §Entry 2026-05-27** capturing FOUR sub-observations including the **NEW reusable Pattern memorialized via NEW memory file `feedback_browser_first_ai_with_server_migration.md`** — "Default new AI batch flows to browser-side execution; add execution-mode dropdown now for seamless future server-side migration; mirror dropdown into W#1's existing modal at the same time so both workflows can migrate together" + Rule 14f free-text escape-hatch calibration data point (2 redirects this session both produced superior outcomes) + P-43 cwd-leak Pattern Class reproduced ~2 more times this session (running tally now ~22+) + Reviews Phase 3 design lock supersedes REVIEWS_PHASE_2_DESIGN.md §A.10/§A.11/§A.12 substantially (§A frozen per Rule 18; locked decisions captured in §B 2026-05-27). NEW §B 2026-05-27 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (NINTH build/deploy-session §B entry per Rule 18; FIRST W5 Session 1.5 entry). NEW §B 2026-05-27 cross-reference pointer entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md`. `docs/COMPETITION_DATA_V2_DESIGN.md` + `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED this session. NEW memory file `feedback_browser_first_ai_with_server_migration.md` + MODIFY `MEMORY.md` adding one-line pointer. **ZERO DEFERRED items at session end (Rule 26)** after the (a.103) capture lands in NEXT_SESSION.md — 14 tasks created; 8 completed; 4 marked `DEFERRED:` with destinations noted; PENDING REVIEWS_PHASE_2_DESIGN.md §B update handled by this doc-batch. **ONE push this session per `feedback_approval_scope_per_decision_unit.md` build-session push pattern:** workflow-branch push carrying BOTH build commit `252e1dd` + end-of-session doc-batch commit; NO ff-merge to main. **No fresh extension zips this session** (build session — no extension changes; W5 Session 1.5 is PLOS-side only). Branch state at end-of-session: `workflow-2-competition-scraping` = `origin/main` + 4 commits ahead (prior session's build `04f74cf` + prior session's doc-batch `25b1a3a` + this session's build `252e1dd` + end-of-session doc-batch SHA). **FORTY-SIXTH end-of-session run under the Rule 30 + §4 Step 4b template.** The 3 plain-terms sections at top of NEXT_SESSION.md + the parent's Personalized Handoff continue per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Previously updated in session:** session_2026-05-27_p49-w5-session-1.5-reviews-phase-3-design-lock (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-06-02 (**Header bump only — no new rules drafted this session.** This session (`session_2026-06-02_p49-w5-session-1-foundation-shipped-output-shape-deferred` — **W#2 polish P-49 Reviews Phase 2 Workstream 5 AI review analysis Session 1 — foundation primitives + plumbing handler SHIPPED at code level via build commit `04f74cf` on `workflow-2-competition-scraping`; v1 prompt content + UI placement re-confirmation + first live end-to-end run DEFERRED to next session (a.102) per director's explicit mid-session directive to plan the per-product analysis output shape FIRST before writing prompt code** — pure BUILD session; NO production deploy; ZERO Rule 9 deploy gates fired this session; build commit stays on workflow branch until W5 deploy session. Build commit `04f74cf` (19 files +2968/-1) carrying NEW `src/lib/competition-scraping/review-analysis/` directory with 7 modules (`client.ts` Anthropic SDK seam + supported-model registry + `pricing.ts` per-model cost math + `cache.ts` SHA-256 reviewsHash + staleness check per §A.12 + `cost-cap.ts` preventive per-run + per-Project monthly caps per §A.7 + `token-counter.ts` pre-flight messages.countTokens wrapper + char/3.6 heuristic + `batch-sizer.ts` adaptive batching to ~80K-token budget per §A.8 + `prompts.ts` v1 per-product prompts PLACEHOLDER will be rewritten next session) + 6 test files (~47 new node:test cases across the primitives) + NEW `src/lib/competition-scraping/handlers/review-analysis-run.ts` (~430 LOC DI-seam POST handler orchestrating auth → load → hash → cache-check → batch → pre-flight cost estimate → cost-cap gate → two-sweep call → parse-JSON → persist ReviewAnalysis row) + NEW `review-analysis-run.test.ts` (~14 cases) + NEW thin API route shim at `src/app/api/projects/[projectId]/competition-scraping/review-analysis/run/route.ts` + NEW `scripts/test-w5-end-to-end.mjs` (--dry-run plumbing test PASSED this session; --live mode DEFERRED to next session) + MODIFY `package.json` + `package-lock.json` for NEW dependency `@anthropic-ai/sdk ^0.98.1`. One-hundred-and-sixty-sixth Claude Code session. Closes (a.101) PARTIALLY (foundation primitives + plumbing handler + thin API route shim + 61 node:test cases + standalone e2e script all shipped at code level via `04f74cf`); opens (a.102) RECOMMENDED-NEXT = **P-49 W5 Session 1.5 — director-driven planning of the per-product analysis output shape** on `workflow-2-competition-scraping` per the new memory file `feedback_plan_output_shape_before_building.md`. **TWO Rule 14f forced-pickers fired this session** — picker #1 = W5 architecture-decision picker (SDK + cost-cap + token-counter Recommended over raw-fetch-mirror-W#1; director picked Recommended) → **1/1 = 100% on the picker that was answered**; picker #2 = "Fire live end-to-end run now?" REDIRECTED by director with verbatim "i want you to wrap this session and begin the next session by you asking me for the suggestions i have for the review analysis function" — picker REDIRECTED by free-text escape-hatch rather than chosen-from-options (positive calibration data point for Rule 14f free-text escape-hatch design). **ZERO Rule 9 deploy gates fired this session** (pure BUILD session; build commit stays on workflow branch until W5 deploy session). **Schema-change-in-flight flag STAYS NO entire session** (entry NO + exit NO; `ReviewAnalysis` model + `ReviewAnalysisLevel` enum schema already shipped via W2 Amazon Session 1's `prisma db push` 2026-05-26 + deployed 2026-05-28). Pre-build /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **838 src/lib** / **64 routes**); Check 6 Playwright SKIPPED per Rule 27. Post-build /scoreboard 5/5 GREEN at new src/lib baseline: src/lib `npm test` = **899/899** (+61 cumulative from 838); npm run build = **65 routes** (+1 for new review-analysis/run shim); extension `npm test` = **910/910** UNCHANGED; Check 6 SKIPPED per Rule 27. **NEW baseline locked: src/lib `npm test` = 899/899** (+61 from 838 entry baseline). **ONE NEW HIGH-importance CORRECTIONS_LOG §Entry 2026-06-02** capturing FIVE sub-observations including the **NEW reusable Pattern memorialized via NEW memory file `feedback_plan_output_shape_before_building.md`** — "Plan AI-output shape (prompts + UI placement) with director BEFORE writing prompt code, even when launch prompt says ship-and-iterate" + W5-vs-W#1 architecture divergence on AI-infrastructure axis + P-43 cwd-leak Pattern Class reproduced ~1-2 more times this session (running tally now ~20+) + Rule 14f free-text escape-hatch calibration data point + session-letter date convention plain (no letter suffix). NEW §B 2026-06-02 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (EIGHTH build/deploy-session §B entry per Rule 18; FIRST W5 entry; captures foundation primitives + plumbing handler shipped at code level + v1 prompt content + UI placement + first live e2e run all DEFERRED to (a.102) per director's mid-session directive + NEW reusable Pattern memorialized + architecture divergence from W#1 + 61 new src/lib tests + 1 new route shim). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED (W5 is PLOS-side AI infrastructure, not extension-side; no cross-reference pointer needed). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED (W5 is AI analysis layer, distinct from W#2 Competition Data table design layer). **ZERO DEFERRED items at session end (Rule 26)** after the (a.102) capture lands in NEXT_SESSION.md — W5 Session 1.5 IS the next-session task per (a.102), not a Claude-defer. **ONE push this session per `feedback_approval_scope_per_decision_unit.md` build-session push pattern:** workflow-branch push carrying BOTH build commit `04f74cf` + end-of-session doc-batch commit; NO ff-merge to main this session. **No fresh extension zips this session** (build session — no extension changes; W5 is PLOS-side only). **NEW memory file outside the repo** at `feedback_plan_output_shape_before_building.md` + MODIFY `MEMORY.md` adding one-line pointer; auto-mirrored to `.codespace-backup/memory/` via PostToolUse hook per Rule 29. Branch state at end-of-session: `workflow-2-competition-scraping` **1 commit ahead of `origin/main`** (build commit `04f74cf`) + end-of-session doc-batch commit (about to land) = expected 2 commits ahead of main after doc-batch push. **FORTY-FIFTH end-of-session run under the Rule 30 + §4 Step 4b template.** The 3 plain-terms sections at top of NEXT_SESSION.md + the parent's Personalized Handoff continue per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Previously updated in session:** session_2026-06-02_p49-w5-session-1-foundation-shipped-output-shape-deferred (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-06-01 (**Header bump only — no new rules drafted this session.** This session (`session_2026-06-01_p49-w2-walmart-sub-cluster-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Walmart sub-cluster ✅ DEPLOYED-AND-VERIFIED 2026-06-01 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — build + deploy + 3-fix-forward cascade + Phase-4-PASS bundled into ONE calendar day. **CLOSES the entire P-49 W2 per-platform extension extraction arc** — all 4 platforms (Amazon 2026-05-28 + eBay 2026-05-30 + Etsy 2026-05-31 + Walmart 2026-06-01) now ✅ DEPLOYED-AND-VERIFIED end-to-end on vklf.com. **UNBLOCKS P-49 W5 AI review analysis Session 1** per (a.101). Session 1 build commit `3316eaa` (3 files +1625/-5) carrying NEW `walmart-review-extractor.ts` (~530 LOC URL-construction) + NEW `walmart-review-extractor.test.ts` (~660 LOC; 88 new node:test cases) + MODIFY `orchestrator.ts` (Walmart dispatch) ff-merged under Rule 9 deploy gate; Vercel auto-redeploy fired. Phase 4 surfaced `LISTING_PAGE_PATH` regex too restrictive (Walmart serves BOTH `/ip/<id>` and `/ip/<slug>/<id>`) → **FF#1 `3321690`** (2 files +60/-1; +6 tests) loosen regex from director's URL paste → FF#1 BUSTED in row-walker phase (all 14 candidate selectors returned 0) → **Rule 14f mid-Phase-4 picker rerouted to diagnostic-instrumentation FF#2 `c953e71`** (1 file +101/-1; TEMPORARY `[PLOS WALMART DIAGNOSTIC]` console.log + auto-download of first star + first page HTML to Downloads; reused 2026-05-30 eBay FF#4 + 2026-05-31 Etsy FF#2 Pattern THIRD consecutive day) → **FF#3 `86cbfbd` empirical fix — the WIN** (2 files +379/-403; anchor on per-review body `data-testid="enhanced-review-content"` + `closest('.overflow-visible')` walk-up + `<span class="ld_Ec">` screen-reader-only star + `<h3>` null-when-missing title + `<p>` body + `aria-label` reviewer name + word-boundary regex `/\b(review|purchase|rating|upvote|downvote)\b/i` filter + `.f7.gray` date + removed `findWalmartReviewsContainer` + removed all FF#2 instrumentation + test file rewritten); director Phase 4 PASS verdict on FF#3 "Everything passed" — third consecutive day the Diagnostic-instrumentation FF Pattern delivered a PASS verdict on the empirical-fix FF. One-hundred-and-sixty-fifth Claude Code session. Closes (a.100) RECOMMENDED-NEXT = P-49 W2 Walmart sub-cluster Session 1 ✅ DEPLOYED-AND-VERIFIED 2026-06-01; opens (a.101) RECOMMENDED-NEXT = **P-49 W5 AI review analysis Session 1** on `workflow-2-competition-scraping` per §C.5 outline. **SEVEN Rule 14f forced-pickers fired this session — all director-Yes-to-Recommended (7/7 = 100% calibration; running cumulative 50/53 = 94.3% across recent 8 sessions):** start-of-session deploy-now-vs-exit picker (THIRD consecutive day) + build-deploy gate + FF#1 deploy gate + mid-Phase-4 diagnostic-vs-defer picker (Recommended Diagnostic instrumentation directly produced FF#3's empirical one-shot fix) + FF#2 diagnostic deploy gate + FF#3 fix deploy gate + end-of-session §4 Step 1c next-task picker (W5 Recommended over P-43 / P-48 / P-50). **FOUR Rule 9 deploy gates fired total this session** (build deploy + FF#1 deploy + FF#2 diagnostic deploy + FF#3 fix deploy — same count as yesterday's Etsy 3-FF). **Schema-change-in-flight flag STAYS NO entire session** (Walmart reuses W2 Amazon schema shipped 2026-05-28). Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **818 ext** / **838 src/lib** / **64 routes**); Check 6 Playwright SKIPPED per Rule 27. Post-FF#3 /scoreboard 5/5 GREEN at new ext baseline: extension `npm test` = **910/910** (+92 cumulative from 818); src/lib 838 UNCHANGED; routes 64 UNCHANGED. **NEW baseline locked: extension `npm test` = 910/910**. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-01** capturing THREE NEW reusable Patterns ("Diagnostic-instrumentation FF Pattern reusability validated across 3 consecutive days/platforms" + "Word-boundary regex for reserved-keyword filtering against natural-language strings" + "closest() walk-up + anchor-on-canonical-data-testid Pattern") + 3 LOW informational sub-observations (P-43 cwd-leak Pattern Class reproduction running tally ~19+ / calibration 7/7 = 100% / §C.2 estimate compression memorialized for THIRD consecutive day). NEW §B 2026-06-01 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (SEVENTH build/deploy-session §B entry per Rule 18; CLOSES the entire P-49 W2 4-platform arc). NEW §B 2026-06-01 cross-reference pointer entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md`. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED. **ZERO DEFERRED items at session end (Rule 26)** — Walmart sub-cluster fully ✅ DEPLOYED-AND-VERIFIED with no remaining scope; entire P-49 W2 4-platform arc COMPLETE. **~10 pushes this session per `feedback_approval_scope_per_decision_unit.md`:** 4 deploy pushes + 4 ping-pong pushes + end-of-session post-deploy doc-batch push + ff-merge push. **5 fresh extension zips at repo root** + 3 debug HTML files from director's diagnostic capture. Branch state at end-of-session: `workflow-2-competition-scraping` even with `origin/main` at `86cbfbd` plus the end-of-session post-deploy doc-batch SHA after ff-merge. **FORTY-FOURTH end-of-session run under the Rule 30 + §4 Step 4b template.** The 3 plain-terms sections at top of NEXT_SESSION.md + the parent's Personalized Handoff continue per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Previously updated in session:** session_2026-06-01_p49-w2-walmart-sub-cluster-session-1 (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-31 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-31_p49-w2-etsy-sub-cluster-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Etsy sub-cluster ✅ DEPLOYED-AND-VERIFIED 2026-05-31 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — build + deploy + 3-fix-forward cascade + Phase-4-PASS bundled into ONE calendar day. Session 1 build commit `c572a42` (3 files +1509/-3) carrying NEW `extensions/competition-scraping/src/lib/content-script/etsy-review-extractor.ts` (~565 LOC; initial URL-construction approach using `buildEtsyReviewUrl` + `fetch+DOMParser`) + NEW `etsy-review-extractor.test.ts` (~580 LOC; 72 new node:test cases) + MODIFY `orchestrator.ts` (Etsy dispatch) ff-merged to main under Rule 9 deploy gate; Vercel auto-redeploy fired; Phase 4 director real-Chrome verification surfaced that the URL-construction approach was BUSTED — Etsy's `View all reviews for this item` overlay loads via AJAX on the same listing URL with NO separate URL for filters or pagination. **Architecture pivot via FF#1 `67aeacd`** (2 files +1058/-705) rewrote etsy-review-extractor from URL-construction to live-DOM driver (click View-all-reviews button → wait for overlay → click histogram filter → wait for AJAX content swap → walk rows → click pagination Next → wait → repeat) based on director's paste of the 41 KB overlay outerHTML. FF#1 BUSTED silently → **Rule 14f mid-Phase-4 picker rerouted to diagnostic-instrumentation FF#2 `a3107b6`** (1 file +163/-2; TEMPORARY `[PLOS ETSY DIAGNOSTIC]` console.log + auto-download of overlay HTML to Downloads via programmatic `<a download>`; reused yesterday's eBay FF#4 Pattern) → **FF#3 `41b03c5` empirical fix — the WIN** (2 files +73/-190; restrict `findOverlayContainer` to `.deep-dive-sheet` only + reject hidden variants [aria-hidden, wt-display-none, hidden attribute] + remove FF#2 instrumentation); director Phase 4 PASS verdict on FF#3 "Everything worked perfectly this time" (identical phrasing to yesterday's eBay FF#5 PASS). One-hundred-and-sixty-fourth Claude Code session. Closes (a.99) RECOMMENDED-NEXT = P-49 W2 Etsy sub-cluster Session 1 ✅ DEPLOYED-AND-VERIFIED 2026-05-31; opens (a.100) RECOMMENDED-NEXT = P-49 W2 Walmart sub-cluster Session 1 on `workflow-2-competition-scraping` per §A.2 priority order (Walmart fourth + FINAL per-platform sub-cluster). **EIGHT Rule 14f forced-pickers fired this session — all director-Yes-to-Recommended (8/8 = 100% calibration; running cumulative 35/38 = 92.1% across recent 6 sessions):** start-of-session deploy-now-vs-exit picker + build-deploy gate + mid-Phase-4 evidence-gathering picker + FF#1 deploy gate + mid-Phase-4 diagnostic-vs-defer picker (Recommended Diagnostic instrumentation directly produced FF#3's empirical one-shot fix) + FF#2 diagnostic deploy gate + FF#3 fix deploy gate + end-of-session §4 Step 1c next-task picker (Walmart Recommended over P-43 / W5 / P-48). **FOUR Rule 9 deploy gates fired total this session** (build deploy + FF#1 deploy + FF#2 diagnostic deploy + FF#3 fix deploy — one fewer than yesterday's eBay 5-FF; Diagnostic-instrumentation FF Pattern enabled FEWER speculative FFs this time). **Schema-change-in-flight flag STAYS NO entire session** (Etsy reuses W2 Amazon schema shipped 2026-05-28; `'etsy'` discriminator already accepted by `CapturedReview.platform String?` column from W2 Session 1). Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **731 ext** / **838 src/lib** / **64 routes**); Check 6 Playwright SKIPPED per Rule 27. Post-FF#3 /scoreboard 5/5 GREEN at new ext baseline: extension `npm test` = **818/818** (+87 cumulative from 731); src/lib 838 UNCHANGED; routes 64 UNCHANGED. **NEW baseline locked: extension `npm test` = 818/818**. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-31** capturing THREE NEW reusable Patterns ("Diagnostic-instrumentation FF Pattern reusability validated across consecutive sessions" + "Over-broad fallback selectors in platform DOM-walkers should be DELETED, not added" + "AJAX-loaded overlay scraping requires live-DOM driver, not URL-construction") + 3 LOW informational sub-observations (P-43 cwd-leak Pattern Class reproduction running tally ~16+ / calibration 8/8 = 100% / §C.2 estimate compression). NEW §B 2026-05-31 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (SIXTH build/deploy-session §B entry per Rule 18). NEW §B 2026-05-31 cross-reference pointer entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md`. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED. **ZERO DEFERRED items at session end (Rule 26)** — Etsy sub-cluster fully ✅ DEPLOYED-AND-VERIFIED with no remaining scope (§C.2 estimate "~2-3 sessions" compressed into "1 calendar day"; Etsy Sessions 2 + 3 from §C.2 CLOSED). **~10 pushes this session per `feedback_approval_scope_per_decision_unit.md`:** 4 deploy pushes + 4 ping-pong pushes + end-of-session post-deploy doc-batch push + ff-merge push. **4 fresh extension zips at repo root** (initial build + FF#1 live-DOM + FF#2 diagnostic + FF#3 WIN) + 1 debug HTML from director's diagnostic capture. Branch state at end-of-session: `workflow-2-competition-scraping` even with `origin/main` at `41b03c5` plus the end-of-session post-deploy doc-batch SHA after ff-merge. **FORTY-THIRD end-of-session run under the Rule 30 + §4 Step 4b template.** The 3 plain-terms sections at top of NEXT_SESSION.md + the parent's Personalized Handoff continue per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Previously updated in session:** session_2026-05-31_p49-w2-etsy-sub-cluster-session-1 (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-30 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-30_p49-w2-ebay-sub-cluster-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 eBay sub-cluster ✅ DEPLOYED-AND-VERIFIED 2026-05-30 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — build + deploy + 5-fix-forward cascade + Phase-4-PASS bundled into ONE calendar day. Session 1 build commit `11e99e1` (3 files +1535/-99) carrying NEW `extensions/competition-scraping/src/lib/content-script/ebay-review-extractor.ts` (~390 LOC; per-platform module mirroring Amazon Patterns) + NEW `ebay-review-extractor.test.ts` (~470 LOC; 64 new node:test cases) + MODIFY `orchestrator.ts` (eBay dispatch) ff-merged to main under Rule 9 deploy gate; Vercel auto-redeploy fired; Phase 4 director real-Chrome verification surfaced TWO co-occurring bugs (seller auto-detect failing + This Item filter returning All Items rows) that survived THREE speculative fix-forwards (FF#1+#2 bundled `23b6221` + FF#3 `c19f187`) before Rule 14f mid-Phase-4 picker rerouted to **diagnostic-instrumentation FF#4 `6675963`** (TEMPORARY HTML-dump + console.log probes) → **FF#5 `23aa851` empirically-verified fix — the WIN** (3 files +176/-135 carrying `extractSellerFromListingHtml(html)` regex against `"sellerUserName"` JSON data island + `[role=tabpanel]:not([hidden])` walker scope + remove FF#4 instrumentation + 8 new test cases); director Phase 4 PASS verdict "Everything worked perfectly". One-hundred-and-sixty-third Claude Code session. Closes (a.98) RECOMMENDED-NEXT = P-49 W2 eBay sub-cluster Session 1 ✅ DEPLOYED-AND-VERIFIED 2026-05-30; opens (a.99) RECOMMENDED-NEXT = P-49 W2 Etsy sub-cluster Session 1 on `workflow-2-competition-scraping` per §A.2 priority order + §C.2 outline. **THREE Rule 14f forced-pickers fired this session — all director-Yes-to-Recommended (3/3 = 100% calibration; running cumulative 27/30 = 90% across recent 5 sessions):** start-of-session deploy-now picker + mid-Phase-4 diagnostic-vs-defer picker (Recommended Diagnostic instrumentation directly produced FF#5's empirical one-shot fix) + end-of-session §4 Step 1c next-task picker (Etsy Recommended over W5 / P-43 / P-48). **FIVE Rule 9 deploy gates fired total this session** (build deploy + FF#1+#2 bundled deploy + FF#3 deploy + FF#4 diagnostic deploy + FF#5 fix deploy — stretches 2026-05-28 Amazon DEPLOY 4-FF Pattern by one further). **Schema-change-in-flight flag STAYS NO entire session** (eBay reuses W2 Amazon schema shipped 2026-05-28). Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **655 ext** / **838 src/lib** / **64 routes**); Check 6 Playwright SKIPPED per Rule 27. Post-FF#5 /scoreboard 5/5 GREEN at new ext baseline: extension `npm test` = **731/731** (+76 cumulative from 655); src/lib 838 UNCHANGED; routes 64 UNCHANGED. **NEW baseline locked: extension `npm test` = 731/731**. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-30** capturing THREE NEW reusable Patterns ("Diagnostic-instrumentation FF as escape valve from speculative-FF antipattern" + "Tabpanel-scoped DOM walking via `[role=tabpanel]:not([hidden])`" + "JSON data-island extraction for server-rendered seller/product metadata") + 3 LOW informational sub-observations (P-43 cwd-leak Pattern Class reproduction running tally ~11+ / calibration data point 3/3 = 100% / §C.2 estimate compression). NEW §B 2026-05-30 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (FIFTH build/deploy-session §B entry per Rule 18). NEW §B 2026-05-30 cross-reference pointer entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md` (extension-side architecture pointer per W2 Amazon precedent). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED. **ZERO DEFERRED items at session end (Rule 26)** — eBay sub-cluster fully ✅ DEPLOYED-AND-VERIFIED with no remaining scope (§C.2 estimate "~3-4 sessions" compressed into "1 calendar day"; eBay Sessions 2 + 3 from §C.2 CLOSED). **~12 pushes this session per `feedback_approval_scope_per_decision_unit.md`:** 5 deploy pushes + 5 ping-pong pushes + end-of-session post-deploy doc-batch push + ff-merge push. **4 fresh extension zips at repo root** (initial + ff1-ff2 bundled + ff3 + ff4 diagnostic). Branch state at end-of-session: `workflow-2-competition-scraping` even with `origin/main` at `23aa851` plus the end-of-session post-deploy doc-batch SHA after ff-merge. **FORTY-SECOND end-of-session run under the Rule 30 + §4 Step 4b template.** The 3 plain-terms sections at top of NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Previously updated in session:** session_2026-05-30_p49-w2-ebay-sub-cluster-session-1 (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-29 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-29_p49-w4-captured-reviews-ui-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 4 Captured Reviews UI extensions ✅ DEPLOYED-AND-VERIFIED 2026-05-29 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — same session ID + same calendar day; session expanded mid-session from build-only to build + deploy + Phase-4-PASS via the end-of-session Rule 14f deploy-or-exit picker (director picked Recommended "Deploy now" over Exit/Defer-Phase-4); originally landed build commit `e89ae50` (9 files +2162/-43) + build-session doc-batch `1e610ce` on `workflow-2-competition-scraping`; then ff-merged `a40e4ba..1e610ce main -> main` under ONE Rule 9 deploy gate; Vercel auto-redeploy fired; Phase 4 director real-Chrome verification 6/6 PASS on vklf.com (Customers-say banner above counter-bar / counter-bar per-star counts / click-to-filter / multi-star OR / bulk-select + delete / drag-to-reorder + reload-persistence — all PASS); TWO Rule 14f forced-pickers fired total this session (Session 1 scope picker 0/1 + deploy-or-exit picker 1/1 = 50% final calibration; running cumulative 24/27 = 88.9%); ONE Rule 9 deploy gate fired; Schema-change-in-flight flag STAYS NO entire session; W4 entire workstream now ✅ DEPLOYED-AND-VERIFIED with no remaining scope (§C.4 estimate "~2-3 sessions" compressed into "1 session shipped + 1 session deployed in same day"; Sessions 2 + 3 from §C.4 CLOSED); P-43 cwd-leak Pattern Class reproduced 3 total this session (running tally ~7-8+); ONE NEW INFORMATIONAL sub-observation appended to the existing CORRECTIONS_LOG §Entry 2026-05-29 capturing post-deploy outcome + NEW reusable Pattern candidate "End-of-build-session deploy-or-exit Rule 14f picker"; NEW post-deploy sub-section appended to existing §B 2026-05-29 in `docs/REVIEWS_PHASE_2_DESIGN.md`; FOUR pushes total this session; **Closes (a.96) + (a.97); opens (a.98) RECOMMENDED-NEXT = P-49 W2 eBay sub-cluster Session 1** on `workflow-2-competition-scraping` per §A.2 priority order + §C.2 implementation outline. **FORTY-FIRST end-of-session run under Rule 30 + §4 Step 4b extended template.** Originally landed via build commit `e89ae50` (9 files +2162/-43) — first build session of the Reviews Phase 2 W4 cluster; all 3 §C.4 pieces shipped in ONE session per the Rule 14f scope picker outcome (director picked Option B "All three" over Recommended Option A "§C.4 split into Sessions 1+2") — star-count counter-bar with click-to-filter per §A.14 + drag-to-reorder via the `sortRank Int?` column shipped W2 Session 1 (reusing the P-46 W3 S3 @dnd-kit shared debounced-mutation Pattern from 2026-05-23-f) per §A.5 + bulk-delete with multi-select checkboxes + confirm modal + new batch-delete API route per §A.6 — directly addresses the 2026-05-28 Phase 4 verification issue #3. One-hundred-and-sixty-second Claude Code session. Closes (a.96) RECOMMENDED-NEXT = P-49 W4 Captured Reviews UI extensions Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-29 via build commit `e89ae50`; opens (a.97) RECOMMENDED-NEXT = P-49 W4 Captured Reviews UI extensions deploy session (bundle build commit `e89ae50` + this doc-batch under ONE Rule 9 gate; Vercel auto-redeploys vklf.com; Phase 4 director real-Chrome verification ~6-step walkthrough) on `workflow-2-competition-scraping` → `main`. **EXACTLY ONE Rule 14f forced-picker fired this session** at start of code mechanics — Session 1 scope picker (director picked Option B "All three" over Recommended Option A "§C.4 split"). NOT director-Yes-to-Recommended — first non-Recommended pick in recent W#2 sessions; calibration data point 0/1 = 0% this session; running cumulative across recent 4 sessions: 23/26 = 88.5%. **ZERO Rule 9 deploy gates fired** entire session (build session only; build commit stays on workflow branch until W4 deploy session per (a.97)). **Schema-change-in-flight flag STAYS NO** entire session (W4 is UI + 2 new API routes only; no schema work; underlying `sortRank Int?` column shipped W2 Session 1 + already in production via 2026-05-28 Amazon DEPLOY). Pre-build /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **655 ext** / **786 src/lib** / **62 routes**); Check 6 SKIPPED per Rule 27. Post-build /scoreboard 5/5 GREEN at new baselines: extension **655/655 UNCHANGED** / **838 src/lib (+52)** / **64 routes (+2)**; Check 6 SKIPPED per Rule 27. **NEW baselines locked:** src/lib **838/838** (+52); npm run build **64 routes** (+2); extension **655/655 UNCHANGED**. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-29 (the FIRST 2026-05-29-dated §Entry)** capturing (a) W4 Session 1 outcome — all 3 §C.4 pieces shipped in one session per Rule 14f scope picker; 52 new src/lib tests; 2 new API routes; (b) **NEW reusable Pattern "Pure helpers extracted from .tsx component file for node:test coverage"** — when a `'use client'` component file contains substantial pure-function logic, extract those helpers into a separate `.ts` module under `src/lib/` so node:test can load + exercise them without the React tree; landed 20 new tests that would otherwise have required Playwright spec or component-test infra; (c) **NEW reusable Pattern "Customers-say split: separate AI-summary row from main reviews via source discriminator"** — when persisting non-review records via existing schema's `source: String` field, consuming UI should split rows by source discriminator at render time + render AI-summary rows as separate banner above main list, so they don't inflate per-star counts, don't participate in drag-reorder or bulk-select; (d) LOW informational sub-observation — P-43 cwd-leak Pattern Class reproduced 2 more times this session (running tally ~6-7+; strong empirical signal still mounting); (e) calibration data point — 0/1 = 0% Yes-to-Recommended this session; running cumulative across recent 4 sessions: 23/26 = 88.5%; framing still well-calibrated overall. NEW §B 2026-05-29 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (FOURTH build/deploy-session §B entry per Rule 18). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED this session (W4 is purely PLOS-side UI + 2 new API routes; no extension-side architectural work). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. **ZERO DEFERRED items at session end (Rule 26)** — no carry-overs at entry or end. **ONE push only this session per `feedback_approval_scope_per_decision_unit.md`** — end-of-session push to `origin/workflow-2-competition-scraping` carrying build commit `e89ae50` + this doc-batch commit. **NO ff-merge to main this session** per the build-session push pattern memorialized 2026-05-27. Branch state at end-of-session: `workflow-2-competition-scraping` 2 commits ahead of `main` (build commit `e89ae50` + this doc-batch commit); `main` stays at `a40e4ba` until W4 deploy session ff-merges everything under Rule 9 gate. **FORTIETH end-of-session run under the Rule 30 + §4 Step 4b template.** The 3 plain-terms sections at top of NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-29_p49-w4-captured-reviews-ui-session-1 (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-28 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-28_p49-w2-amazon-deploy-and-fix-forwards` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Amazon ✅ DEPLOYED-AND-VERIFIED 2026-05-28 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — first production deploy of the Reviews Phase 2 implementation arc. Sessions 1 + 2 build commits + intervening doc-batches all ff-merged to main under ONE Rule 9 deploy gate as the initial deploy (`1914171..0ef8340` carrying 4 commits: Session 1 build `422436f` + Session 1 doc-batch `1323f9a` + Session 2 build `1830074` + Session 2 doc-batch `0ef8340`); Vercel auto-redeploy fired; fresh extension zip `plos-extension-2026-05-28-w2-deploy-37.zip` produced via `npm run zip`; Phase 4 director real-Chrome verification surfaced 4 distinct issues across 2 verification rounds; 3 fix-forward commits shipped under their own Rule 9 deploy gates (FF#1 `8bc2e7e` accept `/dp/<ASIN>` dispatch + FF#2+#3 `b55cdbd` bundled trigger-modal-per-star-checkboxes + progress-indicator-per-star-breakdown + FF#4 `f6944db` pageNumber-increment pagination); final director PASS verdict after FF#4 ("It worked, all reviews scraped past 10"); RESOLVES the Sessions 1 + 2 standing carry-over (first end-to-end real-Chrome extraction test on a real Amazon product page). One-hundred-and-sixty-first Claude Code session. Closes (a.95) RECOMMENDED-NEXT = P-49 W2 Amazon DEPLOY session ✅ DEPLOYED-AND-VERIFIED 2026-05-28 end-to-end after 4-fix-forward cascade; opens (a.96) RECOMMENDED-NEXT = P-49 W4 Captured Reviews UI extensions Session 1 (star-count counter-bar with click-to-filter per §A.14 — directly addresses today's Phase 4 verification issue #3 + drag-to-reorder via `sortRank` column shipped in Session 1 reusing P-46 W3 S3 @dnd-kit Pattern + bulk-delete with multi-select checkboxes + confirm modal + new batch-delete API route per §A.6) on `workflow-2-competition-scraping`. **NINE Rule 14f forced-pickers fired this session** all director-Yes to Recommended (9/9 = 100% calibration data point). **4 Rule 9 deploy gates fired** (initial deploy + FF#1 + FF#2+#3 + FF#4 — within the W#2 P-46 W3 2026-05-24-f Pattern cap of 5 per session). **Schema-change-in-flight flag FLIPPED YES → NO at initial deploy push completion**; STAYED NO through 3 fix-forwards; final state NO. Pre-deploy /scoreboard 5/5 GREEN at expected baselines (root tsc clean / extension tsc clean / **633 ext matches Session 2 baseline** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27 picker. Post-merge trusted per session_2026-05-24-f byte-identical ff-merge Pattern. Post-FF#1 5/5 GREEN: 651/651 (+18). Post-FF#2+#3 5/5 GREEN: 655/655 (+4). Post-FF#4 5/5 GREEN: 655/655 UNCHANGED. **NEW baseline locked: extension `npm test` = 655/655** (+22 cumulative from 633 entry baseline). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-28** capturing (a) DEPLOY outcome — Amazon ✅ DEPLOYED-AND-VERIFIED end-to-end after 4-fix-forward cascade; (b) **NEW reusable Pattern "Phase 4 verification fix-forward cascade scales beyond N=5 when each issue is scoped + reversible + UI-only"** — pairs with + extends the P-46 W3 2026-05-24-f precedent; 4-of-4 fix-forwards landed PASS on re-verification; (c) **NEW reusable Pattern "FF#1 dispatch over-restriction antipattern at the per-platform extractor layer"** — design intent and code drifted apart between Session 1's "live-DOM rooted" approach and Session 2's "all-fetch-based" approach; future eBay / Etsy / Walmart sub-cluster sessions should adopt the symmetric `isXxxScrapableUrl` + `extractAsinFromXxxUrl` helper Pattern from FF#1 from the start; (d) **NEW reusable Pattern "Findnextpage-link-selector empirically falsified by Amazon UI change → pageNumber-direct-increment is the more robust replacement"** — when the underlying URL parameter contract is stable, prefer URL-construction-based pagination over DOM-link-scraping-based pagination; reinforces the P-48 Session 1 ffprobe-first Pattern at the pagination layer; (e) LOW informational sub-observation — P-43 cwd-leak Pattern Class reproduced 4-5+ times this session (Nth in Pattern class; strong empirical signal for the mechanical prevention fix); PLUS calibration data point (9/9 director-Yes to Recommended = 100%). NEW §B 2026-05-28 entries appended to BOTH `docs/REVIEWS_PHASE_2_DESIGN.md` (THIRD build/deploy-session §B entry per Rule 18) AND `docs/COMPETITION_SCRAPING_DESIGN.md` (extension-side architecture cross-reference pointer). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. **ZERO DEFERRED items at session end (Rule 26)** — the Sessions 1 + 2 standing carry-over RESOLVED today via Phase 4 PASS verdict. **~10 pushes this session per `feedback_approval_scope_per_decision_unit.md`:** 4 deploy pushes + 4 ping-pong pushes + end-of-session doc-batch push + end-of-session ff-merge + push for doc-batch. **4 fresh extension zips at repo root** (initial + ff1 + ff2-ff3 + ff4). Branch state at end-of-session: `workflow-2-competition-scraping` even with `origin/main` at `f6944db` (both branches at the same SHA after the 4 deploy ping-pongs); doc-batch ff-merge advances both to the doc-batch SHA. **THIRTY-NINTH end-of-session run under the Rule 30 + §4 Step 4b template.** The 3 plain-terms sections at top of NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Previously updated:** 2026-05-27 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-27_p49-w2-amazon-session-2` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Amazon Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-27 on `workflow-2-competition-scraping` via build commit `1830074` (5 files +1054/-99) — second build session of the Reviews Phase 2 implementation arc atop the Session 1 foundation; cross-star navigation loop + helpful-count sort within star + `Customers say` AI-summary block capture + Shadow DOM trigger popup with per-URL cap override all shipped at code level under one commit. Schema-change-in-flight flag STAYS YES entire session carrying from Session 1's `npx prisma db push` — no schema work this session; STAYS YES until Amazon deploy session ships migration live on vklf.com. Major refactor of `amazon-review-extractor.ts` (+419/-93) — NEW `AMAZON_STAR_FILTERS` const + `AmazonStarFilter` type (one_star..five_star) + helpers; `runAmazonReviewScrape` refactored from "scrape current view" to ASIN-driven cross-star loop (pages 1..N all fetched via fetch+DOMParser); `AmazonScrapeContext.cap` → `capPerStar`; NEW `sortByHelpfulCountDesc` + `extractCustomersSayFromListing` helpers; `AmazonScrapeSaveInput.source` widened to discriminated union `'extension-scrape' | 'extension-scrape:customers-say'`. `orchestrator.ts` (+59/-37) — `start-review-scrape` handler refactored to fire `openScrapeTriggerModal` BEFORE dispatching + build new context shape + saveReview wrapper passes source through. NEW `scrape-trigger-modal.ts` (320 LOC) — Shadow DOM-mounted modal reusing P-47 mount pattern (SECOND consumer of P-47 Pattern after Session 1's progress indicator) + NEW `clampCap` pure helper. NEW `scrape-trigger-modal.test.ts` (44 LOC) — 5 node:test cases for `clampCap`. `amazon-review-extractor.test.ts` (+182) — 17 new node:test cases. TOTAL: 22 new node:test cases. One-hundred-and-sixtieth Claude Code session. Closes (a.94) RECOMMENDED-NEXT = P-49 W2 Amazon Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-27 via build commit `1830074`; opens (a.95) RECOMMENDED-NEXT = P-49 W2 Amazon DEPLOY session (bundle Sessions 1 + 2 build commits + intervening doc-batches as one ff-merge under Rule 9 gate; Vercel auto-redeploy; fresh extension zip via `npm run zip`; Phase 4 director real-Chrome verification on a real Amazon product page) on `workflow-2-competition-scraping` → `main`. **EXACTLY ONE Rule 14f forced-picker fired this session** — the Customers-say block storage-shape picker (director picked Option A Recommended: starRating=5 sentinel + source="extension-scrape:customers-say" discriminator — no schema change, no wire-validator change, reversible). **ZERO Rule 9 deploy gates fired** entire session (build session only; build commits stay on `workflow-2-competition-scraping` until Amazon deploy session per (a.95)). **Schema-change-in-flight flag STAYS YES** carrying from Session 1; STAYS YES through Session 2; FLIPS TO NO at Amazon deploy completion. Pre-build + post-build /scoreboard 5/5 GREEN at new baseline (root tsc clean / extension tsc clean / **extension `npm test` 633/633 +22 from Session 1 baseline 611** — exact match with the 22 new test cases / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27 non-deploy-session convention. **NEW baseline locked from this session:** extension `npm test` = **633/633**. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-27** capturing (a) Session 2 outcome; (b) **NEW reusable Pattern "Build-session end-of-session push pattern: ONE push to workflow branch, NOT two"** — the "ff-merge doc-batch to main" option only applies when doc-batch sits directly on top of main (pure-design sessions OR sessions immediately after a deploy); for interleaved build + doc-batch sessions, the standard ff-merge would unintentionally ship the in-flight build commit; correct pattern is to wait for the eventual deploy session to ff-merge everything together; (c) **NEW reusable Pattern "Pre-emptive design choice rolled into Rule 14f picker"** — when a substantive design choice arises mid-build that wasn't pre-locked, surface it as a picker even if the implementer has a strong default; (d) LOW informational sub-observation — drift surfaced + non-destructively recovered (Session 1's planned 2-push pattern non-destructively recovered by adjusting end-of-session push plan from 2 pushes to 1); (e) calibration data point — Session 2 within estimate; multi-session arcs compound. NEW §B 2026-05-27 entries appended to BOTH `docs/REVIEWS_PHASE_2_DESIGN.md` (SECOND build-session §B entry per Rule 18) AND `docs/COMPETITION_SCRAPING_DESIGN.md` (extension-side architecture cross-reference pointer). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. **ONE DEFERRED carry-over to the Amazon deploy session (Rule 26)** — first end-to-end real-Chrome extraction test on a real Amazon product page (carrying from Session 1; resolves AT the Amazon deploy session per (a.95) by definition). **ONE push only this session per `feedback_approval_scope_per_decision_unit.md`:** push to `origin/workflow-2-competition-scraping` carrying build commit `1830074` + this doc-batch commit. **NO ff-merge to main this session** per the drift analysis. Branch state at end-of-session: `workflow-2-competition-scraping` 4 commits ahead of `main` (Session 1 build + Session 1 doc-batch + Session 2 build + Session 2 doc-batch); `main` stays at `1914171` until Amazon deploy session ff-merges everything under Rule 9 gate. **THIRTY-EIGHTH end-of-session run under the Rule 30 + §4 Step 4b template.** The 3 plain-terms sections at top of NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Previously updated:** 2026-05-26 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-26_p49-w2-amazon-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Amazon Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-26 on `workflow-2-competition-scraping` via build commit `422436f` (20 files +2069/-7) — first build session of the Reviews Phase 2 implementation arc; foundation session bundling schema + shared content-script infrastructure + Amazon DOM walker under one commit. Schema migration shipped via `npx prisma db push` (1.44s; additive only; zero data loss) per §A.13 + §A.16 of `docs/REVIEWS_PHASE_2_DESIGN.md` — new `ReviewAnalysis` model + `ReviewAnalysisLevel` enum (PER_PRODUCT/PER_TYPE/PER_PROJECT) + `CapturedReview.sortRank Int?` + `.helpfulCount Int?` + `.platform String?` + `CompetitorUrl.reviewScrapeCap Int? @default(200)`. Shared content-script infrastructure landed: NEW `scrape-pagination.ts` (~310 LOC) implementing §A.15 conservative anti-bot defaults — generic `paginate()` loop + 1-3s random `randomPaginationDelay()` + `detectCaptcha()` with Amazon + generic CAPTCHA selectors + `isRateLimitStatus()` HTTP 429/503 detection + AbortSignal-cancellable + structured `ScrapeProgress` event stream; NEW `scrape-progress-indicator.ts` (~230 LOC) Shadow DOM-mounted corner indicator reusing the P-47 2026-05-24-d mount pattern + new `PROGRESS_INDICATOR_CSS` constant + subscribes to `ScrapeProgress` events + auto-dismisses on completed + persists on captcha/rate-limit/error per §A.3. Amazon per-platform extractor: NEW `amazon-review-extractor.ts` (~300 LOC) — URL detection (`isAmazonReviewPage` + `extractAsinFromReviewUrl` + `urlsMatchByAsin`) + per-row DOM walker against `[data-hook="review"]` selectors + parsers (`parseStarRating` / `parseHelpfulCount` / `parseAmazonReviewDate`) + `findNextPageUrl` + `runAmazonReviewScrape` orchestrator using `fetch()` + `DOMParser` for pagination (avoids killing the content-script via full-page navigation). Right-click context-menu wiring across `background.ts` + `messaging.ts` + `orchestrator.ts` — new `CONTEXT_MENU_SCRAPE_REVIEWS_ID` entry with `contexts: ['all']` + new `start-review-scrape` ContentScriptMessage + new `create-captured-review` BackgroundRequest + orchestrator matches `pageUrl` against saved CompetitorUrls by ASIN + dispatches to Amazon extractor (eBay/Etsy/Walmart route to friendly "not yet supported" toast). Per-extension api-bridge / api-client additions for the new POST proxy. PLOS-side handler extensions additive to P-46 W2 S4 baseline — `url-reviews.ts` (POST validates new optional `helpfulCount` + `platform` + persists; `toWireShape` returns them) + `reviews-by-id.ts` + `urls.ts` + `urls/[urlId]/route.ts` (`toWireShape` extended) + `src/lib/shared-types/competition-scraping.ts` extended with `CapturedReview` + `CreateCapturedReviewRequest` + `CompetitorUrl` + `UpdateCompetitorUrlRequest` extensions + NEW `ReviewAnalysis` interface + `ReviewAnalysisLevel` type + `isReviewAnalysisLevel` type guard. 49 new extension node:test cases (`scrape-pagination.test.ts` +20 + `amazon-review-extractor.test.ts` +29) + 4 fixture-only updates extending makeRow factories with the new column defaults. One-hundred-and-fifty-ninth Claude Code session. Closes (a.93) RECOMMENDED-NEXT = P-49 W2 Amazon Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-26 via build commit `422436f`; opens (a.94) RECOMMENDED-NEXT = P-49 W2 Amazon Session 2 (cross-star navigation loop + helpful-count sort within star + `Customers say` AI-summary block capture + trigger popup with per-URL cap override) on `workflow-2-competition-scraping`. **ZERO Rule 14f forced-pickers fired this session** — design doc + launch prompt removed all ambiguity from (a.93) scope; per `feedback_default_to_recommendation.md` no clarifying picker needed. **ZERO Rule 9 deploy gates fired** entire session — build commits stay on `workflow-2-competition-scraping`; no production deploy. **Schema-change-in-flight flag flipped NO → YES at `npx prisma db push`** (early-mid session) — stays YES until the eventual Amazon deploy session (~3-5 sessions from now). Pre-build + post-build /scoreboard 5/5 GREEN at expected new baselines (root tsc clean / extension tsc clean / **extension `npm test` 611/611 +49 from baseline 562** — exact match with the 49 new test cases / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27 non-deploy-session convention. **NEW baseline locked from this session:** extension `npm test` = **611/611**. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-26** capturing (a) Session 1 outcome — first build session of P-49 W2 + schema + shared infra + Amazon DOM walker all shipped at code level under one build commit + 49 new tests + 5/5 scoreboard GREEN; (b) **NEW reusable Pattern "Content-script pagination via `fetch()` + `DOMParser` avoids the full-page-navigation kill"** — same-origin content-script fetch on amazon.com returns the server-rendered HTML for any review-page URL; parsing via `new DOMParser().parseFromString(html, 'text/html')` yields a fully-walkable Document that composes with existing DOM walker primitives; clicking the "Next page" link in the live DOM would navigate the actual tab + kill the running content-script; pairs with the P-23 saved-URL-dropdown URL-prefix-dispatch Pattern as a content-script-architecture-level Pattern; (c) **NEW reusable Pattern "Foundation session bundles schema + shared helpers + first per-platform module under one commit"** — mirrors P-46 W1 Schema-as-foundation Pattern from 2026-05-24 but extended (also ships shared content-script infra + first per-platform module so subsequent platform sessions reuse without re-designing); 20-file 2069-line commit feels large but each file is small + the scope is one cohesive deliverable; future per-platform sub-cluster Sessions 1's (eBay / Etsy / Walmart) expected much smaller; (d) calibration data point — Session 1 came in within estimate (~3-4 sessions estimated for Amazon cluster; Session 1 hit ~1.5-2 hours target); 49 new tests proportional to scope (3 new shared infra modules + 1 platform module); (e) **LOW informational sub-observation — pure-design-session investment pays off at first build session.** The 2026-05-25-b Reviews Phase 2 Design Session producing `docs/REVIEWS_PHASE_2_DESIGN.md` (790 lines, 15 frozen decisions) meant Session 1 had ZERO mid-build scope ambiguity; the Pattern from 2026-05-25-b's §Entry — "Director-supplied per-platform DOM specs in capture-session memorialization pay off at design-session ingestion" — extends here: those same specs pay off again at build-session ingestion; each upstream design step yields downstream build session speed. NEW §B 2026-05-26 entries appended to BOTH `docs/REVIEWS_PHASE_2_DESIGN.md` (FIRST build-session §B entry in the brand-new design doc per Rule 18 — captures the actual schema migration + shared helper file paths + Amazon DOM walker selectors + the fetch+DOMParser refinement of §A.3 — additive context, not a §A change) AND `docs/COMPETITION_SCRAPING_DESIGN.md` (extension-side architecture cross-reference pointer for the new shared content-script modules + new context-menu entry + orchestrator handler addition per the P-23 + P-46 precedent). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session (P-49 W2 is review-extraction architecture, not data-shape; the new CapturedReview fields are spec'd by REVIEWS_PHASE_2_DESIGN.md §A.16, not COMPETITION_DATA_V2_DESIGN.md). **ONE DEFERRED carry-over to the eventual Amazon deploy session (Rule 26):** first end-to-end real-Chrome extraction test on a real Amazon product page (the PLOS-side handler changes aren't yet on production vklf.com — they ship at the Amazon deploy session ~3-5 sessions from now; until then an end-to-end test against vklf.com would silently drop the new fields). **TWO pushes this session per `feedback_approval_scope_per_decision_unit.md`:** end-of-session doc-batch push to `origin/workflow-2-competition-scraping` (carries build commit `422436f` + this doc-batch commit) + end-of-session ff-merge + push to `origin/main` for doc-batch only (operationally adjacent + does NOT invoke Rule 9 since the build commit stays on the workflow branch). **THIRTY-SEVENTH end-of-session run under the Rule 30 + §4 Step 4b template.** The 3 plain-terms sections at top of NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)

**Previously updated:** 2026-05-25-b (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-25-b_reviews-phase-2-design-session` — **W#2 polish P-49 Reviews Phase 2 DESIGN SESSION ✅ DONE 2026-05-25-b on `workflow-2-competition-scraping` — pure design session (NO code, NO builds, NO deploys, ZERO Rule 9 gates fired). Workflow Requirements Interview walked the 15-question scaffold preserved verbatim in yesterday's NEXT_SESSION.md `## Proposed interview question scaffold` section; director answered all 15 forced-pickers — 14 direct-Yes-to-Recommended + 1 Q7 substantive refinement on the AI model-version selector (preserved Opus-throughout Recommended path while adding a per-run 4.7-default / 4.6-selectable dropdown alongside per-run + per-Project monthly cost caps). NEW standalone `docs/REVIEWS_PHASE_2_DESIGN.md` shipped (790 lines; §A frozen at 15 interview-locked subsections A.1-A.15 + §B empty append-only per Rule 18 + §C 4 active workstream implementation outlines W1 DONE / W2 / W4 / W5 + W3 Crawler DROPPED placeholder per Q1 outcome). Same structural shape as `docs/COMPETITION_DATA_V2_DESIGN.md` (the P-46 design doc precedent set 2026-05-23 + 2026-05-20-b P-27 design doc split). Revised P-49 scope post-design: 4 active workstreams instead of 5; revised estimate ~15-25 sessions total instead of ~20-50. One-hundred-and-fifty-eighth Claude Code session. Closes (a.92) RECOMMENDED-NEXT = Reviews Phase 2 Design Session ✅ DONE 2026-05-25-b producing `docs/REVIEWS_PHASE_2_DESIGN.md`; opens (a.93) RECOMMENDED-NEXT = P-49 W2 Amazon Session 1 (schema migration + shared scrape-pagination helper + Shadow DOM progress indicator + Amazon DOM walker getting basic end-to-end extraction working on a real Amazon product page) on `workflow-2-competition-scraping` per director Rule 14f forced-picker at end-of-session. THREE Rule 14f forced-pickers fired beyond the 15 interview pickers — the 15 interview pickers themselves + the (a.93) next-session picker. ZERO Rule 9 gates fired. Schema-change-in-flight STAYS NO entire session — pure design-doc capture; no code; no schema. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-25-b (the FOURTH 2026-05-25-dated §Entry — Reviews Phase 2 design-session closing entry) capturing NEW reusable Pattern "Director-supplied per-platform DOM specs in capture-session memorialization pay off at design-session ingestion" + calibration data point (14 of 15 picker direct-Yes-to-Recommended = high signal recommendation framing well-calibrated) + LOW informational sub-observation (Q1 outcome DROPPED entire W3 Crawler workstream — ~5-10 sessions saved on first interview question). NEW §B 2026-05-25-b cross-reference pointer entries appended to BOTH `docs/COMPETITION_DATA_V2_DESIGN.md` AND `docs/COMPETITION_SCRAPING_DESIGN.md` per the dual-doc P-23 + P-46 precedent. ZERO DEFERRED items at session end. TWO pushes planned (doc-batch push + doc-batch ff-merge push; both operationally adjacent + do NOT invoke Rule 9). THIRTY-SIXTH end-of-session run under the Rule 30 + §4 Step 4b template. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.**)

**Previously updated:** 2026-05-25 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-25_reviews-phase-2-capture-session` — **W#2 polish Reviews Phase 2 scope-expansion CAPTURE SESSION ✅ DONE 2026-05-25 on `workflow-2-competition-scraping` — pure-capture session (NO code, NO builds, NO deploys, ZERO Rule 9 gates fired). Director surfaced the long-promised "next round of additions" at session-start before pre-build reads completed; Rule 14f session-direction picker chose pure capture (P-48 Session 3 deferred opportunistically). NEW `P-49 Reviews Phase 2` ROADMAP entry (hub-and-spokes mirroring P-46 — 5 internal workstreams: Design Session NEXT + per-platform extension extraction × 4 platforms + Crawler infrastructure CONDITIONAL + Captured Reviews UI extensions + AI review analysis system at 3 levels) + NEW `P-50 Condition Pathology card` ROADMAP entry (small `main`-branch placeholder card; card-only scope per Rule 14f picker — no W# renumbering). `docs/COMPETITION_DATA_V2_DESIGN.md` §A.1 updated with "RESOLVED 2026-05-25 — see ROADMAP P-49" cross-reference paragraph linking the original 2026-05-23 deferral to the now-captured P-49 hub**) — pure-capture session executing the Rule 14f session-direction picker outcome (Recommended: pure capture today). **THREE Rule 14f forced-pickers fired** — session-direction (Pure capture Recommended) + capture-shape (Hub-and-spokes for P-49 Recommended + card-only scope for P-50 Recommended) + entry-approval + next-session (both entries approved + Reviews Phase 2 Design Session locked as next-session). **ZERO Rule 9 deploy gates fired** (pure-capture; no deploys). **Schema-change-in-flight flag STAYS NO** entire session — pure ROADMAP + design-doc capture; no code; no schema. Pre-build + post-capture /scoreboard NOT run (no code change to verify); baselines unchanged from prior session (root tsc clean / extension tsc clean / **562 ext UNCHANGED** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27. **Closes (a.91) RECOMMENDED-NEXT = P-48 Session 3 (Diagnostic #2)** DEFERRED to opportunistic insertion (still queued via the P-48 ROADMAP entry which already documents Session 3 as the next-within-P-48 step); **opens (a.92) RECOMMENDED-NEXT = Reviews Phase 2 Design Session** (Workflow Requirements Interview producing `docs/REVIEWS_PHASE_2_DESIGN.md`) on `workflow-2-competition-scraping`. **Director mid-pre-build directive at session-start:** the long-promised "next round of competition-scraping additions" surfaced as 4 new features (automated per-platform review collection for Amazon/eBay/Etsy/Walmart via extension and/or crawler + per-product two-sweep AI review analysis + cross-Type pooled AI analysis + cross-everything competitive-landscape AI analysis) plus one small dashboard addition (Condition Pathology card between Competition Scraping & Deep Analysis and Therapeutic Strategy & Product Family Design); 15-question interview-scaffold draft preserved verbatim in next NEXT_SESSION.md ## Proposed interview question scaffold section for next session's Workflow Requirements Interview ingestion. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-25 (the THIRD 2026-05-25-dated §Entry — Reviews Phase 2 scope-expansion capture)** capturing 4 sub-observations including (a) scope-expansion capture outcome; (b) **NEW reusable Pattern "Mid-pre-build scope-expansion redirect — when director surfaces major new scope at session-start before pre-build reads complete, the most-thorough/reliable path is a pure-capture session (pause planned task; run Rule 24 searches; capture as ROADMAP entries; defer planned task) rather than mixed-session attempts"** — pairs as a Rule-30 plain-terms-summary lesson; (c) LOW informational sub-observation — long-deferred concerns naturally resolve when director's mental model of what they want catches up with the deferred scope (A.1 deferred 2026-05-23; resolved 2026-05-25 with full Phase 2 specs; calibration data point for design discipline); (d) operational note — 15 interview-question draft preserved verbatim in NEXT_SESSION.md for next session's Workflow Requirements Interview. **No top-tier corrections-tier slip this session.** **ZERO DEFERRED items at session end (Rule 26)** — P-48 Session 3 carries via the P-48 ROADMAP entry itself (which already documents Session 3 as the next-within-P-48 step), NOT as a NEXT_SESSION standing carry-over per Rule 26. **TWO pushes this session per `feedback_approval_scope_per_decision_unit.md`:** end-of-session doc-batch push to `origin/workflow-2-competition-scraping` (PENDING) + end-of-session ff-merge + push to `origin/main` for doc-batch (PENDING; operationally adjacent + do NOT re-invoke Rule 9 since pure-capture + no destructive operations). **THIRTY-FIFTH end-of-session run under the Rule 30 + §4 Step 4b template.** The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-25-b_reviews-phase-2-design-session (Claude Code, on `workflow-2-competition-scraping` then ff-merged to `main`)

**Previously updated:** 2026-05-25 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-25_p48-session-2-implementation-and-ff1-busted` — **W#2 polish P-48 Session 2 (Implementation + Fix-Forward #1 BUSTED) ✅ DEPLOYED-PARTIAL 2026-05-25 on vklf.com via `workflow-2-competition-scraping` → `main` — `fix-webm-duration` patch shipped end-to-end via build commit `279da34` ff-merged `a486af8..279da34` to main (4 files +211/-9 — extension `package.json` adds `fix-webm-duration ^1.0.6` + `record-controller.ts` +40/-9 wires DI-seam optional dep with try/catch fallback + `record-controller.test.ts` +143 with 4 new node:test cases) under Rule 9 gate #1; director-verified the seek-bar fix on vklf.com; Phase-4 surfaced playback stutter PERSISTS even with duration metadata correct; empirical ffprobe revealed every SCREEN_RECORDING webm captured at ~6-7 fps actual rate (not 30 fps); speculative FF-1 build commit `d8b9507` (1 file +25/-2; attach hidden video element to DOM) shipped under Rule 9 gate #2 but empirically BUSTED (post-FF-1 ffprobe 8.52 fps within noise); per director Rule 14f forced-picker outcome, remaining stutter work deferred to P-48 Session 3 (Diagnostic #2); FF-1 commit STAYS in production as calibration data point**) — BUILD + DEPLOY + FIX-FORWARD session shipping the Session 1 locked fix path end-to-end + an empirical fix-forward attempt that BUSTED. **ONE Rule 14f forced-picker fired** at the deferred-vs-iterate decision point after FF-1 empirically didn't help — picker offered (A) Defer to P-48 Session 3 (Recommended) / (B) one more empirical fix attempt / (C) roll back FF-1 + defer; director picked (A). **TWO Rule 9 deploy gates fired** — Gate #1 (deploy push for `279da34`) + Gate #2 (FF-1 deploy push for `d8b9507`); both director-Yes per `feedback_recommendation_style.md` recommended path. **Schema-change-in-flight flag STAYS NO** entire session — pure UI/extension changes (one new npm runtime dep + ~50 LOC). Pre-deploy /scoreboard 5/5 GREEN at new baselines (root tsc clean / extension tsc clean / **562 ext +4 from baseline 558** — exact match with 4 new fix-webm-duration test cases / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); post-merge /scoreboard partial (root tsc + extension tsc + src/lib 786/786 GREEN; Check 3 + Check 5 trusted at unchanged baselines via clean ff-merge); FF-1 post-edit /scoreboard 5/5 GREEN at unchanged baselines (extension `npm test` 562/562 UNCHANGED since FF-1 changes `productionCropStreamToRegion` only — tests inject their own `cropStreamToRegion` via DI seam); Check 6 Playwright SKIPPED per Rule 27. **NEW baseline locked from this session:** extension `npm test` = **562/562**. **Closes (a.90) RECOMMENDED-NEXT = P-48 Session 2 (Implementation) ✅ DEPLOYED-PARTIAL 2026-05-25**; **opens (a.91) RECOMMENDED-NEXT = P-48 Session 3 (Diagnostic #2)** on `workflow-2-competition-scraping` — empirical instrumentation pass before any more structural fixes ship; estimated 30-60 min in-Claude. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-25 (the SECOND 2026-05-25-dated §Entry)** capturing 5 sub-observations including (a) calibration data point — fix-webm-duration patch worked at the layer designed; Session 1 diagnosis PARTIAL because original symptom had multiple causes; future symptom-level polish items should expect PARTIAL Phase-4 PASSES; (b) **NEW reusable Pattern "Speculative fix-forward without empirical pre-verification = antipattern (reinforcement of Session 1's ffprobe-first Pattern at the fix-forward level)"** — within a Phase-4 fix-forward cascade, articulate THEORY + identify cheap EMPIRICAL TEST + run if cheap + only ship if confirmed; today's FF-1 was shipped without empirical pre-verification of whether attaching the video to DOM would unthrottle it; pairs with §Entry 2026-05-25 (Session 1) sub-observation (b) "ffprobe-first diagnostic reveals hypothesis-bust before any code lands" as the fix-forward-level version; (c) MEDIUM informational — empirical ffprobe finding that every SCREEN_RECORDING webm since P-45 shipped is captured at ~6-7 fps actual rate; (d) calibration data point — Session 2 budget overrun (~2-3 hours actual vs 30-45 min budgeted) due to Phase-4 PARTIAL deepening into FF-1 cycle; implementation sessions shipping under a Phase-4 deploy gate should budget +50-100% time; (e) operational housekeeping — ONE throwaway diagnostic script `scripts/p48-s2-probe-new.mjs` DELETED at session end per Session 1 convention. **No top-tier corrections-tier slip this session.** **ZERO DEFERRED items at session end (Rule 26)** — 13 tasks created this session; all 12 active tasks COMPLETED + 1 task DELETED after director clarified there was no regression; Session 3 task IS the next-session task per (a.91), not a Claude-defer. **SIX pushes this session per `feedback_approval_scope_per_decision_unit.md`:** Deploy #1 + ping-pong #1 + Deploy #2 + ping-pong #2 all DONE; doc-batch push + doc-batch ff-merge push PENDING (the last two operationally adjacent + do NOT re-invoke Rule 9). **THIRTY-FOURTH end-of-session run under the Rule 30 + §4 Step 4b template.** The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-25_p48-session-2-implementation-and-ff1-busted (Claude Code, on `workflow-2-competition-scraping` then ff-merged to `main` twice)

**Previously updated:** 2026-05-25 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-25_p48-session-1-diagnostic` — **W#2 polish P-48 Session 1 (Diagnostic) ✅ DONE 2026-05-25 on `workflow-2-competition-scraping` — pure diagnostic session on screen-recording playback stutters reported during 2026-05-24-f verification; ZERO code lands; root cause empirically identified via `ffprobe` + EBML inspection of 3 SCREEN_RECORDING webm files in production + `curl` HEAD/Range tests of Supabase Storage delivery — missing WebM duration metadata in MediaRecorder-produced container headers; pre-Session-1 capture-side bitrate hypothesis empirically FALSIFIED + file path drift corrected; Session 2 fix path locked = `fix-webm-duration` npm dep + ~10 LOC in `record-controller.ts`**) — pure diagnostic session ruling out capture-side bitrate (cap already shipped) + ruling out delivery-side (Supabase + Cloudflare both clean) + identifying missing WebM duration metadata in MediaRecorder output as the empirical root cause via `ffprobe` + EBML inspection. **ONE Rule 14f forced-picker fired** during diagnostic shaping (capture-flow scoping after my query bug filtering `sourceType: 'DIRECT_BYTES'` excluded the 3rd enum value `SCREEN_RECORDING`; re-query found 3 rows). **ZERO Rule 9 gates fired** (pure diagnostic). **Schema-change-in-flight flag STAYS NO** entire session — diagnostic-only; no code; no schema. Pre-build + post-diagnostic /scoreboard NOT run (no code change to verify); baselines unchanged from prior session (root tsc clean / extension tsc clean / **558 ext UNCHANGED** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27. **Closes (a.89) RECOMMENDED-NEXT = P-48 Session 1 (Diagnostic) ✅ DIAGNOSED 2026-05-25**; **opens (a.90) RECOMMENDED-NEXT = P-48 Session 2 (Implementation)** on `workflow-2-competition-scraping` — small build + deploy session. **Director mid-session directive ending the active work:** *"i can't do any testing right now so add that to the next session and wrap up no"* — 15-step Chrome DevTools Network Panel walkthrough drafted in-session preserved verbatim in next NEXT_SESSION.md ## Pre-session notes section for director to run between sessions. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-25** capturing 5 sub-observations including (a) P-48 hypothesis empirically falsified — capture-side bitrate cap already shipped; root cause is missing WebM duration metadata; (b) **NEW reusable Pattern "ffprobe-first diagnostic reveals hypothesis-bust before any code lands — empirical inspection of artifacts in their actual production state preempts speculative implementation"**; (c) MEDIUM informational sub-observation — query-bug false-alarm where filtering on a 2-of-3 enum value created a false "data integrity bug" report (mechanical prevention candidate: query ALL enum values or `groupBy by sourceType` upfront); (d) calibration data point — Session 1 ran ~45 min in-session (under the 30-60 min budget); director answered 2 Rule 14f pickers in real-time; (e) operational housekeeping — 6 throwaway diagnostic scripts created in `scripts/` during session all DELETED at session end. **No top-tier corrections-tier slip this session.** **ZERO DEFERRED items at session end (Rule 26)** — Session 2 task IS the next-session task per (a.90); director's Network Panel walkthrough preserved as ## Pre-session notes for director-side execution. **TWO pushes this session per `feedback_approval_scope_per_decision_unit.md`:** end-of-session doc-batch push to `origin/workflow-2-competition-scraping` (PENDING) + end-of-session ff-merge + push to `origin/main` for doc-batch (PENDING; operationally adjacent; does NOT invoke Rule 9 since no destructive operation). **THIRTY-THIRD end-of-session run under the Rule 30 + §4 Step 4b template.** The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-25_p48-session-1-diagnostic (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-24-f (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-24-f_p46-w4-phase4-fix-forward-1-then-w5-p47-phase4-pass-plus-p48-capture` — **W#2 polish bundled Phase-4 real-Chrome verification session ✅ DONE 2026-05-24-f — P-46 Workstream 4 + Workstream 5 + P-47 Shadow DOM refactor ALL ✅ DONE-AND-VERIFIED 2026-05-24-f end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` fix-forward #1 ff-merge `5ed754e..d38b036` carrying 1 commit (W4 fix-forward `d38b036` — 5 files +292/-94 UI-only resolving 7 director-observed Phase-4 issues)**) — bundled Phase-4 verification session executing the deferred Phase-4 verifications for W4 (4th consecutive defer) + W5 (1st defer) + P-47 (1st defer) in one session per Rule 26 ## Standing carry-overs Pattern. Walkthrough (a) W4 surfaced 7 director-observed issues (no blinking cursor / H1-H3 not visibly bigger / link not underlined / bullets not showing / Done doesn't save / no font-size option / dark editor background) — fix-forward #1 bundled all 7 into one UI-only build commit under ONE Rule 9 deploy gate; director re-walked all 10 steps post-Vercel-redeploy → ALL PASS. Walkthroughs (b) W5 + (c) P-47 paired on Amazon both PASS first-walk (zero fix-forwards needed for either). **TWO Rule 14f forced-pickers fired during fix-forward shaping** — Light-theme scope (sitewide editor light-theme over W4-only scoping) + Font-size feature (add font-size stepper to W4 editor toolbar). **Schema-change-in-flight flag STAYS NO** entire session — fix-forward #1 was UI-only (no schema, no API, no shared-types). Pre-deploy /scoreboard 5/5 GREEN at unchanged baselines (root tsc clean / extension tsc clean / **558 ext UNCHANGED** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); post-merge /scoreboard partial (root tsc clean / extension tsc clean / src/lib 786/786 GREEN; Check 5 + Check 3 trusted at unchanged baselines since merged commit byte-identical to pre-deploy via clean ff-merge); Check 6 Playwright SKIPPED per Rule 27. **Closes (a.88) RECOMMENDED-NEXT = Bundled Phase-4 real-Chrome verification session ✅ DONE 2026-05-24-f**; **closes P-46 entire 5-workstream polish arc ✅ DONE-AND-VERIFIED end-to-end 2026-05-24-f**; **closes P-47 ✅ DONE-AND-VERIFIED 2026-05-24-f**; **opens (a.89) RECOMMENDED-NEXT = P-48 Session 1 (Diagnostic) on `workflow-2-competition-scraping`** per director's 2026-05-24-f directive to "fix all things remaining for competition scraping" before W#2 graduation + then explicitly ASK for next round of additions. **NEW P-48 polish item captured per Rule 24 search** (no prior treatment — director-observed video playback stutters on vklf.com specific to P-45 screen-recording captures; LOW–MEDIUM severity; 1-2 sessions diagnostic + implementation). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-24-f** capturing 6 sub-observations including (a) Phase-4 verification + fix-forward + Phase-4 PASS lifecycle outcome; (b) **NEW reusable Pattern "Bundled Phase-4 verification surfaces issues concentrated in the most-complex newest surface; subsequent surfaces tend to PASS first-walk"**; (c) LOW informational on UX directive surfacing 3+ months after styling shipped (stylistic directives can surface late as director's mental model matures; fix-forward Pattern handles cleanly via site-wide scope); (d) NEW P-48 capture per Rule 24 search; (e) calibration data point validating "fix-forward in-session unless schema change / significant new code / scope shift" criterion; (f) **NEW reusable Pattern "Editor save-on-Done race condition (debounced save + unmount cleanup)"** — ANY debounced-save editor with parent-controlled mount lifecycle must flush on unmount AND propagate save outcomes back to parent state OR re-fetch from server before unmount. **No top-tier corrections-tier slip this session.** **ZERO DEFERRED items at session end (Rule 26)** — all 3 prior standing carry-overs RESOLVED today via PASS verdicts; P-48 captured as ROADMAP polish-backlog item not a carry-over. **FOUR pushes this session per `feedback_approval_scope_per_decision_unit.md`:** fix-forward #1 deploy push to `origin/main` (DONE under Rule 9 Yes at `d38b036`) + ping-pong push to `origin/workflow-2-competition-scraping` (DONE post-deploy) + end-of-session doc-batch push to `origin/workflow-2-competition-scraping` (PENDING) + end-of-session ff-merge + push to `origin/main` for doc-batch (PENDING; operationally adjacent; does NOT re-invoke Rule 9). **THIRTY-SECOND end-of-session run under the Rule 30 + §4 Step 4b template.** The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-24-f_p46-w4-phase4-fix-forward-1-then-w5-p47-phase4-pass-plus-p48-capture (Claude Code, on `workflow-2-competition-scraping` then ff-merged to `main`)

**Previously updated:** 2026-05-24-e (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-24-e_bundled-w5-p47-deploy` — **W#2 polish P-46 Workstream 5 + P-47 Shadow DOM refactor BOTH ✅ DEPLOYED-PHASE-4-PENDING 2026-05-24-e on vklf.com via `workflow-2-competition-scraping` → `main` bundled ff-merge `9205340..d68885a` carrying 5 commits (W5 build `3c981be` + Reviews polish `41172f1` + W5 doc-batch `4d0f771` + P-47 build `d08f673` + P-47 doc-batch `d68885a`)**) — pure orchestration deploy session of 3 build commits + 2 doc-batch commits behind ONE Rule 9 gate; Vercel auto-redeploy fired; fresh extension zip `plos-extension-2026-05-24-w2-deploy-34.zip` 202.98 KB dropped at repo root. **Headline outcome:** the extension URL save form now collects Type / Description-1 / Description-2 / Price at capture time on vklf.com end-to-end (W5); the Add Review modal duplicate-Save idempotency bug is fixed in production (Reviews polish); the content-script video-capture form now mounts inside an open Shadow DOM root in production, structurally preventing page-level event interference WITHOUT the 80-event-listener band-aid (P-47). **Phase-4 director real-Chrome verification DEFERRED to next session at director directive** — 3 verification walkthroughs preserved verbatim in next NEXT_SESSION.md ## Standing carry-overs section (W4 10-step carried forward AGAIN as 4th consecutive defer + W5 4-step NEW today + P-47 2-step NEW today; the W5 + P-47 walkthroughs pair into ONE Phase-4 step at next session). **TWO §4 Step 1c forced-pickers fired** — Rule 9 deploy gate (director picked "Deploy now — push to origin/main (Recommended)") + Phase-4 in-session vs deferred (director picked "Defer to next session" over Recommended "Run in-session now"; THIRD time the bundled-Phase-4 defer Pattern fires). **Schema-change-in-flight flag STAYS NO** entire session (no schema work in any of the 3 build commits). Pre-deploy + post-merge /scoreboard both 5/5 GREEN at unchanged baselines (root tsc clean / extension tsc clean / **558 ext UNCHANGED** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27 non-deploy-spec convention both times. **Closes (a.87) RECOMMENDED-NEXT = Bundled W5 + P-47 deploy session ✅ DEPLOYED-PHASE-4-PENDING 2026-05-24-e on vklf.com**; **opens (a.88) RECOMMENDED-NEXT = Bundled Phase-4 real-Chrome verification session** covering 3 walkthroughs (W4 10-step + W5 4-step + P-47 2-step). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the bundled W5 + P-47 deploy §Entry 2026-05-24-e** capturing (a) bundled-deploy outcome (5 commits ff'd under ONE Rule 9 gate; 5/5 GREEN pre + post); (b) **NEW reusable Pattern "Bundled-build-commit deploy under ONE Rule 9 gate"** — when N build commits sit on a workflow branch awaiting deploy + each commit already passed /scoreboard GREEN at its own session + all commits are additive + no inter-commit dependencies, the most-thorough/reliable choice is to ff-merge ALL N commits behind ONE Rule 9 gate rather than splitting into N separate deploy sessions; today's 3-build-commit bundle (W5 + Reviews polish + P-47 Shadow DOM) is the second exemplar (first was W2 deploy 2026-05-23-c with 5 build sessions + Workstream 1 schema commits — but those were single-workstream); today's is the first cross-workstream bundle (P-46 W5 + P-46 Reviews polish + P-47 Shadow DOM are 3 distinct polish items shipped together); (c) LOW informational sub-observation TWO more P-43 cwd-leak reproductions (Check 5 pre-deploy + Check 3 post-merge; 6th+ reproduction class); (d) calibration data point — bundled deploy landed cleanly without fix-forwards (Phase-4 deferred so no cascade possible). **No corrections-tier slip occurred this session.** THREE DEFERRED items carry to next session as standing carry-overs (Rule 26) — P-46 W4 Phase-4 verify (4th consecutive defer) + P-46 W5 Phase-4 verify (1st defer) + P-47 Phase-4 verify (1st defer); all 3 walkthroughs preserved verbatim in next NEXT_SESSION.md ## Standing carry-overs section. **THIRTY-FIRST end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → 2026-05-23-d → 2026-05-23-e → 2026-05-23-f → 2026-05-24 → 2026-05-24-b → 2026-05-25 → 2026-05-26 → 2026-05-24-c → 2026-05-24-d → today 2026-05-24-e). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-24-e_bundled-w5-p47-deploy (Claude Code, on `workflow-2-competition-scraping` then ff-merged to `main`)

**Previously updated:** 2026-05-24-d (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-24-d_p47-shadow-dom-refactor-session-1` — **W#2 polish P-47 Shadow DOM refactor Session 1 (content-script video-capture-form mount strategy refactor from `document.body` + 80-event-listener band-aid to open Shadow DOM root with `FORM_CHROME_CSS` injected inside the shadow + band-aid deletion) ✅ DONE-AT-CODE-LEVEL 2026-05-24-d on `workflow-2-competition-scraping` via build commit `d08f673` (2 files +203/-220, net -17 LOC)**) — pure CODE session on `workflow-2-competition-scraping`; NOT pushed to main this session (next session is the bundled W5 + P-47 deploy session per the end-of-session §4 Step 1c picker outcome); build commit sits on workflow branch alongside the 2 prior W5 build commits (`3c981be` + `41172f1`) all awaiting the next deploy session. **Headline outcome:** the content-script video-capture form now mounts inside an open Shadow DOM root attached to a fixed-positioned host `<div>` in `document.body`, rather than appending the backdrop directly into `document.body`; events fired inside the shadow root do not surface to page-level handlers by default, so the per-input event-isolation band-aid shipped in P-45 Build #2 2026-05-22-i (20 events × 4 inputs = 80 listeners on each form open) is no longer needed and has been deleted (the `applyAggressiveEventIsolation` function + its 4 call sites + its 20-line context comment, 62 LOC total). **W#4 Phase-4 director real-Chrome verification deferred a THIRD consecutive session at director's directive** — 10-step verification walkthrough preserved verbatim AGAIN in next NEXT_SESSION.md ## Standing carry-overs section so a future session can copy + execute it without re-deriving. **ONE §4 Step 1c forced-picker fired this session.** **End-of-session picker (next-session scope)** — picker offered (A) Bundled W5 + P-47 deploy to main (Recommended) / (B) P-26 below-fold scroll capture Session 1 / (C) P-43 mechanical prevention small fix / (D) question first; director picked A. **Schema-change-in-flight flag STAYS NO** entire session (pure content-script DOM mount refactor; no schema, no API, no shared-types). Pre-build + post-refactor /scoreboard both 5/5 GREEN at unchanged baselines (root tsc clean / extension tsc clean / **558 ext UNCHANGED** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27 non-deploy-session convention both times. **Closes (a.86) RECOMMENDED-NEXT = P-47 Shadow DOM refactor Session 1**; **opens (a.87) RECOMMENDED-NEXT = Bundled W5 + P-47 deploy session** on `workflow-2-competition-scraping` → `main` per director's end-of-session picker pick. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-47 Shadow DOM refactor Session 1 §Entry 2026-05-24-d** capturing (a) P-47 outcome (Shadow DOM mount lands, band-aid deleted, 5/5 GREEN); (b) **NEW reusable Pattern "Shadow DOM mount as structural replacement for per-listener event-isolation band-aid"** — when a per-listener band-aid (N events × M elements = N×M listeners on each component open) successfully isolates a component from page-level interference, the structural replacement is a Shadow DOM mount providing the same isolation at the boundary level with zero per-listener overhead; pairs with P-45 Build #2 §Entry 2026-05-22-i Pattern B (the band-aid memorialized) as the long-term-fix pairing; (c) LOW informational sub-observation on **CSS extraction into shared exported constant + template-literal interpolation back into composite constant** — single source of truth for both global stylesheet consumers + Shadow DOM root consumer; minor refactor pattern. **No corrections-tier slip occurred this session.** **ONE DEFERRED item carries to next session as standing carry-over (Rule 26)** — P-46 W4 Phase-4 real-Chrome verification (deferred a THIRD consecutive session at director directive *"please defer any real world testing"*; full 10-step walkthrough preserved verbatim AGAIN in next NEXT_SESSION.md ## Standing carry-overs section). THREE prior standing carry-overs RESOLVE at next session's bundled deploy — P-46 W5 deploy + P-46 W5 Phase-4 verify + P-47 Session 1 Phase-4 verify. **THIRTIETH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → 2026-05-23-d → 2026-05-23-e → 2026-05-23-f → 2026-05-24 → 2026-05-24-b → 2026-05-25 → 2026-05-26 → 2026-05-24-c → today 2026-05-24-d). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-24-d_p47-shadow-dom-refactor-session-1 (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-24-c (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-24-c_p46-workstream-5-session-1-extension-url-form-additions-and-reviews-modal-polish` — **W#2 polish P-46 Workstream 5 Session 1 (Extension URL save form additions — Type/Description-1/Description-2/Price fields) ✅ DONE-AT-CODE-LEVEL 2026-05-24-c on `workflow-2-competition-scraping` via build commit `3c981be` (4 files +225/-0) + opportunistic Reviews modal idempotency polish ✅ DONE-AT-CODE-LEVEL 2026-05-24-c via build commit `41172f1` (1 file +9/-1) — fixes the `CapturedReviewAddModal` clientId idempotency bug shipped in W2 Session 4 (2026-05-28) where `crypto.randomUUID()` was regenerated on every Save click defeating server-side P2002 dedup; hoisted to a `useState` seeded per modal-open + reset on close**) — pure CODE session on `workflow-2-competition-scraping`; NEITHER commit pushed to main (W5 deploy DEFERRED at director request); 2 build commits sit on workflow branch awaiting future W5 deploy session. **Headline outcome:** the extension URL save form now collects Type / Description-1 / Description-2 / Price at capture time via a new `makeTextareaField()` helper alongside existing `makeField()`, plus the POST urls handler allowlist + `CreateCompetitorUrlRequest` wire type extended additively; the Reviews modal duplicate-Save idempotency bug fixed in the React component layer. **W#4 Phase-4 director real-Chrome verification deferred a second consecutive session at director's directive** — 10-step verification walkthrough preserved verbatim in next NEXT_SESSION.md ## Standing carry-overs section so a future session can copy + execute it without re-deriving. **TWO §4 Step 1c forced-pickers fired this session.** (1) **Session-start picker** — director deferred original launch-prompt task (W4 Phase-4 verify) + answered "work on next item on roadmap"; resolved via second clarifying picker to **P-46 Workstream 5 (Recommended option)** over P-47 Shadow DOM refactor + P-43 mechanical prevention candidate. (2) **End-of-session picker (next-session scope)** — director gave free-text "Other" answer: *"for the next session, defer any real world testing items that I need to do. instead work on the next item on our road map for workflow#2"*; follow-up picker offered P-47 Shadow DOM refactor (Recommended) / P-26 below-fold scroll capture / P-43 mechanical prevention; director picked P-47. **Schema-change-in-flight flag STAYS NO** entire session (W5 consumes already-deployed W1 schema; Reviews modal polish is React-component-only). Pre-deploy /scoreboard after W5 build commit `3c981be` 5/5 GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **786 src/lib +3 from baseline 783** — exact match with 3 new urls.test.ts cases for the W5 POST allowlist / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per non-deploy-session convention. Post-polish /scoreboard after Reviews polish commit `41172f1` 5/5 GREEN at unchanged baselines (no new tests — React component layer fix; server-side dedup already covered in url-reviews.test.ts). **Closes (a.85) RECOMMENDED-NEXT = P-46 Workstream 5 Session 1 build (opened + closed today)**; **(a.84) RECOMMENDED-NEXT = P-46 W4 Phase-4 verification STAYS OPEN** (deferred a second consecutive session — standing carry-over); **opens (a.86) RECOMMENDED-NEXT = P-47 Shadow DOM refactor Session 1** on `workflow-2-competition-scraping` per director's end-of-session picker pick. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 5 Session 1 + Reviews modal polish §Entry 2026-05-24-c** capturing (a) §C.5 file-list inconsistency informational observation — §C.5 listed `extensions/competition-scraping/src/entrypoints/popup/components/UrlAddForm.tsx` which DOES NOT EXIST in the repo (URL-add form lives ONLY in content-script `src/lib/content-script/url-add-form.ts`); one-less-file simplification, no scope reduction; §C.5 stays frozen per Rule 18; (b) Reviews modal clientId idempotency bug — opportunistic-polish-during-build-session memorialized as **NEW reusable Pattern "Opportunistic-polish-during-build-session — when scanning a related surface for polish, real bugs may surface alongside cosmetic candidates; prioritize real bugs"**; (c) Director DOUBLE-DEFER of W4 Phase-4 + NEW deferral of W5 Phase-4 + W5 deploy informational calibration data point — when director defers Phase-4 across consecutive sessions, the standing carry-overs section in NEXT_SESSION.md becomes the canonical place to preserve verification walkthroughs verbatim across sessions. **No corrections-tier slip occurred this session.** **THREE DEFERRED items captured for next session at end-of-session sweep (Rule 26)** — Task #1 W4 Phase-4 real-Chrome verification (carried from start of session) + Task #11 W5 deploy session (NEW today — 2 build commits sit unpushed-to-main) + Task #12 W5 Phase-4 real-Chrome verification (NEW today — pairs with W5 deploy defer); destinations all captured in next NEXT_SESSION.md ## Standing carry-overs section. **TWENTY-NINTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → 2026-05-23-d → 2026-05-23-e → 2026-05-23-f → 2026-05-24 → 2026-05-24-b → 2026-05-25 → 2026-05-26 → today 2026-05-24-c). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-24-c_p46-workstream-5-session-1-extension-url-form-additions-and-reviews-modal-polish (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-26 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-26_p46-workstream-4-deploy-session-phase-4-verification-deferred` — **W#2 polish P-46 Workstream 4 (Comprehensive Competitor Analysis page) ✅ DEPLOYED-PHASE-4-PENDING 2026-05-26 on vklf.com via `workflow-2-competition-scraping` → `main` ff-merge `cafd3ed..096a2ac` carrying 4 commits (W4 S1 build `283d4d1` + W4 S1 doc-batch `8b30ab3` + W4 S2 build `5854eff` + W4 S2 doc-batch `096a2ac`)**) — pure deploy session of the W4 build sessions that landed cleanly 2026-05-24-b + 2026-05-25 at code level; ff-merge fired against `origin/main` under ONE Rule 9 gate (director picked "Deploy now — push to origin/main (recommended)" after a clarifying picker disambiguated a truncated director response "deploy now but defer any real wor"); Vercel auto-redeploy fired. **Phase-4 director real-Chrome verification DEFERRED to next session at director request** — full 10-step verification walkthrough drafted in-session + preserved verbatim in `docs/NEXT_SESSION.md`. **W4 status now `✅ DEPLOYED-PHASE-4-PENDING` on vklf.com.** **Headline outcome:** the per-Project Comprehensive Competitor Analysis page + internal-hyperlink TipTap extension + "Link to URL" toolbar picker — all four W4 build commits — are live on vklf.com awaiting director real-Chrome verification next session. **TWO additional pickers fired this session beyond the Rule 9 gate** — (1) Truncated-picker-response handling NEW informational Pattern observation: director's first AskUserQuestion answer rendered as truncated string "deploy now but defer any real wor"; Claude fired a clarifying AskUserQuestion picker offering 4 disambiguation options; director picked "Deploy now — but pause BEFORE the push to main" (effectively identical to default Rule 9 behavior). **Memorialize as reusable Pattern "Truncated picker response → fire clarifying picker, don't silently interpret"** — aligns with Rule 14g; could become a feedback memory if it recurs. (2) Rule 27 Check 6 Playwright SKIP vs RUN picker — director picked SKIP since ff-merge bundle has zero `extensions/` source files and the new W4 page surface has no existing Playwright spec coverage. **Schema-change-in-flight flag STAYS NO** entire session (no schema work in W4 at all). Pre-deploy + post-merge /scoreboard both 5/5 GREEN at expected W4 S2 baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 783 src/lib UNCHANGED / **62 routes UNCHANGED**; Check 6 SKIPPED per Rule 27). **TWO P-43 cwd-leak reproductions on Check 5 during /scoreboard execution (LOW informational; 5th+ reproduction in the Pattern class)** — caught + recovered each time with absolute cd retry; mechanical prevention candidate flagged (add absolute cd prefix to ALL .claude/commands/scoreboard.md Bash commands, specifically Check 5). **Closes (a.83) RECOMMENDED-NEXT = P-46 Workstream 4 deploy session ✅ DEPLOYED-PHASE-4-PENDING 2026-05-26 on vklf.com**; **opens (a.84) RECOMMENDED-NEXT = P-46 Workstream 4 Phase-4 verification session** on `workflow-2-competition-scraping` — director performs 10-step real-Chrome verification walkthrough drafted this session (PRESERVED VERBATIM in NEXT_SESSION.md ## Launch prompt section). **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 4 DEPLOY §Entry 2026-05-26** capturing (a) W4 ✅ DEPLOYED-PHASE-4-PENDING 2026-05-26 on vklf.com — Phase-4 verification deferred at director request; (b) NEW informational Pattern "Truncated picker response → fire clarifying picker, don't silently interpret"; (c) LOW informational dual P-43 cwd-leak reproductions on Check 5 + mechanical prevention candidate identified; (d) calibration data point — W4 deploy landed without fix-forwards (Phase-4 deferred so no fix-forward cascade possible this session). **No corrections-tier slip occurred this session.** **TWENTY-EIGHTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → 2026-05-23-d → 2026-05-23-e → 2026-05-23-f → 2026-05-24 → 2026-05-24-b → 2026-05-25 → today 2026-05-26). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-26_p46-workstream-4-deploy-session-phase-4-verification-deferred (Claude Code, on `workflow-2-competition-scraping` then ff-merged to `main`)

**Previously updated:** 2026-05-25 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-25_p46-workstream-4-session-2-internal-hyperlink-tiptap-extension` — **W#2 polish P-46 Workstream 4 (Comprehensive Competitor Analysis page) Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-25 on `workflow-2-competition-scraping`** — second build session of the P-46 Workstream 4 implementation arc (Session 2 of ~2-3 estimated per §C.4); landed the full §C.4 Session 2 scope end-to-end at code level — the internal-hyperlink TipTap extension that lets the director write `#url/<urlId>` shorthand inline and have it render as a clickable hyperlink to the corresponding URL detail page — NEW `url-reference-helpers.ts` (~115 LOC pure helpers) + NEW `url-reference-helpers.test.ts` (19 new node:test cases bringing src/lib to **783 from baseline 764**) + NEW `UrlReferenceExtension.ts` (~105 LOC TipTap Extension via `addProseMirrorPlugins`) + NEW `LinkToUrlPicker.tsx` (~304 LOC toolbar dropdown — lazy-loads URL list via `authFetch`; case-insensitive search filter) + MODIFIED `RichTextEditor.tsx` (+90/-1 LOC; accepts optional `projectId` prop; registers `UrlReferenceExtension`; extends `Link.configure`'s `isAllowedUri` to accept `#url/` hrefs; surfaces `LinkToUrlPicker` in 'full' toolbar when `projectId` provided; inline `<style>` with `InternalLinkStyles` for distinct styling — 🔗 emoji glyph prefix + same blue underlined) + MODIFIED `AnalysisEditor.tsx` + `AnalysisReadView.tsx` + `comprehensive-analysis/page.tsx` all pass `projectId` through. Build commit `5854eff` — 8 files +786/-1 on `workflow-2-competition-scraping`; NOT pushed to main since Session 2 is a build session, not a deploy session. **Headline outcome:** the per-Project Comprehensive Competitor Analysis page now supports inline internal hyperlinks — director can write `#url/<urlId>` shorthand inline OR use the new "Link to URL" toolbar dropdown; clicking the rendered hyperlink in either edit or read mode navigates to the matching URL detail page. **TWO Rule 14f forced-pickers fired at session start** — Session 2 scope (Extension + Link-to-URL picker recommended) + Visual styling of internal hyperlinks (Distinct — small URL icon prefix + same blue recommended). **ZERO Rule 14f sub-pickers fired during execution** per `feedback_default_to_recommendation.md`. **ONE new reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-25 — "Custom TipTap extension via `addProseMirrorPlugins` for click interception of shorthand hrefs without a custom Mark type"** (pairs with W4 S1's Pattern A "Per-Project edit-affordance parallel to per-row edit-affordance" + Pattern B "Editor-as-readonly substitutes for a separate static renderer" as the third W4-arc Pattern). **§A frozen** per Rule 18 — §A.4's example syntax + §A.5's "custom TipTap extension" guidance consumed; no §A amendment. **Schema-change-in-flight flag STAYS NO** entire session — no `prisma db push`. All 6 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **783 src/lib +19 from baseline 764** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.82) RECOMMENDED-NEXT = P-46 Workstream 4 Session 2**; **opens (a.83) RECOMMENDED-NEXT = P-46 Workstream 4 deploy session** on `workflow-2-competition-scraping`. **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous per §C.4 + the "Multi-session workstream deploy gate timing" reusable Pattern memorialized 2026-05-23-c. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 4 Session 2 closing §Entry 2026-05-25** capturing (a) Session 2 closed at code level for W#4; W4 arc COMPLETE at code level across Sessions 1-2; (b) NEW reusable Pattern "Custom TipTap extension via `addProseMirrorPlugins`"; (c) TWO Rule 14f forced-pickers; (d) calibration data point — Session 2 of 2-3 estimated landed cleanly within bundled scope. **No corrections-tier slip occurred this session.** **TWENTY-SEVENTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → 2026-05-23-d → 2026-05-23-e → 2026-05-23-f → 2026-05-24 → 2026-05-24-b → today 2026-05-25). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-25_p46-workstream-4-session-2-internal-hyperlink-tiptap-extension (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-24-b (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-24-b_p46-workstream-4-session-1-comprehensive-analysis-page` — **W#2 polish P-46 Workstream 4 (Comprehensive Competitor Analysis page) Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-24-b on `workflow-2-competition-scraping`** — first build session of the P-46 Workstream 4 implementation arc (Session 1 of ~2-3 estimated per §C.4); landed the full §C.4 Session 1 scope end-to-end at code level — NEW `comprehensive-analysis.ts` DI-seam handler factory (~225 LOC mirroring `user-table-preferences.ts` precedent) + NEW `comprehensive-analysis.test.ts` (20 new node:test cases bringing src/lib to **764 from baseline 744**) + W1's 501-stub at `comprehensive-analysis/route.ts` replaced by thin DI-seam shim + NEW page route `comprehensive-analysis/page.tsx` (~269 LOC) + NEW `AnalysisEditor.tsx` (~151 LOC parallel-component to W2 S1's PerItemAnalysisBox but PUT method + `{ contentJson }` body + variant='full' toolbar) + NEW `AnalysisReadView.tsx` (~76 LOC; renders via `RichTextEditor readOnly=true` to keep typography identical across read/edit modes) + extended `RichTextEditor.tsx` variant='full' toolbar per §A.5 (H1/H2/H3 + Bold + Italic + Underline + bullet/numbered lists + Link + Code block) + standalone navigation button row above `ColumnVisibilityBar` in `CompetitionScrapingViewer.tsx` (+31 LOC) + `@tiptap/extension-underline@3.23.6` promoted to top-level dep. Build commit `283d4d1` — 9 files +1258/-32 on `workflow-2-competition-scraping`; NOT pushed to main since Session 1 is a build session, not a deploy session. **Headline outcome:** the per-Project Comprehensive Competitor Analysis page enters its W#4 first build — director can navigate from the Competition Data page via a new standalone button, lands on a per-Project rich-text doc that reads + writes against the W1 `ComprehensiveCompetitorAnalysis` Prisma model. **TWO Rule 14f forced-pickers fired at session start** — Session 1 scope (Full §C.4 scope recommended over Conservative pointer scope + Handler-only this session per `feedback_recommendation_style.md`) + Navigation surface placement (Standalone button above the bar recommended over Tab strip + Button inside ColumnVisibilityBar). **FIRST APPLICATION of the 2026-05-24 Rule 14a tightening** — the §C.4 vs. NEXT_SESSION.md pointer divergence on Session 1 scope was surfaced via Rule 14f picker rather than silently picking the pointer's recommendation; director picked the §C.4 binding spec. **ZERO Rule 14f sub-pickers fired during execution** per `feedback_default_to_recommendation.md`. **TWO new reusable Patterns memorialized in CORRECTIONS_LOG §Entry 2026-05-24-b** — Pattern A "Per-Project edit-affordance parallel to per-row edit-affordance" (pairs with W2 S1's "PerItemAnalysisBox extraction" Pattern + W2 S3's "OverallAnalysisBox parallel component" Pattern as meta-shape — when a shared per-action lifecycle is needed at N scopes per-row / per-category / per-Project, each new scope adds a thin parallel component wrapping the same editor primitive) + Pattern B "Editor-as-readonly substitutes for a separate static renderer" (§A.5 mentions `generateHTML` for non-editor read views; AnalysisReadView instead uses `RichTextEditor readOnly=true` to keep typography identical between read/edit modes without a duplicate render path). **Schema-change-in-flight flag STAYS NO** the entire session — no `prisma db push`; consumes the existing `ComprehensiveCompetitorAnalysis` model. All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **764 src/lib +20 from baseline 744** — exact match with 20 new comprehensive-analysis.test.ts cases / **61 routes UNCHANGED** — W1 501-stub at existing path filled in); Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.81) RECOMMENDED-NEXT = P-46 Workstream 4 Session 1**; **opens (a.82) RECOMMENDED-NEXT = P-46 Workstream 4 Session 2** on `workflow-2-competition-scraping` — internal-hyperlink TipTap extension per §C.4 Session 2 spec. **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous per §C.4 Session 2 spec. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 4 Session 1 closing §Entry 2026-05-24-b** capturing (a) Session 1 closed at code level for W#4; (b) NEW Pattern A "Per-Project edit-affordance parallel to per-row edit-affordance"; (c) NEW Pattern B "Editor-as-readonly substitutes for a separate static renderer"; (d) FIRST APPLICATION of the 2026-05-24 Rule 14a tightening — the §C.4 vs. pointer divergence on Session 1 scope was surfaced via Rule 14f picker; (e) calibration data point — Session 1 of 2-3 estimated landed in ~150-180 min. **No corrections-tier slip occurred this session.** **TWENTY-SIXTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → 2026-05-23-d → 2026-05-23-e → 2026-05-23-f → 2026-05-24 → today 2026-05-24-b). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-24-b_p46-workstream-4-session-1-comprehensive-analysis-page (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-24 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-24_p46-workstream-3-deploy-and-five-fix-forwards` — **W#2 polish P-46 Workstream 3 (Competition Data table redesign) SHIPPED + DEPLOYED + REAL-CHROME-VERIFIED on vklf.com 2026-05-24 via `workflow-2-competition-scraping` → `main` initial ff-merge `51e68f8..c727da9` carrying 6 commits + 5 in-session fix-forwards `f8293f1` → `0703174` → `712efa0` → `7358963` → `ac45737` resolving 11 Phase-4 verification issues end-to-end**) — pure deploy session that converted Workstream 3's three build sessions into 6 production deploys to vklf.com in one calendar day; 11 distinct UI issues surfaced at Phase-4 director verification + all 11 resolved + all 11 re-verified PASS on vklf.com after fix-forward #5; director's exact end-of-session words: *"pass"*. **Headline outcome: P-46 Workstream 3 ✅ DONE-AND-VERIFIED 2026-05-24 on vklf.com end-to-end.** Combined W1+W2+W3 total spend = 11 sessions vs. 7-11 estimated per §C.1/§C.2/§C.3 — top end of estimate with no overrun. **Six deploy pushes to `origin/main` today — highest count in any single session to date** (initial deploy + fix-forwards #1-5); ~14 pushes total across deploys + ping-pong syncs + end-of-session bundle. Pre-deploy /scoreboard 5/5 GREEN at baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 744 src/lib UNCHANGED / 61 routes UNCHANGED); Check 6 Playwright SKIPPED per Rule 27. Post-merge /scoreboard 5/5 GREEN at same baselines. After each fix-forward — root tsc + Next.js build re-verified at 61 routes; full scoreboard not re-run per fix-forward (calibration data point: baselines stayed UNCHANGED across all 6 deploys — no new tests, no new routes, no new dependencies). **NEW reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-24 — "Phase-4 verification fix-forward cascade in a single deploy session"** — when Phase-4 director verification surfaces multiple issues post-deploy, fix-forward in-session rather than deferring; each fix-forward becomes its own build commit + own Rule 9 gate + own Phase-4 reverify cycle; today set new high-water mark at 6 deploys; pairs with the P-45 Build #2 2026-05-22-i fix-forward Pattern (1+1) — today extends to N≥5 fix-forwards in one session showing the pattern scales when issues are scoped + reversible + UI-only. **MEDIUM informational — Rule 18 spec-capture gap surfaced + mechanical prevention added to working methodology:** director's column-order specification was never echoed into binding docs; Session 2 defaulted to "additive append"; surfaced at Phase-4 verification + fixed-forward #3; mechanical prevention added — any UI-shape spec given mid-build (column order, button position, layout decision, sort default) must be echoed into binding docs + read back per Rule 14a BEFORE implementation lands. **Schema-change-in-flight flag STAYS NO** entire session — ZERO `prisma db push`; all 5 fix-forwards consume the W1 schema live since 2026-05-24 + deployed since 2026-05-23-c. **Closes (a.80) RECOMMENDED-NEXT = P-46 Workstream 3 deploy session ✅ DONE-AND-VERIFIED 2026-05-24 on vklf.com**; **opens (a.81) RECOMMENDED-NEXT = P-46 Workstream 4 (Comprehensive Competitor Analysis page) first build session** on `workflow-2-competition-scraping` per Q10's locked sequencing — ~2-3 sessions estimated per §C.4. **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous per §C.4 + Q10's locked sequencing + the "Multi-session workstream deploy gate timing" Pattern memorialized 2026-05-23-c (next workstream's foundation build begins after the prior workstream's deploy closes ✅ DONE-AND-VERIFIED). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 3 DEPLOY CLOSING §Entry 2026-05-24** capturing (a) W3 ✅ DONE-AND-VERIFIED end-to-end via initial deploy + 5 fix-forwards + 6/6 Phase-4 verifies PASS; (b) NEW reusable Pattern "Phase-4 verification fix-forward cascade in a single deploy session" memorialization; (c) MEDIUM informational Rule 18 spec-capture gap observation + mechanical prevention added; (d) LOW informational P-43 cwd-leak class re-reproduced ~9 times today across pre-deploy + post-merge + single-check sanity passes (pattern stable; SAME LOW informational class as multiple prior reproductions); (e) LOW informational date-stamping anomaly continuation — calendar date today is 2026-05-24 but 5 fix-forward commit MESSAGES used suffix labels `2026-05-23-g` through `2026-05-23-k` continuing yesterday's `-f` sequence (git commit TIMESTAMPS correct; only human-readable in-message labels diverged). **No corrections-tier slip occurred this session.** **TWENTY-FIFTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → 2026-05-23-d → 2026-05-23-e → 2026-05-23-f → today 2026-05-24). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-24_p46-workstream-3-deploy-and-five-fix-forwards (Claude Code, on `workflow-2-competition-scraping` then ff-merged to `main` then ping-ponged back; six deploy cycles + ping-pong syncs)

**Previously updated:** 2026-05-23-f (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-23-f_p46-workstream-3-session-3-column-resize-drag-reorder-font-size` — **W#2 polish P-46 Workstream 3 (Competition Data table redesign) Session 3 ✅ DONE-AT-CODE-LEVEL 2026-05-23-f on `workflow-2-competition-scraping`** — third + final build session of the P-46 Workstream 3 implementation arc (Session 3 of 3-4 estimated per §C.3 — landed inside the estimated window); landed the bundled-scope Session 3 deliverables — **column resize** (drag the right edge of any column header; widths persist per-user-per-project via `UserTablePreferences.columnWidths`; clamped to `MIN_COLUMN_WIDTH=60` / `MAX_COLUMN_WIDTH=600`) + **drag-to-reorder rows** (grab the ⋮⋮ handle on the left edge of any row; row order persists via `UserTablePreferences.rowOrder`; built on `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities`) + **table-wide font-size stepper** (− / Npt / + at the right of the horizontal bar; 10pt-24pt range per W1's `fontSize` Prisma constraint; persists via `UserTablePreferences.fontSize`). All three controls route through Session 1's `prefsTimerRef` 500 ms debounced PUT lifecycle so a burst of drag events from any control coalesces into one network write. Build commit `7ad7eff` — 6 files +781/-178 on `workflow-2-competition-scraping`; NOT pushed to main since Session 3 is a build session, not a deploy session. **Headline outcome:** the Competition Data table now carries the full §C.3 personalization affordance set — column visibility (Session 1) + click-to-edit cells + 17 columns (Session 2) + column resize + drag-to-reorder + font-size stepper (today). Workstream 3 implementation arc COMPLETE at code level across Sessions 1-3. **Three new npm dependencies landed** (`@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` — first time the Competition Data table layer takes a runtime drag-and-drop library; ~30 KB gzipped one-time cost). **ONE Rule 14f forced-picker fired this session** — session-start scope-pick: director picked "Bundle all 3 controls (recommended)" over partial-bundle alternatives per `feedback_recommendation_style.md`. **NEW reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-23-f — "Shared debounced-mutation lifecycle reused across an N-control surface"** — when a foundation session wires a debounced PUT lifecycle for ONE field of a shared per-user prefs row, the next session can land N additional controls without re-deriving the batching logic by routing every new control through the SAME timer ref + the SAME flush callback (pairs with W2 S5's "Field-allowlist subset extraction" Pattern as another example of "foundation-session primitive becomes Pattern when reused by N=2+ subsequent sessions"). **Schema-change-in-flight flag STAYS NO** — no `prisma db push` this session; all three fields (`columnWidths` / `rowOrder` / `fontSize`) are already in `UserTablePreferences` from W1's 2026-05-24 schema. All 5 /scoreboard checks GREEN at baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **744 src/lib UNCHANGED** — no new server-side code; UI-only session per pointer expectation / **61 routes UNCHANGED**); Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.79) RECOMMENDED-NEXT = P-46 Workstream 3 Session 3**; **opens (a.80) RECOMMENDED-NEXT = P-46 Workstream 3 deploy session** on `workflow-2-competition-scraping` — Phase-4 deploy ff-merging `workflow-2-competition-scraping` → `main` (carries Sessions 1+2+3's build commits + the three doc-batch commits as one fast-forward, 6 commits total) → Vercel auto-redeploy → ping-pong sync → Phase-4 director real-Chrome cross-platform verify across the Competition Data table surfaces. **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous per §C.3 Workstream 3 completion + the "Multi-session workstream deploy gate timing" reusable Pattern memorialized 2026-05-23-c (deploy session lands after the LAST build session that contains user-visible UI). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 3 Session 3 closing §Entry 2026-05-23-f** capturing (a) successful application of W1's prefs plumbing pattern at N=3 controls (validates the new "Shared debounced-mutation lifecycle" Pattern); (b) the NEW reusable "Shared debounced-mutation lifecycle reused across an N-control surface" Pattern memorialization; (c) P-43 cwd-leak class re-reproduced ONCE during /scoreboard (SIXTH reproduction overall; LOW informational; same Pattern as 2026-05-22-i + 2026-05-23-b + 2026-05-23-c + 2026-05-23-e); (d) calibration data point — Session 3 of 3-4 estimated Workstream 3 sessions landed cleanly within the bundled scope. **No corrections-tier slip occurred this session.** **TWENTY-FOURTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → 2026-05-23-d → 2026-05-23-e → today 2026-05-23-f). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-23-f_p46-workstream-3-session-3-column-resize-drag-reorder-font-size (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-23-e (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-23-e_p46-workstream-3-session-2-click-to-edit-cell-editors-and-eight-new-data-columns` — **W#2 polish P-46 Workstream 3 (Competition Data table redesign) Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-23-e on `workflow-2-competition-scraping`** — second build session of the P-46 Workstream 3 implementation arc (Session 2 of 3-4 estimated per §C.3); landed the full §A.2 click-to-edit cell editors (every cell becomes its appropriate inline editor on click — text / multiline-text / integer / decimal / boolean / enum / date / url via new `InlineCells.tsx` ~800 LOC compact in-table editor primitives parallel-component-set to URL detail page's `EditableField.tsx`) + 8 new data columns (5 W1-additive — `type` / `description1` / `description2` / `price` / `competitionScore`; 3 pre-existing-but-unsurfaced — `resultsPageRank` / `sellerStarRating` / `numSellerReviews`) bringing the Competition Data table from 9 to 17 columns + extended the `urls/[urlId]` PATCH route allowlist for `competitionScore` via NEW `competition-score-validation.ts` trust-boundary helper (direct application of W2 S5's "Field-allowlist subset extraction" reusable Pattern; 18 new node:test cases bringing src/lib to 744) + whole-row `onClick={handleRowOpen}` removed in favor of explicit per-row "↗" Open button in row-actions column (UX behavior change mandated by §A.2 since whole-row onClick conflicted with cell-level click-to-edit). Build commit `899afd4` — 7 files +1414/-91 on `workflow-2-competition-scraping`; NOT pushed to main since Session 2 is a build session, not a deploy session. **Headline outcome:** the Competition Data table is now fully click-to-edit per §A.2 — director can click any cell to begin editing in-place; Tab/Enter saves; Escape cancels; optimistic update + rollback-on-throw + inline error pill until next edit. **Rule 14f forced-picker fired ONCE this session** — session-start scope-pick: director picked "Bundle: new columns + click-to-edit together (recommended)" over "Click-to-edit on existing columns only" per `feedback_recommendation_style.md`. **NEW reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-23-e — "In-table inline-cell parallel-component set to URL-detail-page EditableField primitives"** — when a feature needs the same edit-affordance shape in two different visual contexts (per-cell editing in a dense table row vs. per-field editing in a labeled detail panel), prefer a parallel-component set over forcing the existing components to handle both via configuration props (pairs with W2 Session 3's "OverallAnalysisBox parallel to PerItemAnalysisBox" Pattern as meta-shape covering parallel-component for visual-context-divergence across both EDIT + ANALYSIS surfaces). **Schema-change-in-flight flag STAYS NO** — no `prisma db push` this session; the 8 new data columns are all already in `CompetitorUrl` from W1's 2026-05-24 schema. All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **744 src/lib node:test +18 from baseline 726** — exact match with 18 new competition-score-validation.test.ts cases / **61 routes UNCHANGED** — `competitionScore` added to existing route's allowlist; no new routes); Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.78) RECOMMENDED-NEXT = P-46 Workstream 3 Session 2**; **opens (a.79) RECOMMENDED-NEXT = P-46 Workstream 3 Session 3** on `workflow-2-competition-scraping` — column resize (drag column edges) + drag-to-reorder rows + adjustable font size per §C.3 Session 3 spec; consumes existing `columnWidths` / `fontSize` / `rowOrder` JSON columns on `UserTablePreferences` from W1's schema via Session 1's auth-derived-userId route. **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous per the §C.3 Workstream 3 plan (Session 3 = column resize + drag-to-reorder + font size). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 3 Session 2 closing §Entry 2026-05-23-e** capturing (a) successful application of W2 S5's "Field-allowlist subset extraction" Pattern to `competitionScore` (Pattern reusability validated after one prior application); (b) the NEW reusable "In-table inline-cell parallel-component set" Pattern memorialization; (c) the UX behavior change — whole-row onClick removed in favor of per-row "↗" Open button mandated by §A.2; (d) P-43 cwd-leak class re-reproduced ONCE during /scoreboard (FIFTH reproduction overall; LOW informational; same Pattern as 2026-05-22-i + 2026-05-23-b + 2026-05-23-c); (e) calibration data point — Session 2 of 3-4 estimated Workstream 3 sessions landed cleanly within the bundled scope. **No corrections-tier slip occurred this session.** **TWENTY-THIRD end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → 2026-05-23-d → today 2026-05-23-e). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-23-e_p46-workstream-3-session-2-click-to-edit-cell-editors-and-eight-new-data-columns (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-23-d (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-23-d_p46-workstream-3-session-1-table-preferences-and-column-visibility-bar` — **W#2 polish P-46 Workstream 3 (Competition Data table redesign) Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-23-d on `workflow-2-competition-scraping`** — first build session of the P-46 Workstream 3 implementation arc (Session 1 of 3-4 estimated per §C.3); landed the foundational `UserTablePreferences` plumbing (DI-seam handler factory + 34 new node:test cases + new route at the auth-derived-userId path matching W#2 convention) + horizontal `ColumnVisibilityBar` (two checkbox groups: Platforms All+7 single-select preserving all 7 platforms from the deleted sidebar + Columns 9 toggles multi-select) at the top of the Competition Data table + deleted the left-side `PlatformSidebar`; column visibility persists per-user-per-project via auth-derived-userId path with cross-device sync. Build commit `d846a97` — 10 files +1369/-224 on `workflow-2-competition-scraping`; NOT pushed to main since Session 1 is a build session, not a deploy session. **Headline outcome:** the Competition Data page enters its W#3 visual + behavioral redesign — the left-side platform navigation is gone in favor of a horizontal bar combining platform filters + per-column show/hide toggles; checkbox state syncs across devices via the `UserTablePreferences` Prisma model Workstream 1 shipped + the new auth-derived-userId route this session shipped. **Path-convention refactor at Rule 14f session-start:** W1 originally scaffolded the table-preferences route under `/api/users/[userId]/table-preferences/[projectId]` which diverged from W#2's auth-derived-userId convention (extension-state at `/api/extension-state` + every other W#2 project-scoped route at `/api/projects/[projectId]/competition-scraping/...`); director picked "Refactor — match platform convention (recommended)" per `feedback_recommendation_style.md` over "Keep current path, add ownership check" — the W1 501-stub comment explicitly flagged this as a "surface before next workstream lands" item. **NEW reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-23-d — "Foundation-workstream path-convention drift surfaced by next-workstream session-start picker"** — when a foundation workstream scaffolds a route at a path that diverges from the platform's convention, the next workstream's session-start should fire a Rule 14f picker on the path before implementation lands (pairs with the 2026-05-28 Session 4 "W1 path-divergence finding + architectural-move resolution" Pattern but for URL-shape rather than nesting depth). NEW `src/lib/competition-scraping/handlers/user-table-preferences.ts` (~340 LOC DI-seam handler factory mirroring `url-text.ts` / `reviews-by-id.ts` precedent — `extractTablePreferencesPatch` strict trust-boundary validator with field allowlist for the 6 editable fields per `WriteUserTablePreferencesRequest` + `toWireShape` coercing Prisma JsonValue columns to typed shapes with bad-DB-shape fallback + `makeUserTablePreferencesHandlers` factory returning GET + PUT) + 34 new node:test cases (extract validation across all 6 fields columnVisibility / columnWidths / fontSize / rowOrder / lastUsedSortColumn / lastUsedSortDirection; toWireShape coercion + bad-DB defaults + filter-non-boolean; GET/PUT happy paths + auth-failure + 400-on-bad-shape + 500-on-prisma-throw). NEW `src/app/api/projects/[projectId]/competition-scraping/table-preferences/route.ts` (~70 LOC thin shim wiring DI seam to `verifyProjectAuth` + `withRetry` + `recordFlake` + CORS; adapts `verifyProjectAuth`'s NextResponse error shape to the DI seam's HandlerResult shape). DELETED W1's `src/app/api/users/[userId]/table-preferences/[projectId]/route.ts` 501-stub at the old path. NEW `ColumnVisibilityBar.tsx` (~180 LOC horizontal bar; single-select platform chips with "All Platforms" reset semantics; multi-select column toggles driven by props; owns no state; exports `isColumnVisible(map, columnId)` with missing-key-default-true semantics matching §A.3). NEW `url-table-columns.ts` (~35 LOC canonical `TABLE_COLUMN_DEFS` registry of 9 column id ↔ label pairs matching UrlTable's SortKey + `TABLE_COLUMN_IDS` Set + relocated `ScopeFilter` type from the deleted PlatformSidebar; stable id strings double as keys in `UserTablePreferences.columnVisibility` JSON). MODIFIED `CompetitionScrapingViewer.tsx` (drops the 220px grid + sidebar layout for a vertical stack with the bar above the table; adds `columnVisibility` state seeded from GET `/api/projects/[projectId]/competition-scraping/table-preferences` at mount with silent 404/network fallback to empty map; `handleToggleColumn` with 500ms debounced PUT via `prefsTimerRef` so the latest body wins on burst toggles; imports `ScopeFilter` from `url-table-columns` after PlatformSidebar's deletion). MODIFIED `UrlTable.tsx` (adds optional `columnVisibility` prop; computes `visibleColumns` via `useMemo` filtering `COLUMNS` with missing-key-default-true semantics; extracts cell renderers into a `Record<SortKey, (row) => ReactNode>` map so the tbody iterates `visibleColumns.map((col) => cellRenderers[col.key](row))` without duplicate visibility branches per column; trash-button column stays unfiltered as row-action surface). MODIFIED `src/lib/shared-types/competition-scraping.ts` (doc-comment updates on `ReadUserTablePreferencesResponse` + `WriteUserTablePreferencesRequest` to reflect the new auth-derived path; wire shapes themselves unchanged — W1 already shipped them correctly). DELETED `PlatformSidebar.tsx` (replaced by `ColumnVisibilityBar`). **Schema-change-in-flight flag STAYS NO** — no `prisma db push` this session; consumes existing Workstream 1 schema (`UserTablePreferences` model + columnVisibility/columnWidths/fontSize/rowOrder/lastUsedSortColumn/lastUsedSortDirection JSON columns already in place); default expectation: stays NO through Sessions 2-3 (UI-only consuming existing schema); re-evaluate if a new schema column surfaces during Sessions 2-3 (unlikely — the 12 new data columns are all already in `CompetitorUrl` from W1). All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **726 src/lib node:test +34 from baseline 692** — exact match with 34 new user-table-preferences.test.ts cases / **61 routes UNCHANGED** — new table-preferences route replaced deleted W1 stub, net 0); Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.77) RECOMMENDED-NEXT = P-46 Workstream 3 Session 1**; **opens (a.78) RECOMMENDED-NEXT = P-46 Workstream 3 Session 2** on `workflow-2-competition-scraping` — click-to-edit cell editors per §C.3 Session 2 spec (text/number/decimal/enum/boolean/date/tags inline editors reusing Session 5's `EditableTextField` + `EditableEnumField<T>` from `EditableField.tsx`); Session 2 may also begin landing the ~12 new data columns since the new columns + click-to-edit are mechanically coupled. **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous per the §C.3 Workstream 3 plan (Session 2 = click-to-edit cell editors; potentially bundled with the 12 new data columns). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 3 Session 1 closing §Entry 2026-05-23-d** capturing (a) the path-convention refactor decision narrative + the Rule 14f session-start picker outcome; (b) the NEW reusable Pattern memorialized "Foundation-workstream path-convention drift surfaced by next-workstream session-start picker" (pairs with Session 4's path-divergence Pattern); (c) calibration data point — Session 1 of 3-4 estimated Workstream 3 sessions landed cleanly within scope (foundational preferences plumbing + horizontal bar + sidebar removal in one session matches §C.3 Session 1 spec). **No corrections-tier slip occurred this session.** **TWENTY-SECOND end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → 2026-05-23-c → today 2026-05-23-d). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-23-d_p46-workstream-3-session-1-table-preferences-and-column-visibility-bar (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-23-c (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-23-c_p46-workstream-2-deploy-session` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) SHIPPED + DEPLOYED + REAL-CHROME-VERIFIED across all 6 surfaces on vklf.com 2026-05-23-c via `workflow-2-competition-scraping` → `main` ff-merge `783abf4..9969427` (49 files +7504/-477 — 13 commits ff'd as one fast-forward carrying Workstream 1's 2026-05-24 schema + Sessions 1-5's UI/route from 2026-05-25 through 2026-05-23-b)** — pure orchestration deploy session, no new code, no new schema, no new dependencies, no fresh extension zip (extension untouched in this deploy bundle — the lone `extensions/` file in the ff-merge is `captured-text-validation.test.ts` +11 lines test-only); closes P-46 Workstream 2 end-to-end after Workstream 1 schema (2026-05-24) + Sessions 1-5 builds (2026-05-25 through 2026-05-23-b) + today's deploy (2026-05-23-c); Phase 1 pre-deploy /scoreboard all 5 checks GREEN at Session 5 baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 692 src/lib UNCHANGED / 61 routes UNCHANGED); Check 6 Playwright SKIPPED via Rule 27 picker (recommended; test-only extension file in the bundle). Phase 2 Rule 9 director-Yes gate fired ONCE via AskUserQuestion picker for `git push origin main` — director picked "Deploy now (recommended)". Phase 3 ff-merge `783abf4..9969427` clean; post-merge /scoreboard all 5 checks GREEN on main (tree-identity preserved through ff); push succeeded; Vercel auto-redeploy fired; ping-pong sync was a NO-OP (workflow-2 already at `9969427` from the prior session's doc-batch push). Phase 4 director real-Chrome cross-platform verify ALL 6 SURFACES PASS clean with zero caveats — Captured Text + Image + Video + Reviews card-list sections all PASS / URL-level affordances Type+Description-1+Description-2+Price+Scraping Status toggle+Overall Competitor Analysis PASS / Competition Data table Status pill column bidirectional mirror PASS — the cleanest end-of-workstream verification in any P-46 session. **Schema-change-in-flight flag FLIPPED YES → NO at deploy completion** (Workstream 1's schema columns + 3 new tables + new enum now have production code reading + writing them on vklf.com after being live on Supabase since 2026-05-24). **Closes (a.76) RECOMMENDED-NEXT = P-46 Workstream 2 deploy session**; closes **P-46 Workstream 2 ✅ DONE-AND-VERIFIED 2026-05-23-c** (5 build sessions vs. 3-5 estimated — top end of range; combined Workstream 1+2 = 7 sessions vs. 4-8 estimated; right on plan). **Opens (a.77) RECOMMENDED-NEXT = P-46 Workstream 3 (Competition Data table redesign) first build session** per Q10's locked sequencing — ~3-4 sessions covering ~12 new columns + click-to-edit on every cell + resizable widths + drag-reorder rows + show/hide toggles + adjustable font size + cross-device-synced UserTablePreferences (model already shipped in Workstream 1; UI not yet built). ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-23-c — the P-46 Workstream 2 DEPLOY CLOSING §Entry capturing Workstream 2 done end-to-end + the new reusable Pattern **"Multi-session workstream deploy gate timing"** (deploy session lands after the last UI build session, not after schema; pairs with the 2026-05-24 "Workstream Foundation Build Bundle" Pattern) + TWO LOW informational sub-observations (P-43 cwd-leak class re-reproduced TWICE during /scoreboard Check 5 in both pre-deploy + post-merge runs — caught + recovered both times; pointer-file off-by-one on main-tip SHA — NEXT_SESSION.md said `ee8c79d` but actual main was `783abf4` from the prior doc-batch; ff-merge count of 13 still correct) + calibration data point that Workstream 2 came in at the top end of the 3-5 estimate. **No top-level corrections-tier slip occurred this session.** NEW §B 2026-05-23-c entry appended to `docs/COMPETITION_DATA_V2_DESIGN.md` before END OF DOCUMENT marker capturing Workstream 2 deploy outcome + schema-change-in-flight flag YES → NO transition + the new Pattern + the two LOW informational sub-observations; §A stays frozen per Rule 18 — §C.2 reaches 100% complete; §C.3 begins next session. **TWENTY-FIRST end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → 2026-05-23-b → today 2026-05-23-c). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-23-c_p46-workstream-2-deploy-session (Claude Code, on `workflow-2-competition-scraping` then ff-merged to `main` then ping-ponged back)

**Previously updated:** 2026-05-23-b (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-23-b_p46-workstream-2-session-5-url-level-structural-fields` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 5 ✅ DONE-AT-CODE-LEVEL 2026-05-23-b on `workflow-2-competition-scraping`** — fifth + final build session of the P-46 Workstream 2 implementation arc (Session 5 of 3-5 estimated per §C.2); landed the last §C.2 sub-scope for Workstream 2 — URL-level structural fields (Type / Description-1 / Description-2 / Price as 4 new free-text fields at the top of the URL box) + Scraping Status toggle (Incomplete/Complete enum write via the NEW `EditableEnumField<T>` segmented-control component) + remove Sizes/Options UI (full delete per project rules; underlying `CompetitorSize` table data stays per Q6 hide-UI-keep-data; Git history preserves reversibility) + add read-only Status pill column (green Complete / gray Incomplete) second-from-left after URL in the Competition Data table bidirectionally mirroring the URL detail page toggle per Q8 §A.8. Build commit `374f1a3` — 6 files +669/-109 on `workflow-2-competition-scraping`; NOT pushed to main since Session 5 is a build session, not a deploy session. **Headline outcome: Workstream 2 is now COMPLETE at code level — Sessions 1-5 of 3-5 estimated all DONE.** NEW `src/lib/competition-scraping/url-structural-fields-validation.ts` (~105 LOC pure trust-boundary helper `extractUrlStructuralFieldsPatch(body)` returning `{ ok, patch } | { ok, error }`; trim-or-null normalization for the 4 text fields; strict enum-acceptance for `scrapingStatus` via `isScrapingStatus`); 22 new node:test cases bringing src/lib to 692. NEW generic `EditableEnumField<T extends string>` segmented-control component in `EditableField.tsx` (reusable by Workstream 3 for click-to-edit enum cells). `EditableTextField` extended with optional `multiline` + `rows` props for db.Text fields. `SizesSubsection` function + helpers fully deleted. Status pill column added to UrlTable second-from-left after URL via new `scrapingStatusBadgeStyle(status)` helper. **Director picked Proceed at the Rule 14f session-start operational scope picker** — confirmed Session 5's scope (the only §C.2 sub-scope remaining); per `feedback_default_to_recommendation.md` the sub-picker was skipped. **ZERO Rule 14f sub-pickers fired during execution** — every sub-decision was a default-to-recommendation skip per `feedback_recommendation_style.md`. **Schema-change-in-flight flag STAYS YES** (carrying from Workstream 1's 2026-05-24 `prisma db push`; stays YES through the future Workstream 2 deploy session — flips back to NO at that deploy completion). All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **692 src/lib node:test +22 from baseline 670** / **61 routes UNCHANGED** — no new routes; only extended existing `urls/[urlId]` PATCH allowlist) — Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.75) RECOMMENDED-NEXT = P-46 Workstream 2 Session 5**; **opens (a.76) RECOMMENDED-NEXT = P-46 Workstream 2 deploy session** on `workflow-2-competition-scraping` — Phase-4 deploy ff-merging `workflow-2-competition-scraping` → `main` (carries Workstream 1's schema commits + Sessions 1-5's UI/route commits as one fast-forward) → Vercel auto-redeploy → ping-pong sync → Phase-4 director real-Chrome cross-platform verify across 5 surfaces. **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous (Workstream 2 deploy is the only remaining step before Workstream 3 begins). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 2 Session 5 closing §Entry** capturing Workstream 2 completion + the new "Field-allowlist subset extraction" reusable Pattern + the date-stamping anomaly informational observation (today's date per director confirmation is 2026-05-23 but doc-history's recent stamps run 2026-05-24 → 2026-05-28; session suffix `-b` per Rule 14 disambiguates) + P-43 cwd-leak class re-reproduction ONCE at Check 5 (caught + recovered immediately; LOW informational; captured in §B 2026-05-23-b NOT promoted to a separate §Entry). **No corrections-tier slip occurred this session.** **TWENTIETH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → 2026-05-28 → today). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-23-b_p46-workstream-2-session-5-url-level-structural-fields (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-28 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-28_p46-workstream-2-session-4-captured-reviews-ui` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 4 ✅ DONE-AT-CODE-LEVEL 2026-05-28 on `workflow-2-competition-scraping`** — fourth build session of the P-46 Workstream 2 implementation arc (Session 4 of 3-5 estimated per §C.2); landed the Captured Reviews UI end-to-end via a NEW `CapturedReviewsSection` + `CapturedReviewCard` + `CapturedReviewAddModal` (with a 1-5 star-rating picker widget) + per-item Analysis box below each review + Overall Reviews Analysis box at the bottom of the section (via the existing Session 3 `OverallAnalysisBox` with `category='reviews'`) + 4 fully-implemented route handlers behind the Workstream 1 501-stub surface (POST list + GET list at the collection path + PATCH + DELETE at the per-record path) + an architectural-move fix relocating the per-record review route from W1's deeper `urls/[urlId]/reviews/[reviewId]` path to the shallow `reviews/[reviewId]` path matching the text/[textId] / images/[imageId] / videos/[videoId] precedent for the other 3 capture types. Build commit `82d390a` — 9 files +2350/-69 on `workflow-2-competition-scraping`; NOT pushed to main since Session 4 is a build session, not a deploy session. **Headline outcome:** Reviews is now a first-class capture type on the URL detail page — the last greenfield capture type missing UI; manual-add modal lands so director can enter reviews by hand on vklf.com (no extension Reviews capture in v1 per Q1's deferral); per-item Analysis below each card via the existing `PerItemAnalysisBox`; Overall Reviews Analysis box at the bottom of the section via Session 3's `OverallAnalysisBox`. NEW `src/lib/competition-scraping/handlers/url-reviews.ts` (~295 LOC DI seam for POST + GET collection handlers following the `url-text.ts` P-31 pattern; validates clientId + starRating in [1,5] + body + tags + reviewerName + reviewDate + analysis + source; 404 on parent-not-found; 201 happy; 200 idempotent on P2002 unique-constraint via clientId lookup) + NEW `src/lib/competition-scraping/handlers/reviews-by-id.ts` (~290 LOC DI seam for per-record PATCH + DELETE — sets a NEW PRECEDENT relative to text/[textId] / images/[imageId] / videos/[videoId] which were direct-shape with no DI seam; extracted because the analysis-field trust boundary + per-field allowlist warrant unit coverage at the handler layer) + 32 new node:test cases across the 2 new handler test files. MODIFIED `urls/[urlId]/reviews/route.ts` (W1 501-stub → thin shim ~82 LOC); NEW `reviews/[reviewId]/route.ts` (~80 LOC at SHALLOW PRECEDENT PATH); DELETED W1's `urls/[urlId]/reviews/[reviewId]/route.ts` (nested 501-stub; review IDs globally unique so deeper path adds no security and shallower path keeps per-record surface uniform across all 4 capture types); NEW `CapturedReviewAddModal.tsx` (~479 LOC; 1-5 star-rating picker widget rendered as ☆ / ★ buttons + body textarea + reviewer-name input + native HTML date input + tags input + Save disabled until starRating AND body populated); MODIFIED `UrlDetailContent.tsx` (+398 LOC; renders `<CapturedReviewsSection>` between `<CapturedVideosGallery>` and the page-bottom URL-level `<OverallAnalysisBox>`). **Director picked Option A at the Rule 14f session-start scope picker between 2 §C.2-aligned candidates** (A Captured Reviews UI recommended / B URL-level structural fields alt). **NO Rule 14f sub-pickers fired during execution** per `feedback_default_to_recommendation.md` (the architectural move from W1's nested per-record path to the shallow precedent path was a clear "most thorough/reliable" choice per `feedback_recommendation_style.md`). **Schema-change-in-flight flag STAYS YES** (carrying from Workstream 1's 2026-05-24 `prisma db push`; stays YES through subsequent W#2 / W#3 sessions until the first deploy session ships the schema-aware code to vklf.com — the `CapturedReview` Prisma model + the `overallAnalyses.reviews` JSON slot are both already deployed at schema level via Workstream 1; this session lands the route-handler + UI halves together so the round-trip works on workflow-2 even though the deploy is later). All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **670 src/lib node:test +32 from baseline 638 — exact match with 17 new url-reviews + 15 new reviews-by-id handler tests** / **61 routes UNCHANGED** — deleted W1 nested per-record stub + added new shallow per-record route = net 0; collection 501→DI-shim is a path already counted) — Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.74) RECOMMENDED-NEXT = P-46 Workstream 2 Session 4**; **opens (a.75) RECOMMENDED-NEXT = P-46 Workstream 2 Session 5** — recommended scope is the URL-level structural fields (Type / Description-1 / Description-2 / Price + Scraping Status toggle + remove Sizes/Options UI per §C.2) since that's the last §C.2 sub-scope remaining for Workstream 2 (Sessions 1-3 covered the Analysis surface arc; Session 4 covered Reviews; Session 5 covers structural fields). After Session 5, Workstream 2 is complete and the next session can be the Workstream 2 deploy session. **§4 Step 1c forced-picker NOT FIRED** — Session 5 task unambiguous per the §C.2 Workstream 2 plan (structural fields are the only §C.2 sub-scope left). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 2 Session 4 closing §Entry** capturing the W1 path-divergence finding + the architectural-move resolution executed within this session + the new reusable Pattern memorialized: "when a foundation workstream scaffolds a 501-stub at a path that diverges from the precedent shape of the sibling capture types, surface the divergence at the next-workstream session start + execute the path move + commit + announce in the build commit; treat W1 path divergences as informational findings to fix not blockers." Pairs with the design doc §B 2026-05-28 reusable Pattern "Per-record handler DI-seam precedent extension". **No corrections-tier slip occurred this session.** **NINETEENTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → 2026-05-27 → today). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-28_p46-workstream-2-session-4-captured-reviews-ui (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-27 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-27_p46-workstream-2-session-3-url-level-and-per-category-overall-analysis-boxes` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 3 ✅ DONE-AT-CODE-LEVEL 2026-05-27 on `workflow-2-competition-scraping`** — third build session of the P-46 Workstream 2 implementation arc (Session 3 of 3-5 estimated); completed the "Analysis surface" arc across all capture levels via a NEW `OverallAnalysisBox` parallel component shipping URL-level Overall Competitor Analysis + per-category Overall Analysis boxes (one per capture category — Text / Image / Video) at the bottom of each capture subsection + one URL-level box at the bottom of the URL detail page + extended the existing `urls/[urlId]` PATCH route allowlist for the two new fields with a NEW `isValidOverallAnalysesBag` strict-shape trust-boundary guard + bag-merge over replace at the PATCH so saving one category doesn't wipe sibling categories. Build commit `4773b62` — 5 files +452/-1 on `workflow-2-competition-scraping`; NOT pushed to main since Session 3 is a build session, not a deploy session. **Headline outcome:** the URL detail page now carries per-category Overall Analysis text boxes (one per capture category synthesizing all items in that category) + one URL-level Overall Competitor Analysis text box (synthesizing all categories) — all consuming the same Session 1 `RichTextEditor` wrapper with `variant='minimal'`, all persisting to Workstream-1 schema columns `CompetitorUrl.overallCompetitorAnalysis` + `CompetitorUrl.overallAnalyses` without needing any new schema. NEW `OverallAnalysisBox.tsx` (~200 LOC) is a parallel component to Session 1's `PerItemAnalysisBox.tsx` — same save-lifecycle but different wire body shape driven by a `field` discriminator prop. NEW `isValidOverallAnalysesBag` trust-boundary guard in `tiptap-helpers.ts` (+37 LOC) strict-rejects unknown category keys + requires each known-category value pass `isValidAnalysisPayload`. Existing `urls/[urlId]` PATCH route allowlist extended (+65 LOC) for the two new fields with bag-merge semantics for `overallAnalyses`. 10 new node:test cases for `isValidOverallAnalysesBag`. **Director picked Option A at Rule 14f session-start scope picker between 3 §C.2-aligned candidates** (A URL-level + per-category Overall Analysis recommended / B Captured Reviews UI alt / C URL-level structural fields alt). **NO Rule 14f sub-pickers fired during execution** per `feedback_default_to_recommendation.md` (bag-merge over replace at PATCH was the only valid choice; strict unknown-key rejection in the guard is most-thorough; parallel `OverallAnalysisBox` component over overloading `PerItemAnalysisBox` preserves wire-contract clarity at callsites). **Schema-change-in-flight flag STAYS YES** (carrying from Workstream 1's 2026-05-24 `prisma db push`; stays YES through subsequent W#2 / W#3 sessions until the first deploy session ships the schema-aware code to vklf.com). All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **638 src/lib node:test +10 from baseline 628** / **61 routes UNCHANGED** — no new routes; only extended existing `urls/[urlId]` PATCH allowlist) — Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.73) RECOMMENDED-NEXT = P-46 Workstream 2 Session 3**; **opens (a.74) RECOMMENDED-NEXT = P-46 Workstream 2 Session 4** — multiple §C.2-aligned candidates remain (Captured Reviews UI RECOMMENDED first because §C.2 originally sequenced this as Session 3 of Workstream 2; new URL-level Type/Description/Price fields + Scraping Status toggle + remove Sizes/Options UI alt). Director picks at Session 4 start; pointer file enumerates candidates. **§4 Step 1c forced-picker NOT FIRED** — Session 4 candidates ALL align with §C.2 Workstream 2 plan; director-pick choice between them is a scope-ordering preference rather than an unknown next-session task. **NO new CORRECTIONS_LOG §Entry this session** — no fix-shape ambiguity; no estimate slip; no process slip; Session 3 landed cleanly within the 3-5-session Workstream 2 estimate. (The P-43 cwd-leak class re-reproduced ONCE during /scoreboard Check 5; caught + recovered immediately; LOW informational pattern captured in design doc §B 2026-05-27 NOT promoted to CORRECTIONS_LOG.) **No corrections-tier slip occurred this session.** **EIGHTEENTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → 2026-05-26 → today). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-27_p46-workstream-2-session-3-url-level-and-per-category-overall-analysis-boxes (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-26 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-26_p46-workstream-2-session-2-per-item-analysis-on-captured-image-and-video` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-26 on `workflow-2-competition-scraping`** — second build session of the P-46 Workstream 2 implementation arc (Session 2 of 3-5 estimated); applied Session 1's locked card-list precedent + the `PerItemAnalysisBox` reusable component to Captured Image + Captured Video + extended the corresponding PATCH routes (`images/[imageId]` + `videos/[videoId]`) for the `analysis` field using the same one-line fix-shape Session 1 set for `text/[textId]`. Build commit `9747f63` — 4 files +325/-102 on `workflow-2-competition-scraping`; NOT pushed to main since Session 2 is a build session, not a deploy session. **Headline outcome:** the URL detail page's `CapturedImagesGallery` switches from a thumbnail grid to a **vertical card list** matching Session 1's Captured Text precedent (pill + trash + existing `ThumbnailButton` as image hero + metadata rows + `PerItemAnalysisBox`); the `CapturedVideosGallery` switches from a 2-col card grid to the same vertical card-list shape (pill + inline `<iframe>` or `<video controls>` hero + metadata rows + `PerItemAnalysisBox`); per-row trash NOT added to video cards (matches Build #5's "renderer only" scope); removed now-unused `thumbnailTrashButtonStyle`. **Route handlers extended:** both `images/[imageId]/route.ts` + `videos/[videoId]/route.ts` PATCH allowlists extended for `analysis` field validated via `isValidAnalysisPayload` (identical fix-shape as Session 1's `text/[textId]` extension). 6 new edge-case node:test cases for `isValidAnalysisPayload` pinning down the trust-boundary guard's contract. **NO Rule 14f forced-picker fired this session** — every layout choice for the Image + Video cards was a direct application of Session 1's locked precedent per `feedback_default_to_recommendation.md`. **Schema-change-in-flight flag STAYS YES** (carrying from Workstream 1's 2026-05-24 `prisma db push`; stays YES through subsequent W#2 / W#3 sessions until the first deploy session ships the schema-aware code to vklf.com). All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **628 src/lib node:test +6 from baseline 622** / **61 routes UNCHANGED** — no new routes; only extended existing `images/[imageId]` + `videos/[videoId]` PATCH allowlists) — Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.72) RECOMMENDED-NEXT = P-46 Workstream 2 Session 2**; **opens (a.73) RECOMMENDED-NEXT = P-46 Workstream 2 Session 3** — multiple §C.2-aligned candidates surface (URL-level Overall Competitor Analysis + per-category Overall Analysis boxes RECOMMENDED first to complete the "Analysis surface" arc before structural fields; Captured Reviews UI alt; new URL-level Type/Description/Price fields + Scraping Status toggle + remove Sizes/Options UI alt). Director picks at Session 3 start; pointer file enumerates candidates. **§4 Step 1c forced-picker NOT FIRED** — Session 3 candidates ALL align with §C.2 Workstream 2 plan; director-pick choice between them is a scope-ordering preference rather than an unknown next-session task. **NO new CORRECTIONS_LOG §Entry this session** — no fix-shape ambiguity; no estimate slip; no process slip; Session 2 landed cleanly within the 3-5-session Workstream 2 estimate mirroring Session 1's clean landing. **No corrections-tier slip occurred this session.** **SEVENTEENTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → 2026-05-25 → today). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-26_p46-workstream-2-session-2-per-item-analysis-on-captured-image-and-video (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-25 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-25_p46-workstream-2-session-1-tiptap-wrapper-and-per-item-analysis-on-captured-text` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-25 on `workflow-2-competition-scraping`** — first build session of the P-46 Workstream 2 implementation arc (Session 1 of 3-5 estimated); landed the TipTap shared rich-text editor wrapper + the per-item Analysis text box on Captured Text as the first user-visible slice + the route-handler half (`PATCH /api/projects/[projectId]/competition-scraping/text/[textId]` now accepts an `analysis` field at the trust boundary validated via `isValidAnalysisPayload`). Build commit `b6e43fe` — 8 files +1572/-149 on `workflow-2-competition-scraping`; NOT pushed to main since Session 1 is a build session, not a deploy session. **Headline outcome:** the URL detail page's `CapturedTextSubsection` switches from a 5-column HTML table to a **vertical card list** per the Rule 14f forced-picker outcome (director picked Option A "Card layout — replace the table (recommended)" over 3 alternatives); each card carries a TipTap rich-text Analysis editor wired to the existing `CapturedText.analysis` JSON column Workstream 1 shipped. Layout precedent locked for the remaining 3 capture types (Image / Video / Review) in subsequent Workstream 2 sessions. **3 new npm dependencies** — `@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/extension-link` at 3.23.6 (50 packages added transitively). **Schema-change-in-flight flag STAYS YES** (carrying from Workstream 1's 2026-05-24 `prisma db push`; stays YES through subsequent W#2 / W#3 sessions until the first deploy session ships the schema-aware code to vklf.com). All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **622 src/lib node:test +20 from baseline 602** / **61 routes UNCHANGED** — no new routes; only extended existing `text/[textId]` PATCH allowlist) — Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.71) RECOMMENDED-NEXT = P-46 Workstream 2 Session 1**; **opens (a.72) RECOMMENDED-NEXT = P-46 Workstream 2 Session 2** — wire per-item Analysis on Captured Image + Captured Video using the same `PerItemAnalysisBox` component (different `apiUrl` prop) + convert image gallery + video gallery to card-list layout matching Captured Text precedent. **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous per the §C.2 Workstream 2 plan (after Captured Text comes Captured Image + Video next; Reviews + per-category Overall Analysis + Type/Description/Price URL fields + Scraping Status toggle later). **ONE Rule 14f forced-picker fired this session** — visual layout of the per-item Analysis box on Captured Text; 4 options previewed with ASCII mockups; director picked card layout. **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 2 Session 1 closing §Entry** capturing (a) the Rule 14f picker outcome + the card-layout-replaces-table precedent set for the remaining 3 capture types + (b) the `PerItemAnalysisBox` extraction Pattern memorialized as a reusable component shape (one component owns per-row save lifecycle; reusable across capture types via `apiUrl` prop) for future per-row-edit-affordance work + (c) calibration data point — Session 1's "1 of 3-5 sessions" estimate landed cleanly within scope. **No corrections-tier slip occurred this session.** **SIXTEENTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → 2026-05-24 → today). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-25_p46-workstream-2-session-1-tiptap-wrapper-and-per-item-analysis-on-captured-text (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-24 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-24_p46-workstream-1-schema-first-build-session` — **W#2 polish P-46 Workstream 1 (Schema) ✅ DONE-AT-CODE-LEVEL 2026-05-24 on `workflow-2-competition-scraping`** — first build session of the P-46 5-workstream implementation arc; schema migration landed live on Supabase under Rule 9 director-Yes gate at `npx prisma db push` (1.32s; zero data loss; additive only); 4 new API route shells scaffolded as 501-stubs; shared-types extended for new wire shapes; 12 new src/lib node:test cases. Build commit `caad82a` — 21 files +781/-11 on `workflow-2-competition-scraping`; NOT pushed to main since Workstream 1 is a build session, not a deploy session. **Headline outcome:** live database now carries 3 new tables (`CapturedReview` / `ComprehensiveCompetitorAnalysis` / `UserTablePreferences`) + 8 new `CompetitorUrl` columns + 1 new `analysis` JSON column on each of CapturedText/Image/Video + 1 new `ScrapingStatus` enum — full faithful implementation of `docs/COMPETITION_DATA_V2_DESIGN.md` §A.11. **Schema-change-in-flight flag FLIPPED NO → YES** at `npx prisma db push` completion. All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext unchanged / **602 src/lib node:test +12 from baseline 590** / **61 routes +4 from baseline 57**) — Check 6 Playwright SKIPPED per non-deploy-session convention. **Closes (a.70) RECOMMENDED-NEXT = P-46 Workstream 1 (Schema) first build session**; **opens (a.71) RECOMMENDED-NEXT = P-46 Workstream 2 (URL detail page redesign) first build session** per Q10's locked sequencing. **§4 Step 1c forced-picker NOT FIRED** — next-session task unambiguous per Q10's locked sequencing. **Workstream 1 came in under estimate** (1 session vs. 2-3 planned per §C.1 — design doc explicitly allowed combining Sessions 1+2; the "Workstream Foundation Build Bundle" Pattern memorialized in `docs/COMPETITION_DATA_V2_DESIGN.md` §B 2026-05-24 captures the reusable shape). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-46 Workstream 1 closing §Entry** capturing empirical fix-shape narrative + the new "Workstream Foundation Build Bundle" reusable Pattern + 3 embedded informational sub-observations (P-43 bug-class re-reproduction 1× at Check 5; Workstream 1 under-estimate as informational calibration data point; §A.3 path-convention disagreement deferred as §B refinement candidate). **No corrections-tier slip occurred this session.** **FIFTEENTH end-of-session run under the Rule 30 + §4 Step 4b template** (sequence prior to today: 2026-05-21-b → 2026-05-21-c → 2026-05-21-d → 2026-05-22 → 2026-05-22-b → 2026-05-21 → 2026-05-22-c → 2026-05-22-d → 2026-05-22-e → 2026-05-22-f → 2026-05-22-g → 2026-05-22-h → 2026-05-22-i → 2026-05-23 → today). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-24_p46-workstream-1-schema-first-build-session (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-23 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-23_p46-w2-phase-2-design-session` — **W#2 polish P-46 W#2 Phase 2 design session ✅ DONE 2026-05-23 on `workflow-2-competition-scraping`** — pure DESIGN session, no code commits, no deploys, no Rule 9 gates fired, no fresh extension zip, no ping-pong sync since main doesn't move) shipped a new `docs/COMPETITION_DATA_V2_DESIGN.md` (~700 lines; §A frozen interview answers covering 10 questions + §B append-only refinements log empty + §C per-workstream implementation outlines covering 5 workstreams) — mirrors the §A frozen-shape pattern of `docs/CAPTURED_VIDEOS_DESIGN.md` per Rule 18 + parallels the 2026-05-20-b P-27 design-doc-split precedent. Director walked the 10 deferred clarification questions captured in the P-46 ROADMAP entry via Rule 14f forced-pickers — 8 pickers fired + 1 Default-to-recommendation skip on Q8 + 1 director "Other" pick on Q9 DROPPING the Select-preview-thumbnail feature from P-46 + 1 follow-up picker on Q1 DEFERRING per-platform Reviews extraction (v1 surface narrowed to schema + URL-detail-page view + vklf.com-side manual entry form; NO extension Reviews gesture in v1). Q1's deferral + Q9's drop shrink P-46 from the original 15-25 sessions estimate to a revised 11-17 sessions across 5 workstreams. Q10 locks workstream sequencing as Schema → URL detail page → Competition Data table → Comprehensive Analysis page → Extension + Reviews. **Closes (a.69) RECOMMENDED-NEXT = P-46 W#2 Phase 2 design session**; **opens (a.70) RECOMMENDED-NEXT = P-46 Workstream 1 (Schema) first build session** per the locked Q10 decision. Schema-change-in-flight flag entered + exited NO this session (pure design; no `prisma db push`); flips NO → YES at start of P-46 Workstream 1 first session. ZERO new corrections-tier §Entries this session — 3 informational drift-check observations captured in the new design doc + drift-check report, NOT corrections-tier. ZERO new Rule 14f / Rule 18 / Rule 23 / Rule 24 / Rule 26 / Rule 30 changes this session. ZERO procedural slips. ZERO open DEFERRED items at session end (5 tasks all completed). ONE push only — end-of-session doc-batch to `origin/workflow-2-competition-scraping`. This is the **FOURTEENTH end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3; second was 2026-05-21-c Build #4; third was 2026-05-21-d Build #5; fourth was 2026-05-22 Build #6; fifth was 2026-05-22-b Build #7; sixth was 2026-05-21 Build #8; seventh was 2026-05-22-c Build #9; eighth was 2026-05-22-d Build #1a; ninth was 2026-05-22-e Build #1b; tenth was 2026-05-22-f P-42; eleventh was 2026-05-22-g P-43; twelfth was 2026-05-22-h P-44; thirteenth was 2026-05-22-i P-45 Build #2 deploy). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-23_p46-w2-phase-2-design-session (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-22-i (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-22-i_p45-build-2-deploy-with-phase-1-fix-forward` — **W#2 polish P-45 screen recording SHIPPED + DEPLOYED + REAL-CHROME-VERIFIED across Amazon + Ebay + Walmart + Etsy 2026-05-22-i on vklf.com via `workflow-2-competition-scraping` → `main` ff-merge `d4a2940..ee8c79d` (11 commits — Build #1a `7e2eb2c` + 1a-doc `d944a0b` + Build #1b `80713ff` + 1b-doc `88c9f43` + P-42 `2e3270d` + P-42-doc `9612636` + P-43 `4afea35` + P-43-doc `8043bf2` + P-44 `57ed383` + P-44-doc `e03a317` + today's Build #2 fix-forward `ee8c79d` — 39 files +5067/-200 against main as a single fast-forward)** — Build #2's Phase 1 dev-time verify on Amazon surfaced THREE FIXABLE ISSUES in `ee8c79d` (3 files changed +78/-30): Issue 1 = `selfBrowserSurface: 'include'` added to `record-controller.ts createProductionDeps` so the requesting tab appears in Chrome's "Choose what to share" Tab picker (Chrome's default was 'exclude'); Issue 2 = aggressive 20-event stopPropagation band-aid applied to all four text inputs in `video-capture-form.ts` (newCategoryInput + compositionArea + embeddedArea + tagsInput) plus click-handler force-focus with `queueMicrotask` retry to override page-level focus-stealing — empirically verified working on Amazon during Phase 4 (the P-27 Bug #11 platform); Issue 3 = `recording-bytes-upload.ts` line 115 Content-Type changed from `input.blob.type || mimeType` (full MIME with codec params `video/webm;codecs=vp9,opus`) to `mimeType` (normalized base `video/webm`) since Supabase bucket's `allowedMimeTypes` is strict-equal against `['video/mp4', 'video/webm', 'video/quicktime']` — codec is still preserved in container metadata. Phase 1 caught a structural-ceiling 4th issue at the FINALIZE call (`'Couldn't save (400): Request body must include clientId + sourceType (EMBED|DIRECT_BYTES) + originalSrcUrl...'`) — root cause was that production vklf.com routes didn't have the Build #1a `isVideoSourceType('SCREEN_RECORDING') === true` change yet (undeployed code at the schema-change-in-flight boundary); decision: declare Phase 1 done up to finalize, proceed to Phase 2 → Phase 3, finalize verification deferred to Phase 4 post-deploy where production code carries the new validator. Pre-deploy /scoreboard ALL 6 CHECKS GREEN at EXACT baselines (root tsc clean / extension tsc clean / 57 routes / 590 src/lib / 558 ext / 94 Playwright in 2.7m). Rule 9 deploy gate FIRED via AskUserQuestion picker — director Yes approved `git push origin main`. Post-merge /scoreboard Checks 1-5 GREEN on main (Check 6 Playwright SKIPPED per ff-merge-is-pointer-move). Pushes: `git push origin main` `d4a2940..ee8c79d` → Vercel auto-redeploy fired → ping-pong sync `git push origin workflow-2-competition-scraping` `e03a317..ee8c79d` — both branches now at `ee8c79d`. Fresh extension zip `plos-extension-2026-05-22-w2-deploy-33.zip` 202.75 KB at repo root via `npm run zip` in 2.2s (exits cleanly thanks to P-44's wxt-zip.mjs wrapper — no pkill workaround needed). **Schema-change-in-flight flag FLIPPED YES → NO at deploy completion** — SCREEN_RECORDING enum value has been live on Supabase since 2026-05-22-d 14:09 UTC; production routes on vklf.com now read + accept it as of this deploy. **Phase 4 director real-Chrome cross-platform verify (cooperative):** all 4 platforms passed clean — Amazon + Ebay + Walmart + Etsy. Director reported "All passed" with zero caveats — the cleanest cross-platform PASS in any W#2 cooperation session to date. Validated end-to-end: new "Record video for PLOS" right-click menu item / selfBrowserSurface fix / aggressive event-isolation band-aid (Amazon P-27 Bug #11 platform) / Content-Type fix / production validator broadening / URL detail page renderer broadening. **Closes (a.68) RECOMMENDED-NEXT = P-45 Build #2**; **closes P-45 (screen recording) ✅ DONE-AND-VERIFIED 2026-05-22-i** (no Build #3 needed since Phase 4 passed clean across all 4 platforms); **closes P-27 Bug #11 ✅ DONE-AND-VERIFIED 2026-05-22-i** via the aggressive event-isolation band-aid empirically verified on Amazon during Phase 4 — per the launch prompt's roadmap summary that the P-27 leftovers may be closed without explicit fixes if P-45 covers the user-visible surface; **P-27 Bug #9 (Amazon hover-preview deeper-walk) + Bug #15 (Ebay native-controls quirk) remain DEFERRED** as low-priority polish since they weren't exercised by P-45's flow. **Opens (a.69) RECOMMENDED-NEXT = P-46 W#2 Phase 2 design session** on `workflow-2-competition-scraping` per the §4 Step 1c forced-picker between P-46 vs. P-47 (new — Shadow DOM refactor) vs. P-27 leftovers vs. P-26 below-fold scroll capture — P-46 is the natural next step per the long-standing roadmap commitment that P-46's deep design starts AFTER P-45 ships; 1 dedicated DESIGN session (no code) running through the 10 deferred clarification questions + creating new `docs/COMPETITION_DATA_V2_DESIGN.md`; ~15-25 subsequent BUILD sessions across 5 workstreams. **Opens NEW polish ROADMAP entry P-47** — "Shadow DOM refactor of video-capture-form.ts to replace the 80-event-listener Issue 2 band-aid with proper isolation" per Rule 24 outcome — band-aid works empirically but Shadow DOM mount of form backdrop+content would isolate from page-level handlers permanently; LOW priority since band-aid works for current scope. **TaskList sweep:** ZERO DEFERRED tasks at session end — all 10 tasks completed (4-phase deploy + 3 fix-forward sub-tasks + re-build/re-verify + end-of-session). **TWO Rule 9 gates fired effectively as ONE** per `feedback_approval_scope_per_decision_unit.md` — the doc-batch push doesn't re-invoke Rule 9 per the approval-scope-per-decision-unit convention; ONE director-Yes picker fired for the deploy. **Pushes this session: 4 total** — `git push origin main` (deploy) + ping-pong `git push origin workflow-2-competition-scraping` + end-of-session push of build commit `ee8c79d` + this doc-batch push together to both branches (operationally adjacent per the canonical 3-push pattern in `feedback_approval_scope_per_decision_unit.md`). **ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry this session — the P-45 CLOSING §Entry** capturing empirical fix narrative for all 3 Phase-1 issues + Reusable Pattern A (Supabase signed-URL bucket validation 400 via Content-Type normalization) + Reusable Pattern B (P-27-Bug-#11-class page-level focus-stealing dampened via aggressive event isolation with Shadow DOM as proper long-term fix captured as P-47) + Reusable Pattern C (verifying a feature whose server-side code is undeployed at Phase 1 — recognize the structural ceiling, document the chained dependency, defer the unverifiable step to Phase 4 post-deploy), plus TWO embedded informational sub-observations — P-43 bug class re-reproduction at Claude's inline-typed Bash layer (FIRST occurrence pre-deploy Check 5 cwd-leak from prior `cd extensions/...` call producing extension build instead of Next.js build; caught immediately from output content + recovered with absolute cd; SECOND occurrence post-merge Check 5 same cwd-leak; output `0` routes both times, both recovered with absolute cd; both reproductions reinforce that template hardening protects verbatim-template-read pathway but not Claude's inline-typed shortcuts; LOW informational). HANDOFF_PROTOCOL header bump only — no new rules drafted this session. P-45's deploy graduation across 4 platforms closes the screen-recording arc end-to-end after Build #1a (engine foundation 2026-05-22-d) + Build #1b (wiring 2026-05-22-e) + Build #2 (deploy + Phase 1 fix-forward + cross-platform verify 2026-05-22-i).) is the **THIRTEENTH end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3; second was 2026-05-21-c Build #4; third was 2026-05-21-d Build #5; fourth was 2026-05-22 Build #6; fifth was 2026-05-22-b Build #7; sixth was 2026-05-21 Build #8; seventh was 2026-05-22-c Build #9; eighth was 2026-05-22-d Build #1a; ninth was 2026-05-22-e Build #1b; tenth was 2026-05-22-f P-42; eleventh was 2026-05-22-g P-43; twelfth was 2026-05-22-h P-44). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes.)
**Last updated in session:** session_2026-05-22-i_p45-build-2-deploy-with-phase-1-fix-forward (Claude Code, on `workflow-2-competition-scraping` then ff-merged to `main` then ping-ponged back)

**Previously updated:** 2026-05-22-h (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-22-h_p44-wxt-zip-build-parent-process-hang-fix` — W#2 polish P-44 SHIPPED + ✅ DONE-AND-VERIFIED 2026-05-22-h on `workflow-2-competition-scraping` — closes the multi-reproduction `wxt build` + `wxt zip` parent-process hang class via build commit `57ed383` (5 files changed +61/-11): empirical root cause confirmed as wxt 0.20.25 → Vite 8.0.11 → Rolldown 1.0.0-rc.18 (the new Rust-based bundler that replaced Rollup in Vite 8) — the native Rolldown binaries (`@rolldown/binding-linux-x64-gnu` etc.) spawn worker threads that aren't released when the build promise resolves, leaving Node's event loop with active handles; wxt's CLI awaits the build then relies on natural event-loop drain that never happens. Fix shipped via NEW `extensions/competition-scraping/scripts/wxt-build.mjs` + NEW `scripts/wxt-zip.mjs` (16 LOC each programmatic-API wrappers — `import { build } from 'wxt'; await build(); process.exit(0);` with browser-flag passthrough + try/catch for non-zero exit on error) + `extensions/competition-scraping/package.json` 4 script rewires (`build` + `build:firefox` + `zip` + `zip:firefox`) + `.claude/commands/scoreboard.md` + `.claude/commands/deploy.md` KNOWN ISSUE blocks replaced with one-paragraph wrapper-notes; Rule 24 scope-expansion to also cover `wxt zip` because `wxt zip` internally calls `internalBuild()` from the same `core/utils/building/internal-build.mjs` codepath. Post-fix verification: `npm run build` cold 4s exit 0 dist valid; `npm run zip` cold 3s exit 0 with 202,601-byte zip artifact valid; warm + 3x rapid-fire all GREEN. Pre-fix reproduction `rm -rf .output && timeout 45 npm run build` hung past 45s (exit 124 timeout). Pre-end-of-session /scoreboard all 5 checks GREEN at exact baselines (root tsc clean / extension tsc clean / 558 ext / 590 src/lib / 57 routes); Check 6 Playwright SKIPPED per non-deploy-session convention. THIRD polish-detour in a row ahead of P-45 Build #2's deploy session — 2026-05-22-f was P-42 backup-memory hook fix; 2026-05-22-g was P-43 absolute-paths discipline; today P-44 closes the wxt build/zip hang class so deploy Step 8 (fresh zip) no longer needs the pkill workaround; PURE LOCAL INFRASTRUCTURE session paralleling the two prior detours — no production code touched; no main push; no ff-merge; no Vercel redeploy; no fresh deploy-tagged extension zip; no Rule 9 destructive-op gate fired; director directive at session start ("please defer any testing in this session and work on the next item that does not require manual testing") forced a pivot from the previously-planned P-45 Build #2 DEPLOY session; §4 Step 1c forced-picker DID FIRE between P-44 vs. P-26 vs. "I have a question first." — director picked P-44 per `feedback_default_to_recommendation.md`; does NOT close (a.67) — (a.67) was P-45 Build #2, which remains the natural continuation for the next session and re-opens as (a.68) RECOMMENDED-NEXT; ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry — the P-44 CLOSING §Entry capturing the empirical root-cause narrative + the reusable Pattern for "how to escape a Node CLI tool's event-loop-drain hang via programmatic-API + process.exit(0)" + ONE embedded informational sub-observation about a P-43-bug-class second-reproduction at Claude's typing layer (Claude dropped the absolute `cd` prefix from inline Check 3, got loud-fail "Missing script: test", recovered cleanly — LOW informational, NOT a procedural failure); schema-change-in-flight flag stays **YES** independently of this session's scope until P-45 Build #2 deploys the new SCREEN_RECORDING enum live on vklf.com) is the **TWELFTH end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3; second was 2026-05-21-c Build #4; third was 2026-05-21-d Build #5; fourth was 2026-05-22 Build #6; fifth was 2026-05-22-b Build #7; sixth was 2026-05-21 Build #8; seventh was 2026-05-22-c Build #9; eighth was 2026-05-22-d Build #1a; ninth was 2026-05-22-e Build #1b; tenth was 2026-05-22-f P-42; eleventh was 2026-05-22-g P-43). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. P-44 closes the multi-reproduction `wxt build` + `wxt zip` parent-process hang class via programmatic-API wrappers that force-exit after the promise resolves, bypassing the broken event-loop drain entirely. After 3 successive detours (P-42 → P-43 → P-44), the operational substrate is now significantly more robust entering P-45 Build #2 — P-42 closed the Layer-1 backup-memory hook gap; P-43 hardened all `.claude/commands/` slash-command templates against the CWD-leak class via absolute paths; P-44 closed the wxt build/zip hang class.)
**Last updated in session:** session_2026-05-22-h_p44-wxt-zip-build-parent-process-hang-fix (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-22-g (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-22-g_p43-absolute-paths-scoreboard-deploy-ship-polish-item` — W#2 polish P-43 SHIPPED + ✅ DONE-AND-VERIFIED 2026-05-22-g on `workflow-2-competition-scraping` — closes the FOUR-reproduction `.claude/commands/scoreboard.md` parallel-Bash CWD-leak class via build commit `4afea35` (3 files changed +20/-20 — all `.claude/commands/` template polish): every relative `cd extensions/competition-scraping && ...` invocation in `.claude/commands/scoreboard.md` + `.claude/commands/deploy.md` + `.claude/commands/ship-polish-item.md` rewritten as absolute `cd /workspaces/brand-operations-hub/extensions/competition-scraping && ...`; every `$(find src/lib -name '*.test.ts')` rewritten as `$(find /workspaces/brand-operations-hub/src/lib -name '*.test.ts')`; baselines refreshed across all three templates from stale 2026-05-19-g values (53 routes / 536 src/lib / 428 ext / 79 Playwright) to current 2026-05-22-g values (57 / 590 / 558 / 94); scope expansion beyond the ROADMAP entry's narrow scoreboard.md-only fix shape per the entry's same-session commentary recommending extension to ALL `.claude/commands/` templates — three files + baseline refresh is the most-thorough fix per Rule 14f Default-to-recommendation; most-thorough verification ran fixed Checks 1, 3, 4 from `/tmp` to prove absolute-path discipline survives cwd drift — ALL GREEN at exact baselines; PURE LOCAL INFRASTRUCTURE session paralleling yesterday's P-42 detour — no production code touched; no main push; no ff-merge; no Vercel redeploy; no fresh extension zip; no Rule 9 destructive-op gate fired; director directive at session start ("please defer any testing in this session and work on the next item that does not require you to walk me through any real world testing steps") forced a pivot from the previously-planned P-45 Build #2 DEPLOY session; §4 Step 1c forced-picker DID FIRE between P-43 vs. P-44 vs. "I have a question first." — director picked P-43 per `feedback_default_to_recommendation.md`; does NOT close (a.66) — (a.66) was P-45 Build #2, which remains the natural continuation for the next session and re-opens as (a.67) RECOMMENDED-NEXT; ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry — the P-43 CLOSING §Entry capturing the empirical fix narrative + the reusable Pattern for "how to harden a parallel-Bash slash-command template against CWD-leak" + two informational sub-observations (the backgrounded-`npm run build` 0-byte-output reproduction recurrence and Claude's own test-slip when writing the verification); schema-change-in-flight flag stays **YES** independently of this session's scope until P-45 Build #2 deploys the new SCREEN_RECORDING enum live on vklf.com) is the **ELEVENTH end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3; second was 2026-05-21-c Build #4; third was 2026-05-21-d Build #5; fourth was 2026-05-22 Build #6; fifth was 2026-05-22-b Build #7; sixth was 2026-05-21 Build #8; seventh was 2026-05-22-c Build #9; eighth was 2026-05-22-d Build #1a; ninth was 2026-05-22-e Build #1b; tenth was 2026-05-22-f P-42). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. P-43 closes the FOUR-reproduction CWD-leak class via the absolute-path normalization across all `.claude/commands/` slash-command templates + adds the reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-22-g for future templates so the class doesn't recur.)
**Last updated in session:** session_2026-05-22-g_p43-absolute-paths-scoreboard-deploy-ship-polish-item (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-22-f (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-22-f_p42-backup-memory-dir-hook-fix` — W#2 polish P-42 SHIPPED + ✅ DONE-AND-VERIFIED 2026-05-22-f on `workflow-2-competition-scraping` — closes the THREE-reproduction backup-memory-dir Layer-1 hook gap empirically via build commit `2e3270d` (4 files changed +178/-4): `.claude/hooks/backup-memory-dir.sh` jq selector `.tool` → `.tool_name` (the root cause confirmed by capturing the actual stdin JSON shape into `/tmp/plos-hook-debug.log` via temporary diagnostic instrumentation on a disposable memory-file Write — Claude Code's PostToolUse stdin field is `.tool_name`, not `.tool`; both hooks' `jq -r '.tool // empty'` always resolved to `""` so the Edit/Write gate always exited without doing work; three P-42 reproductions across 2026-05-20-b, 2026-05-20-c, 2026-05-21 all stemmed from this single field-name mismatch); same fix applied to `.claude/hooks/track-edited-docs.sh` per Rule 24 scope expansion (sibling hook had the identical bug; the `.claude/session-modified-docs.log` had been 0-byte since May 20 because of the same root cause); NEW `.claude/hooks/check-memory-mirror-staleness.sh` SessionStart canary (~175 LOC) for defense-in-depth Layer-3b — compares source memory dir against `.codespace-backup/memory/` mirror at every session start + emits a 🚨 MEMORY MIRROR STALENESS alert via `additionalContext` if file count / presence / per-file size diverges; tested in 3 scenarios (healthy / source-only file / size mismatch) — all expected outputs; `.claude/settings.json` wires the new canary into the SessionStart hooks array alongside the existing `inject-next-session-pointer.sh` + `check-memory-dir-status.sh`. Integration test post-fix (Write of disposable `_integration_test_p42.md` → backup mirror picked up the file → canary then reports empty additionalContext) confirmed both layers work end-to-end before commit. Strongly-recommended pre-flight detour per the 2026-05-22-e NEXT_SESSION.md alternates list ("HIGH severity; STRONGLY RECOMMENDED before any future big session"); does NOT close (a.65) — (a.65) was P-45 Build #2, which remains the natural continuation for the next session and re-opens as (a.66) RECOMMENDED-NEXT; NO main push this session; NO ff-merge; NO Vercel redeploy; NO fresh extension zip; NO Rule 9 destructive-op gate fired this session — pure local infrastructure session; schema-change-in-flight flag stays **YES** independently of this session's scope until P-45 Build #2 deploys the new SCREEN_RECORDING enum live on vklf.com) is the **TENTH end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3; second was 2026-05-21-c Build #4; third was 2026-05-21-d Build #5; fourth was 2026-05-22 Build #6; fifth was 2026-05-22-b Build #7; sixth was 2026-05-21 Build #8; seventh was 2026-05-22-c Build #9; eighth was 2026-05-22-d Build #1a; ninth was 2026-05-22-e Build #1b). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. P-42 closes the Layer-1 (Mechanical) gap in Rule 29's 4-layer protective architecture + adds a NEW Layer-3b (Defense-in-depth Detection) canary that catches source-vs-mirror divergence at session start.)
**Last updated in session:** session_2026-05-22-f_p42-backup-memory-dir-hook-fix (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-22-e (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-22-e_p45-build-1b-screen-recording-wiring-slice` — W#2 polish P-45 Build #1b SHIPPED at code level on `workflow-2-competition-scraping` — screen-recording WIRING slice via build commit `80713ff`: 3 NEW screen-recording helpers (`recording-bytes-upload.ts` smart-client Phase 1+2+3 orchestrator + `thumbnail-extraction.ts` first-frame canvas grab that NEVER throws per §A.12 + canvas-crop region constraint extension to `record-controller.ts` via optional `cropStreamToRegion` DI dep) + 2 NEW content-script overlays (`video-region-record-overlay.ts` forked from `region-screenshot-overlay.ts` + `recording-indicator-overlay.ts` with red dashed border + REC ● pulsing badge + M:SS countdown + Stop + Cancel toolbar) + 9 wiring file edits (`messaging.ts` new types + `api-bridge.ts` new helpers + `background.ts` `CONTEXT_MENU_RECORD_VIDEO` with locked label **"Record video for PLOS"** + `orchestrator.ts` new `enter-video-region-record-mode` handler + `video-capture-form.ts` new `kind:'screen-recording'` branch + `styles.ts` new CSS including `plos-cs-rec-pulse` keyframe + `captured-video-validation.ts` bytes-required gate broadened symmetrically + `record-controller.ts` extended with optional canvas-crop DI dep + `record-controller.test.ts` +5 canvas-crop cases) + 3 NEW test files mirroring §C.18 enumeration (8+8+12 cases). 16 files changed +2418/-15; extension test count 524 → 558 (+34); no main push; no ff-merge; no Vercel redeploy; no fresh extension zip this session — director directive late-session deferred dev-time verify on Amazon to Build #2, captured as Build #2's Phase 1 binding input in NEXT_SESSION.md per the 1a precedent for cross-session binding inputs) is the **NINTH end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3; second was 2026-05-21-c Build #4; third was 2026-05-21-d Build #5; fourth was 2026-05-22 Build #6; fifth was 2026-05-22-b Build #7; sixth was 2026-05-21 Build #8; seventh was 2026-05-22-c Build #9; eighth was 2026-05-22-d Build #1a). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. The verify-deferral handling is per the 1a precedent — when a clearly-named next session has a natural binding input (Build #2's Phase 1 = dev-time verify before deploy), capture the input in NEXT_SESSION.md rather than as an orphan TaskList DEFERRED entry per Rule 26's intent. Build #1b closes (a.64) RECOMMENDED-NEXT (partial — wiring half shipped at code level; dev-time verify deferred to Build #2); opens (a.65) RECOMMENDED-NEXT = P-45 Build #2 (DEPLOY session — 4-phase: Phase 1 dev-time verify on Amazon (deferred from this session) → Phase 2 `/scoreboard` GREEN → Phase 3 `/deploy` orchestration with Rule 9 director-Yes gate + ff-merge → main + push origin/main + Vercel auto-redeploy + ping-pong sync + fresh extension zip → Phase 4 director real-Chrome cross-platform verify across Amazon + Ebay + Walmart + Etsy) on `workflow-2-competition-scraping`.)
**Last updated in session:** session_2026-05-22-e_p45-build-1b-screen-recording-wiring-slice (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-22-d (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-22-d_p45-build-1a-screen-recording-engine-foundation` — W#2 polish P-45 Build #1a SHIPPED at code level on `workflow-2-competition-scraping` — screen-recording engine FOUNDATION slice via build commit `7e2eb2c`: schema migration adding `SCREEN_RECORDING` to the `VideoSourceType` enum via `npx prisma db push` (Rule 9 director-Yes gate fired; live on Supabase since 14:09 UTC) + NEW `extensions/competition-scraping/src/lib/screen-recording/record-controller.ts` (~280 LOC state machine + DI surface) + 29 NEW node:test cases + correctness updates to `isFinalizeVideoUploadRequest` + finalize-route bytes-required gate + list-route signed-URL minting gate; original "single Build #1" framing was split at session start via Rule 14f scope-pacing forced-picker into 1a [foundation, this session] + 1b [wiring + dev verify, next session] per the most-thorough/reliable picker option; no main push; no ff-merge; no Vercel redeploy; no fresh extension zip this session — no orchestrator wiring landed yet) is the **EIGHTH end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3; second was 2026-05-21-c Build #4; third was 2026-05-21-d Build #5; fourth was 2026-05-22 Build #6; fifth was 2026-05-22-b Build #7; sixth was 2026-05-21 Build #8; seventh was 2026-05-22-c Build #9). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. Build #1a closes (a.63) RECOMMENDED-NEXT (partial — Build #1a's foundation half shipped; Build #1b's wiring half carries the remainder); opens (a.64) RECOMMENDED-NEXT = P-45 Build #1b (wiring slice — `video-region-record-overlay.ts` fork from `region-screenshot-overlay.ts` + `recording-indicator-overlay.ts` with red dashed border + REC badge + floating Stop toolbar + `thumbnail-extraction.ts` first-frame canvas grab + `recording-bytes-upload.ts` smart-client Phase 1+2+3 orchestrator + `messaging.ts` new message types + `api-bridge.ts` new background-request helpers + `background.ts` `CONTEXT_MENU_RECORD_VIDEO` registration with locked label **"Record video for PLOS"** + `orchestrator.ts` new `enter-video-region-record-mode` handler + `video-capture-form.ts` new `kind: 'screen-recording'` branch + `styles.ts` overlay CSS + dev-time happy-path walkthrough on Amazon) on `workflow-2-competition-scraping`.)
**Previously updated in session:** session_2026-05-22-d_p45-build-1a-screen-recording-engine-foundation (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-22-c (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-22-c_p27-build-9-devtools-diagnosis-and-p45-screen-recording-pivot` — P-27 Captured-videos Build #9 DevTools-cooperative debugging + P-45 screen-recording design-pivot session on `workflow-2-competition-scraping`; Bug #12 Save-fails-Network-unreachable diagnosed in ONE DevTools-cooperative step (root cause: blob: URL from Media Source Extensions; unreachable from background SW); director's escape-hatch question surfaced screen recording as a universal solution; director chose "skip the short-term fix — design screen recording properly NOW" at the sequencing picker; 12 P-45 design decisions locked through Rule 14f pickers; NO code commits this session — pure design + diagnostic work; no deploy; no schema changes; no Rule 9 trigger) is the **SEVENTH end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3; second was 2026-05-21-c Build #4; third was 2026-05-21-d Build #5; fourth was 2026-05-22 Build #6; fifth was 2026-05-22-b Build #7; sixth was 2026-05-21 Build #8). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. NEW Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. Build #9 closes (a.62) RECOMMENDED-NEXT (partial — Bug #12 diagnosed + design pivoted to P-45 universal solution; Bug #11 + Bug #9 deeper-walk + Bug #15 deferred + partially obsoleted by P-45); opens (a.63) RECOMMENDED-NEXT = P-45 Build #1 (Rule 18 interview-cluster deepening of `docs/CAPTURED_VIDEOS_DESIGN.md` §C → full §C.0-§C.N structure + ship `record-controller.ts` ~150 LOC + fork region-screenshot overlay → `video-region-record-overlay.ts` + add "Record video" right-click menu item + in-progress visual indicator + schema migration adding `SCREEN_RECORDING` enum (Rule 9 gate fires) + dev-time happy-path verification on Amazon) on `workflow-2-competition-scraping`.)
**Previously updated in session:** session_2026-05-22-c_p27-build-9-devtools-diagnosis-and-p45-screen-recording-pivot (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-21 (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-21_p27-build-8-fix-forward-polish-with-deferred-bugs-11-12` — P-27 Captured-videos Build #8 fix-forward polish session SHIPPED + DEPLOYED on vklf.com via `workflow-2-competition-scraping` → `main` ff-merge `bd7cedd..a47a95f` (2 commits; +645 / -19 across 7 files; fresh extension zip `plos-extension-2026-05-21-w2-deploy-32.zip` 198,508 bytes); director real-Chrome re-verification ran in-session — PARTIAL PASS (Etsy + Walmart + Ebay-form-opens + Amazon-click-into-overlay all PASS; Bug #11 + #12 + new Bug #15 + Bug #9 deeper-walk all deferred to Build #9)) is the **SIXTH end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3; second was 2026-05-21-c Build #4; third was 2026-05-21-d Build #5; fourth was 2026-05-22 Build #6; fifth was 2026-05-22-b Build #7). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. NEW Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. Build #8 closes (a.61) RECOMMENDED-NEXT (partial — 5 code-level fixes + deploy + re-verify shipped; 2 bugs deferred to Build #9 + 2 deeper-diagnoses also deferred); opens (a.62) RECOMMENDED-NEXT = P-27 Build #9 (DevTools-cooperative debugging session — Claude narrates one DevTools diagnostic step at a time + director reports what they see live; targets Bug #11 Amazon "Add new" input dead + Bug #12 Save fails "Network unreachable" on Amazon + Ebay + Bug #9 Amazon hover-preview walker deeper-walk + NEW Bug #15 Ebay native-controls quirk UX recovery) on `workflow-2-competition-scraping`.)
**Previously updated in session:** session_2026-05-21_p27-build-8-fix-forward-polish-with-deferred-bugs-11-12 (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-21-d (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-21-d_p27-build-5-url-detail-page-renderer` — P-27 Captured-videos Build #5 — URL detail page renderer for captured videos + signed-URL list endpoint shipped at code level on `workflow-2-competition-scraping` via commit `467af4c`) is the **THIRD end-of-session run under the Rule 30 + §4 Step 4b template** (first was 2026-05-21-b Build #3, second was 2026-05-21-c Build #4). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. NEW Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. Build #5 closes (a.58) RECOMMENDED-NEXT (partial — URL detail page renderer + signed-URL list endpoint shipped; Playwright extension-context spec remains for Build #6); opens (a.59) RECOMMENDED-NEXT = P-27 Build #6 (single-platform amazon Playwright extension-context spec covering the right-click capture happy path + embed path + popup paste path) on `workflow-2-competition-scraping`.)
**Previously updated in session:** session_2026-05-21-d_p27-build-5-url-detail-page-renderer (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-21-c (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-21-c_p27-build-4-popup-paste-saved-video-indicator` — P-27 Captured-videos Build #4 — popup paste form `CapturedVideoPasteForm.tsx` + saved-video indicator overlay `already-saved-video-icon.ts` + 13 new node:test cases shipped at code level on `workflow-2-competition-scraping` via commit `ea32fa5`) is the **SECOND end-of-session run under the new Rule 30 + §4 Step 4b template** (first was the prior 2026-05-21-b Build #3 session). The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff continue carrying the 3 mandatory plain-terms sections at the top per director's standing 2026-05-21 directive. NEW Rule 30 + Step 4b template extension + Step 7b start-of-session enforcement all stayed unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. Build #4 closes (a.57) RECOMMENDED-NEXT (partial — popup paste form + saved-video indicator overlay shipped; URL detail page renderer remains for Build #5); opens (a.58) RECOMMENDED-NEXT = P-27 Build #5 (URL detail page renderer for captured videos) on `workflow-2-competition-scraping`.)
**Previously updated in session:** session_2026-05-21-c_p27-build-4-popup-paste-saved-video-indicator (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-21-b (**Header bump only — no new rules drafted this session.** This session (`session_2026-05-21-b_p27-build-3-extension-content-script-form` — P-27 Captured-videos Build #3 — extension content-script form + helper + validator + 41 new node:test cases shipped at code level on `workflow-2-competition-scraping` via commit `02709c4`) is the **FIRST end-of-session run under the new Rule 30 + §4 Step 4b template** shipped in the prior 2026-05-21 session. The plain-terms sections in NEXT_SESSION.md + the parent's Personalized Handoff are the live test of the new template — director should verify the plain-terms sections render correctly + give feedback for any refinements before the next big session. NEW Rule 30 + Step 4b template extension are unchanged from the 2026-05-21 ship; no new rules added; no procedural changes; no scope changes. Build #3 closes (a.56) RECOMMENDED-NEXT (partial); opens (a.57) RECOMMENDED-NEXT = P-27 Build #4 (popup paste form + saved-video indicator overlay) on `workflow-2-competition-scraping`.)
**Previously updated in session:** session_2026-05-21-b_p27-build-3-extension-content-script-form (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-21 (**Session-bookends plain-language summaries — NEW Rule 30 + §4 Step 4b template extension shipped.** NEW Rule 30 "Session bookends: plain-language summaries at start + end" inserted between Rule 29 and §4 END-OF-CHAT PROTOCOL. Rule 30 codifies the standing directive that every session must bookend with plain-language summaries the director can scan in under 30 seconds — at session START (what this session will do) and at session END (what was done + what next session will do + what's pending on the roadmap). Director's verbatim 2026-05-21 directive: *"From here on, for every next session, I want you to also tell me in simple terms what we will do in the session and summarize what was done in the session and what we will do in the next session. I also want you to check and tell me what work is pending according to the roadmap in simple terms."* Companion change: §4 Step 4b end-of-session handoff template extended with 3 NEW MANDATORY SECTIONS AT THE TOP — "What we did this session (in plain terms)" / "What we'll do next session (in plain terms)" / "What's still left on the total roadmap (in plain terms)" — BEFORE the existing "Files changed this session" section. The 3 new plain-terms sections + the existing technical sections together form the canonical handoff shape. Why: the director is a non-programmer (Rule 6); session-boundary moments are the highest-leverage communication moments; without explicit plain-terms summaries, the director has to parse technical narrative to figure out what's happening. Rule 30 specializes Rule 6 for session boundaries even when there's no decision point. Start-of-session enforcement via companion CLAUDE_CODE_STARTER.md Step 7b (NEW 2026-05-21 — "Step 7b — Plain-terms session summary" added between drift check (Step 7) and wait-for-go-ahead (Step 8)). End-of-session enforcement via this §4 Step 4b template extension. Operational-memory cross-reference: NEW `feedback_session_bookends_plain_summary.md` at `/home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/` (auto-mirrored to `.codespace-backup/memory/` per Rule 29 — though manually mirrored via `cp` this session after the auto-mirror hook P-42 reproduction #3 fired). The doc-batch this session is the FIRST handoff to be generated under the new Rule 30 + Step 4b template — director can verify the plain-terms sections render correctly + give feedback for any refinements.)
**Previously updated in session:** session_2026-05-21_p27-build-2-api-routes-shared-types (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-20 (**Codespace-rebuild memory-loss recovery + Rule 29 protective architecture ship session.** NEW Rule 29 "Pre-destructive-container-operation audit" inserted between Rule 28 and §4 END-OF-CHAT PROTOCOL. Rule 29 fires when Claude is about to recommend any operation that touches the Codespace container itself (Codespaces: Rebuild Container, devcontainer.json edits that auto-trigger rebuild, container reset/delete-and-recreate, manual home-dir wipes); requires a Pre-Container-Audit + Rule 14f forced-picker with backup-first as the recommended option. Why: 2026-05-21 session recommended a Codespaces rebuild walkthrough (to verify P-18's devcontainer postCreateCommand) WITHOUT pre-rebuild audit; rebuild succeeded at P-18 but silently wiped the entire `/home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/` directory (~9 cross-session operational-memory files) + the Claude Code CLI binary (director had to manually reinstall + reauthenticate). No PLOS or vklf.com work lost (everything in `/workspaces/` + git + GitHub remote + Vercel + Supabase preserved); the loss was Claude's own internal operational memory + dev tooling. This session reconstructed all 10 memory files functionally from references in this doc + shipped the 4-layer protective architecture: MECHANICAL `.claude/hooks/backup-memory-dir.sh` PostToolUse hook auto-mirrors every memory write into `/workspaces/brand-operations-hub/.codespace-backup/memory/` (persistent volume); PROCEDURAL Rule 29 + `scripts/codespace-rebuild-audit.sh`; DETECTION `.claude/hooks/check-memory-dir-status.sh` SessionStart canary; RECOVERY `scripts/restore-memory-from-backup.sh`. Companion change: CLAUDE_CODE_STARTER.md Rule 8 augment names Codespaces rebuild as destructive + cross-references Rule 29. Verified inline this session: all 4 hooks/scripts tested with healthy/missing/partial-loss cases; restore script tested with safe-mode refuse + force-mode actual restore; pre-deploy scoreboard GREEN at baselines (53 routes + 536 src/lib + 428 ext + 91 Playwright in 1.8 min); P-18 closed ✅ DONE-AND-VERIFIED via the Playwright run in the rebuilt environment. Captured as polish backlog item P-41 ✅ DONE-AND-VERIFIED 2026-05-20. **Follow-up amendment same session:** §4 Step 4b end-of-session handoff template extended with new mandatory "🔧 RECOVERY INSTRUCTIONS" section listing concrete recovery commands by type (tracked file via git checkout; memory dir via restore script; reverted commit via git reflog; uncommitted file via git fsck --lost-found; full repo via git clone from GitHub; total Codespace deletion via fresh Codespace + reauth; GitHub repo deletion via GitHub Support) + the full layer-by-layer backup map (GitHub remote / Vercel / Supabase / .codespace-backup/ / persistent volume). Every future handoff now includes the recovery playbook automatically — director never has to wonder how to get something back.)
**Last updated in session:** session_2026-05-20_codespace-rebuild-memory-loss-recovery-and-rule29-prevention-DEPLOYED (Claude Code, on `workflow-2-competition-scraping`)

**Previously updated:** 2026-05-19-g-3 (Working-mode update + .claude/ tooling integration — same Claude session as 2026-05-19-g P-23 ship + 2026-05-19-g-2 P-27 capture; post-handoff director directive captured as a new permanent operational principle. **NEW Rule 14f exception section: "Default-to-recommendation" when the picker is asking permission to proceed on a path the director would default-approve.** Director's verbatim 2026-05-19-g-3: *"I spend a lot of time asking you to proceed with the suggested plan of actions that you recommend. How do I reduce the need to give permissions? I only want you to ask me questions to clarify things and not on how to proceed because my default is to go with your suggestions. Even if something is risky, since I am a novice, I rely on your deep analysis and thoroughness to ensure the risk is mitigated with backups and minimized."* The exception establishes the test: BEFORE firing a Rule 14f forced-picker, ask "is this question about clarifying the director's INTENT (e.g., scope, naming, workflow design), OR is it asking permission to proceed on a path the director would default-approve?" If the latter, skip the picker and proceed with the recommendation. Rule 9 destructive operations (deploys, force-pushes, rm -rf, prisma migrate reset, SQL DELETE/DROP/TRUNCATE) STILL require explicit forced-picker — those gates remain non-negotiable. Scope decisions + workflow design + Rule 18 interview clusters STILL fire pickers — those are genuine intent-clarification moments. **Companion tooling integration:** §3 Rule 24 cross-references the new `/rule-24-search [keyword]` slash command at `.claude/commands/rule-24-search.md`; §4 Step 1 cross-references the new `plos-doc-batch` agent at `.claude/agents/plos-doc-batch.md` (spawn via Agent tool with `subagent_type: "plos-doc-batch"` at end-of-session); §3 Rule 23 + Rule 27 verification cross-references the new `/scoreboard` slash command at `.claude/commands/scoreboard.md`. New PostToolUse hook `track-edited-docs.sh` auto-logs every `docs/` edit to `.claude/session-modified-docs.log` (session-scoped, gitignored) so the doc-batch agent has mechanical knowledge of which docs got touched — replacing manual "I think I touched these" memory recall. Operational memory cross-reference: `feedback_default_to_recommendation.md`. **All four extensions committed in `480d3ae` on main + workflow-2 (push before this doc update).** This doc update lives at HANDOFF_PROTOCOL header bump + Rule 14f exception section + brief cross-references in §3/§4. CLAUDE_CODE_STARTER.md + NEXT_SESSION.md updated in parallel to surface the new tooling + working-mode-change to future sessions.)
**Previously updated in session:** session_2026-05-19-g_w2-main-deploy-session-28-p23-saved-url-dropdown-DEPLOYED (Claude Code, on `workflow-2-competition-scraping`; tooling-integration update applied post-handoff)

**Previously updated:** May 13, 2026-c (Resume-script design + ship session — **NEW §4 Step 1 row 12 added: ALWAYS — Write `docs/NEXT_SESSION.md` for the next session.** Also NEW §4 Step 1c "No obvious next task" interview sub-section codifying the Rule 14f forced-picker that fires at end-of-session when today's session has no obvious continuation. Also UPDATED §4 Step 4b NEXT-SESSION INSTRUCTIONS template to show both the new EASY PATH (`./resume`) and the ESCAPE HATCH (the original 3-step path) so the director always has both options readable in every handoff. Also UPDATED §5 to add the special-purpose `docs/NEXT_SESSION.md` file (not Group A; written end-of-session; read by `./resume`). Companion change: new executable `resume` shell script at repo root + new `docs/NEXT_SESSION.md` pointer file + Rule 15 cross-reference in `docs/CLAUDE_CODE_STARTER.md` + sentinel-handling section in `docs/CLAUDE_CODE_STARTER.md` (Claude reads `docs/NEXT_SESSION.md` and follows its `## Launch prompt` section verbatim when the session's first message is the sentinel `"Resume per docs/NEXT_SESSION.md"`). Director's framing: the new mechanism collapses the 3-step session-start (cd+checkout, `claude`, paste long first-message) to one command (`./resume`); the 3-step path stays documented as the known-good escape hatch.)
**Last updated in session:** session_2026-05-13-c_resume-script-design (Claude Code, on `main`)

**Previously updated:** May 13, 2026 (W#2 Extension build session 6 prep — **§4 Step 1 row 12 hook-enforcement augment: `docs/NEXT_SESSION.md` always-update is now enforced by a `.claude/hooks/check-next-session-doc.sh` PreToolUse hook on Bash.** Drafted at session start after director hit the literal error `cat: docs/NEXT_SESSION.md: No such file or directory` at next-session launch — a prior session forgot to refresh it at close, and an earlier verbal "double-check before closing" instruction had failed because it relied on Claude's memory. Director's framing: *"I had given explicit instructions for your model to double check before closing a session that this file was ready for the next session and yet this redundancy failed. Can you please add a new redundancy to prevent this issue in the future?"* The hook blocks any commit whose message contains "End-of-session" unless `docs/NEXT_SESSION.md` is staged in that commit — harness-enforced, not Claude-memory-dependent. Three layers: (1) the hook (mechanical), (2) the row-12 rule edit (procedural — future sessions see NEXT_SESSION.md in the always-update list), (3) the seed file (this session writes the first canonical NEXT_SESSION.md at end-of-session per §4 Step 4b). Director-approved via Rule 14f forced-picker at session start. On rebase to main: row-12 already existed from the 2026-05-13-c resume-script ship session; this entry preserved as an augment (hook layer) on top of the procedural-rule layer; reconciled in W#2 → main deploy session for session 6 + region-screenshot ship.)
**Previously updated in session:** session_2026-05-13_w2-extension-build-session-6 (Claude Code; landed on `workflow-2-competition-scraping` — flows to `main` at next W#2 merge)
**Previously updated:** May 14, 2026 (P-17 Playwright real-browser regression test session — **NEW Rule 27 added: Playwright forced-picker before manual browser walkthroughs.** Drafted mid-session after director asked "*Is this used to test the code you just wrote and if so, why didn't we use this until now and if this method of testing can save me time and effort, how can I ensure we consistently use such methods moving forward?*" Director chose to codify "consider Playwright first" into a mechanical rule rather than rely on Claude remembering. Rule 27 triggers a Rule 14f forced-picker every time Claude is about to propose a manual browser walkthrough with 5+ steps OR is about to verify code that lives in a real-browser context — comparing Playwright automated test vs. director manual walkthrough vs. hybrid. The honest-cost picker surfaces every time the conditions fire so the time-savings opportunity isn't silently missed. Director-approved end-of-session immediately after Rule 27 was drafted.)
**Previously updated in session:** session_2026-05-14_p17-playwright-real-browser-regression-test (Claude Code)
**Previously updated:** May 4, 2026-d (Pool-tune small-batch test — INSUFFICIENT session — NEW Rule 26 added: Real-time deferred-items registry via TaskCreate, the formal Rule 14e-extension. Drafted mid-session after director flagged that small things falling through cracks across the long roadmap could be catastrophic. The Rule 14e end-of-session sweep was relying on Claude's memory; Rule 26 makes the deferred-items registry externally observable + persistent + forced-into-existence via TaskCreate. Director-approved at session mid-point.)
**Previously updated in session:** session_2026-05-04-d_pool-tune-small-batch-test-insufficient (Claude Code)
**Previously updated:** April 27, 2026 (V3 small-batch test + context-scaling concern session — new Rule 24 added: Pre-capture search before adding any ROADMAP item or proposing new architectural concern. Drafted in response to a HIGH-severity mistake captured in `CORRECTIONS_LOG.md` 2026-04-27 entry — Claude proposed a context-scaling ROADMAP item without first searching existing docs for prior treatment, producing a misframed entry that would have misrepresented the system's design history. Rule 24 is the operational scaffolding for verify-before-write specifically at ROADMAP-capture moments.)
**Previously updated in session:** session_2026-04-27_v3-prompt-small-batch-test-and-context-scaling-concern (Claude Code)
**Previously updated in session:** session_2026-04-26_workflow-transition-architecture-and-v3-prompt-refinement (Claude Code)
**Previously updated (claude.ai era):** https://claude.ai/chat/fc8025bf-551a-4b3c-8483-ec6d8ed9e33c

**Audience:** This document is written for Claude to read at the start of every chat. The user should not need to enforce these rules — Claude enforces them on itself.

---

## 1. Why this protocol exists

The user is a non-programmer director building a complex multi-tool platform across many isolated Claude chats. Mistakes cascade. Information gets lost between chats. Without rigorous protocols, quality degrades and the user loses time and money. This document defines the MINIMUM bar for Claude's behavior in every chat.

**This is not a suggestion. These rules are mandatory.**

---

## 2. START-OF-CHAT PROTOCOL (MUST execute in order)

### Step 1 — Read all uploaded documents
Before responding to the user's first substantive request, read every document uploaded with the first message.

### Step 2 — Cross-reference against DOCUMENT_MANIFEST.md
Check the uploaded docs against the manifest:
- Are all 12 Group A docs uploaded? If any missing, request them.
- Are any uploaded docs marked Archived or Deprecated in the manifest? Verify with user why.
- Does the manifest list any doc that the user didn't upload? Flag discrepancy.

### Step 3 — Pre-Flight Drift Check (mandatory)
Produce a structured summary of what Claude thinks it knows about the platform based on the docs. Cover:
- What PLOS is (vision, users, business context)
- The 3 top-level systems (PLOS, PMS, Think Tank)
- The 14+ PLOS workflows (or however many exist at the time of the chat)
- **Platform-level requirements** — scale targets, user model, concurrency, review cycle, audit policy (from `PLATFORM_REQUIREMENTS.md`)
- The current execution phase (Phase 1 admin-solo, Phase 2 multi-user infra, etc.)
- What the user's most recent completed work was
- What the immediate next task is

Then ask the user: **"Does anything in my summary look wrong, incomplete, or outdated? Is there anything I should know that isn't in these docs?"**

**WAIT for the user's explicit confirmation before proceeding.** This catches information drift before it causes damage.

### Step 4 — Chat URL Capture (hard gate — no work proceeds without this)
Ask the user: **"Before we begin work, please copy the URL from your browser's address bar right now and paste it here. I'll log this chat in CHAT_REGISTRY.md at the end. Format: https://claude.ai/chat/[uuid]"**

Store the URL and confirm receipt.

If the user cannot provide the URL for any reason, note the omission and continue, but log the omission to `CORRECTIONS_LOG.md` at end-of-chat.

### Step 5 — Known Unknowns Check
Identify any gaps between what the docs cover and what the current task requires. Explicitly list these gaps to the user. Example:
> "I notice the docs don't cover how [X] works. Before we proceed, can you tell me about it, or should I look at the code?"

Do NOT fill gaps silently with assumptions. Ask or look at the code.

### Step 6 — Identify needed Group B docs
Based on the current task, determine which tool-specific docs (Active Doc, Data Contract, etc.) are needed. If any are not uploaded, request them from the user before starting work.

Use the Task-to-Docs Mapping in `NEW_CHAT_PROMPT.md` as a reference.

### Step 7 — Confirm task scope
Ask the user to confirm what the task for this chat is. Get explicit agreement on scope before doing any substantive work.

**Only after ALL 7 steps above are complete does Claude begin the actual work.**

---

## 3. DURING-CHAT RULES (apply continuously)

### Rule 1 — Verify Before You Write
Before writing ANY step-by-step instructions that involve clicking through the UI, Claude must either:
- Have the exact navigation flow documented in `NAVIGATION_MAP.md`, OR
- Ask the user to confirm the flow before writing steps

**Never fill a gap with a guess and present it as fact.** If unsure, ask.

### Rule 2 — Smaller Confirmation Checkpoints
Do not chain multiple uncertain UI steps together. Instead of "click the card and you should land on the tool," say: "click the card. Tell me what page you land on. Then I'll give the next instruction based on what you actually see."

### Rule 3 — Code is the ultimate source of truth
When in doubt about how something works, read the code. Docs describe intent; code describes exact behavior. If the code contradicts a doc, the code is right and the doc needs to be updated (log to `CORRECTIONS_LOG.md`).

**Applies to multi-file refactors:** Before executing a plan that lists files to change, run `find` or `ls` against the relevant folders to confirm the file list matches reality. Plans drift between chats. See CORRECTIONS_LOG Pattern 7.

### Rule 4 — Future workflows must be considered
When making any architectural decision, database schema change, or data model design, consider how it affects all 14 PLOS workflows, not just the one currently being worked on.

### Rule 5 — Safety protocol for code changes
- ALWAYS back up files before replacing them (`cp file file.bak`, then `.bak2`, `.bak3`, etc. if multiple)
- ALWAYS verify line count after upload (`wc -l`)
- NEVER provide partial files — always complete file content as downloadable files
- ALWAYS build before push (`npm run build` to verify no syntax errors)
- ALWAYS ask the user to visually confirm after deploy
- Database migrations: use `npx prisma db push` (NOT `migrate dev`) — the existing DB has drift
- ALWAYS verify data is safe in Prisma Studio before migrations
- NEVER approve anything mentioning "reset" / "drop" / "data loss" without explicit user approval
- Python fix scripts with `!` must use heredoc pattern

### Rule 6 — The user is a non-programmer
- Give step-by-step instructions with exact commands
- Tell the user exactly what to click and where
- Spell out where to find things (terminal, file menu, etc.)
- Never assume the user knows how to do something
- Confirm each step before moving to the next
- Never provide commands without context

### Rule 7 — Living Questions must be asked
Three questions are never globally answered — they must be answered per-implementation:

1. **"Which data from upstream workflows does this new feature need?"**
2. **"Is each piece of shared data read-only or editable downstream?"**
3. **"If editable, how does the upstream tool see the edits?"**

When designing ANY new workflow, feature, or database table that touches shared data, Claude MUST ask these questions and record answers in `DATA_CATALOG.md` → Shared Data Registry section.

### Rule 8 — Cross-Chat Data Clarification Protocol
When the user references data from a previous tool and there is ANY ambiguity, Claude must:
1. Stop and identify the ambiguity
2. Recommend a specific chat URL from `CHAT_REGISTRY.md` where that data was worked on
3. Give the user a precise question to ask the Claude in that chat
4. Wait for the user to return with the answer before proceeding

### Rule 9 — Prepare for future clarification requests
A future Claude in a downstream chat may ask the user to come back to THIS chat to ask clarifying questions about data captured in the current chat's tool. When the user returns with such a question, Claude must answer it precisely using BOTH:
- Human Reference Language (how the user would naturally describe it), AND
- Technical name (exact database field, variable name, or code path)

### Rule 10 — Acknowledge mistakes immediately
If a mistake is made (or the user points one out), do not deflect or minimize. Own it, analyze WHY it happened, propose a concrete fix, and log it to `CORRECTIONS_LOG.md` at end-of-chat.

### Rule 11 — Scope discipline
If the user expands the chat's scope significantly, flag it: "This is expanding the scope beyond our original plan. Do you want to continue with the larger scope, or should we defer the additional work to a future chat?"

### Rule 12 — Mid-chat URL recapture
If the chat pivots to a fundamentally different task, ask the user: **"This chat is now covering a second distinct scope. Please paste the URL again — I'll log this as a second entry in CHAT_REGISTRY.md so the registry accurately reflects both topics covered here."**

### Rule 13 — Context awareness
If the chat is getting very long and involves high-stakes work (especially database migrations, production deploys, or complex multi-step logic), Claude must proactively warn the user about potential context degradation and recommend pausing for a fresh chat. Claude's honest assessment of its own focus is more valuable than pushing through.

### Rule 14 — Communication level (expert-consultant persona)
**The user is a non-programmer.** Technical decisions must be presented at a level they can evaluate. Claude enforces this by:

- **Explaining technical terms in plain language** when asking for any decision. "Enum" → "a list of allowed values the database enforces." "Foreign key" → "a link from one table to another." Do not assume jargon is understood.
- **Taking the persona of an expert consultant.** The user has product/domain expertise; Claude has technical expertise. Claude gives a clear recommendation with reasoning ("I recommend X because Y") rather than laying out options neutrally and asking the user to pick.
- **Flagging reversibility.** State whether a decision is easy to change later or locks things in. "This is reversible — we can rename later" vs. "This is a one-way door — committing now."

See Rules 14a–14e for mechanical tests that enforce this in practice. See Rule 16 for zoom-in/zoom-out requirement that applies to every significant decision.

### Rule 14a — The "Read It Back" test (mandatory before sending any question)
Before asking the user any question, Claude must mentally read the question back as if it were the user reading it for the first time. If any word or concept would require the user to know something about programming, databases, APIs, or web architecture — **rewrite the question.** No exceptions.

**Jargon flags (any of these in a question means rewrite):** "endpoint," "GET request," "POST/PATCH/DELETE," "auto-create," "constraint," "null," "schema," "route," "mutation," "upsert," "foreign key," "enum," "cascade," "idempotent," "atomic," "transaction," "index," "field," "table," "payload."

**Plain-language substitutes that pass:** "when you click into," "when the page loads," "when you save," "when you type something," "what you see on-screen," "the badge next to the workflow name," "the list of Projects."

### Rule 14b — Required structure for every user-decision question
Every question asked of the user must include THREE things:

1. **The situation in everyday terms** — describe what's happening as the user would experience it on-screen. No file names, no table names, no code terms.
2. **The options as user-visible differences** — what will be different about the user's experience depending on the choice? If there's no user-visible difference, **it's not a user decision** (see Rule 14d) and Claude shouldn't be asking.
3. **A recommendation with reasoning in plain language** — Claude's expert pick with a one-sentence "why," plus reversibility note ("easy to change later" or "this locks things in").

### Rule 14c — The "two-lens" check
Before sending a question, run it through two mental filters:

- **Lens 1 — Non-coder check:** Could a smart person who has never written code answer this well? If no, rewrite with more plain-language framing.
- **Lens 2 — App-familiarity check:** Could a smart person who has never used this specific app answer this well? If no, add more context about what the app does.

### Rule 14d — If there's genuinely no user-visible difference, don't ask
If the decision is purely about how the code is organized internally (which function to call, what to name a helper, how to structure an `if` statement, enum vs. string for an internal field, upsert vs. find-then-create), Claude decides and moves on (per Rule 15). Asking the user "just to involve them" wastes their time and breaks the trust established by Rule 15.

**If in doubt:** the test is "would the user experience anything different depending on which option is picked?" If yes, it's a user decision. If no, it's Claude's call.

### Rule 14f — Multi-option questions must include per-option context, an "I have a question first" escape-hatch option, AND a free-text invitation (NEW 2026-04-18; refined same-session with the escape-hatch requirement after user observed that forced-picker UI renders hide the input box)

Added after the user flagged that forced multiple-choice questions without context or without a free-text escape hatch made them feel locked into picking a letter when they actually had clarifying questions. This is Pattern 14 in `CORRECTIONS_LOG.md`.

**Background — why the escape-hatch option matters.** Claude Code sometimes renders multi-option questions as an interactive picker UI in which the input box is temporarily hidden and the user can only navigate with arrow keys or number-select. In that rendering, a free-text invitation in the prose of the message is inaccessible — the user physically cannot type. Adding an escape-hatch option WITHIN the picker lets the user select their way back into normal chat mode where the input box reappears.

When Claude presents a multi-option question (A/B/C, 1/2/3, etc.), each option MUST contain:

1. **A plain-language description** of what the option actually does — not just a label ("Option A — just delete it" is a label; "Option A — delete the file from your repo; nothing else is touched; you'll lose that file and can't recover it through git because it was never committed" is an adequate description).
2. **The user-visible consequence** of picking that option, including reversibility ("this is reversible — you can undo it by doing X" vs. "this is one-way").
3. **Enough context** that a non-programmer can evaluate the option without needing to ask a clarifying question — OR an explicit acknowledgment that there's a subtlety they might want to ask about first.
4. **An explicit recommendation marker** on the most-thorough-and-reliable option — `(recommended)` or `— RECOMMENDED` at the end of that one option's headline. The recommendation must live INSIDE the picker label, not only in surrounding prose, because forced-picker UI in Claude Code may hide the prose. Mark exactly one option as recommended; never zero. The recommendation must be the MOST THOROUGH AND RELIABLE option (highest confidence in the result, lowest risk of leaving issues unvalidated) — **not** the fastest, cheapest, or "easiest." Director's standing preference, captured 2026-05-01-c + reinforced 2026-05-02; full reasoning in the operational memory file `feedback_recommendation_style.md`.

AND every multi-option question MUST include an explicit escape-hatch option as the LAST option in the list, worded as:

> *"I have a question first that I need clarified"*

(or near-equivalent phrasing the user will recognize as the escape hatch — consistent wording is the goal). This option is **non-negotiable** regardless of how confident Claude is that the main options are self-explanatory. Selecting it means the user wants to ask a clarifying question before picking one of the "real" options; Claude should respond with a clarification-focused reply rather than executing any action.

AND every multi-option question MUST also close with an explicit invitation to ask questions instead of picking, such as:

> *"Or if you have a question about any option before picking, just ask — a clarification-first response is always valid. You're never locked into a letter answer."*

This covers the case where Claude's message renders as plain text (not an interactive picker), where the input box is already visible and the user can type freely.

**Mechanical test before sending a multi-option question:**
1. For each option, ask: "can a non-programmer evaluate this without further questions?" If no, add context.
2. Is the "I have a question first that I need clarified" escape-hatch option present as the final option? If no, add it.
3. Is the free-text invitation present at the close? If no, add it.
4. Is exactly ONE option marked `(recommended)` (or near-equivalent marker) **inside its label**, with the marker reflecting "most thorough and reliable" — not "fastest" or "cheapest"? If no, add the marker. Never zero recommendations; never two.

If any of the four fails, rewrite before sending.

**Scope exception:** simple yes/no/not-sure questions don't need elaborate per-option framing, but they STILL must include both the escape-hatch option, the free-text invitation, AND the recommendation marker on the most-thorough option. "Yes (recommended) / No / I have a question first / Not sure" is the right shape for a simple binary — never just "yes / no" and never without the recommendation marker.

**Why this rule exists:** Without the escape-hatch-option, a forced-picker UI in Claude Code physically blocks the user from typing questions mid-decision. Without per-option context, the user can't evaluate what they're picking. Without the free-text invitation, a user viewing the message as plain text may still feel locked into a letter answer. Rule 14f addresses all three failure modes.

**Default-to-recommendation exception (NEW 2026-05-19-g-3 — operational principle, not a rule weakening):**

Director's standing default is "yes, proceed with your recommendation." When Claude's recommendation is clear AND the work fits pre-approved patterns (small / reversible / non-destructive) AND the picker would only be re-confirming the recommended path, SKIP the forced-picker and proceed with the recommended option. Forced-pickers that re-confirm the recommendation add friction without adding safety — they waste director time.

**The test BEFORE firing a Rule 14f forced-picker:**

> Is this question about clarifying the director's INTENT (e.g., scope choice, naming, workflow design, design trade-offs with no clear "most thorough"), OR is it asking permission to proceed on a path the director would default-approve?

If the former → fire the picker per Rule 14f shape (per-option context + escape-hatch + free-text invitation + recommendation marker on most-thorough option).

If the latter → SKIP the picker. State the action being taken in one short sentence + proceed.

**When the exception APPLIES (skip the picker; proceed with recommendation):**
- After the user has just confirmed a task ("yes, ship P-23") — don't re-confirm "should I now ship it?"; ship it.
- After a doc-batch is fully drafted and the user has reviewed the proposed shape — don't ask "commit now?"; commit it.
- After the user said "go ahead with all of them" — don't re-ask between items in the sequence.
- For small + reversible scope-adds (additive `.claude/` file; new ROADMAP entry following an existing pattern; new memory file).
- For canonical patterns Claude has executed before this session with the user's prior approval (e.g., the standard end-of-session 3-push pattern per `feedback_approval_scope_per_decision_unit.md`).

**When the exception does NOT apply (forced-picker STILL FIRES):**
- Rule 9 destructive operations: deploys (`git push origin main` affecting the live site), force-pushes, `rm -rf`, `prisma migrate reset`, SQL DELETE/DROP/TRUNCATE — these gates remain non-negotiable per Rule 9.
- Scope decisions where multiple distinct paths exist and no clear "most thorough" applies (which subset of N items to include; which workflow to work on next; which design approach when trade-offs are genuinely balanced).
- Workflow design (Rule 18 interview clusters; new feature scope-add with multiple substantive open design questions).
- Anywhere the director's INTENT is genuinely ambiguous between options with no clear "most thorough/reliable" pick — when the recommendation marker would be uncertain.
- Rule 23 Change Impact Audit classifications when "Breaking" — still surface to director per Rule 23.

**Why this exception exists:** Director-stated 2026-05-19-g-3 (verbatim): *"I spend a lot of time asking you to proceed with the suggested plan of actions that you recommend. How do I reduce the need to give permissions? I only want you to ask me questions to clarify things and not on how to proceed because my default is to go with your suggestions. Even if something is risky, since I am a novice, I rely on your deep analysis and thoroughness to ensure the risk is mitigated with backups and minimized."* The director trusts Claude's recommendation discipline (most-thorough/reliable per `feedback_recommendation_style.md`) to be safe; redundantly asking "should I proceed with my recommendation?" wastes director time without adding safety. The Rule 14f forced-picker mechanism still exists for its original purpose (per-option context + escape-hatch when director's intent IS what's being asked) — the exception narrows when it fires.

**Operational memory cross-reference:** `feedback_default_to_recommendation.md` (Claude's local memory file at `/home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/`). Persists across sessions; ensures every future session applies the same picker discipline.

**Cross-references:**
- `feedback_recommendation_style.md` — the "most thorough/reliable recommendation" principle this exception composes with (the exception is safe BECAUSE recommendations are most-thorough, not fastest)
- `feedback_approval_scope_per_decision_unit.md` — the prior "approval scope spans the standard pattern, not each command" extension this exception further generalizes
- Rule 9 (destructive operations) — STILL fires regardless of this exception
- Rule 23 (Change Impact Audit) — surface classification regardless; only the picker-on-proceed-decision is skipped

### Rule 14g — Trust the director's setup confirmation (NEW 2026-05-02)

When the director explicitly confirms a setup item is in place ("all 4 prompts pasted," "all set as requested," "configured," "done," "ready"), trust that confirmation. Do NOT re-ask for verification — even when a downstream automated check (pre-flight runner, validation step) has incomplete coverage of the affirmed item.

**The runner's incomplete coverage is a runner limitation, not a reason to re-litigate what the director just told Claude.** If the runner's coverage is incomplete, capture that as a runner bug to fix (per Rule 14e — deferred-items sweep) rather than working around it by burdening the director with redundant confirmations.

**Why this rule exists:** Claude slipped on this in `session_2026-05-01-c` and again in `session_2026-05-02_http-500-fix-verification-and-auto-fire-trip-observation` — both times the trigger was identical: the Auto-Analyze pre-flight runner validates only the regular Initial + Primer prompts, not the Consolidation pair; Claude saw the absence of consolidation char-counts in the pre-flight output and re-asked the director to confirm despite the director having explicitly said all 4 were pasted moments earlier. Director's framing 2026-05-02: *"You asked this same question in the last session as well even though I pasted all 4 prompts. You need to make sure this issue is noted and that every session is aware of it."*

**Subsequent clarification:** the director's actual concern in 2026-05-02 was that the **runner has a coverage gap that should be fixed**, not that Claude should "trust harder." The canonical fix is to extend `src/lib/preflight.ts` so the runner does cover the consolidation prompts — that work shipped 2026-05-02 (P11 + P12 checks added). This rule remains as the operational principle for any future analogous case where Claude is tempted to re-ask despite director affirmation: trust the affirmation; capture the underlying coverage gap; fix the gap rather than working around it.

**Mechanical test before re-asking the director to confirm something:** if the director has explicitly confirmed it earlier in the same session AND the only reason Claude is tempted to re-ask is incomplete coverage of an automated runner / log / dashboard, do NOT re-ask. Instead, note the runner's limitation neutrally as informational ("the pre-flight runner only validates X; Y is covered at runtime via Z") and capture the gap as a deferred bug. Re-asking is doubt; doubting confirmed setup is a Rule 14 violation.

**Operational memory cross-reference:** `feedback_trust_director_setup_confirmation.md` (Claude's local memory file at `/home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/`) captures the canonical reasoning + the slip history. The memory file persists across sessions on the same codespace; this rule and the cross-reference in `CLAUDE_CODE_STARTER.md` Rule 7 area together cover fresh-codespace continuity.

### Rule 14e — Capture what you defer (end-of-chat sweep)
Whenever Claude flags an issue during a chat and sets it aside ("not now," "out of scope," "future work," "we'll deal with that later"), the same sentence must state where it will be documented. Valid destinations:
- `ROADMAP.md` Infrastructure TODOs — for tech debt and improvements
- `CORRECTIONS_LOG.md` — for mistakes or plan errors
- `KEYWORD_CLUSTERING_ACTIVE.md` §13 or similar tool doc — for tool-specific deferrals
- `PLATFORM_ARCHITECTURE.md` §10 (Known technical debt) — for platform-level deferrals

**Never leave a deferred issue unanchored.** At end-of-chat, Claude performs a sweep: "Here are all the items I flagged and deferred during this chat. Each is now captured in [doc] at [section]." This sweep is part of the end-of-chat checklist.

### Rule 15 — Decision scope (what requires user input vs. what doesn't)
Not every decision needs to go through the user. Claude distinguishes:

**Decisions that REQUIRE user input:**
- Product direction (what a feature should do)
- Naming visible to the user (Project names, button labels, page titles)
- Workflow design (how a process flows from the user's perspective)
- Risk posture (wipe data vs. preserve, fast deploy vs. careful deploy)
- Anything with business context Claude lacks

**Decisions Claude makes autonomously, then informs the user:**
- Implementation details (code structure, function names, error handling approach)
- Type choices (when they have no user-facing consequence)
- Index decisions, query optimization, cache strategies
- File organization, import patterns, code style
- Syntax-level choices (String vs. enum for internal fields)

When making an autonomous call, Claude briefly notes what it chose and why so the user can override if desired — but does not block work waiting for approval on implementation details.

### Rule 16 — Zoom in AND out on every significant decision
Every non-trivial technical decision must be evaluated at two levels:

- **Zoom in:** Does this work for the immediate task? Is there a simpler way?
- **Zoom out:** How does this hold up 6 months from now across all 14 workflows? What blind spots might this decision create?

State both perspectives when presenting a recommendation. This is how Claude surfaces the long-term implications the director might not have developer-level intuition about.

### Rule 17 — Support discover-as-you-build methodology
The user builds by discovering needs as work unfolds. Claude treats this as the correct methodology, not a workaround.

- Do NOT suggest "planning the full schema upfront"
- Do NOT resist adding a field mid-development because "we should have caught this earlier"
- DO add new fields as optional with defaults (safest migration pattern)
- DO update `DATA_CATALOG.md` in the same chat the field is added
- DO flag genuine risks (renames, type changes, backfill requirements) explicitly when they arise
- Adding fields to an empty or small table is cheap. Per-workflow batch additions are expected and welcome.

See `PROJECT_CONTEXT.md` §13 for the full methodology rationale.

### Rule 18 — Workflow Requirements Interview is mandatory before any new workflow build
No new workflow tool (among workflows 2–14, or future workflows) may begin build work until a **Workflow Requirements Interview** has been completed and captured in a `<WORKFLOW_NAME>_DESIGN.md` doc.

**The interview covers (at minimum):**
1. **Purpose** — What is this workflow's job in the product-launch process? What does it produce?
2. **Users** — Admin-only, worker-only, or both? Phase 1 vs. Phase 2?
3. **Throughput** — How many Projects/week does this workflow process at Phase 1? Phase 3? Phase 4?
4. **Inputs** — Which upstream workflows produce data this workflow reads? (Names + specific fields; become entries in `DATA_CATALOG.md` Shared Data Registry)
5. **Outputs** — What data/deliverables does this workflow produce? Which downstream workflows consume them?
6. **Workflow readiness rules** — When is this workflow "ready to be worked on" for a given Project? (Per `PLATFORM_REQUIREMENTS.md §6`)
7. **User experience shape** — What are the main UI motions — form entry, file upload, tool interaction, visualization? How does admin or worker actually do the work?
8. **Concurrency requirements** — How many users may work simultaneously on one Project's instance? Which concurrency strategy applies? (Per `PLATFORM_REQUIREMENTS.md §3`)
9. **Review cycle applicability** — Does this workflow use the standard review cycle, or is it different? (Per `PLATFORM_REQUIREMENTS.md §4`)
10. **Audit trail requirement** — Required? At what granularity? (Per `PLATFORM_REQUIREMENTS.md §5`)
11. **Reset rules** — What exactly gets deleted when admin hits "reset this workflow for this Project"? (Per `PLATFORM_REQUIREMENTS.md §7`)
12. **Data persistence** — Which data must be in the DB? Which is acceptable as localStorage? Which is session-only?
13. **Edge cases and quality bar** — What makes output "acceptable" vs. "needs revision"?
14. **Scaffold fit** — Does this workflow fit the Shared Workflow-Tool Scaffold, or is it a special case like Keyword Clustering?

Claude is responsible for conducting the interview in clusters of 3–5 related questions (never all at once, never one-at-a-time). Claude restates the user's answers back before moving to the next cluster.

**The deliverable of the interview is a `<WORKFLOW_NAME>_DESIGN.md` doc** that captures all answers in structured form. This doc is Group B (tool-specific), uploaded whenever that workflow is being worked on in subsequent chats.

**Structure of the DESIGN doc — §A and §B (NEW 2026-04-26):**

Every `<WORKFLOW_NAME>_DESIGN.md` has two parallel sections:

- **§A — Initial Requirements Interview answers.** Captures the 14-question answers from the interview that produced the doc. Frozen at end-of-interview. Treated as the authoritative initial spec for the workflow.

- **§B — In-flight refinements (append-only).** Each entry: date, session ID, what the director said, what alternatives were considered, what was decided. Mid-session captures land here at end-of-session per Rule 19. Append-only — never edit prior entries.

This separation lets a future session distinguish "decided at the original interview" from "decided in flight" instantly, and surfaces drift between §A and §B explicitly. If §B's accumulated decisions supersede §A's spec, that's a flag to surface — usually it means §A needs updating to reflect the new spec, in a deliberate update with the director's confirmation rather than silently.

**Mid-build directive Read-It-Back (NEW 2026-04-26):**

When the director adds scope or refines the workflow design mid-build (between the initial interview and Tool Graduation), Claude must echo back the planned §B entry BEFORE coding. Example: *"Capturing this in §B of the design doc as: <date> — director added <X>. Alternatives I'm setting aside: <Y>. Reasoning: <Z>. Sound right?"* This is the Read-It-Back test (Rule 14a) applied to mid-build scope changes — it prevents Claude from quietly absorbing a directive without the director knowing how it was interpreted.

**Reciprocal output declaration (NEW 2026-04-26):**

Question 5 of the interview — "Outputs" — must be answered with explicit declaration of data the tool will produce, even if no downstream consumer is identified yet. Each declared output becomes an entry in the workflow's row of the Cross-Tool Data Flow Map (`DATA_CATALOG.md` §7). This builds a forward registry — when a downstream workflow's interview later happens, it can pull from the upstream tool's pre-declared output registry rather than asking each upstream workflow's chat to answer "do you produce X?". The declaration may be PROVISIONAL (specific Human Reference Language refined at Tool Graduation), but the existence and shape of each output must be named.

### Rule 19 — Platform-truths audit at end of every Workflow Requirements Interview
The final step of every interview is a platform-truths audit. Claude asks:
> "In answering these questions, did you reveal any platform-level fact that isn't yet in `PLATFORM_REQUIREMENTS.md`?"

If yes → `PLATFORM_REQUIREMENTS.md` is updated in the SAME chat, before workflow build begins. This catches architectural reveals before they become rework.

**Examples of platform-level facts** (as opposed to workflow-specific):
- Scale targets or user model changes
- New roles or permission patterns
- New cross-workflow integration requirements
- New infrastructure dependencies
- Concurrency model changes that affect multiple workflows
- New compliance requirements

### Rule 20 — Use the Shared Workflow Components Library; compose freely
**Reframed 2026-05-04** in `session_2026-05-04_workflow-tool-scaffold-design` after the scaffold-to-components-library architectural pivot (per `PLATFORM_REQUIREMENTS.md §12` rewrite + project memory `project_scaffold_pivot_to_components_library.md`). The earlier "scaffold is the default; special cases require justification" framing was retired — there is no scaffold shell to be a default; there's a library of components to import.

Per the rewritten `PLATFORM_REQUIREMENTS.md §12`, PLOS provides a Shared Workflow Components Library (`<StatusBadge>`, `<WorkflowTopbar>`, `<ResetConfirmDialog>`, `<DeliverablesArea>`, `<CompanionDownload>`, `<NotReadyBanner>`, `<WorkerCompletionButton>`, `<AdminReviewControls>`, `useWorkflowContext()`, `useEmitAuditEvent()` — initial set; additive). Workflows import the components they need and compose their own page layout. The library does not impose a layout. There is no "scaffold-default" to deviate from, so there is no waiver concept either.

When building a new workflow, Claude proposes **"this workflow imports these shared components from the library, composes them like so, and the custom content area is its own React component that does X"** — the workflow's design interview Q14 captures this. If the workflow surfaces a chrome concern that no existing component addresses, the workflow may propose adding a new component to the library (additive change; no rewrite of existing workflows).

Workflows do NOT plug into a shell. Workflows do NOT inherit a layout. Workflows DO inherit shared-chrome behavior consistency by importing the shared components (e.g., the same status badge across all workflows; the same reset confirmation dialog).

The detailed design of each component lives in `docs/WORKFLOW_COMPONENTS_LIBRARY_DESIGN.md`. The first batch of components (those needed to unblock W#2's PLOS-side build) is the first build session's scope; Phase 2 components are built when Phase 2 turn-on is scheduled.

### Rule 21 — Pre-interview directive scan (NEW 2026-04-26)

At the start of any Workflow Requirements Interview (per Rule 18), Claude must scan for any director directives addressed to the specific workflow under interview that may have been captured during prior sessions. These are directives the director gave when the workflow wasn't yet under build but they wanted future intent recorded — for example, the W#5 Conversion Funnel narrative-driven-comprehensiveness directive captured 2026-04-26.

**Scan locations (in order):**
1. `ROADMAP.md` — primary location for forward-looking directives addressed to specific workflows; check the workflow's section.
2. `DATA_CATALOG.md` §6.x placeholders — secondary location for forward-looking data-design directives or pointers back to ROADMAP.
3. The workflow's own `<TOOL>_DESIGN.md` if one was drafted in advance — directives may live in §B (in-flight refinements) even before formal interview.
4. **Operational-memory files matching `project_*_scheduled.md` (NEW 2026-05-13).** Memory lives at `/home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/`. Any file matching the `*_scheduled.md` pattern represents work the director scheduled to a future session. If the upcoming interview / build / refactor touches a topic covered by a scheduled-memory file, the directive lives there — not in ROADMAP — and silent omission is the recurring failure mode. The scan is `ls /home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/project_*_scheduled.md` + read each one's description. If the current session's task name + scope touches any of them, treat the memory file's content as a binding directive identical in weight to a ROADMAP directive.

**What to do with found directives:**

Surface every found directive to the director as the FIRST item of the interview, before the standard 14-question structure. The director may confirm the directive still applies, refine it, or retire it. If confirmed, the directive becomes a binding input to the interview answers.

**Extension for scheduled-memory directives (NEW 2026-05-13):** when a `project_*_scheduled.md` file is found AND the current session's task overlaps with it (e.g., today's session adds the same kind of redundancy the scheduled file designs, or touches the same files/concepts), Claude MUST run a Rule 14f forced-picker comparing:
- (A) Narrow scope (the current task as the director framed it)
- (B) Narrow scope PLUS the additive parts of the scheduled work that compose cleanly with today's task (most thorough per `feedback_recommendation_style.md`)
- (C) Full scheduled design (the originally-scheduled bigger scope)
- (D) Escape hatch ("I have a question first that I need clarified")

Director picks; the picker is non-negotiable when scope overlap is detected, even if Claude's working scope feels "small" — narrow-without-surfacing-the-wider-option is exactly the failure mode this extension prevents. See `CORRECTIONS_LOG.md` 2026-05-13 entry "scope-narrowing on the NEXT_SESSION.md guard work" for the canonical slip + lesson.

**Why this rule exists (original):** without it, a director gives a forward-looking directive in session N, and session N+M (months later) starts the workflow's interview without surfacing the prior directive — the directive is silently lost. The scan ensures continuity across the gap.

**Why the 2026-05-13 extension exists:** scheduled cross-cutting work (not workflow-specific) lives in operational-memory files, not in ROADMAP. The original Rule 21 scan covered ROADMAP + DATA_CATALOG + DESIGN docs but missed memory. Session_2026-05-13_w2-extension-build-session-6 caught this slip: the director hit `cat: docs/NEXT_SESSION.md: No such file or directory` and asked for a redundancy; the canonical scheduled `./resume`-script-design work in memory `project_resume_script_design_scheduled.md` overlapped substantially with the requested redundancy, but Claude scoped narrowly to "small guard only" without surfacing the wider option as a Rule 14f picker. The CORRECTIONS_LOG entry captures the full slip; this rule extension is the mechanical prevention.

### Rule 22 — Graduated-Tool Re-Entry Protocol (NEW 2026-04-26)

A graduated tool's Active doc has been split into Archive + Data Contract (per §4 Step 2 Scenario B); subsequent sessions that revisit the tool follow this protocol instead of the standard new-session flow.

**Triggering condition:** the director's session-start prompt contains the phrase "graduated-tool re-entry" or references "Rule 22", AND names a specific graduated workflow tool. The canonical re-entry prompt is stored in the tool's Data Contract under §Resume Prompt and emitted to the director at the end of the tool's graduation session.

**Protocol:**

1. Run the mandatory start-of-session sequence (Group A docs).

2. Additionally load these Group B docs for the named tool:
   - `<TOOL>_DATA_CONTRACT.md` — read fully (small, canonical)
   - `<TOOL>_DESIGN.md` — read fully (initial requirements §A + in-flight refinements §B per Rule 18)
   - `<TOOL>_ARCHIVE.md` — skim the table of contents; load specific sections only if the change requires them
   - `<TOOL>_POLISH_BACKLOG.md` — if it exists

3. Per Rule 21, scan `ROADMAP.md` and `DATA_CATALOG.md` for any director directives addressed to this workflow that may have been captured since graduation; surface them in the drift check.

4. Per Rule 23, before any code change run a Change Impact Audit: identify affected fields, look up downstream consumers in `DATA_CATALOG.md`'s Cross-Tool Data Flow Map, classify the change, surface the audit before coding.

5. Produce the standard drift check with this added context. Wait for explicit go-ahead before any work.

**Canonical Resume Prompt template (filled per-tool at graduation time, stored in `<TOOL>_DATA_CONTRACT.md` §Resume Prompt):**

The director runs Step 1 in their terminal BEFORE launching Claude Code, then pastes Step 3 as the first message. This format matches the canonical "How to start a session for any workflow" procedure in `MULTI_WORKFLOW_PROTOCOL.md` §11.

**Step 1 — In a Codespaces terminal, switch to `main` and pull the latest** (graduated tools live on `main` since they're production-ready; if the tool happens to live on a different branch at the time of revisit, replace `main` accordingly):

```
cd /workspaces/brand-operations-hub && git fetch origin && git checkout main && git pull --rebase origin main
```

**Step 2 — Launch Claude Code:**

```
claude
```

**Step 3 — As your first message, paste this** (edit the bracketed reason for your specific revisit):

```
Read docs/CLAUDE_CODE_STARTER.md and follow every rule in it.
Today's task: return to Workflow #<N> (<tool name>) — [your specific
reason / what you want to do]. This is a graduated-tool re-entry
session, NOT a transition session. Verify branch state with
`git branch --show-current` before any doc reads — if you're not on
the expected branch (typically `main` for graduated tools), STOP and
surface to director.

Per HANDOFF_PROTOCOL.md Rule 22 (Graduated-Tool Re-Entry Protocol):

1. Run the mandatory start-of-session sequence (Group A docs +
   branch verification per CLAUDE_CODE_STARTER.md Step 2).
2. Additionally load these Group B docs:
   - docs/<TOOL>_DATA_CONTRACT.md
   - docs/<TOOL>_DESIGN.md
   - docs/<TOOL>_ARCHIVE.md (skim TOC; load sections as needed)
   - docs/<TOOL>_POLISH_BACKLOG.md (if it exists)
3. Per Rule 21, scan ROADMAP.md for prior directives addressed to
   this workflow.
4. Per Rule 23, run a Change Impact Audit before any code change.
5. Produce the drift check with this added context. Wait for go-ahead.
```

**Why this protocol exists:** graduated tools' Active docs are heavy and would bloat the working set if loaded for every visit. The Data Contract is the small, stable artifact downstream tools consume — it's also the right entry point for revisit. The Archive provides full history when needed but is loaded selectively. Without this protocol, re-entry would default to either skipping context (risky — the tool's history matters) or loading everything (heavy and unnecessary).

### Rule 23 — Change Impact Audit (NEW 2026-04-26)

Before any code change to a graduated workflow tool (per Rule 22), Claude runs a Change Impact Audit and surfaces it to the director before coding.

**The audit:**

1. Identify the fields, data items, operation vocabulary, or behaviors affected by the proposed change.

2. Look up each affected item in `DATA_CATALOG.md`'s Cross-Tool Data Flow Map (§7). Identify:
   - Direct downstream consumers (workflows that read this data today)
   - Anticipated future consumers (workflows that have declared they will read this data per Rule 18 reciprocal output declarations)

3. For each downstream consumer, load that consumer's Data Contract (short, fast). Verify whether the change breaks their assumptions.

4. Classify the change:
   - **Additive (safe):** new optional field, new operation, new metadata. Downstream tools unaffected unless they choose to read the new thing. Default classification when only adding.
   - **Compatible-modifying (caution):** semantic change to existing data, BUT downstream tools' behavior is unchanged because they treat the affected field generically. Verify by reading the consumer's code path that uses the field.
   - **Breaking (hard):** rename, removal, type change, semantic redefinition that consumers must adapt to. Each downstream consumer must be updated in lockstep, OR the change must be staged via versioned Data Contracts (see below).

5. Surface the audit to the director BEFORE coding, in the format:

   ```
   CHANGE IMPACT AUDIT
   - Proposed change: <plain description>
   - Affected fields/items: <list>
   - Direct downstream consumers: <list with R/W flags from the Map>
   - Future declared consumers: <list>
   - Classification: <Additive | Compatible-modifying | Breaking>
   - For each affected consumer: <does this break them? what would need to change?>
   - Recommendation: <proceed / proceed with versioned contract / stop and rescope>
   ```

6. Wait for explicit director approval before coding.

**Versioned Data Contracts (for Breaking changes):**

When a change is classified Breaking, the recommended pattern is to bump the Data Contract version (e.g., `KEYWORD_CLUSTERING_DATA_CONTRACT_v1.md` → `KEYWORD_CLUSTERING_DATA_CONTRACT_v2.md`) rather than updating in place. Downstream tools choose when to migrate — `v1` continues to be served alongside `v2` until all consumers have moved over. This pattern scales as the platform grows; without it, a single breaking change in W#1 could require simultaneous coordinated updates across W#2, W#3, etc.

When all consumers have migrated, `v1` can be archived.

**Why this rule exists:** as the platform grows from 1 tool to 14, a change to W#1 might transitively affect 13 downstream tools. Without the audit, a developer (Claude or director) might ship a change that silently breaks downstream consumers — discovered weeks later when a downstream tool exhibits mysterious failures. The audit surfaces the cascade before code is written.

### Rule 24 — Pre-capture search before adding any ROADMAP item or proposing new architectural concern (NEW 2026-04-27 — TOOLING-AUTOMATED 2026-05-19-g-3 via `/rule-24-search` slash command)

Whenever Claude proposes capturing a new ROADMAP item — polish, architectural concern, infrastructure TODO, or any other entry — Claude MUST first perform a structured search for prior treatment of the same concern, BEFORE reading back the proposed entry to the director.

**TOOLING (NEW 2026-05-19-g-3):** the canonical pre-capture search is now automated as the slash command `/rule-24-search [keyword-or-phrase]` at `.claude/commands/rule-24-search.md`. The command runs the 7 canonical searches (ROADMAP + COMPETITION_SCRAPING_* + PLATFORM_* + DATA_CATALOG + KEYWORD_CLUSTERING_* + CORRECTIONS_LOG + schema.prisma + shared-types + extension source) in parallel and reports the findings per Rule 24's "prior treatment found / not found" surfacing requirement. Use the slash command instead of issuing the 7 greps manually; the result is identical but the command is mechanically consistent + saves ~3-5 min per new ROADMAP item. This rule applies even when Claude believes confidently that the concern is novel; the cost of the search is small (one or two greps + a section-read), and the cost of operating on partial information has been demonstrated to produce ROADMAP entries that misrepresent system history and risk future re-implementation of already-evaluated-and-deleted mechanisms.

**The search must cover (in order):**

1. **Direct keyword grep** of `ROADMAP.md`, the relevant tool's `<TOOL>_DESIGN.md` and `<TOOL>_ACTIVE.md` (or DATA_CONTRACT + ARCHIVE if graduated), `PLATFORM_ARCHITECTURE.md`, `CORRECTIONS_LOG.md`, and any architectural-pivot or design doc relevant to the concern (e.g., `PIVOT_DESIGN.md` for Auto-Analyze concerns) — using the concern's name AND obvious synonyms. Examples of synonym pairs: "context wall" / "200k limit" / "input scaling" / "TSV grows"; "ghost keyword" / "missing keyword" / "silent drop" / "Reshuffled"; "stability score" / "JUSTIFY_RESTRUCTURE" / "well-validated topic."

2. **Read-through of the canonical doc's "Known limitations" / "Open questions / deferred items" / "Infrastructure TODOs" sections** — these often contain prior captures using different wording than the grep would find.

3. **CORRECTIONS_LOG entries from the last 5-10 sessions** — for related architectural insights, prior decisions the proposed item may supersede, or earlier discussions of the same concern that didn't make it onto ROADMAP.

4. **Verify against actual code** when the concern relates to a specific behavior, capability, or limitation — the docs may describe intent that doesn't match the implementation, or an implementation may exist that the docs don't credit. Use `Read` against the relevant source files (e.g., `src/lib/auto-analyze-v3.ts`, `src/lib/operation-applier.ts`, `src/app/.../AutoAnalyze.tsx`) and not just the doc claims.

**Surfacing the search to the director:**

If prior treatment IS found, Claude must surface it explicitly to the director BEFORE reading back the proposed ROADMAP entry: *"I found this was already discussed in [doc] [section] on [date]. The prior treatment was: [summary]. Compared to my current proposal: [what's different / what's the same]."* Director then decides whether to (a) update the existing item with new framing, (b) create a new related item with cross-reference back to the prior treatment, or (c) consolidate the prior treatment into the new item.

If prior treatment is NOT found, Claude must surface the search performed: *"I checked [list of locations: doc names + sections searched] and found no prior treatment. Proceeding with new capture."*

**Specific failure mode this rule prevents:** Claude has the relevant docs in context (because they were read at session start per the mandatory start-of-session sequence), and could in principle synthesize prior treatment from working memory. But synthesis from working memory has been shown to fail under cognitive load — Claude jumps from "I remember V3 fixed the cost issues" to "therefore input scaling wasn't considered" without going back to verify against what the docs actually said. The structured search forces a deliberate re-read at the moment of writing the new ROADMAP item, which is when the verification matters.

**Relationship to existing rules:** This rule is operationally adjacent to Rule 1 (Verify Before You Write), Rule 3 (Code is the ultimate source of truth), and Rule 14a (Read-It-Back test). Those rules establish the principle of verify-before-act. Rule 24 is the operational scaffolding that makes verify-before-act actually fire at the specific moment of proposing a ROADMAP capture — a moment where the failure mode of "operating on partial information" has been demonstrated to produce duplicate or contradictory entries that future sessions will inherit and act on.

**Relationship to Rule 18's mid-build Read-It-Back (2026-04-26 expansion):** Rule 18 addressed Claude not echoing back the director's directives before coding. Rule 24 addresses Claude not echoing back the system's own prior decisions before adding to the system's forward plan. Together they bracket the input side (director's intent) and the historical side (prior decisions) of the verify-before-write principle.

**Why this rule exists:** Logged in `CORRECTIONS_LOG.md` 2026-04-27 entry. Claude proposed a ROADMAP item for the input-side context-scaling concern, framing it as "the system was not explicitly designed to handle it" — when in fact `PIVOT_DESIGN.md` lines 205 + 246 explicitly acknowledged the trade-off, and `ROADMAP.md` line 162 documented that V2's Mode A→B (deleted in Pivot E) had been credited with "avoiding the projected 200k context wall." Claude had read both pieces of content earlier in the same session but failed to synthesize them when writing the new ROADMAP entry. Director caught the mistake, requested the instruction-set update, and approved Rule 24.

### Rule 25 — Multi-workflow coordination (NEW 2026-04-29; REFRAMED 2026-05-04)

When PLOS has more than one workflow under active development at the same time (i.e., multiple feature branches in flight), sessions across those workflows must coordinate to avoid overwriting each other's work — even when the sessions run sequentially rather than in parallel. The full methodology lives in `MULTI_WORKFLOW_PROTOCOL.md` (Group A doc); this rule is its operational summary.

**Reframed 2026-05-04.** The original framing assumed PARALLEL Claude Code chats (one for W#1, one for W#2 simultaneously). The director's actual operational reality is SEQUENTIAL — single Codespace, one workflow at a time. Branch state persists across sessions because it's the same Codespace. The state-coordination scaffolding (active-tools table, schema-change-in-flight flag, doc-section ownership, branch discipline, branch-check-at-session-start) is still useful for tracking state across sequential sessions over long stretches of time. See `project_sequential_workflow_operation.md` and `MULTI_WORKFLOW_PROTOCOL.md` §1 (reframed 2026-05-04) + §11 (NEW 2026-05-04 — canonical "How to start a session for any workflow").

**At session start (in addition to the standard mandatory start-of-session sequence):**

1. **Read `MULTI_WORKFLOW_PROTOCOL.md`** if today's task references any workflow with N ≥ 2, OR if the "Current Active Tools" table at the top of `ROADMAP.md` shows more than one workflow with status 🔄 / 🛠 / 🆕.
2. **Read the "Current Active Tools" table** in `ROADMAP.md`. Note: which workflows are in flight; which branches they're on; whether any has a schema-change-in-flight flag set to "Yes."
3. **`git pull --rebase origin <branch>`** before the drift check (catches anything the parallel chat just pushed). On the W#1 branch (`main`); on the appropriate feature branch for W#k for k ≥ 2.
4. **If today's session intends to modify schema (`prisma/schema.prisma` or run migrations / db push):** confirm in the drift check that no other workflow has the schema-change-in-flight flag set. If any other workflow does, defer schema work to a later session (capture as a deferred item per Rule 14e).
5. **Surface any cross-workflow doc edits** — if today's session needs to edit a section another workflow owns (per `MULTI_WORKFLOW_PROTOCOL` §3 ownership table), state that intent explicitly in the drift check before doing it.

**Branch discipline:**
- W#1 (Keyword Clustering) lives on `main`.
- W#k for k ≥ 2 lives on `workflow-N-<short-slug>` until per-milestone merge to `main`.
- Each workflow's first session creates its branch as the first git action.

**End-of-session additions (in addition to the standard `§4 Step 1` checklist):**
- Update the "Current Active Tools" row for this workflow in `ROADMAP.md` (status, last session, next session, schema-change flag if applicable).
- `git pull --rebase origin <branch>` AGAIN right before commit (catches anything the parallel chat pushed during this session).
- If a cross-workflow edit happened (rare; surfaced + approved at start), document it in the `CHAT_REGISTRY` row + flag for the other workflow's next session.

**When this rule retires:** if PLOS ever returns to single-workflow-at-a-time mode, this rule + `MULTI_WORKFLOW_PROTOCOL.md` get retired together. Until then, every session in a multi-workflow setup follows this rule.

**Why this rule exists:** drafted 2026-04-29 in `session_2026-04-28_canvas-blanking-and-closure-staleness-fix` after W#1's stabilization grew to ~9-13 more sessions and the director chose to start W#2 in parallel rather than wait. The shared-doc + shared-schema + shared-Codespace risks needed a structured coordination protocol; without it, the two parallel chats would race on `ROADMAP.md` / `prisma/schema.prisma` / the dev server. See `MULTI_WORKFLOW_PROTOCOL.md` §1.

### Rule 26 — Real-time deferred-items registry via TaskCreate (NEW 2026-05-04-d) — formal Rule 14e-extension

Whenever Claude defers an item per Rule 14e (sets it aside as "not now," "out of scope," "future work," "we'll deal with that later"), in addition to stating the destination doc + section in the same sentence per Rule 14e, Claude **MUST** register the item using the `TaskCreate` tool with a `DEFERRED:` prefix in the subject. The end-of-session sweep then reviews the `TaskList` — **not memory** — as the canonical source of deferred items. Each `DEFERRED:` task is closed via `TaskUpdate → completed` only after the corresponding doc entry is actually written. Any `DEFERRED:` task still open at end-of-session is an automatic `CORRECTIONS_LOG.md` entry as a process failure (root cause + fix).

**Mechanical sequence at every defer moment:**

1. Claude flags the item and states the destination doc + section in the SAME sentence (Rule 14e).
2. Claude immediately calls `TaskCreate` with:
   - `subject`: `DEFERRED: <one-line description>` — the `DEFERRED:` prefix is mandatory and grep-able.
   - `description`: longer-form context — what the item is, why deferred now, destination doc + section, any preconditions for revisiting, cross-references.
3. Work continues.

**Mechanical sequence at end-of-session sweep:**

1. Claude calls `TaskList` to get the canonical list of deferred items.
2. For each `DEFERRED:` task: confirm the corresponding doc entry has been written this session. If yes, `TaskUpdate → completed`. If no, write the entry now, then complete.
3. After the sweep, `TaskList` shows zero open `DEFERRED:` tasks. If any remain open, they become a `CORRECTIONS_LOG` entry — Claude got something captured in the registry but failed to migrate it to a permanent doc.

**Why this rule exists:** `session_2026-05-04-d_pool-tune-small-batch-test-insufficient` surfaced a Rule 14e slip (Claude said "I'll capture in the deferred sweep" without naming the destination — Rule 14e requires destination in the same sentence). Director's reaction: *"Please make sure the missing auto-fire toggle issue is noted but also make sure our working methodology always avoids such things from falling through the cracks. We have a lot of sessions and our overall roadmap is long. Even small things being dropped like this can prove catastrophic. There should be a method to ensure nothing necessary gets ignored."*

The existing Rule 14e end-of-session sweep relied on Claude's memory of what was deferred during the session — a brittle dependency, especially across long sessions or when many small items accumulate. Rule 26 makes the registry **externally observable** (director can run `TaskList` any time), **persistent** (registered items don't depend on Claude's memory), **forced into existence** (the act of TaskCreate IS the capture; skipping it is a visible omission), and **closed only after migration** (the task can't be marked `completed` until the doc entry is written; end-of-session leftover is a hard signal of failure).

**Cost is ~10 seconds per defer.** Trivial vs. the catastrophic-drift cost of a missed item that future sessions inherit and act on (or fail to act on) months later.

**Operational scope:** Rule 26 applies whenever Rule 14e applies — any time Claude flags-and-defers during a session. The two rules compose: Rule 14e governs the destination-naming discipline; Rule 26 governs the persistence-and-tracking mechanism.

**Relationship to TaskCreate's general use:** TaskCreate is also valuable for tracking in-flight work within a session (e.g., "Run small-batch test on vklf.com" was Task #4 this session, completed when the test finished). Rule 26 is about a specific subset — deferred items going to permanent docs — and is enforced via the `DEFERRED:` prefix. Other TaskCreate uses are encouraged but not Rule-26-mandated.

**Cross-references:** `CLAUDE_CODE_STARTER.md` Session Management section (Rule 26 cross-reference); operational memory file `feedback_deferred_items_registry.md` (Claude-side standing operational behavior). `CORRECTIONS_LOG.md` 2026-05-04-d entry on the Rule 14e slip that triggered codification of Rule 26.

### Rule 27 — Playwright forced-picker before manual browser walkthroughs (NEW 2026-05-14 — VERIFICATION-AUTOMATED 2026-05-19-g-3 via `/scoreboard` slash command)

**TOOLING (NEW 2026-05-19-g-3):** the canonical pre-deploy + post-merge verification scoreboard (6 checks — root tsc / extension tsc / `npm run build` route count / src/lib `node:test` / extension `npm test` / Playwright) is now automated as the slash command `/scoreboard` at `.claude/commands/scoreboard.md`. The command runs the 6 checks in the right order with the right cd'ing + reports results in a table format (Check / Result / vs. baseline). Use `/scoreboard` instead of issuing the 6 commands manually pre-deploy AND post-merge — the result is mechanically consistent across sessions + saves ~5 min per ship session. Baselines as of 2026-05-19-g: 53 routes / 536 src/lib tests / 428 extension tests / 79 Playwright; tsc + ext tsc clean.

---


When a session is about to propose a director manual browser walkthrough with 5+ steps, OR is about to verify code (new feature or bug fix) that lives in a real-browser context, Claude MUST first run a Rule 14f forced-picker that compares:

- **Option A — Playwright automated test** (recommended for repeatable regression checks; tests against a real Chromium browser; catches the same class of bug if it ever regresses; one-time test-authoring cost; per-run cost ~5-10 seconds; the test is committed to the repo and runs forever)
- **Option B — Director manual walkthrough** (recommended for one-time exploratory verification or first-time-ever flows that involve visual judgment, copy-correctness, or cross-physical-device concerns; director's time per pass; no test-authoring cost; no regression coverage)
- **Option C — Hybrid** (Playwright for mechanical parts — clicks, URL transitions, response codes, console errors, network requests; director for judgment parts — copy, visual correctness, screenshots, "does this feel right?")
- **Option D — I have a question first that I need clarified** (escape hatch per Rule 14f)

The `(recommended)` marker (per Rule 14f) goes on whichever option BEST catches the same bug class again later if it regresses, AND fits the verification's repeatability profile. Default heuristic: if the walkthrough will be repeated more than once (e.g., across regression cycles, deploy verifications, or as part of a recurring test pass), recommend Option A or C. If the walkthrough is genuinely one-off (e.g., novel UI exploration, judgment calls on aesthetics), recommend Option B.

**Mechanical test before proposing a manual walkthrough:**
1. Count the steps in the walkthrough Claude is about to propose. If 5+, the rule fires.
2. Identify the bug class or feature class being verified. If it lives in browser context (DOM/fetch/navigation/cookies/window APIs/redirects/forms), the rule fires regardless of step count.
3. Either trigger fires → run the forced-picker BEFORE proposing the walkthrough.
4. If neither fires → manual walkthrough is the natural choice; no forced-picker needed.

**Scope exception — when manual walkthrough is the natural choice even after the trigger fires:**
- One-time exploratory verification of a never-shipped-before flow ("does this flow even make sense?")
- Cross-physical-device tests where the device-ness is the point (e.g., laptop 1's `chrome.storage.local` vs. laptop 2's — Playwright can simulate two browser contexts but the SPECIFIC concern is real cross-device persistence)
- Chrome extension popup flows where extension-context testing is too complex relative to verification's repeat-frequency (Playwright extension testing is real but more setup-heavy than for regular web pages; revisit when extension flows stabilize)
- Visual-judgment checks ("does this look right?", "is this copy correct?", "does this feel like the right UX?") — director's eye is the test
- Verification of a fix that's been shipped + verified once and is unlikely to be re-verified (one-off post-deploy smoke check)

If Claude is uncertain whether a walkthrough fits the scope exception, Claude runs the forced-picker anyway and lets the director call it. False-positive pickers are cheap (~30 seconds of director's time); false-negative skips lose the time-savings opportunity entirely.

**Concrete examples — past walkthroughs Rule 27 would have flipped to Playwright:**
- `/plos` redirect test (2026-05-13 (a.12) — would have been a 3-line Playwright test asserting `/plos` → `/projects`)
- P1V-2 silent token refresh verification (2026-05-12 — 5-second automated test instead of a >1-hour passive wait for token expiry)
- `/projects` "← Back" button check (2026-05-13 — a Playwright click + URL assertion would have caught the no-op self-loop in seconds, before the slip was flagged via Rule 10)

**Concrete examples — past walkthroughs Rule 27 leaves as manual (scope exceptions):**
- P3B-1..P3B-11 cross-device sign-in proof (2026-05-12 — device-ness is the point)
- S4-A 12-step popup paste flow (2026-05-12 — extension popup context; relative cost too high until extension testing stabilizes)
- Director's "does the new `/plos` shape feel right?" judgment (2026-05-13 — visual)

**Why this rule exists:** Director asked 2026-05-14, after P-17's Playwright ship — *"Is this used to test the code you just wrote and if so, why didn't we use this until now and if this method of testing can save me time and effort, how can I ensure we consistently use such methods moving forward?"* — and chose to codify the "consider Playwright first" reflex into a mechanical rule rather than rely on Claude remembering. Honest answer at the time was that Playwright IS a director-time saver for repeatable browser-context verifications, but the historical pattern was for Claude to default to "let me give you an N-step walkthrough" without surfacing the automated-test alternative. Rule 27 forces the alternative to surface every time the trigger conditions fire.

**Relationship to Rule 14f:** Rule 14f governs HOW multi-option questions are shaped (per-option context + recommended marker + escape hatch + free-text invitation). Rule 27 names WHEN this specific picker fires (manual walkthrough proposal moment) and what its OPTIONS are. The two compose: Rule 14f is the picker-machinery; Rule 27 is the trigger + content for one specific picker kind.

**Cross-references:** Rule 14f (the forced-picker mechanism Rule 27 uses); operational memory `feedback_playwright_for_repeatable_walkthroughs.md` (Claude-side standing operational behavior); `tests/playwright/authFetch-regression.spec.ts` (the first session-shipped Playwright suite + reference shape for future Playwright tests in the repo); `README.md` §"Running the Playwright regression tests" (how to run + the system-libs install workaround); ROADMAP P-18 (the devcontainer postCreateCommand follow-up that makes the test-runner setup zero-touch on fresh Codespaces).

### Rule 28 — Resume-flow multi-layered defense (NEW 2026-05-14)

The `./resume` one-command session-handoff design (introduced 2026-05-13-c) is enforced and survived by FOUR composed layers of defense, codified here so future sessions don't accidentally regress the design. The 2026-05-14 director-flagged bug (`exec claude "$SENTINEL"` doesn't auto-submit in interactive mode → director had to manually paste the launch prompt despite running `./resume`) revealed that a single-mechanism design is structurally fragile; multi-layered defense is the correct shape.

**The four layers (in order of activation, with redundancy guaranteed):**

| # | Layer | Mechanism | When it fires | What it catches |
|---|---|---|---|---|
| 1 | **Primary, mechanical** | `SessionStart` hook at `.claude/hooks/inject-next-session-pointer.sh` (wired in `.claude/settings.json`); reads `docs/NEXT_SESSION.md` and emits its contents as JSON `additionalContext` so Claude has the pointer-file content as a system reminder BEFORE the user's first prompt | Every Claude Code session start with `matcher: startup` (i.e., not `--resume` or `--continue`) where the workflow branch has the hook + settings file | Director sends ANY first message (even just "go" + Enter) → Claude already has the pointer-file content as context → reads it and follows the launch prompt verbatim. Director-zero-effort beyond a single wake-up keystroke. |
| 2 | **Procedural fallback** | `CLAUDE_CODE_STARTER.md` sentinel-string match rule — if the user's first message contains "Resume per docs/NEXT_SESSION.md", Claude reads the pointer and follows it | When Layer 1 didn't fire (e.g., `claude --bare` skips hooks; branch doesn't yet have the hook because the fix is mid-distribution; hook script erroneously emitted empty additionalContext) AND director happened to paste the sentinel manually | Director still has a path to one-command resume even when the hook layer is unavailable. Claude-side discipline (the rule), not harness-side enforcement. |
| 3 | **Manual escape hatch** | Documented 3-step path in every end-of-session handoff: `cd /workspaces/brand-operations-hub && git fetch origin && git checkout <branch> && git pull --rebase origin <branch>` + `claude` + paste full launch prompt | When `./resume` itself errors out OR when director prefers the manual path for any reason | Director always has a working session-start path. Multi-step but fully self-contained — doesn't depend on hooks, doesn't depend on Claude recognizing a sentinel; the pasted launch prompt IS the first user message and works in interactive mode. |
| 4 | **Procedural enforcement** | `NEXT_SESSION.md` guard hook at `.claude/hooks/check-next-session-doc.sh` (wired as a PreToolUse hook on Bash in `.claude/settings.json`); blocks any `git commit` whose message contains "End-of-session" unless `docs/NEXT_SESSION.md` is staged in that commit | Every end-of-session commit attempt | Layer 1's hook is useless if `docs/NEXT_SESSION.md` is stale or missing; Layer 4 enforces that every session refreshes the pointer file before closing. The four layers together guarantee that EITHER the pointer file is fresh AND the hook will fire AND the resume flow works, OR (if any layer fails) at least one fallback path is available. |

**Test discipline:** every time a new session ships changes to `.claude/hooks/inject-next-session-pointer.sh`, `.claude/settings.json` SessionStart entry, `./resume`, or `docs/CLAUDE_CODE_STARTER.md` sentinel section, the session-AFTER-ship is responsible for **verifying the full flow end-to-end** by running `./resume` + observing whether the SessionStart hook fired (look for the "🟢 RESUME-FLOW POINTER" marker in the session's first system reminder) + sending only a single-word wake-up message + confirming Claude reads the pointer + executes the launch prompt without manual paste. If the test fails, the next session must capture the failure to CORRECTIONS_LOG and propose a fix BEFORE doing the planned work.

**Why this rule exists:** Director's verbatim 2026-05-14 framing after hitting the original `./resume` bug: *"Why didn't the 'cd /workspaces/brand-operations-hub && ./resume' work alone and despite many redundancies in place, it looks like the methodology you applied still led to issues for starting the session off correctly with all the correct instructions given to claude at the correct time. How can you not only fix this issue but also make sure it doesn't happen again."* The director's framing has two parts: (a) fix the immediate bug (Layer 1's SessionStart hook does this); (b) make sure it doesn't happen again (Layer 2/3/4 redundancy + the test discipline above do this).

**The "type ONE thing to wake Claude up" constraint:** Claude Code's interactive-mode CLI requires SOME user input to begin a session's first response. There is no way (as of 2026-05-14) to launch `claude` in interactive mode with an auto-submitted first message — the positional `[prompt]` only auto-submits in non-interactive print mode (`-p`). This is a hard constraint of the CLI, not a script bug. After Rule 28 ships, the realistic flow is: director runs `./resume` → terminal prints pointer + launch instructions → claude launches with hook-injected pointer context → director types literally one word ("go" / "proceed" / "yes") + Enter → Claude responds by following the launch prompt verbatim. The "one-command" UX promise is honest: ONE shell command + ONE keystroke. Never ZERO keystrokes.

**Cross-references:**
- `docs/CLAUDE_CODE_STARTER.md` "Resume-flow handling" section (Layer 1 + Layer 2 user-facing description)
- `.claude/hooks/inject-next-session-pointer.sh` (Layer 1 implementation)
- `.claude/hooks/check-next-session-doc.sh` (Layer 4 implementation)
- `.claude/settings.json` (hook wiring)
- `./resume` (the entry-point shell script; Layer 1 + Layer 3 hand-off point)
- `CORRECTIONS_LOG.md` 2026-05-14 "Resume-flow design flaw" entry (the slip that triggered Rule 28)
- §4 Step 1 row 12 of this doc (the ALWAYS-update rule that enforces a fresh pointer at end-of-session)
- §4 Step 1c "No obvious next task" interview (how end-of-session pointer-writing is shaped when there's no obvious continuation)

### Rule 29 — Pre-destructive-container-operation audit (NEW 2026-05-20)

Before Claude recommends ANY operation that touches the Codespace container itself — `Codespaces: Rebuild Container`, devcontainer.json edits that would auto-trigger rebuild, container reset/delete-and-recreate, manual home-dir wipes (e.g., `rm -rf ~/.claude/`), or anything that recreates the container from scratch — Claude **MUST** run a Pre-Container-Audit BEFORE making the recommendation. This is non-negotiable; Rule 14f's "Default-to-recommendation exception" does NOT apply here (this is Rule-9-class destructive).

**Why this rule exists:** 2026-05-20 slip. Claude recommended a Codespaces rebuild walkthrough (to verify P-18's devcontainer postCreateCommand) WITHOUT first auditing what the rebuild would wipe. The rebuild succeeded at its purpose (P-18 validated zero-touch Chromium-libs reinstall) but it silently wiped the entire `/home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/` directory — 9 cross-session operational-memory files including `feedback_recommendation_style.md`, `feedback_default_to_recommendation.md`, `feedback_trust_director_setup_confirmation.md`, `feedback_playwright_for_repeatable_walkthroughs.md`, `feedback_deferred_items_registry.md`, `feedback_approval_scope_per_decision_unit.md`, `project_sequential_workflow_operation.md`, `project_scaffold_pivot_to_components_library.md`, `project_resume_script_design_scheduled.md`, and `MEMORY.md` index — plus the Claude Code CLI binary at `/usr/local/share/nvm/...` (which director had to reinstall manually). Director caught the slip post-rebuild + requested permanent protective scaffolding. Full slip + 4-layer fix narrative in `CORRECTIONS_LOG.md` 2026-05-20 entry.

**The Pre-Container-Audit (mechanical checklist):**

1. **Run the audit script:** `bash scripts/codespace-rebuild-audit.sh`. Output structured for inclusion in the Rule 14f picker. The script lists:
   - **Memory dir contents:** `ls -la /home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/` + file count + total size (CRITICAL — Claude's persistent operational memory).
   - **Globally installed npm packages:** `npm ls -g --depth=0` (CRITICAL if any non-default packages exist).
   - **Apt-installed packages beyond default image:** comparison against a known baseline (EASILY-REINSTALLED if all are in `.devcontainer/install-playwright-deps.sh` or similar; CRITICAL if any were installed ad-hoc this session).
   - **Local binaries:** `ls ~/.local/bin/` (CRITICAL if any non-default binaries exist).
   - **Shell profile edits:** `diff` against baseline `.bashrc` / `.zshrc` (CRITICAL if non-default edits exist).
   - **VS Code extensions:** the audit notes which extensions are Settings-Sync-managed (account-level; persists) vs. session-installed (lost on rebuild).

2. **Classify each item** as CRITICAL (loss is hard to recover from; no easy reinstall) vs. EASILY-REINSTALLED (script or single command brings it back).

3. **Surface the audit to the director via a Rule 14f forced-picker BEFORE running the operation,** with options shaped:
   - **(A) Back up critical items into `/workspaces/brand-operations-hub/.codespace-backup/`, then proceed (recommended per [[feedback-recommendation-style]])** — copies the memory dir + any other CRITICAL items into the persistent volume. Restorable post-operation via `bash scripts/restore-memory-from-backup.sh`.
   - **(B) Proceed without backup** — only if (a) the audit shows no CRITICAL items will be lost, OR (b) the director explicitly waives backup for a known reason. NOT recommended.
   - **(C) Cancel the operation** — defer or find an alternative path.
   - **(D) I have a question first that I need clarified** (escape hatch per Rule 14f).

4. **If (A) is picked:** execute the backup BEFORE the destructive operation. The standard backup target is `/workspaces/brand-operations-hub/.codespace-backup/<subpath>/` (which lives in the persistent volume + is gitignored). Mirror the memory dir + any other CRITICAL items the audit surfaced.

5. **Document the recovery path in `docs/NEXT_SESSION.md`** so the post-operation session knows where the backups are + how to restore. Include the exact `bash scripts/restore-memory-from-backup.sh` invocation + any other restoration steps.

**Operations that trigger this rule (the trigger list):**

- "Codespaces: Rebuild Container" (the Codespaces command palette entry)
- "Rebuild Container" via Dev Containers VS Code extension
- `devcontainer.json` edits that would trigger an automatic rebuild on save (especially `image`, `features`, `customizations` changes)
- "Reset Codespace" or "Delete Codespace + Recreate" operations
- Manual `rm -rf ~/.claude/` or any home-dir wipe
- Any operation that recreates the container from scratch
- Any operation that wipes globally installed packages (`npm uninstall -g`, `apt purge` of dev tools)

**Scope exceptions (rule does NOT fire):**

- `git push origin main` — already gated by Rule 9; doesn't touch container state
- `prisma migrate reset` — already gated by Rule 9; touches database not container
- `rm -rf node_modules/` — touches `/workspaces/` which is persistent + rebuildable via `npm install`; not container-level
- Standard `apt-get update` / `apt-get upgrade` — additive; not destructive at container level
- `wxt build` / `npm run build` / any standard build — touches `/workspaces/` artifacts only

**Composition with the protective hook layer (NEW 2026-05-20):**

Rule 29 is the procedural layer. The mechanical layer is the PostToolUse hook `.claude/hooks/backup-memory-dir.sh` which auto-mirrors every memory file write into `/workspaces/brand-operations-hub/.codespace-backup/memory/`. The detection layer is the SessionStart hook `.claude/hooks/check-memory-dir-status.sh` which alerts if the memory dir is empty/short at session start. The recovery layer is `scripts/restore-memory-from-backup.sh` which restores from the backup or prints reconstruction instructions. The four layers compose:

- Mechanical layer (PostToolUse mirror): backups happen automatically; Claude doesn't have to remember
- Procedural layer (Rule 29 + audit + picker): Claude surfaces the loss-risk BEFORE the operation
- Detection layer (SessionStart check): if loss happens despite the other layers, the next session immediately knows
- Recovery layer (restore script + reconstruction playbook): one command brings memory back from backup; falls through to manual reconstruction from this protocol doc if backup is also missing

**Recovery playbook (when memory loss has occurred — for the inheriting session):**

1. Run `bash scripts/restore-memory-from-backup.sh`. If the backup exists (under `/workspaces/brand-operations-hub/.codespace-backup/memory/`), this restores all files in seconds.
2. If the backup is ALSO missing (e.g., the backup was never created because the PostToolUse hook hadn't shipped yet, or `.codespace-backup/` itself was deleted), fall back to **reconstruction from this protocol doc**: each memory file's canonical name + operational principle + cross-reference is documented across Rules 14f / 14g / 18 / 20 / 21 / 25 / 26 / 27 / 28 / 29. The 2026-05-20 reconstruction is the canonical example.
3. As a last resort, open a GitHub Support ticket at https://support.github.com asking whether Codespaces has server-side backups of the prior container state. Response time 1-3 business days; outcome uncertain.
4. After restoration, run `ls -la /home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/` to verify the canonical files are present (current canonical list: 9 memory files + MEMORY.md index per this file's index section).

**Cross-references:**

- `scripts/codespace-rebuild-audit.sh` (the one-command audit invoked in step 1)
- `scripts/restore-memory-from-backup.sh` (the recovery script invoked in the playbook)
- `.claude/hooks/backup-memory-dir.sh` (PostToolUse mirror hook — mechanical layer)
- `.claude/hooks/check-memory-dir-status.sh` (SessionStart detection hook — detection layer)
- `.codespace-backup/README.md` (architecture documentation for the backup directory)
- `docs/CLAUDE_CODE_STARTER.md` Rule 8 augment (destructive-operations bullet that names Codespaces rebuild)
- `docs/CORRECTIONS_LOG.md` 2026-05-20 entry "Codespaces rebuild silently wiped memory dir" (the slip + 4-layer fix narrative)
- Rule 9 (destructive operations) — Rule 29 is parallel + complementary; Rule 9 covers terminal-driven destructive ops, Rule 29 covers IDE-driven container-level destructive ops

---

### Rule 30 — Session bookends: plain-language summaries at start + end (NEW 2026-05-21)

Every session MUST bookend with plain-language summaries the director can scan in under 30 seconds — at session START (what this session will do) and at session END (what was done + what next session will do + what's still left on the total roadmap).

**Director's verbatim 2026-05-21 standing directive:**

> *"From here on, for every next session, I want you to also tell me in simple terms what we will do in the session and summarize what was done in the session and what we will do in the next session. I also want you to check and tell me what work is pending according to the roadmap in simple terms."*

**Why this rule exists.** The director is a non-programmer (Rule 6). Session-boundary moments are the highest-leverage communication moments — they're when scope gets confirmed (start) and outcomes get communicated (end). Without explicit plain-terms summaries, the director has to parse Claude's technical narrative to figure out what's happening — a load Rule 6 already commits us to remove. The technical narrative is still needed (for traceability, for the next Claude session, for the corrections log), but the plain-terms summaries are needed FIRST so the director can scan + confirm before drilling in.

**What "plain terms" means operationally.**

- Avoid acronyms (P-NN, W#N, ff-merge, ping-pong, scoreboard, Rule 9, Rule 23, Rule 27, etc.) unless paired with a parenthetical paraphrase.
- Avoid file paths + commit hashes + line numbers as primary nouns (use them as parentheticals if needed for traceability).
- Use sentence-form, not bullet-form, for the narrative beats (sentences read like English; bullets read like task lists).
- Say what the director will SEE or NOT SEE at session-end (e.g., "no deploy — nothing on vklf.com changed yet" vs. "extension UI now sideloadable + you can right-click a video to test").
- Cover roadmap-level context the director needs without re-reading ROADMAP.md: which polish item we're inside, which polish items are still gating workflow graduation, what infrastructure TODOs exist offline.

**Start-of-session enforcement** is via the companion CLAUDE_CODE_STARTER.md Step 7b (NEW 2026-05-21 — "Step 7b — Plain-terms session summary" added between the drift check (Step 7) and the wait-for-go-ahead (Step 8) in the start-of-session sequence). Claude produces the plain-terms session summary BEFORE the director gives go-ahead, so director can confirm or redirect before any work starts.

**End-of-session enforcement** is via §4 Step 4b template extension (NEW 2026-05-21 — 3 new mandatory sections at the TOP of the handoff template, BEFORE the existing "Files changed this session" section):

1. **What we did this session (in plain terms)** — 1-3 sentences in sentence-form English, naming the polish item we worked on + what the director will / won't see at session-end + whether a deploy happened.
2. **What we'll do next session (in plain terms)** — 1-3 sentences in sentence-form English, naming the next session's task + what new thing the director will be able to see / click / test at next session-end.
3. **What's still left on the total roadmap (in plain terms)** — 3-8 bullets max, in plain language, spanning the active workflow's remaining polish items + future workflows (W#3-W#14 status as one-liners) + infrastructure TODOs.

The technical sections (Files changed / Push status / Deferred items / End-of-session instructions / Next-session instructions / Open questions / Recovery instructions) remain unchanged and are still required — they follow the 3 new plain-terms sections.

**Cross-references.**

- §4 Step 4b template (this doc, lines 936+) — extended with the 3 new mandatory plain-terms sections at the TOP.
- CLAUDE_CODE_STARTER.md START-OF-SESSION ROUTINE Step 7b (NEW 2026-05-21) — the start-of-session enforcement point.
- `feedback_session_bookends_plain_summary.md` (operational memory file capturing director's verbatim 2026-05-21 directive) — the canonical primary reference.
- Rule 6 (The user is a non-programmer) — the standing rule Rule 30 specializes for session-boundary moments.
- Rule 14a (The "Read It Back" test) + Rule 14b (Required structure for every user-decision question) — parallel rules that already commit to plain-language framing for decision points; Rule 30 extends that discipline to session boundaries even when there's no decision point.

---

### Rule 31 — Polish-item spec capture: verbatim director instructions are checked into the repo (NEW 2026-05-28)

Whenever the director introduces a NEW polish item with detailed instructions — OR adds substantial new scope to an existing polish item — the responsible Claude session MUST IMMEDIATELY (same session) create or extend a spec doc at `docs/polish-item-specs/<item-id>-<slug>.md` capturing the director's verbatim instructions.

**Director's verbatim 2026-05-28 standing directive (the rule's source):**

> *"I want us to make a note of this huge disparity in what I had instructed and what you developed and put something in the documentation that will prevent such a thing from happening in the future in such a way that whenever I add new items in any workflow and provide instructions, you should put my original instructions in some part of the repo so that when working on the programming at any later point, you can and will access those instructions again as well. Any adjustments we make as part of our discussions should be added to that document as well."*

**Why this rule exists.** The 2026-05-28 scope-misread incident (CORRECTIONS_LOG §Entry 2026-05-28, pending) memorializes a major divergence between what the director instructed for the W#2 P-49 W5 Session 4 "By Category-Type" surface and what Claude actually built — director's spec required TWO separate pages + flat 13-column tables + first-row-carries-label grouping + server-side AI runs + 4 AI flows + click-to-edit + drag-to-reorder + Excel export + write-back to URL detail; Claude built ONE combined page + card-style group/expand UI + browser-side AI + 1 AI flow + none of the other surfaces. Root cause: the pre-build joint-confirmation step pinned high-level shape but not concrete column lists, button labels, AI-flow counts, or execution-model semantics — director's "all good as proposed" approved an abstract proposal that diverged from the concrete spec in director's mind. The wrong-spec build commit `5fa1f53` shipped to vklf.com and was hard-reverted (revert commit `958ccf8`).

Rule 31 prevents recurrence by ensuring the director's ORIGINAL verbatim instructions live in a stable, version-controlled location in the repo — readable at any future session start (and during build sessions when context drifts) — instead of being interpreted-once and lost.

**Directory + naming convention.**

- All spec docs live in `docs/polish-item-specs/`.
- One file per polish item OR per session-bounded sub-item: `<item-id>-<slug>.md`.
- Examples: `P-49-W5-S4-category-page.md`, `P-49-W5-S5-type-page.md`, `P-51-comprehensive-analysis-ai-summary.md`.
- For multi-session arcs, the spec doc spans all sessions inside that arc (don't proliferate one file per session unless the sessions cover semantically distinct surfaces).

**Required file structure (every spec doc has these 5 sections):**

```
# <Title>

**Polish-item ID:** P-XX (cross-reference to ROADMAP entry)
**Created:** YYYY-MM-DD
**Session that captured §1:** session_YYYY-MM-DD_<slug>
**Status:** [SPEC LOCKED at §1 level | §3 IN ACTIVE DISCUSSION | SHIPPED + VERIFIED ON LIVE SITE | …]

## §1 — Original director instructions (VERBATIM, append-only)

> [Director's exact text, in blockquote, dated, with any markdown they used preserved]

(Multiple verbatim drops over time may stack here — each timestamped + tagged with the session that captured it. §1 is APPEND-ONLY — never edited or summarized.)

## §2 — Joint-discussion adjustments (append-only, chronological)

- **YYYY-MM-DD (session_…):** [decision text + who/why]
- **YYYY-MM-DD (session_…):** [next adjustment]

(Every joint-discussion adjustment between director + Claude appends here with date + session tag. §2 is APPEND-ONLY — never edited.)

## §3 — Current consolidated spec (rolled-up source-of-truth)

[The current operational spec — rolled up from §1 + §2. Code reads from THIS section at session start. §3 IS edited as agreements land — but the audit trail is preserved in §1 + §2.]

## §4 — Open questions (still under discussion)

- [questions awaiting director decision]

## §5 — Cross-references

- ROADMAP entry: P-XX
- Design doc §B entry: docs/REVIEWS_PHASE_2_DESIGN.md §B YYYY-MM-DD
- Related polish items: P-XX, P-YY
- Reverted commits (if applicable; forensic audit trail)
- Canonical code references: files touched by the implementation, sibling-pattern files to mirror
```

**The protocol for new polish items.**

1. Director surfaces new scope (in any session).
2. Claude IMMEDIATELY creates `docs/polish-item-specs/<item-id>-<slug>.md` with director's verbatim text in §1.
3. Claude updates the corresponding ROADMAP entry's "Where" or "Cross-reference" field to include the spec doc path explicitly (e.g., *"Spec doc: `docs/polish-item-specs/P-XX-slug.md`"*).
4. Every subsequent joint discussion appends to §2 with date + session tag.
5. §3 stays current as the consolidated source-of-truth.

**The protocol for build sessions on existing polish items.**

1. At start of every session that will touch a polish item with a spec doc, Claude MUST read §3 of the spec doc end-to-end as part of the start-of-session sequence (alongside the Group A doc reads). §1 + §2 read as needed to verify §3 hasn't drifted from director's intent.
2. The session's plain-terms summary at Step 7b should reference the spec doc's §3 explicitly (e.g., *"Per §3 of docs/polish-item-specs/P-XX-slug.md, this session will build …"*).
3. Pre-build joint-confirmation per `feedback_plan_output_shape_before_building.md` STILL FIRES on top of §3, but the bar is now: confirm §3 is correct + complete for this session's scope, NOT re-derive the spec from scratch.
4. Any redirects during the build session append to §2 in real-time + update §3 accordingly.

**Mechanical read-guarantee (NEW 2026-05-28-b).** The `.claude/hooks/inject-next-session-pointer.sh` SessionStart hook scans the resume-flow pointer's content for `P-NN` token references and auto-appends a "🔵 RULE 31 MANDATORY READ" block listing every matching `docs/polish-item-specs/P-NN-*.md` file path to the injected additionalContext. This closes the gap surfaced 2026-05-28-b where today's session narrowly followed a launch prompt's Category-page scope without auditing the shipped Reviews Analysis Table page on the same workstream. Test: `echo '{}' | bash .claude/hooks/inject-next-session-pointer.sh | python3 -c 'import json,sys; print(json.loads(sys.stdin.read())["hookSpecificOutput"]["additionalContext"][-2000:])'` shows the auto-detection block at the tail.

**Audit-shipped-state mandate for in-flight workstreams (NEW 2026-05-28-b).** When a session is scoped to ONE surface inside a multi-session in-flight workstream (e.g., the Category page inside P-49 W5), Claude MUST audit ALL sister surfaces on the same workstream against the master verbatim spec BEFORE finalizing this session's scope. Concretely: run a focused code-truth audit of each sister page / handler / endpoint against the master spec doc's §1 verbatim. If divergences exist, surface them to director via Rule 14f forced-picker before any code lands. The trigger today (2026-05-28-b): the launch prompt scoped today as "Category page Block 1 planning resume" but the shipped `/competitor-reviews-analysis` page on the same workstream had 12 untracked divergences from spec; today's audit caught them after director re-paste, not at session-start scoping. This mandate prevents the same miss in future sessions on any workstream that has multiple surfaces in flight simultaneously.

**Backfill for the 2026-05-28 rollback session itself.**

Three spec docs were authored as the first artifact of the corrective-rebuild work:
- `docs/polish-item-specs/P-49-W5-S4-category-page.md`
- `docs/polish-item-specs/P-49-W5-S5-type-page.md`
- `docs/polish-item-specs/P-51-comprehensive-analysis-ai-summary.md`

These docs are the canonical reference implementations of Rule 31's file structure — any new polish-item spec doc should mirror them.

**Cross-references.**

- `feedback_plan_output_shape_before_building.md` — the related rule (plan AI prompts + UI surfaces before coding). Rule 31 is the structural backstop for that rule — even if plan-output-shape gets shortchanged in a future session, the spec doc still carries the verbatim intent for recovery.
- `CORRECTIONS_LOG.md` §Entry 2026-05-28 (HIGH-importance scope-misread + plan-output-shape-before-building re-violation, pending capture this session). Rule 31 is the operational prevention mechanism for this class of incident.
- `CLAUDE_CODE_STARTER.md` Step 7b (plain-terms session summary) — the standing rule that gates session start; Rule 31's §3 reads happen at the same point.

---

## 4. END-OF-CHAT PROTOCOL

### Tooling — `plos-doc-batch` agent + `track-edited-docs.sh` hook (NEW 2026-05-19-g-3)

For the end-of-session doc-batch updates, Claude SHOULD spawn the specialized subagent `plos-doc-batch` defined at `.claude/agents/plos-doc-batch.md` via the Agent tool with `subagent_type: "plos-doc-batch"`. The agent runs in isolated context + has access to Read/Edit/Write/Bash/Grep/Glob limited to docs work. The agent reads the session-modified-docs log (auto-populated by the `track-edited-docs.sh` PostToolUse hook at `.claude/hooks/track-edited-docs.sh` — every `docs/` edit during the session gets logged to `.claude/session-modified-docs.log` with timestamp + tool name + relative path) to mechanically determine which Group A / Group B docs got touched this session — replacing the manual "I think I touched these" recall pattern.

The parent Claude still handles: the deferred-items sweep (Step 1b — driven by TaskList per Rule 26); the commit + push + ping-pong sync per `feedback_approval_scope_per_decision_unit.md`; the Personalized Handoff Message (Step 4 / 4b); the §4 Step 1c forced-picker for next-session-task selection (when no obvious continuation exists). The agent is for the doc-bundle WRITING; the parent keeps responsibility for the protocol gates.

Skip the agent if the doc-batch is trivially small (e.g., a header-amendment-only ADDENDUM with no new entries — like the 2026-05-19-c-2 corrective pattern). The agent's overhead vs. inline work flips for small updates.

### Step 1 — Run the Document Update Checklist (MANDATORY)

Claude must explicitly answer each of these questions before producing handoff files. The answers determine which docs get updated.

| # | Question | If YES, update |
|---|---|---|
| 1 | Did we create/change any routes or pages? | `NAVIGATION_MAP.md` + `PLATFORM_ARCHITECTURE.md` |
| 2 | Did we create/change any database tables, fields, or indexes? | `PLATFORM_ARCHITECTURE.md` + `DATA_CATALOG.md` |
| 3 | Did we capture any new data item anywhere in the platform? | `DATA_CATALOG.md` |
| 4 | Did we share any data between workflows (add to Shared Data Registry)? | `DATA_CATALOG.md` |
| 5 | Did any mistake occur during this chat (ours or user-caught)? | `CORRECTIONS_LOG.md` |
| 6 | Did we complete any roadmap item or identify a new one? | `ROADMAP.md` |
| 7 | Did we work on a specific tool? | That tool's `<TOOL>_ACTIVE.md` |
| 8 | Did a tool graduate (reach stable completion)? | Execute Tool Graduation Ritual (see Step 2) |
| 9 | Did we start a new tool? | Create `<NEW_TOOL>_ACTIVE.md` |
| 10 | Did we add or remove any docs to the system? | `DOCUMENT_MANIFEST.md` |
| 11 | **ALWAYS** — Update these regardless | `CHAT_REGISTRY.md`, `NEW_CHAT_PROMPT.md`, `DOCUMENT_MANIFEST.md` (timestamps) |
| 12 | **ALWAYS** — Write the pointer file so the next session can run `./resume`. Pick the next session's branch + launch prompt + pre-session notes. If today's session has a clear RECOMMENDED-NEXT item, write that verbatim. If today's session wrapped a phase / tool graduation / methodology design with no obvious continuation, run the "no obvious next task" interview in Step 1c BEFORE writing — never silently guess. **Enforced by `.claude/hooks/check-next-session-doc.sh` PreToolUse hook on Bash** (NEW 2026-05-13 via W#2 Extension build session 6), which blocks any commit whose message contains "End-of-session" unless `docs/NEXT_SESSION.md` is staged in that commit — harness-enforced, not Claude-memory-dependent. Any session that closes without this file updated is an automatic `CORRECTIONS_LOG.md` entry by the next session at start. | `docs/NEXT_SESSION.md` |
| 12a | **If today's session worked on W#1 (Keyword Clustering — graduated)** — ALSO update `docs/KEYWORD_CLUSTERING_NEXT_SESSION.md` with the next W#1 task. This is the per-workflow next-session pointer for the graduated tool, symmetric with row 12's `docs/NEXT_SESSION.md` (which tracks W#2 active dev). Read by `./resume-workflow 1` for one-command W#1 re-entry. If today's session COMPLETED a polish-backlog item, pick the next-highest-priority OPEN item from `docs/KEYWORD_CLUSTERING_POLISH_BACKLOG.md` and write it here. If multiple equally-priority items remain, fire the §4 Step 1c "No obvious next task" forced-picker. (NEW 2026-05-19-g-7 alongside `./resume-workflow` enhancement; canonical W#1 pointer file convention.) | `docs/KEYWORD_CLUSTERING_NEXT_SESSION.md` |
| 12b | **Future workflows W#3-W#14** — when a workflow first kicks off, create `docs/<WORKFLOW_NAME>_NEXT_SESSION.md` (parallel to row 12 + 12a). Update the `./resume-workflow` script's case statement to add the new workflow's branch + pointer path. Pattern established 2026-05-19-g-7. | `docs/<WORKFLOW_NAME>_NEXT_SESSION.md` (when applicable) |

Claude should state out loud: "Running end-of-chat doc update checklist..." and answer each question honestly, then proceed to updates.

### Step 1b — Deferred-items sweep (per Rule 14e)

Before producing handoff files, Claude runs a sweep of everything flagged-and-deferred during the chat. For each item, state:
- What the item is
- Where it's being captured (which doc + section)

No deferred item is allowed to leave a chat uncaptured.

### Step 1c — "No obvious next task" interview (NEW 2026-05-13-c — supports §4 Step 1 row 12)

Row 12 of the §4 Step 1 checklist requires every session to update `docs/NEXT_SESSION.md` so the next session can run `./resume` instead of the 3-step path. Most sessions have a clear "RECOMMENDED-NEXT" item (the `(a.NN)` pattern in recent ROADMAP rows) that gets copied verbatim into the pointer file.

But some sessions wrap cleanly with no obvious continuation:
- A tool just graduated (HANDOFF_PROTOCOL §4 Step 2 Scenario B).
- A phase or milestone closed.
- A methodology / design session completed (like `session_2026-05-13-c_resume-script-design`).
- The director shifts focus to a different workflow at end-of-session.

For those sessions, Claude **MUST** ask the director via a Rule 14f forced-picker before writing `docs/NEXT_SESSION.md` — never silently guess.

**Mechanical trigger:** if today's session does NOT have a written `(a.NN) RECOMMENDED-NEXT` item (or equivalent next-task pointer somewhere in this session's doc batch), the interview fires.

**Forced-picker shape (Rule 14f-compliant):**

> *"Today's session wrapped <what just shipped>; there's no obvious continuation item from this session itself. Which workflow should the next session work on? Each option below maps to a branch + a launch-prompt template per `MULTI_WORKFLOW_PROTOCOL.md` §11.*
>
> *(A) Continue platform-wide work on `main` — name a specific task (e.g., further infrastructure, components-library work, a roadmap item flagged earlier). I'll write the launch prompt verbatim into `docs/NEXT_SESSION.md`. — Branch: `main`.*
>
> *(B) Return to a graduated tool per Rule 22 (e.g., W#1 Keyword Clustering). I'll pull the canonical Resume Prompt from that tool's Data Contract (`<TOOL>_DATA_CONTRACT.md` §Resume Prompt) and copy it verbatim into `docs/NEXT_SESSION.md`. — Branch: typically `main`; check the Data Contract.*
>
> *(C) Continue an in-flight workflow on its feature branch (e.g., W#2 on `workflow-2-competition-scraping`, or future W#k for k ≥ 2 on `workflow-N-<slug>`). Tell me which workflow + the specific next task. I'll write the launch prompt verbatim into `docs/NEXT_SESSION.md`. — Branch: the workflow's feature branch.*
>
> *(D) Start a never-before-started workflow (W#3 through W#14). Tell me which one + the rough goal. I'll write a Workflow-Requirements-Interview launch prompt per Rule 18. — Branch: created on first session from `main` per MULTI_WORKFLOW_PROTOCOL.md §7.*
>
> *(E) I have a question first that I need clarified."*

Director's answer fills in the Branch + Launch prompt + Pre-session notes fields of the pointer file. The standard end-of-session doc batch + Rule 9 deploy gate then proceeds normally.

**Why this rule exists:** the director's framing 2026-05-13-c was *"The only exception is when there is nothing more left to do in a session, in that case the session should ask enough questions about the next workflow that needs to be worked on so that the docs/NEXT_SESSION.md file can be updated appropriately."* Without Step 1c, sessions that wrap cleanly would either (a) leave the pointer file stale — triggering the automatic `CORRECTIONS_LOG.md` entry — or (b) tempt Claude to silently guess what the director wants next, which Rule 14a + Rule 14d would already prohibit but Step 1c makes the asking-discipline mechanical at this specific session-boundary moment.

### Step 2 — Determine scenario and execute

**Scenario A — Work on current tool is ONGOING:**
Update docs identified in the checklist. Tailor `NEW_CHAT_PROMPT.md` with resume instructions for next chat.

**Scenario B — Current tool is COMPLETE, new tool starting next chat:**

Execute the full Tool Graduation Ritual. The deliverables stack (UPDATED 2026-04-26 to incorporate the Workflow Requirements Interview, Cross-Tool Data Flow Map, and Resume Prompt artifacts):

**Outgoing tool's graduation deliverables:**

1. Split current `<W_OUT>_ACTIVE.md` into `<W_OUT>_ARCHIVE.md` (full history; loaded only if revisiting) + `<W_OUT>_DATA_CONTRACT.md` (small, stable; <200 lines target; what downstream consumers need).

2. Add §Resume Prompt section to the Data Contract, filled in with the canonical re-entry prompt template (per Rule 22). Emit the filled prompt to the director explicitly at end of session so they can copy it directly when they next want to return to the tool.

3. Conduct Data Capture Interview with director (Doc Architecture §5) to finalize Human Reference Language for every data item the tool captured.

4. Update `DATA_CATALOG.md` — entries for outgoing tool's data items move from "PROVISIONAL" to finalized HRL + Data Contract pointer.

5. Update Cross-Tool Data Flow Map in `DATA_CATALOG.md` §7 — outgoing tool's row gets filled in with data items, R/W flags, and downstream consumers identified or "TBD."

6. Move outgoing tool's outstanding polish items into `<W_OUT>_POLISH_BACKLOG.md` (a thin sidecar — so the polish list doesn't lock the tool in active state). The Active doc is no longer needed and can be deleted (full history preserved in Archive).

7. Update `ROADMAP.md` — outgoing tool moves to ✅ COMPLETE; the polish backlog sidecar is referenced.

8. Update `DOCUMENT_MANIFEST.md` — Active doc archived; Data Contract added to Group B inventory; Resume Prompt location documented.

**Incoming tool's setup deliverables (Workflow Requirements Interview deliverables — Rule 18):**

9. Conduct Workflow Requirements Interview for incoming tool, producing `<W_IN>_DESIGN.md` with §A (initial answers — frozen) + §B (empty, append-only mid-build) per Rule 18.

10. Per Rule 21, surface any prior director directives addressed to the incoming workflow as the first item of the interview.

11. Per Rule 19, run the Platform-Truths Audit at the end of the interview — update `PLATFORM_REQUIREMENTS.md` if anything platform-level surfaced.

12. Update Cross-Tool Data Flow Map in `DATA_CATALOG.md` §7 — incoming tool's column gets prepopulated with what it'll READ from upstream tools (per the interview's Q4 upstream-data answers); incoming tool's row gets prepopulated with what it'll PRODUCE for downstream tools (per the Q5 reciprocal output declarations).

13. Update `DATA_CATALOG.md` §6.x — provisional entries for the incoming tool's expected data outputs (so future workflows have something to plan against, even before the incoming tool ships).

14. Create `<W_IN>_ACTIVE.md` (or — under the post-Phase-1α scaffold model — confirm scaffold fit per Rule 20 and create the workflow-specific extensions).

15. Update `ROADMAP.md` — incoming tool moves to 🔄 IN PROGRESS.

16. Update `DOCUMENT_MANIFEST.md` with the new DESIGN + Active docs.

A transition session is heavy. Two sessions may be warranted if the incoming tool's interview is long. Steps 1-8 (graduation) and Steps 9-16 (incoming setup) can split across two sessions if needed — graduate the outgoing tool first; conduct the incoming interview second.

### Step 3 — URL confirmation
Ask the user: **"Final step — confirm the URL I'm logging matches your current browser address bar. If you've opened this chat in a new window or the URL changed, paste the current one now. I'll write the final CHAT_REGISTRY.md entry with this URL."**

### Step 4 — Produce the Personalized Handoff Message (MANDATORY)

This is a tailored message for the user. It MUST include these sections, filled in with the specifics from THIS chat:

```
## YOUR PERSONALIZED HANDOFF

### What we accomplished this chat
[1-3 sentences]

### Files I updated (download these)
[List with brief description of what changed in each]

### Files I created new (download these)
[List if any]

### What to do with these files
1. Download all the files above
2. Upload them to your repo at [specific path or "wherever you keep the handoff docs"]
3. Verify the files are saved

### For your NEXT chat — exact instructions
1. Start a new Claude chat at claude.ai
2. Upload these SPECIFIC documents at the start:
   [Tailored list: always include all 11 Group A docs, plus specific Group B docs based on what the next task needs]
3. In the message box, paste the contents of NEW_CHAT_PROMPT.md
4. Send. Claude will then:
   - Read all docs
   - Run the Pre-Flight Drift Check
   - Ask you for the new chat's URL
   - Confirm the first task (which is: [SPECIFIC NEXT TASK])

### Open questions/decisions carried over
[Any unresolved questions from this chat]

### Safety checklist for next chat
[Any specific warnings]
```

This message is non-negotiable.

### Step 4b — Claude Code variant of the handoff (use this, not the claude.ai template above, when session happens in Claude Code)

The template above was authored for claude.ai's upload/download workflow. In Claude Code, there are no uploads or downloads — Claude edits files directly and commits to git. The handoff must reflect that, and must include explicit session-boundary instructions for a non-programmer user.

**Mandatory Claude Code handoff template:**

```
## YOUR PERSONALIZED HANDOFF

### What we did this session (in plain terms) — NEW 2026-05-21 per Rule 30
[1–3 sentences in sentence-form English. Name the polish item we worked on. Say what the director will / won't see at session-end. Say whether a deploy happened. Avoid acronyms unless paired with a parenthetical paraphrase. Avoid file paths / commit hashes as primary nouns.]

### What we'll do next session (in plain terms) — NEW 2026-05-21 per Rule 30
[1–3 sentences in sentence-form English. Name the next session's task. Say what new thing the director will be able to see / click / test at next session-end. Set expectations: deploy or no deploy.]

### What's still left on the total roadmap (in plain terms) — NEW 2026-05-21 per Rule 30
[3–8 bullets in plain language. Cover: the active workflow's remaining polish items (W#2 remaining gates) + future workflows (W#3–W#14 status as one-liners) + infrastructure TODOs / offline director steps. Director should be able to read this section alone and know the full pending picture without opening ROADMAP.md.]

### What we did this session
[2–4 sentences, plain language, no jargon — the existing technical narrative; complements the plain-terms section above with the full session story.]

### Files changed this session
[List with a one-line description of what changed in each, plus the commit hash(es)]

### Push status
[Either: "Committed locally as <hash> — NOT pushed yet, your call when ready."
 Or:     "Committed as <hash> AND pushed to origin/main — live site will redeploy automatically. Watch vklf.com for build completion."]

### Deferred items captured
[Every flagged-and-set-aside item from this session, with the specific doc + section where it's now captured. Per Rule 14e, no item leaves a session uncaptured.]

### 🚪 END-OF-SESSION INSTRUCTIONS — what you do NOW to close this session

Step-by-step, concrete. Example:

1. Claude Code is still running in your terminal. Type `exit` and press Enter to leave.
2. (Optional) Close the terminal tab — or leave it open for later; either works.
3. (Optional) If you paused vklf.com runs, nothing to do — they stopped when you cancelled.

### 🚪 NEXT-SESSION INSTRUCTIONS — what you do when you come back

Step-by-step, concrete. **You have two paths.** The EASY PATH is `./resume` (one command, reads `docs/NEXT_SESSION.md` for the right branch + launch prompt, switches branches + pulls + launches Claude with the right first-message). The ESCAPE HATCH is the original 3-step path — always documented in this handoff, always works if `./resume` ever doesn't behave. The full "how to start a session for any workflow" procedure lives in `docs/MULTI_WORKFLOW_PROTOCOL.md` §11.

**EASY PATH (recommended — added 2026-05-13-c):**

1. Open a new Codespaces terminal — OR reopen the one you left running.

2. Type this exact command and press Enter:
   ```
   cd /workspaces/brand-operations-hub && ./resume
   ```

That's it. The script reads `docs/NEXT_SESSION.md`, switches to the right branch, pulls the latest, prints the pointer file's contents so you see what's about to happen, then launches Claude Code with the sentinel first-message ("Resume per docs/NEXT_SESSION.md") that tells Claude to read the pointer file and follow the launch prompt inside it. Claude then runs the standard start-of-session sequence (branch verification, Group A doc reads, drift check) before waiting for your go-ahead.

**ESCAPE HATCH (3-step path — use this if `./resume` ever fails or you'd rather drive it manually):**

1. Open a new Codespaces terminal — OR reopen the one you left running.

2. Type this exact command and press Enter (this switches to the right branch and pulls the latest — substitute `<branch>` with the branch the next session needs per the MULTI_WORKFLOW_PROTOCOL §11 table; for W#1 or platform work it's `main`; for W#2 it's `workflow-2-competition-scraping`; etc.):
   ```
   cd /workspaces/brand-operations-hub && git fetch origin && git checkout <branch> && git pull --rebase origin <branch>
   ```

3. Then launch Claude Code:
   ```
   claude
   ```

4. Claude Code will start. As your very first message, paste this exactly (edit only the bracketed task description if you want a different task — but "[EXACT NEXT TASK]" is the default from this handoff):
   ```
   Read docs/CLAUDE_CODE_STARTER.md and follow every rule in it. Today's task: [EXACT NEXT TASK]. Verify branch state with `git branch --show-current` before any doc reads — if you're not on the expected branch ([branch name from Step 2]), STOP and surface to director. Start by running the mandatory start-of-session sequence.
   ```

5. Press Enter. Claude will read the starter, run the branch verification, read all handoff docs, check git state, produce a drift check, and wait for your go-ahead before doing any work.

### Anything you need to do offline between sessions
[Only include if applicable — e.g., "Find the V2 prompt file on your laptop," "Check Vercel env vars." Otherwise say "Nothing — you're all set."]

### 🔧 RECOVERY INSTRUCTIONS — if you find lost or accidentally deleted files later

Concrete commands by recovery type. Save this section — every handoff includes it so you always have the playbook.

1. **Tracked file (anything in `git ls-files`):**
   - Restore last committed version: `git checkout HEAD -- <path>`
   - Restore any prior version: `git log --oneline -- <path>` to find SHA, then `git checkout <SHA> -- <path>`

2. **Claude's persistent memory directory** (if SessionStart hook fires the 🚨 alert at session start):
   - One-command restore: `bash scripts/restore-memory-from-backup.sh`
   - Force mode (if dest already populated but stale): `bash scripts/restore-memory-from-backup.sh --force`

3. **A commit you reverted by accident:**
   - `git reflog` — lists every HEAD position from this Codespace
   - `git checkout <SHA>` to peek; `git reset --hard <SHA>` to fully restore (Rule 9 destructive — confirm with Claude first)

4. **"Uncommitted but I had it open" file:**
   - `git fsck --lost-found` — surfaces dangling blobs that may still hold the content
   - `git stash list` — surfaces stashed work
   - Recover a dangling blob: `git cat-file -p <SHA>` to view; redirect to a file to save

5. **Entire repo deleted from this Codespace:**
   - `cd ~ && git clone https://github.com/vkrmsngh63/brand-operations-hub`
   - GitHub remote is the off-site authoritative copy of ALL tracked code + docs

6. **Total Codespace deletion:** same as #5 from a fresh Codespace. vklf.com (Vercel) + the database (Supabase) are unaffected. Reinstall Claude Code: `npm install -g @anthropic-ai/claude-code` + run `claude` + reauthenticate via web flow.

7. **GitHub repo deletion (catastrophic; never happened):** open https://support.github.com immediately. Paid GitHub accounts recover deleted repos within 90 days.

**Layer-by-layer backup map** (always intact unless something explicitly breaks the layer):

- **GitHub remote (`origin`)** — full off-site backup of every tracked file + commit history (currently 364 tracked files).
- **Vercel** — full off-site backup of the production deploy (vklf.com); Vercel keeps deploy history + rollback via dashboard.
- **Supabase** — full off-site backup of the database; automated daily snapshots depending on plan tier.
- **`.codespace-backup/memory/`** (NEW 2026-05-20 per Rule 29) — persistent-volume backup of Claude's operational memory; auto-mirrored on every memory write by `.claude/hooks/backup-memory-dir.sh`.
- **`/workspaces/` persistent volume** — survives container rebuild but NOT total Codespace deletion. `.zip` archives + `node_modules` + everything inside `/workspaces/brand-operations-hub/` lives here.

**The one vulnerable surface:** untracked `.zip` archive files at repo root (extension build bundles). These live only in the persistent volume + aren't off-site backed up. All are reproducible from the tracked extension source via `cd extensions/competition-scraping && npm run zip`, so low priority for off-site backup.

### Open questions / carry-overs for the next session
[Any unresolved questions from this session that the next one needs to know about]
```

**Why this template is non-negotiable in Claude Code:** the user is a non-programmer. Session-boundary moments are high-confusion moments ("what do I type? which terminal? what message do I paste?"). The same mechanical rigor that Rule 14a demands mid-session — every imperative paired with a concrete method — applies here. The 🚪 sections must be **copy-paste-ready terminal commands** and **copy-paste-ready message text**, with no ambiguity.

### Step 5 — Present all updated files via `present_files`

---

## 5. Always-loaded docs (Group A — 12 files)

1. `NEW_CHAT_PROMPT.md`
2. `HANDOFF_PROTOCOL.md` (this file)
3. `PROJECT_CONTEXT.md`
4. `PLATFORM_ARCHITECTURE.md`
5. `PLATFORM_REQUIREMENTS.md` (NEW — created 2026-04-17; authoritative on platform-wide scale/user/concurrency/review/audit requirements)
6. `NAVIGATION_MAP.md`
7. `DATA_CATALOG.md`
8. `ROADMAP.md`
9. `CORRECTIONS_LOG.md`
10. `CHAT_REGISTRY.md`
11. `DOCUMENTATION_ARCHITECTURE.md`
12. `DOCUMENT_MANIFEST.md`

Plus whatever Group B (tool-specific) docs are relevant to the current work.

**Special-purpose handoff file (NEW 2026-05-13-c):** `docs/NEXT_SESSION.md` is the session-handoff pointer — written by every end-of-session per §4 Step 1 row 12, read by `./resume` at the start of the next session to find the right branch + launch prompt. It is NOT Group A (not always loaded by Claude at every session start), but it IS always written and always present in the repo. Loaded by Claude when the session's first message is the sentinel `"Resume per docs/NEXT_SESSION.md"` — at which point Claude reads it as its first action and treats its `## Launch prompt` section as if the director had pasted that launch prompt directly.

---

## 6. The ask_user_input_v0 tool

When Claude needs to elicit user preferences with multiple-choice options, use the `ask_user_input_v0` tool when available. Easier for the user than typing.

Do NOT use for free-text responses (URLs, descriptions) or simple yes/no.

When using the tool, the question text AND the option labels must still pass Rules 14a–14d. Tappable buttons don't excuse jargon — they make it worse, because the user can't ask for clarification without switching off the tool.

---

## 7. Exceptions and edge cases

### If the user says "skip the protocol"
Comply, log to `CORRECTIONS_LOG.md`, warn of the risk.

### If docs contradict each other
`DOCUMENTATION_ARCHITECTURE.md` is authoritative on meta-questions. For all else, most recently updated wins. Flag contradictions to the user.

### If the user wants to deviate from the protocol
Discuss, document WHY, update this protocol if change is permanent. Never deviate silently.

### If the manifest is out of sync
Reconcile at start-of-chat by asking user to list all docs in their repo.

---

## 8. Enforcement

The user CAN check at any point whether Claude is following this protocol. Claude should be prepared to explain, step by step, which rule it is executing. If the user notices a rule was skipped, Claude acknowledges it, explains why, and logs to `CORRECTIONS_LOG.md`.

---

## 9. Claude Code vs. claude.ai — applicability of this protocol

This protocol was originally written for claude.ai (chat-based, upload-based). After Phase M Ckpt 9, the project migrates to Claude Code (repo-integrated, direct execution). See `docs/CLAUDE_CODE_MIGRATION.md` for full migration plan.

**Core principle: the entire protocol applies to both environments.** Every rule (Rules 1–20, Rules 14a–14e) applies identically in Claude Code. The 20 rules encode accumulated lessons that don't become less relevant in a different tool.

**Mechanical differences (do not skip a rule — just adapt how it's executed):**

| Protocol step | claude.ai behavior | Claude Code behavior |
|---|---|---|
| Step 1 (read uploaded docs) | Claude reads docs uploaded to the chat | Claude reads docs from `/docs/` in the repo filesystem |
| Step 3 (Pre-Flight Drift Check) | Same — structured summary + wait for user confirmation | Same — plus run `git log` / `git status` to ground the check in repo reality |
| Step 4 (URL capture) | Claude asks user to paste browser URL | Claude captures session identifier: `session_YYYY-MM-DD_topic-slug` (Rule M4 in migration doc) |
| Step 6 (identify needed Group B docs) | Claude asks user to upload any missing ones | Claude opens them directly from `/docs/` on demand |
| Rule 5 (safety for code changes) | Claude gives commands; user runs and pastes output | Claude runs commands directly — BUT gated by Rule M1 (destructive ops need explicit confirmation) |
| End-of-chat Step 5 (present_files) | Claude generates files via `present_files` tool for user to download | Claude edits and commits files directly via git; end-of-session handoff confirms commit hash |

**Rules that become STRONGER in Claude Code, not weaker:**

- **Rule 5** (safety for code changes) — Claude Code executes commands directly, so the confirmation gate is MORE important. Migration doc §5 Rule M1 formalizes this as "STOP before destructive operations."
- **Rule 14a** (Read-It-Back test) — Pattern 11's recurrence across 4 chats shows documentation alone doesn't prevent communication slips. Claude Code should apply Rule 14a to every instruction AND every explanation given to the user.
- **Rule 10** (acknowledge mistakes) — Claude Code's direct execution means mistakes can have faster consequences. Acknowledging promptly matters more.

**Rules that become EASIER in Claude Code (but don't go away):**

- **Rule 1** (Verify Before You Write) — Claude Code can read the actual code to verify, so "ask the user" falls back to "read the file" for most questions.
- **Rule 3** (Code is source of truth) — trivially enforced since Claude reads code directly.
- **Rule 8** (Cross-Chat Data Clarification Protocol) — may become rarer since Claude Code can search the filesystem for past decisions documented in code comments or commit messages. But when ambiguity about HUMAN reference language arises, same protocol applies.

**For end-of-session doc updates in Claude Code:**
Step 1 (Document Update Checklist) runs the same 11 questions. Step 5 (present_files) is replaced by: Claude edits docs in place, stages them, commits with a clear message, and the Personalized Handoff Message confirms the commit hash. The user then has the option to push immediately or defer.

**Session management in Claude Code:**
- Each Claude Code session starts fresh (same as a new claude.ai chat). Memory persists ONLY through docs and git.
- Rule 13 (proactive context-degradation warning) applies — Claude Code can feel stretched on long sessions just like claude.ai.
- Rule 12 (mid-chat URL recapture for scope pivots) becomes "register a second session identifier" — same spirit.

**When in doubt:** fall back to claude.ai-style paste-dance for a single operation (Migration Rule M6). Rare but useful escape hatch.

---
END OF DOCUMENT
