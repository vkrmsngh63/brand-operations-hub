# CLAUDE CODE STARTER PROMPT
## Paste this (or reference it) at the start of EVERY Claude Code session

**Purpose:** Establishes the non-negotiable working rules for every Claude Code session. Read at the start of every session, before any work begins. This file is stored in the repo at `docs/CLAUDE_CODE_STARTER.md` so Claude Code can read it directly.

**Last updated:** 2026-06-03-h — Header bump only this session (`session_2026-06-03-h_w2-residue-retired-and-w1-m2-cost-forecasting` — **TWO work items: (1) W#2 (Competition Scraping) residue FULLY RETIRED (P-26 / P-27 / P-53 → ✅ CLOSED won't-do via alternate methods — W#2 is COMPLETE; docs-only close-out `e3238a4`, NO Rule 9 gate) AND (2) W#1 (Keyword Clustering) polish M-2 (Auto-Analyze cost forecasting + spend cap + out-of-credit handling) ✅ DEPLOYED-AND-VERIFIED + CLOSED on real Chrome (vklf.com, director "Pass") across TWO deploys to `main`. TWO-HUNDRED-AND-FOURTH Claude Code session — STARTED on `workflow-2-competition-scraping`, switched to `main` for M-2; ended on `main`; both branches synced at `ab24154`. EIGHTH session of 2026-06-03 (suffix `-h`); TRUST harness `currentDate` = 2026-06-03; do NOT regress `-h`. ITEM 1 (`e3238a4`, docs-only): director retired P-26/P-27/P-53 won't-do via a Rule 14f picker after a code-truth walkthrough (P-53 ABSORBED by P-55 spreadsheets; P-26 covered by the two-halves workaround + spreadsheets; P-27 sidestepped by P-45 screen-recording — bug #15 a non-fixable Chrome limit); W#2 residue now FULLY EMPTY. ITEM 2 (M-2): Deploy 1 `129cfcb` (NEW pure helper `src/lib/cost-estimator.ts` + `cost-estimator.test.ts` [+19 node:test] wired into `AutoAnalyze.tsx` — inline live cost forecast + optional spend cap that pauses + smart out-of-credit stop-and-Resume) + Deploy 2/FF1 `ab24154` (explicit "No cap" checkbox replacing the unintuitive "0 = no cap"); both director "Yes — deploy to main"; NO schema, NO new route. EXIT BASELINES: root tsc clean · ext tsc clean (not re-run) · ext test 915/915 (not re-run) · src/lib node:test 1446 (+19) · build 77 routes; Playwright SKIPPED per Rule 27. Schema-change-in-flight NO entry→NO exit (next session W#1 H-1 anticipates an additive `AuditEvent` Prisma model → YES). Closes (a.138) = W#2 residue ✅ CLOSED (W#2 complete) + W#1 M-2 ✅ DONE/CLOSED; §4 Step 1c → (a.139) = W#1 H-1 (action history table + per-action undo) — a multi-session epic (~3-5 sessions), design + the `AuditEvent` DB schema next, designed WITH the director before coding; on `main` via `./catch-up-workflow 1`. ZERO open DEFERRED at exit (Rule 26). ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-03-h (the residue retired-via-alternate-methods decision + the no-balance-API-bound-spend-with-a-cap pattern + the explicit-toggle-over-magic-sentinel UX fix). NO new memory file this session. **EIGHTY-SECOND end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-03-g`) follows for reference. — Header bump only this session (`session_2026-06-03-g_p63-phase-2-self-serve-ai-registry-and-p64-model-reorder` — **P-63 Phase 2: the SELF-SERVE central AI-model registry is now BUILT + DEPLOYED-AND-VERIFIED end-to-end across FOUR staged deploys, PLUS a Rule-32 registry-doc drift fix, PLUS a NEW roadmap item P-64 (drag-reorder models) captured AND built+shipped+verified the SAME session — SIX deploys total, ALL director PASS on real Chrome (vklf.com). TWO-HUNDRED-AND-THIRD Claude Code session on `workflow-2-competition-scraping` (both branches synced at `835543d`). SEVENTH session of 2026-06-03 (suffix `-g`); TRUST harness `currentDate` = 2026-06-03; do NOT regress `-g`. Six deploys: `759cfa3` Phase 2a (additive Prisma `AiModelRegistryEntry` + seed-on-read + CRUD API + clean `prisma db push`), `5283cce` Phase 2b (the `/ai-models` admin screen + 4-step Add-a-model wizard + integration-pending popover [D2]), `e0f42d3` Phase 2c (all 8 pickers repointed to the live `useModelsForMenu` DB-backed hook — admin edits propagate everywhere with no deploy), `d793179` Phase 2d (the W#2 run path is now registry-driven for BOTH validation + cost math — polymorphic `calculateCostUsd`/`estimateCostUsd` accept `string | ModelPricing` — closing a self-serve-model-crashes-at-cost-estimation gap), `b4c3ab1` Rule-32 drift fix (split `models.ts`/`pricing.ts` into their own §1 rows #5/#6 + reword a false-tripping comment), `835543d` P-64 (drag-reorder on `/ai-models` + `PATCH /api/ai-models` reorder via pure `resolveReorder`; no schema change). EXIT BASELINES: root+ext tsc clean · ext test 915/915 · src/lib node:test 1427 (+40) · build 77 routes (+3); Playwright SKIPPED per Rule 27. Schema-change-in-flight NO→YES (additive `AiModelRegistryEntry`)→YES→NO at the Phase-2a `prisma db push`→NO at exit. Closes (a.137) P-63 Phase 2 + NEW P-64 (same-session CLOSED); opens (a.138) = W#2 residue (P-53/P-26/P-27) — re-entry `./catch-up-workflow 2`, FIRST action = read COMPETITION_SCRAPING_PRIMER §5 residue table + present P-53/P-26/P-27. NEW FUTURE task captured: P-63 Phase 3 (OpenAI/ChatGPT + Google Gemini provider adapters), verbatim in spec §1. ZERO open DEFERRED items at exit (Rule 26). ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-03-g (the staged read-path migration + the whole-run-path-registry-driven pattern + the polymorphic-helper back-compat refactor + the drift-hook literal-substring false-positive). NO new memory file this session. **EIGHTY-FIRST end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-03-f`) follows for reference. — Header bump only this session (`session_2026-06-03-f_p63-phase-1-central-ai-registry-consumed-everywhere` — **P-63 Phase 1: the central AI-model registry is now FULLY CONSUMED platform-wide AND DEPLOYED-AND-VERIFIED across THREE clean director-PASSed deploys (the held Phase 0 from `-e` shipped with Deploy 1); plumbing only — ZERO visible change on every surface. TWO-HUNDRED-AND-SECOND Claude Code session on `workflow-2-competition-scraping` (both branches now at `8d4099e`). SIXTH session of 2026-06-03 (suffix `-f`); TRUST harness `currentDate` = 2026-06-03; do NOT regress `-f`. Three deploys `256306b`/`474ac4b`/`8d4099e`. EXIT BASELINES: root+ext tsc clean · ext test 915/915 · src/lib node:test 1387 · build 74 routes; Playwright SKIPPED per Rule 27. Schema-change-in-flight NO entry→NO exit (Phase 2 anticipates additive Prisma YES). Closes (a.136) P-63 Phase 1; opens (a.137) = P-63 Phase 2 (self-serve admin screen + DB-backed storage; confirm D1+D4 with director before coding). NEW `docs/AI_MODEL_REGISTRY_PRIMER.md` shipped.**)

**Previously updated:** 2026-06-03-e — Header bump only this session (`session_2026-06-03-e_p50-condition-pathology-card-and-p63-ai-registry-phase-0` — **TWO work items: P-50 (Condition Pathology dashboard card) ✅ DEPLOYED-AND-VERIFIED + CLOSED on real Chrome (vklf.com, director "PASS") AND P-63 (NEW — self-serve central AI-model registry) Phase 0 BUILT but HELD UNDEPLOYED (director "Hold, batch with Phase 1" + "Pause here; review first"). TWO-HUNDRED-AND-FIRST Claude Code session. FIFTH session of 2026-06-03 (suffix `-e`); FIRST `session_2026-06-03` = P-61, SECOND `-b` = W#2 graduation, THIRD `-c` = W#1 Rule-33 backfill, FOURTH `-d` = P-43 subshell cwd fix; TRUST harness `currentDate` = 2026-06-03; do NOT regress the `-e` suffix. On `workflow-2-competition-scraping`. TWO build commits (`a107e42` P-50 [parent `a51eea2`] + `6c6959c` P-63 Phase 0 [parent `a107e42`]); ONE deploy (P-50, `main` `a51eea2 → a107e42`); ONE Rule 9 deploy gate (P-50 "Yes — deploy to main"); P-63 Phase 0 committed but NOT deployed — `6c6959c` stays on `workflow-2-competition-scraping`; `main` remains at `a107e42`. The end-of-session doc-batch push is BRANCH-ONLY (NO ff-merge to main — an ff-merge would deploy the held Phase 0). P-50: a 🩺 "Condition Pathology" placeholder card added at dashboard position 3 in BOTH `WORKFLOW_DEFS` arrays (`src/app/projects/page.tsx` + `src/app/projects/[projectId]/page.tsx`); `active:false, route:null`; NO schema, NO new route, NO extension change, NO W# renumbering; NEW spec `docs/polish-item-specs/P-50-*.md`. P-63 (NEW): a single central place for AI models + company + thinking options + pricing with a self-serve add/remove UI auto-propagating to EVERY model picker (no hardcoded lists); supersedes/extends P-52 + ABSORBS both P-52 carry-overs (Opus 4.8 pricing; W#1 `AutoAnalyze.tsx` migration); designed WITH the director via a 4-Q Rule 14f picker. KEY DESIGN = TWO LAYERS — (A) data-driven presentation + (B) a provider-adapter integration layer; a `runnable`-vs-`integration-pending` status gate makes it issue-free (a model can't be runnable unless its provider's adapter is shipped — prevents the silent-fail trap); new-provider handling = save-as-`integration-pending` + an in-UI popover with the exact Claude instruction; scope = platform-wide incl. W#1. Phase 0 (`6c6959c`, PURELY ADDITIVE, nothing consumes it yet): NEW `src/lib/ai-models/` — `types.ts` + `registry.ts` (seeded FROM the existing W#2 `models.ts`/`pricing.ts`) + `provider-adapter.ts` (`AiProviderAdapter` + the shipped `anthropicAdapter` + `isProviderIntegrated`) + `registry.test.ts`/`provider-adapter.test.ts` (+15 node:test incl. the "every runnable model has an integrated provider" invariant); registered as site #4 in `docs/AI_MODEL_REGISTRY.md` §1; NEW spec `docs/polish-item-specs/P-63-*.md`. P-56 Option-2 → CLOSED as WON'T-DO (director). **Schema-change-in-flight NO at entry → STAYED NO → NO at exit** (P-63 DB storage deferred to Phase 2; Phase 0 code-only additive; P-50 static card edit). NEW baselines locked: root + extension tsc clean (UNCHANGED) + extension `npm test` = **915/915 UNCHANGED** + src/lib `node:test` = **1384/1384** (+15 — P-63 Phase 0 tests; P-50 added 0) + `npm run build` = **74 routes UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.135) (P-50 ✅ DEPLOYED-AND-VERIFIED — P-50 CLOSED). Opens (a.136) RECOMMENDED-NEXT = **P-63 Phase 1** on `workflow-2-competition-scraping` — repoint every model picker (6 W#2 modals + W#1 `AutoAnalyze.tsx`) to the central registry; route the live `messages.create` calls through the provider adapter (zero Anthropic behavior change); physically move `models.ts`/`pricing.ts` under `ai-models/` with back-compat re-export shims; reconcile W#1's thinking options; one surface at a time, scoreboard green between each; **FIRST action next session = read `docs/polish-item-specs/P-63-*.md` §6+§7, then DEPLOY the held Phase 0 commit `6c6959c` together with Phase 1's first surface.** §4 Step 1c forced-picker NOT fired (next task unambiguous — the director chose pause-and-review with Phase 1 as the clear continuation). **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty at entry; zero in-session TaskList tasks. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-03-e (the P-63 two-layer design + the `runnable`-vs-`integration-pending` issue-free invariant + the "additive Phase 0 = provably non-breaking" pattern + the P-56 Option-2 won't-do decision). NO new memory file this session. **SEVENTY-NINTH end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-03-d`) follows for reference. — Header bump only this session (`session_2026-06-03-d_w2-p43-subshell-cwd-leak-mechanical-prevention` — **W#2 residue P-43 — the recurring CWD-leak class is now STRUCTURALLY KILLED via mechanical prevention — a TOOLING-TEMPLATE-ONLY session (zero live-site impact; no schema, no deploy gate). TWO-HUNDREDTH Claude Code session. FOURTH session of 2026-06-03 (suffix `-d`); the FIRST was `session_2026-06-03` (no suffix) = P-61, the SECOND was `session_2026-06-03-b` = W#2 graduation, the THIRD was `session_2026-06-03-c` = W#1 Rule-33 backfill; TRUST harness `currentDate` = 2026-06-03; do NOT regress the `-d` suffix. On `workflow-2-competition-scraping`. The genuine residue was NOT the original P-43 file-fix (that shipped ✅ DONE-AND-VERIFIED 2026-05-22-g via `4afea35` — all three `.claude/commands/` templates were already 100% absolute-`cd`); the residue was the leak STILL RECURRING (~32-35× logged) despite the absolute-`cd` fix. Rule 3 finding: an absolute `cd /abs && cmd` is INSUFFICIENT in this persistent-shell harness — the working directory persists between Bash calls, so `cd /abs && cmd` LEAVES the session sitting in `/abs` for the next command to inherit. The fix: wrap every cd-bearing command in all three command templates (`scoreboard.md` + `deploy.md` + `ship-polish-item.md`) in a disposable sub-shell `( cd /abs && ... )` so the change is discarded on exit; add a CWD-discipline note at the top of `scoreboard.md`; refresh the stale 2026-05-22-g baselines (57 routes / 590 src/lib / 558 ext / 94 PW) to current VERIFIED values (74 / 1369 / 915 / 94-skipped). ONE build commit (`ac9c8bf`, 3 files +39/-33; parent `8dfcdc8`), NO Rule 9 deploy gate (the director authorized "commit this and close the session" — `.claude/` template-only, ZERO live-site impact), TWO Rule 14f pickers (the P-43 fix-shape picker → Recommended "sub-shell wrap + refresh baselines"; the §4 Step 1c next-pick → P-50). NEW REUSABLE PATTERN — "Absolute `cd` doesn't prevent a CWD leak in a persistent-shell harness — wrap the cd in a disposable subshell `( cd /abs && cmd )` so the change is discarded on exit; verify by running the command from an arbitrary cwd (e.g. `/tmp`) and confirming `pwd` is unchanged." **Schema-change-in-flight NO at entry → STAYED NO → NO at exit** (zero prisma work). **TOOLING-TEMPLATE-ONLY — a template change cannot move test/route counts; all counts UNCHANGED, verified THIS session from an arbitrary `/tmp` cwd (proving the subshell discipline survives cwd drift):** extension `npm test` = **915/915 UNCHANGED** + src/lib `node:test` = **1369/1369 UNCHANGED** + `npm run build` = **74 routes UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.134)'s P-43 slice (the recurrence class structurally killed by construction — ~32-35× → 0). Opens (a.135) RECOMMENDED-NEXT = **P-50** (NEW "Condition Pathology" card — small single-session user-facing UI addition; director already approved scope — the director's §4 Step 1c forced-picker choice) on `workflow-2-competition-scraping`; **FIRST action next session = read any existing `docs/polish-item-specs/P-50-*.md` (CREATE per Rule 31 if missing — it does NOT exist yet) + design the card shape WITH the director per `feedback_plan_output_shape_before_building` BEFORE coding.** **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty at entry; 3 in-session tasks created + all completed. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-03-d (the P-43-already-DONE-yet-recurring Rule 3 finding + the absolute-cd-leaks-in-persistent-shell root cause + the subshell-wrapping PATTERN + the ~32-35×→0 structural close). NO new memory file this session. **SEVENTY-EIGHTH end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-03-c`) follows for reference. — Header bump only this session (`session_2026-06-03-c_w1-keyword-clustering-rule33-backfill` — **W#1 (Keyword Clustering) was BACKFILLED under HANDOFF_PROTOCOL Rule 33 — a DOCS-ONLY session (no code, no schema, no deploy, no in-session commits). One-hundred-and-ninety-NINTH Claude Code session. THIRD session of 2026-06-03 (suffix `-c`); the FIRST was `session_2026-06-03` (no suffix) = P-61, the SECOND was `session_2026-06-03-b` = W#2 graduation; TRUST harness `currentDate` = 2026-06-03; do NOT regress the `-c` suffix. On `main` (W#1 is graduated and lives on `main`; a deliberate branch change from the prior session's `workflow-2-competition-scraping`; both branches in sync at `7fe4dbe`). W#1 graduated 2026-05-12 but predated Rule 33, so it lacked the one-page continuity primer + the one-paste catch-up command that W#2 received last session; this session closes that gap. What shipped (docs-only): (1) NEW `docs/KEYWORD_CLUSTERING_PRIMER.md` (copied `docs/templates/WORKFLOW_PRIMER_TEMPLATE.md`, mirrored the W#2 worked example `docs/COMPETITION_SCRAPING_PRIMER.md`, filled from the existing W#1 docs + a Rule 3 code-truth scan of W#1's real surfaces; every pointer verified to resolve); (2) registered `./catch-up-workflow 1` (→ `main` + the new primer; bash syntax-checked OK); (3) updated `docs/WORKFLOW_GRADUATION_CONTINUITY_DESIGN.md` (§4 W#1-backfill → ✅ RESOLVED 2026-06-03-c; §2 entry; §3 + Status line). The older Rule 22 path `./resume-workflow 1` coexists. NO Rule 14f picker fired (the recommended primer shape was obvious + default-approved per `feedback_default_to_recommendation`; the §4 Step 1c next-pick did NOT fire because the director EXPLICITLY named the next task); ZERO deploys; NO Rule 9 deploy gate. NEW reusable PATTERN — "graduated ≠ finished": a graduated workflow's continuity-primer §5 must reflect the REAL backlog state (point at the live backlog doc when one exists; don't assume residue — W#1 still has a live, non-empty polish backlog, unlike W#2's drained queue). **Schema-change-in-flight NO at entry → STAYED NO → NO at exit** (docs-only; zero `prisma db push`). **DOCS-ONLY — baselines UNCHANGED by definition (not re-run; legitimately skipped per the docs-only scoreboard exception):** extension `npm test` = **915/915 UNCHANGED** + src/lib `node:test` = **1369/1369 UNCHANGED** + `npm run build` = **74 routes UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.133) (Backfill W#1 under Rule 33 → ✅ DONE — primer + `./catch-up-workflow 1` shipped). Opens (a.134) RECOMMENDED-NEXT = **Remaining W#2 (Competition Scraping) residue items** (P-53 / P-43 / P-50 / P-26 / P-27 / the two P-52 carry-overs / P-56 Option-2 — all LOW/non-blocking) — the director EXPLICITLY chose this; **re-entry via `./catch-up-workflow 2` on branch `workflow-2-competition-scraping`**; **FIRST action next session = read the W#2 continuity primer `docs/COMPETITION_SCRAPING_PRIMER.md` §5 residue table, present the items, let the director pick.** NOTE: P-62 is a FUTURE-WORKFLOW (W#11) item, not a W#2 residue item. **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty at entry; 3 in-session tasks created + all completed. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-03-c (the W#1 backfill + the "graduated ≠ finished" primer-§5 PATTERN). NO new memory file this session. **SEVENTY-SEVENTH end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-03-b`) follows for reference. — Header bump only this session (`session_2026-06-03-b_w2-graduation-continuity-first` — **W#2 (Competition Scraping & Deep Analysis) was FORMALLY GRADUATED — continuity-first, per HANDOFF_PROTOCOL Rule 33 — a DOCS-ONLY session (no code, no schema, no deploy, no in-session commits). One-hundred-and-ninety-EIGHTH Claude Code session. SECOND session of 2026-06-03 (suffix `-b`); the FIRST was `session_2026-06-03` (no suffix) = P-61; TRUST harness `currentDate` = 2026-06-03; do NOT regress the `-b` suffix. Now that the W#2 polish queue is fully drained (P-54…P-61 all closed), the director — via two AskUserQuestion picks — chose graduation depth = continuity-first (finalize the continuity primer + `./catch-up-workflow 2`, mark W#2 ✅ GRADUATED, DEFER the §5 Step 1 Archive/Data-Contract split + the finalized-HRL Data Capture Interview until W#3 needs to read W#2 data per the §4 create-on-need rule) + residue = one consolidated non-blocking primer §5 section; verified all 14 primer pointers resolve. THREE Rule 14f pickers (graduation depth + residue + the §4 Step 1c next-pick → Backfill W#1 under Rule 33, confirmed via a disambiguation follow-up — the bare answer token "3" was ambiguous because option 1 was literally about 'W#3', so Claude confirmed BEFORE acting per `feedback_no_fabricated_instructions`); ZERO deploys; NO Rule 9 deploy gate. NEW reusable PATTERN — at workflow graduation, defer the Archive/Data-Contract split + the finalized-HRL Data Capture Interview until a downstream workflow discovers a need to read the data (DOCUMENTATION_ARCHITECTURE §4 create-on-need); the continuity PRIMER is the load-bearing re-entry artifact and is sufficient at graduation. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit** (docs-only; zero `prisma db push`). **DOCS-ONLY — baselines UNCHANGED by definition (not re-run):** extension `npm test` = **915/915 UNCHANGED** + src/lib `node:test` = **1369/1369 UNCHANGED** + `npm run build` = **74 routes UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.132) (Begin W#2 graduation → ✅ DONE). Opens (a.133) RECOMMENDED-NEXT = **Backfill W#1 (Keyword Clustering) under Rule 33** — write `docs/KEYWORD_CLUSTERING_PRIMER.md` (copy `docs/templates/WORKFLOW_PRIMER_TEMPLATE.md`) + register `./catch-up-workflow 1` so graduated W#1 gets the same one-paste re-entry kit W#2 now has; **W#1's branch is `main` — the next session runs on `main`, start command `./resume` (or `./resume-workflow 1`)** (already tracked as the "OPEN (low priority) — W#1 backfill" item in `docs/WORKFLOW_GRADUATION_CONTINUITY_DESIGN.md` §4); **FIRST action next session = read HANDOFF_PROTOCOL Rule 33 + WORKFLOW_GRADUATION_CONTINUITY_DESIGN §4 + the W#1 docs, then copy the primer template.** **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty at entry; 4 in-session tasks created + all completed. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-03-b (W#2 graduated continuity-first + the defer-the-Data-Contract-until-downstream-need PATTERN + the disambiguated-bare-token process-good note). NO new memory file this session. **SEVENTY-SIXTH end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-03`) follows for reference. — Header bump only this session (`session_2026-06-03_p61-extension-default-categories` — **W#2 polish P-61 — server-side DEFAULT categories per platform per content-type ✅ DEPLOYED-AND-VERIFIED on real Chrome via `workflow-2-competition-scraping` → `main` (director verbatim "Pass") — P-61 is now CLOSED, the LAST substantive W#2 polish item, so W#2 graduation is now fully clear. EXTENSION + PLOS-server change; NEW additive Prisma model `CategoryDefault`; ONE NEW PLOS route. One-hundred-and-ninety-SEVENTH Claude Code session. DATE-BOUNDARY: this session OPENED on 2026-06-02 (the 10th session of that day, in-flight `-j`) and the harness `currentDate` rolled to 2026-06-03 MID-DEPLOY; STAMPED 2026-06-03 (the FIRST session of 2026-06-03, NO suffix) per trust-the-harness; in-flight artifacts (the P-61 spec §2 + the `fdedaa5` commit message) carry the earlier `-j` stamp — expected/harmless. TWO build commits (`fdedaa5` the P-61 feature + `60f9455` the P-58 served-artifact refresh), ONE deploy, ONE Rule 9 deploy gate (director "Deploy + run db push") + SIX Rule 14f pickers total (FOUR design — Q1 sharing = per-Project / Q2 show-defaults = "★ Defaults" optgroup [director OVERRIDE] / Q3 inline ★ + checkbox-on-add / Q4 contextual "★ Make default" checkbox — + the deploy gate + the §4 Step 1c next-pick → W#2 graduation); `main` went `8e71cda → fdedaa5 → 60f9455`. **FIRST action = a Rule 3 code-truth audit (Explore agent)** — categories live in ONE project-scoped `VocabularyEntry` table (no platform, no default flag); per-(platform, content-type) defaults are genuinely NEW. **The P-61 spec ALREADY EXISTED — READ, not re-created (Rule 31)** — §2 was updated with the audit + the 4-picker design. NEW reusable PATTERN — when two design picks conflict against a platform constraint (a "★ Defaults" optgroup in a NATIVE `<select>` + a requested per-row star toggle, which native `<option>` rows can't host), surface the tension and reconcile WITH the director via one focused follow-up picker. **What shipped (`fdedaa5`):** the NEW additive Prisma model `CategoryDefault` (keyed `projectId + platform + vocabularyType + value`, shipped to prod via `prisma db push`) + the NEW route `competition-scraping/category-defaults` (GET/POST/DELETE; route 73 → 74) + NEW shared types + the NEW pure helper `category-defaults.ts` (+6 node:test) + the extension api-client/api-bridge/3 background handlers/messaging plumbing + the NEW shared content-script helper `category-defaults-picker.ts` wired into the text / image / video capture forms. **Schema-change-in-flight NO at entry → flipped YES (the `CategoryDefault` model) → flipped YES→NO at the deploy `prisma db push` → NO at exit.** NEW baselines locked: extension `npm test` = **915/915 UNCHANGED** + src/lib `node:test` = **1369/1369** (+6) + `npm run build` = **74 routes** (+1); Check 6 SKIPPED per Rule 27. Fresh sideload zip `plos-extension-2026-06-02-w2-p61-default-categories.zip` (219 KB), director real-Chrome verified. Closes (a.131) (P-61 ✅ DEPLOYED-AND-VERIFIED — P-61 CLOSED). Opens (a.132) RECOMMENDED-NEXT = **Begin W#2 graduation (Rule 33)** (write the graduation/handoff materials, run `./catch-up-workflow 2`, formally mark W#2 essentially complete now that the polish queue is drained — the director's §4 Step 1c forced-picker choice) on `workflow-2-competition-scraping`; **FIRST action next session = read HANDOFF_PROTOCOL Rule 33 + the graduation methodology.** **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-03 (the DATE-BOUNDARY crossing + the reconcile-conflicting-picks PATTERN + the clean additive `prisma db push`). NO new memory file this session. **SEVENTY-FIFTH end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-02-i`) follows for reference. — Header bump only this session (`session_2026-06-02-i_p59-detailed-user-guide-update` — **W#2 polish P-59 — the in-app "Detailed User Guide" is now brought CURRENT with ALL Competition Scraping functionality (PLOS-side + extension) ✅ DEPLOYED-AND-VERIFIED on vklf.com via `workflow-2-competition-scraping` → `main` (director verbatim "PASS") — P-59 is now CLOSED. PLOS-side content-only change to ONE existing component (`DetailedUserGuide.tsx`); NO extension SOURCE change; NO schema change; NO new route. One-hundred-and-ninety-SIXTH Claude Code session. NINTH session of 2026-06-02 (suffix `-i`); prior REAL sessions today = `session_2026-06-02` (no suffix) + `-b` + `-c` + `-d` + `-e` + `-f` + `-g` + `-h`; TRUST harness `currentDate` = 2026-06-02; do NOT regress the `-i` suffix. ONE build commit (`3741078`), ONE deploy, ONE Rule 9 deploy gate (director "Deploy to main") + FIVE Rule 14f pickers total (THREE plan-shape — Q1 scope = Comprehensive / Q2 AI-flow depth = Plain / Q3 = gray screenshot placeholders, all recommended — + the deploy gate + the §4 Step 1c next-pick → P-61); `main` went `ba31ec3 → 3741078`. **FIRST action = a Rule 3 code-truth feature-inventory (Explore agent)** that diffed the existing guide against every shipped surface — it found the guide covered only extension install + basic capture and was silent on ~70% of the shipped PLOS-side surface, AND caught a stale image context-menu label ("Save image to PLOS — Competition Scraping" → the code-true "Add to PLOS — Image"). **FIRST artifact = CREATING the P-59 spec** per Rule 31 — it did not exist before. NEW reusable PATTERN — for a docs/content surface, run a code-truth feature-inventory (Explore) and diff it against the current doc to enumerate gaps + stale labels before writing, then plan the output shape WITH the director per `feedback_plan_output_shape_before_building`. **What shipped (`3741078`):** a comprehensive NEW "Part 3 — On the PLOS website (vklf.com)" (the 5-tab nav, the Competitor Content Table tools, the competitor detail page, capture & manage reviews, the three Reviews Analysis tables + AI summaries at PLAIN depth, the Comprehensive Analysis page) + the Part 2 extension gaps filled (NEW "Capture a video" + "Capture a competitor's reviews") + the stale-label correction + an intro rewrite to the 3-part shape; gray screenshot placeholders. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit.** NEW baselines locked = same as entry (all UNCHANGED): extension `npm test` = **915/915 UNCHANGED** + src/lib `node:test` = **1363/1363 UNCHANGED** + `npm run build` = **73 routes UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.130) (P-59 ✅ DEPLOYED-AND-VERIFIED — P-59 CLOSED). Opens (a.131) RECOMMENDED-NEXT = **P-61** (extension server-side default categories per platform per content-type; MEDIUM — the spec `docs/polish-item-specs/P-61-extension-default-categories.md` ALREADY EXISTS — the director's §4 Step 1c forced-picker choice) on `workflow-2-competition-scraping`; **FIRST action next session = read the existing P-61 spec + confirm the Schema-change-in-flight flag (may flip YES) before any prisma work.** This is the LAST substantive W#2 polish item — after it, W#2 graduation is fully clear. **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-02-i (the feature-inventory found the guide silent on ~70% of the shipped surface + the stale label + the inventory-diff-then-plan-shape PATTERN). NO new memory file this session. **SEVENTY-FOURTH end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-02-h`) follows for reference. — Header bump only this session (`session_2026-06-02-h_p60-open-detail-icon-analysis-tables` — **W#2 polish P-60 — the open-detail ↗ icon is now on the Product Name column of all THREE reviews-analysis tables (the per-competitor `competitor-reviews-analysis` page, the `reviews-analysis-by-category` page, and the `reviews-analysis-by-type` page), mirroring the ↗ open-detail link on the main Competitor URLs table ✅ DEPLOYED-AND-VERIFIED on vklf.com via `workflow-2-competition-scraping` → `main` (director verbatim "PASS") — P-60 is now CLOSED. PLOS-side UI-only change; NO extension SOURCE change; NO schema change; NO new route. One-hundred-and-ninety-FIFTH Claude Code session. EIGHTH session of 2026-06-02 (suffix `-h`); prior REAL sessions today = `session_2026-06-02` (no suffix) + `-b` + `-c` + `-d` + `-e` + `-f` + `-g`; TRUST harness `currentDate` = 2026-06-02; do NOT regress the `-h` suffix. ONE build commit (`e08684a`), ONE deploy, ONE Rule 9 deploy gate (director "Deploy to main") + TWO Rule 14f pickers total (the deploy gate + the §4 Step 1c next-pick → P-59); NO design picker fired (verbatim unambiguous directive — the default-to-recommendation exception applied); `main` went `2e9c0c5 → e08684a`. **FIRST action = a Rule 3 code-truth audit (Explore agent)** of the four table surfaces — it CONFIRMED the premise with one clarification: the by-category/by-type pages DID already have a ↗, but it is the `data-testid="*-source-review-jump"` anchor in the AI comprehensive-summary BANNER cell (jumps to `…/url/${urlId}#review-${reviewId}`), NOT on the Product Name cell — so all three Product Name cells genuinely lacked an open-detail ↗. **FIRST artifact = CREATING the P-60 spec** per Rule 31 — it did not exist before. NEW reusable PATTERN — mirror an existing cross-page navigation affordance by matching the LOCAL anchor convention of the target pages (an `<a href>` already used for source-jump) rather than importing the source page's button+router mechanism. **What shipped (`e08684a`):** each Product Name cell wraps its existing `InlineTextCell` (still click-to-edit) in a `display:flex` container with a trailing `<a>` ↗ anchor → `/projects/[projectId]/competition-scraping/url/[urlId]`, styled `color:#58a6ff fontSize:13px`; `projectId` threaded into `renderUrlRowCell` + `SortableCategoryRow`/`SortableTypeRow` → `renderDataCell`. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit.** NEW baselines locked = same as entry (all UNCHANGED): extension `npm test` = **915/915 UNCHANGED** + src/lib `node:test` = **1363/1363 UNCHANGED** + `npm run build` = **73 routes UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.129) (P-60 ✅ DEPLOYED-AND-VERIFIED — P-60 CLOSED). Opens (a.130) RECOMMENDED-NEXT = **P-59** (update the existing `DetailedUserGuide.tsx`; LOW/MEDIUM — the director's §4 Step 1c forced-picker choice) on `workflow-2-competition-scraping`; **FIRST action next session = read `docs/polish-item-specs/P-59-*.md` if it exists (CREATE per Rule 31 if missing — it may NOT exist yet).** **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-02-h (the Rule 3 audit clarified a ROADMAP premise + the local-anchor-convention PATTERN + the default-to-recommendation exception skipped a design picker). NO new memory file this session. **SEVENTY-THIRD end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-02-g`) follows for reference. — Header bump only this session (`session_2026-06-02-g_p57-delete-coverage-gaps` — **W#2 polish P-57 — the delete-coverage gaps on the competitor URL detail page are FILLED: (A) a per-row delete control for captured VIDEOS + (B) category-label deletion (content / image / video categories) with a project-wide "delete items too" cascade fired from a ✕-on-the-category-pill control ✅ DEPLOYED-AND-VERIFIED on vklf.com via `workflow-2-competition-scraping` → `main` (director: deploy 1 "I was able to delete videos" + deploy 2/FF1 "pass") — P-57 is now CLOSED. PLOS-side change; NO extension SOURCE change; NO schema change; ONE NEW PLOS route. One-hundred-and-ninety-FOURTH Claude Code session. SEVENTH session of 2026-06-02 (suffix `-g`); prior REAL sessions today = `session_2026-06-02` (no suffix) + `-b` + `-c` + `-d` + `-e` + `-f`; TRUST harness `currentDate` = 2026-06-02; do NOT regress the `-g` suffix. TWO build commits (`9c4b548` deploy 1 + `418e6ca` deploy 2/FF1), TWO deploys, TWO Rule 9 deploy gates (both "Deploy to main") + FIVE Rule 14f pickers (Q1 category-delete behavior [director OVERRIDE → Option C "delete items too"] + Q2 placement [first "picker trash", re-picked "✕ on pill" OVERRIDE] + deploy gate ×2 + §4 Step 1c next-pick); `main` went `a6081da → 9c4b548 → 418e6ca`. **FIRST action = a Rule 3 code-truth audit (Explore agent) of all SEVEN item types' delete coverage** — found reviews / captured text / captured images ALREADY deletable, leaving TWO real gaps (captured-video delete UI + category-label deletion). **FIRST artifact = CREATING the P-57 spec** per Rule 31 — it did not exist before. **Deploy 2/FF1 was an audit-shipped-state correction** — the deploy-1 picker-trash was undiscoverable because the `VocabularyPicker` is NOT shown inline on the detail page; the director re-picked "✕ on each category pill." **What shipped:** a NEW pure helper `src/lib/competition-scraping/category-vocabulary.ts` (+10 node:test) + a NEW route `competition-scraping/categories` (GET usage count + DELETE cascade; routes 72 → 73) + a reusable `CategoryPill` component. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit.** NEW baselines locked: extension `npm test` = **915/915 UNCHANGED** + src/lib `node:test` = **1363/1363** + `npm run build` = **73 routes**; Check 6 SKIPPED per Rule 27. Closes (a.128) (P-57 ✅ DEPLOYED-AND-VERIFIED — P-57 CLOSED). Opens (a.129) RECOMMENDED-NEXT = **P-60** (add the open-detail ↗ icon to the 3 analysis tables' Product Name column; LOW — the director's §4 Step 1c forced-picker choice) on `workflow-2-competition-scraping`; **FIRST action next session = read `docs/polish-item-specs/P-60-*.md` if it exists (CREATE per Rule 31 if missing).** **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-02-g (the audit-shipped-state correction + the count-bearing-confirm PATTERN). NO new memory file this session. **SEVENTY-SECOND end-of-session run under the Rule 30 + §4 Step 4b template.**) — Prior header (`session_2026-06-02-f`) follows for reference. — Header bump only this session (`session_2026-06-02-f_p58-download-extension-zip-latest-build` — **W#2 polish P-58 — the in-app "Download Extension (zip)" button on the Competition Scraping workflow page now serves the LATEST built extension ✅ DEPLOYED-AND-VERIFIED on vklf.com via `workflow-2-competition-scraping` → `main` (director verbatim "pass") — P-58 is now CLOSED. PLOS-side change (a static `public/` artifact + a deploy-step wiring); NO extension SOURCE change; NO schema change; NO new route. One-hundred-and-ninety-THIRD Claude Code session. SIXTH session of 2026-06-02 (suffix `-f`); prior REAL sessions today = `session_2026-06-02` (no suffix) + `-b` + `-c` + `-d` + `-e`; TRUST harness `currentDate` = 2026-06-02; do NOT regress the `-f` suffix. ONE build (`3dc47fb`), ONE deploy, ONE Rule 9 deploy gate (director "Deploy to main") + THREE Rule 14f pickers (Q1 design-fork + deploy gate + §4 Step 1c next-pick), all Recommended/Yes; `main` went `ca45eae → 3dc47fb`. **FIRST artifact = CREATING the P-58 spec** per Rule 31 — it did not exist before. **Rule 3 code-truth audit caught an audit-shipped-state correction** — the in-app download was a DEAD PLACEHOLDER (`url="#download-extension-pending"`) downloading NOTHING (the capture had assumed a "stale committed artifact"; it was never wired). **What shipped (director-picked Option A "Deploy-time committed file"):** the freshly-built extension zip is committed at `public/competition-scraping/plos-extension-latest.zip`; the button URL → `/competition-scraping/plos-extension-latest.zip`; `/deploy` Step 8 refreshes + commits that served artifact at every extension deploy so the in-app download always matches the deployed build. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit.** NEW baselines locked: extension `npm test` = **915/915 UNCHANGED** + src/lib `node:test` = **1353/1353 UNCHANGED** + `npm run build` = **72 routes UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.127) (P-58 ✅ DEPLOYED-AND-VERIFIED — P-58 CLOSED). Opens (a.128) RECOMMENDED-NEXT = **P-57** (fill the delete-coverage gaps — reviews / videos / category-labels; MEDIUM — the director's §4 Step 1c forced-picker choice) on `workflow-2-competition-scraping`; **FIRST action next session = read `docs/polish-item-specs/P-57-*.md` if it exists (CREATE per Rule 31 if missing) + design WITH the director if non-trivial.** **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-02-f (the audit-shipped-state correction + the served-`public/`-artifact PATTERN). NO new memory file this session. **SEVENTY-FIRST end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-06-02-e — Header bump only this session (`session_2026-06-02-e_p56-amazon-highlight-flicker-pause-while-selecting` — **W#2 polish P-56 — the Amazon "Highlight Terms" flicker that was BLOCKING text selection for capture is FIXED ✅ DEPLOYED-AND-VERIFIED on real Amazon (real-Chrome) via `workflow-2-competition-scraping` → `main` (director verbatim "pass") — P-56 is now CLOSED. EXTENSION-ONLY; NO vklf.com user-facing change; NO schema change; NO new route. One-hundred-and-ninety-SECOND Claude Code session. FIFTH session of 2026-06-02 (suffix `-e`); prior REAL sessions today = `session_2026-06-02` (no suffix) + `session_2026-06-02-b` + `session_2026-06-02-c` + `session_2026-06-02-d`; TRUST harness `currentDate` = 2026-06-02; do NOT regress the `-e` suffix. ONE build (`802224f`), ONE deploy, ONE Rule 9 deploy gate (director "Deploy to main") + THREE Rule 14f pickers (fix-shape + deploy gate + §4 Step 1c next-pick), all Recommended/Yes; `main` went `71645bc → 802224f`. **FIRST action = the deferred P-20 real-Amazon diagnostic trace** (`docs/p-20-trace-script.js` on a real Amazon PDP, product B07V57NDNC): ~137,358 chars added (~4,576 chars/sec), ~14 would-be refresh() rescans (~one every 2s) — which DISPROVED the assumed root cause (the P-20 fingerprint short-circuit works as designed; Amazon legitimately adds matchable text ~every 2s, so a correct strip-and-reapply fires mid-drag + collapses the selection). **Fix:** `highlight-terms.ts` `refresh()` defers the strip-and-reapply while `window.getSelection()` is a non-collapsed/non-empty text selection; a `'selectionchange'` listener re-runs it once the selection clears; NEW pure helper `isActiveTextSelection` (+5 extension tests). Fresh sideload zip `plos-extension-2026-06-02-w2-p56-amazon-flicker-1.zip` (218 KB), director real-Amazon verified. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit.** NEW baselines locked: extension `npm test` = **915/915** (+5) + src/lib `node:test` = **1353/1353 UNCHANGED** + `npm run build` = **72 routes UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.126) (P-56 ✅ DEPLOYED-AND-VERIFIED — P-56 CLOSED). Opens (a.127) RECOMMENDED-NEXT = **P-58** (Download-Extension-zip serves the LATEST build; MEDIUM — the director's §4 Step 1c forced-picker choice) on `workflow-2-competition-scraping`; **FIRST action next session = read `docs/polish-item-specs/P-58-*.md` if it exists (CREATE per Rule 31 if missing — it may not exist yet) + design WITH the director if non-trivial.** **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-02-e (the trace-first methodology disproved the assumed root cause + a NEW defer-while-selecting PATTERN + a real-site console-trace handoff-clarity note). NO new memory file this session. **SEVENTIETH end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-06-02-d — Header bump this session (`session_2026-06-02-d_p55-phase-3-primer-wiring-editable-primer-and-graduation-methodology` — **W#2 polish P-55 Phase 3 — the teaching primer WIRED into `/comprehensive-analysis` (a "Competitive Analysis Primer" Word `.docx` download + an "↡ Insert primer" editor button) AND made EDITABLE + SAVABLE (an "✎ Edit" rich-text modal with Save + Reset-to-default) ✅ DEPLOYED-AND-VERIFIED on vklf.com via `workflow-2-competition-scraping` → `main` (director real-Chrome: "pass all five" + "pass") — P-55 is now ESSENTIALLY COMPLETE — plus the Workflow Graduation Continuity methodology (NEW HANDOFF_PROTOCOL Rule 33 + `./catch-up-workflow <N>`) + a P-56..P-62 roadmap-capture batch. One-hundred-and-ninety-first Claude Code session. FOURTH session of 2026-06-02 (suffix `-d`); prior REAL sessions today = `session_2026-06-02` (no suffix) + `session_2026-06-02-b` + `session_2026-06-02-c`; TRUST harness `currentDate` = 2026-06-02. TWO deploys (`894563b` primer wiring + `ef4a47c` editable primer), TWO Rule 9 deploy gates (both Yes; the pt3 deploy included a `prisma db push` for the additive nullable `ComprehensiveCompetitorAnalysis.primerJson Json?`) + three docs/tooling pushes to workflow-2 (`a0339e8` + `e167952` + `59edc2d`); `main` went `7ead4dc → 894563b → ef4a47c`. NEW client dependency `docx ^9.7.1`; NEW endpoint `.../comprehensive-analysis/primer` (route count 71 → 72). **Schema-change-in-flight NO at entry → flipped YES (pt3 `primerJson`) → flipped YES→NO at the pt3 deploy push → NO at exit.** NEW baselines locked: src/lib `node:test` = **1353/1353** (+18) + `npm run build` = **72 routes** (+1) + extension = **910/910 UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.125) (P-55 Phase 3 part 2 + part 3 ✅ DEPLOYED-AND-VERIFIED — P-55 ESSENTIALLY COMPLETE). Opens (a.126) RECOMMENDED-NEXT = **P-56** (HIGH — Amazon highlight-terms flicker blocks text selection; resumes the deferred P-20 real-Amazon verification) on `workflow-2-competition-scraping`; **FIRST action next session = re-run `docs/p-20-trace-script.js` on a real Amazon PDP, design the fix WITH the director before coding, fresh extension zip + real-Amazon verification.** **ZERO open DEFERRED items at exit (Rule 26)** — TaskList empty. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-02-d. NO new memory file this session. **SIXTY-NINTH end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-06-02-c — Header bump only this session (`session_2026-06-02-c_p55-without-individual-reviews-export-variants` — **W#2 polish P-55 CONTINUED — THREE NEW "without individual reviews" download buttons added to the "Comprehensive Competitive Analysis Files" box on `/comprehensive-analysis` (the three reviews spreadsheets, trimmed of every per-individual-review column) ✅ DEPLOYED-AND-VERIFIED on vklf.com via `workflow-2-competition-scraping` → `main` (director real-Chrome verdict: "pass") + the four-file Comprehensive Analysis Files export verification DEFERRED at 2026-06-02-b now CLOSED (all four PASS). One-hundred-and-ninetieth Claude Code session. THIRD session of 2026-06-02 (suffix `-c`); prior REAL sessions today = `session_2026-06-02` (no suffix) + `session_2026-06-02-b`; TRUST harness `currentDate` = 2026-06-02. ONE build (`8420739`), ONE deploy, ONE Rule 9 deploy gate (director Yes) + ONE pre-coding AskUserQuestion design picker (flat-file trim depth → "Drop ALL review detail (recommended)"); `main` went `d7c8894 → 8420739`. The box now lists SEVEN files (original four UNCHANGED + three trimmed variants); "Download all (.zip)" bundles all seven. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit.** NEW baselines locked: src/lib `node:test` = **1335/1335** (+5) + `npm run build` = **71 routes UNCHANGED** + extension = **910/910 UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.124) carry-forward 1 (director verification of the four exports: all four PASS). Opens (a.125) RECOMMENDED-NEXT = **P-55 Phase 3 part 2** (the primer .docx + "Insert primer" button + Files-box entry; content generator + approved wording already committed on main inert at `dfa4af1`) on `workflow-2-competition-scraping`; **FIRST action next session = confirm whether `.docx` generation needs a new library (e.g. `docx`) + flag it at that build.** **ZERO open DEFERRED items at exit (Rule 26)** — 4 in-session tasks all completed. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-02-c (a RECURRENCE of the P-43 cwd-leak during the pre-deploy scoreboard; caught + re-run from absolute cd; P-43 tally ~32-35+). NO new memory file this session (covered by `feedback_exports_include_all_table_data`). **SIXTY-EIGHTH end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-06-02-b — Header bump only this session (`session_2026-06-02-b_p55-phase-2b-ii-grouped-spreadsheets-audit-and-primer-content` — **W#2 polish P-55 CONTINUED — Phase 2b-ii (the two grouped "Reviews Analysis By Competitor Category" + "By Competitor Type" spreadsheets) ✅ DEPLOYED on vklf.com via `workflow-2-competition-scraping` → `main` + a full four-file export audit + the Phase 3 primer CONTENT generator built + director-approved (HELD BACK, inert/unwired at `dfa4af1`). One-hundred-and-eighty-ninth Claude Code session. SECOND session of 2026-06-02 (suffix `-b`). FIVE builds (`e705f10` / `fd63d45` / `0718711` / `1be0f62` / `41481f0`), FIVE deploys, FIVE Rule 9 deploy gates (all Yes); `main` went `b34a8b8 → e705f10 → fd63d45 → 0718711 → 1be0f62 → 41481f0`. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit.** NEW baselines locked: src/lib `node:test` = **1330/1330** (+13) + `npm run build` = **71 routes UNCHANGED** + extension = **910/910 UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.123) PARTIALLY (Phase 2b-ii ✅ DEPLOYED; director verification of the four exports DEFERRED). Opens (a.124) RECOMMENDED-NEXT = **P-55 Phase 3 part 2** (the primer .docx + "Insert primer" button + Files-box entry) on `workflow-2-competition-scraping`; **FIRST action next session = VERIFY the deferred four-file exports on vklf.com.** **TWO open DEFERRED carry-forwards at exit (Rule 26).** NEW memory file `feedback_exports_include_all_table_data.md`. **SIXTY-SEVENTH end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-06-02 — Header bump only this session (`session_2026-06-02_p55-comprehensive-analysis-files-and-main-table-additions` — **W#2 NEW polish item P-55 — `/comprehensive-analysis` downloadable materials (Excel exports of the four competition tables + a teaching "primer") + main-table "Overall Competitor Analysis" column + Columns-box ordering fixes — a requirements-gathering opener that became a multi-phase BUILD + DEPLOY session, SIX deploys ALL ✅ DEPLOYED end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` (Phases 1 + 2a director-VERIFIED "pass"; **Phase 2b-i + its FF1 DEPLOYED but NOT YET director-verified — next session's FIRST item is to verify 2b-i**). One-hundred-and-eighty-eighth Claude Code session. **DATE-BOUNDARY NOTE: this session OPENED on 2026-06-01 (continuing the `2026-06-01-d` pointer) + the harness `currentDate` rolled to 2026-06-02 mid-session; stamped 2026-06-02; TRUST the harness `currentDate`.** BUILD + DEPLOY session: SIX builds (`4ed4b63` / `8f7db25` / `6952817` / `05a33b6` / `19c21d8` / `e263be5`), SIX deploys, SIX Rule 9 deploy gates (all Yes) + pre-coding + mid-build AskUserQuestion design pickers; `main` went `396a9ed → 4ed4b63 → 8f7db25 → 6952817 → 05a33b6 → 19c21d8 → e263be5` (six clean ff-merges); both branches at `e263be5`. **The director gave a CONCRETE new direction (P-55) that SUPERSEDES the old P-51 skeleton** — P-55 PREPARES downloadable materials so the director runs the AI themselves, instead of an in-app "AI summarize" button. NEW spec doc `docs/polish-item-specs/P-55-comprehensive-analysis-files-and-main-table-additions.md`. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit** (zero `prisma db push`; everything additive client-side or reused existing routes). NEW baselines locked: src/lib `node:test` = **1317/1317** (+26) + `npm run build` = **71 routes UNCHANGED** (no new route — downloads client-side) + extension = **910/910 UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.122) PARTIALLY (P-55 Phases 1 + 2a + 2b-i shipped). Opens (a.123) RECOMMENDED-NEXT = **P-55 Phase 2b-ii then Phase 3** on `workflow-2-competition-scraping`; **FIRST action next session = VERIFY Phase 2b-i** on vklf.com. **ZERO open DEFERRED items at exit (Rule 26)** — all 7 session tasks completed; Phase 2b-ii + Phase 3 are the documented roadmap continuation, NOT TaskList defers. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-02 (the stated-behavior-must-match-implementation observation + 4 NEW Patterns + the date-boundary crossing). **SIXTY-SIXTH end-of-session run under the Rule 30 + §4 Step 4b template.**

**Previously updated:** 2026-06-01-d — Header bump only this session (`session_2026-06-01-d_p54-competition-scraping-phase-5-dynamic-category-columns` — **W#2 polish P-54 Phase 5 — the dynamic content/image/video category columns on the MAIN `/competition-scraping` Competitor URLs table (R7 / R8 / R9 per D3/D4/D5/D7/D8/D9) ✅ DEPLOYED-AND-VERIFIED 2026-06-01-d end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 verbatim verdict: "pass". **P-54 is now CLOSED — Phase 5 was the LAST piece (all 5 phases shipped).** One-hundred-and-eighty-seventh Claude Code session. **DATE-STAMP NOTE: FOURTH session of 2026-06-01 (suffix `-d`); the prior REAL sessions today are `2026-06-01` (P-49 W5 Type page) + `2026-06-01-b` (P-54 Phases 1–3) + `2026-06-01-c` (P-54 Phase 4, build `87d8efa`); TRUST the harness `currentDate`.** BUILD + DEPLOY session: ONE build (`6363300`), ONE deploy, ONE Rule 9 deploy gate (director "Yes — deploy to main") + ONE 3-question design picker (2 design Recommended + 1 process override) BEFORE coding per `feedback_plan_output_shape_before_building`; `main` went `b5298c1 → 6363300` (clean ff-merge); both branches at `6363300`. For each captured content/image/video category, the main table grows a locked column-PAIR — the captured/embedded text + its glued "[category] Analysis" column; multi-item cells stack as aligned rowSpan sub-rows (D3); a "Content / Image / Video Categories" group of checkboxes shows/hides each kind (D7); cells edit IN-table — plain text inline, "Your Analysis" via a click-to-open pop-out editor reusing `PerItemAnalysisBox` (Q-I) — writing back through the EXISTING per-item PATCH routes (D5/D9); refetch-on-return (D4); pairs move together under reorder (D8) and align in BOTH the flat AND the Phase-4 grouped render paths. NEW pure helper `src/lib/competition-scraping/dynamic-columns.ts` (+ `.test.ts`; +21 node:test); additive optional `MainTableCaptures` / `MainTableCapturedItem` shared-types + `CompetitorUrl.captures?`; opt-in `?withCaptures=1` include on the existing GET `/urls`; `UrlTable.tsx` physical-column model + `ColumnVisibilityBar.tsx` 3 group checkboxes + `CompetitionScrapingViewer.tsx` captures fetch. NEW reusable PATTERN — "Locked column-pairs with NO schema change: store ONLY the value key in the shared `columnOrder`; glue the analysis column at render; the existing `moveColumnKey` drags the pair as one for free" + the opt-in `?withCaptures=1` include kept routes at 71. **Schema-change-in-flight NO at entry → STAYED NO → NO at exit** (no `prisma db push`; the prior handoff's predicted schema change was AVOIDED — the dynamic column keys ride in the existing `ProjectTablePreferences` Json maps). NEW baselines locked: src/lib `node:test` = **1291/1291** (+21) + `npm run build` = **71 routes UNCHANGED** (opt-in `?withCaptures=1` + existing per-item PATCH routes) + extension = **910/910 UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.121) RECOMMENDED-NEXT (P-54 Phase 5 ✅ DEPLOYED-AND-VERIFIED 2026-06-01-d). Opens (a.122) RECOMMENDED-NEXT = **the deferred (a.119) `/comprehensive-analysis` AI-summary requirements-gathering** on `workflow-2-competition-scraping` (ASK-DIRECTOR-FIRST; reconcile with the P-51 skeleton). **ZERO open DEFERRED items at exit (Rule 26)** — the 6 session tasks all completed; the (a.119) defer is now the next pick, NOT a TaskList DEFERRED item. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-01-d (the locked-column-pairs-no-schema-change PATTERN + the post-deploy stale-bundle diagnosis). **SIXTY-FIFTH end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-06-01-c — Header bump only this session (`session_2026-06-01-c_p54-competition-scraping-main-table-phase-4-sort-by-grouping` — **W#2 polish P-54 Phase 4 — the "Sort By" grouping box on the MAIN `/competition-scraping` Competitor URLs table (R6 / D2) ✅ DEPLOYED-AND-VERIFIED 2026-06-01-c end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 verbatim verdict: "pass". One-hundred-and-eighty-sixth Claude Code session. **DATE-STAMP NOTE: THIRD session of 2026-06-01 (suffix `-c`); the prior REAL sessions today are `2026-06-01` (P-49 W5 Type page) + `2026-06-01-b` (P-54 Phases 1–3, build `7a10ba4`); TRUST the harness `currentDate`.** BUILD + DEPLOY session: ONE build (`87d8efa`), ONE deploy, ONE Rule 9 deploy gate (director "Yes — deploy + db push") + ONE Q-H 3-question design picker (all 3 Recommended chosen) BEFORE coding per `feedback_plan_output_shape_before_building`; `main` went `95f8fd9 → 87d8efa` (clean ff-merge); both branches at `87d8efa`. A NEW "Sort By" box (beside Platforms / Columns in `ColumnVisibilityBar`) groups the main table's rows by Platform / Category / Type into banner-row groups (mirroring the By-Category / By-Type pages); "None" (flat) stays the default; within a group both per-column click-sort and row-drag work; group banners are draggable (two-level drag); the Phase-3 column reorder stays independent; the empty bucket renders "(Uncategorized)" / "(Untyped)" pinned last. NEW pure helper `src/lib/competition-scraping/main-table-grouping.ts` (`buildMainGroupedRows` re-buckets ALREADY-SORTED rows — grouping is a re-bucketing LAYER, not a re-sort — + `groupKeyOf` / `groupLabelOf` / `reorderableGroupKeys` / `coerceGroupOrderMap`; +21 node:test); additive `ProjectTablePreferences.groupBy String @default("none")` + `groupOrder Json @default("{}")` shipped to prod via `prisma db push` (1.28s, zero data loss); `UrlTable.tsx` grouped render REUSES `applyCategoryDrag` / `applyCompetitorDrag` from `category-table-layout` (generic pure string-array ops); the flat path UNCHANGED (Phases 1–3 untouched). NEW reusable PATTERN — "Grouping as a re-bucketing LAYER over already-sorted rows; the two-level-drag re-rank helpers generalize across pages because they are pure string-array ops". Schema-change-in-flight NO at entry → YES (Phase 4) → FLIPPED YES→NO at the deploy push. NEW baselines locked: src/lib `node:test` = **1270/1270** (+21) + `npm run build` = **71 routes UNCHANGED** (reuses the existing `project-table-preferences` endpoint) + extension = **910/910 UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.120) RECOMMENDED-NEXT (P-54 Phase 4 ✅ DEPLOYED-AND-VERIFIED 2026-06-01-c). Opens (a.121) RECOMMENDED-NEXT = **P-54 Phase 5 — the dynamic text/image/video category columns** (R7/R8/R9; D3–D9; LIKELY a schema change; resolve Q-F + Q-I first) on `workflow-2-competition-scraping`; (a.119) `/comprehensive-analysis` remains DEFERRED/queued behind P-54 (NOT closed). **ZERO open DEFERRED items at exit (Rule 26)** — Tasks #1–#5 all completed; the (a.119) defer is a SESSION-LEVEL reprioritization from 2026-06-01-b, NOT a TaskList DEFERRED item. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-01-c (the Q-H clean resolution + the NEW re-bucketing-layer PATTERN). **SIXTY-FOURTH end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-06-01-b — Header bump only this session (`session_2026-06-01-b_p54-competition-scraping-main-table-enhancements` — **W#2 NEW polish item P-54 — `/competition-scraping` MAIN-PAGE (Competitor URLs table) enhancements — a requirements-gathering opener that became a 3-phase BUILD + DEPLOY session, FIVE deploys ALL ✅ DEPLOYED-AND-VERIFIED 2026-06-01-b end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 PASS on all five. One-hundred-and-eighty-fifth Claude Code session. **DATE-STAMP NOTE: SECOND session of 2026-06-01 (suffix `-b`); the prior REAL session is `2026-06-01` (the P-49 W5 Type page, build `f23df1b`).** The session was scheduled to open (a.119) but the DIRECTOR DEFERRED (a.119) at session start (verbatim: "I want to work on something different today and defer this step for the session after the requirements of the features mentioned today are finished") and directed a NEW batch of 9 main-page enhancements (R1–R9), captured VERBATIM in a NEW spec doc `docs/polish-item-specs/P-54-competition-scraping-main-table-enhancements.md` per `feedback_no_fabricated_instructions` + `feedback_plan_output_shape_before_building`; requirements-gathering ran via multiple AskUserQuestion rounds (all director-answered). **(a.119) remains DEFERRED/queued — NOT closed.** BUILD + DEPLOY session: 5 builds, 5 deploys, 5 Rule 9 deploy gates (all Yes); `main` went `69da40c → b134b5d → e0661ba → b27841f → 1f18308 → 7a10ba4`; both branches at `7a10ba4`. Phase 1 (`b134b5d`) = R1 last-column right-edge resize + R3 remove the redundant blue "Comprehensive Competitor Analysis" button. Phase 2 (`e0661ba` + FF1 `b27841f` + FF2 `1f18308`) = R2 + R4 whole-page window-scroll both axes + header sticky-to-window (D1 REVISED — whole-page scrollbars over the earlier floating bar), FF1 = shrink-wrap the table wrapper (`width:max-content`) + 48px trailing gap, FF2 = grey panel full table width + `MAX_COLUMN_WIDTH` 600→4000 + auto-scroll-during-resize (document-space width `[clientX+scrollX]` + RAF `window.scrollBy`). Phase 3 (`7a10ba4`) = R5 drag-reorder columns by the header (a ⠿ grip per header; horizontal `SortableContext` sharing the ONE `DndContext` with the vertical row-reorder list, id-discriminated) + share the WHOLE main-table layout across the Project via a NEW additive `ProjectTablePreferences` model (`projectId @unique`, NEW `columnOrder Json`) + NEW handler `project-table-preferences.ts` (SEED-ON-READ from `UserTablePreferences`) + NEW route + NEW pure helper `column-order.ts` (`applyColumnOrder` + `moveColumnKey`, +11 node:test); the per-user `UserTablePreferences` is untouched. FOUR NEW reusable PATTERNS — shrink-wrap-table-wrapper page-scroll + auto-scroll-window-during-resize + two-sortable-axes-one-DndContext + seed-on-read per-user→per-Project migration. Schema-change-in-flight NO at entry → YES (Phase 3) → FLIPPED YES→NO at the Phase 3 deploy push (`prisma db push` 1.27s additive, zero data loss). NEW baselines locked: src/lib `node:test` = **1249/1249** (+11) + `npm run build` = **71 routes** (+1, the new `project-table-preferences` endpoint) + extension = **910/910 UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes NONE (a.119 was DEFERRED by the director, not closed). Opens (a.120) RECOMMENDED-NEXT = **P-54 Phase 4 — the "Sort By" grouping box** (group rows by Platform / Category / Type as banner-row groups; D2; resolve Q-H first) on `workflow-2-competition-scraping`; then Phase 5 = the text/image/video dynamic category columns (R7/R8/R9; D3–D9; likely a schema change). **ZERO open DEFERRED items at exit (Rule 26)** — Tasks #1–#5 all completed; the (a.119) defer is a SESSION-LEVEL reprioritization captured in ROADMAP + the P-51 spec, NOT a TaskList DEFERRED item. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-01-b (the (a.119) defer + the 4 NEW Patterns). **SIXTY-THIRD end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-06-01 — Header bump only this session (`session_2026-06-01_p49-w5-type-page-sessions-4-5` — **W#2 polish P-49 W5 Type page Sessions 4-5 — the entire "Reviews Analysis By Competitor Type Table" page (`/competition-scraping/reviews-analysis-by-type`) built in ONE all-in-one session + ✅ DEPLOYED-AND-VERIFIED 2026-06-01 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 PASS (both per-Type AI flows verified, incl. the non-bulleted button). **P-49 W5 is now CLOSED** (the Category page closed 2026-05-30-d; the Type page was its last piece). One-hundred-and-eighty-fourth Claude Code session. BUILD + DEPLOY session: build `f23df1b`; `main` went `cb00f53 → f23df1b` (clean ff); both branches at `f23df1b`. ONE Rule 9 deploy gate (the fully-informed director "Yes — commit, db push, deploy" picker; no redundant re-fire). DATE-BOUNDARY: started 2026-05-31, harness rolled to 2026-06-01 mid-build; stamped 2026-06-01. A SEPARATE fabricated/reverted session earlier on 2026-05-31 is recorded ONLY as the CORRECTIONS_LOG §Entry 2026-05-31 TOP-TIER SLIP (NOT a normal prior session). The Type page is a LEAN mirror of the closed Category page: grouping key `competitionCategory` → `type`, an `(Untyped)` fallback bucket, and the intentional Type↔Category column SWAP (Type = col 1, Category = col 3). Born with EVERYTHING: the 13-column grouped table + show/hide + click-to-edit + per-review stacked Stars/Reviews Summary + the interactive batch (type banner rows + two-level @dnd-kit drag + hide-with-restore) + the per-page layout memory (NEW additive nullable `UserTablePreferences.typeTableLayout Json?` column; `prisma db push` to prod) + BOTH per-Type AI flows (bulleted dedup + non-bulleted prose) + the Source Reviews column + all 4 of the 2026-05-30-d director adjustments. LEAN architecture (KEY pattern): rather than duplicating ~450 lines, REUSED the grouping-agnostic helpers (`category-analysis-aggregation`, `buildCategorySourceReviewRows`, the per-category prompt parsers) + GENERALIZED the `review-analysis-run-batch` per-category branch (parameterized by grouping `level` PER_TYPE + the type-worded `PER_TYPE_BULLETED/NONBULLETED` prompts + a `typeKey` group-key alias) — the per-category path stays byte-identical. NEW reusable PATTERN — "Mirror a sibling page by REUSING grouping-agnostic helpers + GENERALIZING a shared handler branch instead of duplicating it". Schema-change-in-flight YES at entry → FLIPPED YES→NO at the deploy push. NEW baselines locked: src/lib `node:test` = **1238/1238** (+56) + `npm run build` = **70 routes** (+1, the new `reviews-analysis-by-type` route) + extension = **910/910 UNCHANGED**; Check 6 SKIPPED per Rule 27. Closes (a.118) RECOMMENDED-NEXT (P-49 W5 Type page Sessions 4-5 ✅ DEPLOYED-AND-VERIFIED 2026-06-01; **P-49 W5 CLOSED**). Opens (a.119) RECOMMENDED-NEXT = **Comprehensive Analysis page (`/competition-scraping/comprehensive-analysis`) AI-summarizing functionality** on `workflow-2-competition-scraping` (DIRECTOR DIRECTIVE: next session MUST START by asking the director for `/comprehensive-analysis` AI-summary requirements — Q&A FIRST, NOT coding-first, per `feedback_plan_output_shape_before_building`). **ZERO open DEFERRED items at exit (Rule 26)** — Task #6 (Excel "Export Table" for both Category + Type pages — never built on Category either) DEFERRED → NEW ROADMAP polish-backlog entry P-53, written this doc-batch. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-01 (the mirror-by-reuse-+-generalize PATTERN + the Rule 3 nav/schema/Excel findings + the date-boundary crossing + a cwd-leak near-miss). **SIXTY-SECOND end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-05-30-d — Header bump only this session (`session_2026-05-30-d` — **W#2 polish P-49 W5 Category page Session 2 FINISH — ✅ DEPLOYED-AND-VERIFIED 2026-05-30-d end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 PASS on BOTH deploys; the whole Category Session 2 feature shipped as one unit. TWO Rule 9 deploy gates, both Yes; `main` went `bdec02e → e2030e8 → c641b3d`. Deploy 1 (`e2030e8`) = the Source Reviews column rendering, deployed TOGETHER with the previously-held-back Category Session 2 backend+frontend (`d1659d7` + `fb772ad`) + last session's doc-batch (`fbd318f`) — so the two per-category AI summaries (bulleted dedup + non-bulleted prose) + the NEW "Source Reviews" column all went live as one unit: NEW 14th column `catSourceReviews` (read-only, categoryLevel) in `category-table-columns.ts` + NEW client-safe pure helper `buildCategorySourceReviewRows(categories, reviewsById)` in `reviews-traceability.ts` (resolves each category bullet's cross-competitor union of reviewIds → product + stars + merged title/body + urlId; +3 node:test) + a per-review anchor/scrollMarginTop/scroll-on-hash jump link in `UrlDetailContent.tsx`. Deploy 2 (`c641b3d`) = 4 director Phase-4 adjustments: top-align the banner category name; removed the per-category non-bulleted prose write-back into competitor notes boxes (the non-bulleted COLUMN keeps its prose); NEW shared `components/HoverTooltip.tsx` (portal+fade + `autoHideMs`) on the two AI buttons; Source Reviews layout restructured to a multi-`<tr>` banner block so each bulleted complaint is its own sub-row aligned with its source reviews. NEW reusable PATTERN — "Per-row alignment across two adjacent table columns via a multi-`<tr>` banner block with rowSpan'd flanking cells". One-hundred-and-eighty-third Claude Code session. DEPLOY session: 2 deploys. ZERO non-deploy Rule 14f forced-pickers (the Source Reviews shape was locked 2026-05-30-c; built under the default-to-recommendation path); running cumulative 151/155 = 97.4%. Schema-change-in-flight NO entire session (entry NO → exit NO; the column reads already-persisted PER_CATEGORY data; the write-back removal is a deletion). NEW baselines locked: src/lib `node:test` = **1182/1182** + `npm run build` = **69 routes** + extension = **910/910 UNCHANGED**. Closes (a.117) RECOMMENDED-NEXT (P-49 W5 Category page Session 2 FINISH ✅ DEPLOYED-AND-VERIFIED 2026-05-30-d; the Category page is now essentially CLOSED). Opens (a.118) RECOMMENDED-NEXT = **P-49 W5 Type page Sessions 4-5** on `workflow-2-competition-scraping` (the `Reviews Analysis By Competitor Type Table` inherits ALL Category behaviors + the Source Reviews column + the 4 adjustments, substituting type/`CompetitorUrl.type` for category, with its own `typeTableLayout` memory column; Schema-change-in-flight likely YES at entry — confirm at that session's start). **ZERO open DEFERRED items at exit (Rule 26)** — Task #10 (mirror the 4 adjustments onto the Type page) was DEFERRED in-session and its destination spec entries were WRITTEN in this doc-batch. **SIXTY-FIRST end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-05-30-c — Header bump only this session (`session_2026-05-30-c` — **W#2 polish P-49 W5 — a MIXED session: (1) an FU-3 bug fix ✅ DEPLOYED-AND-VERIFIED 2026-05-30-c end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 verbatim verdict: "PASS, table is back" — the "Overall Analysis — Captured Reviews" bullet↔reviews traceability table on the URL detail page no longer disappears after the non-bulleted prose summary runs (NEW pure helper `selectBulletedAnalysisRow(rows, urlId)` selects the structured bulleted PER_PRODUCT row by `analysisJson.flow` rather than by recency; single build `bdec02e` under ONE Rule 9 deploy gate; NEW reusable PATTERN — "A second same-level row discriminated only by `analysisJson.flow` can silently shadow the structured row a reader needs — select by flow, not recency"); (2) a NEW FEATURE designed + approved — the "Source Reviews" column for the Category page (each Category Comprehensive bulleted bullet shows every individual source review across all in-category competitors that traced up to it; TWO Rule 14f decisions LOCKED — bullet-by-bullet always-visible layout + category-bullets-only scope; the SAME feature is a director directive for the Type page at Sessions 4-5, DEFERRED); (3) Category page Session 2 BUILD COMMITTED on `workflow-2-competition-scraping` (backend `d1659d7` + frontend `fb772ad`) but NOT DEPLOYED this session (deploy queued next session together with the Source Reviews column). One-hundred-and-eighty-second Claude Code session. MIXED session: 1 deploy + 2 undeployed build commits; `main` at `bdec02e`, `workflow-2-competition-scraping` 2 commits ahead. SIX Rule 14f decisions — 5 Recommended + 1 override (continue-vs-pause; director "keep going"); running cumulative 149/153 = 97.4%. Schema-change-in-flight NO entire session (entry NO → exit NO). NEW baselines locked: src/lib `node:test` = **1179/1179** + `npm run build` = **69 routes** + extension = **910/910 UNCHANGED**. Closes (a.116) RECOMMENDED-NEXT PARTIALLY (the two Category AI flows + the Source Reviews backend BUILT, undeployed; the FU-3 bug fixed & DEPLOYED). Opens (a.117) RECOMMENDED-NEXT = **P-49 W5 Category page Session 2 FINISH** on `workflow-2-competition-scraping` (build the Source Reviews column rendering against the already-produced backend data, THEN deploy the whole Category Session 2 feature together; the category code is already committed on workflow-2 ahead of main — next session continues on top, does NOT re-do it; Schema-change-in-flight NO at entry). **TWO open DEFERRED carry-forwards at exit (Rule 26).** **SIXTIETH end-of-session run under the Rule 30 + §4 Step 4b template.**)

**Previously updated:** 2026-05-30-b — Header bump only this session (`session_2026-05-30-b_p49-w5-category-page-interactive-batch` — **W#2 polish P-49 W5 Category page "interactive batch" made the Reviews Analysis By Competitor Category Table fully interactive — banner-row layout restructure + two-level @dnd-kit drag-to-reorder + hide-with-restore + a NEW per-user/per-Project page-layout "memory" area — TWO deploys, BOTH ✅ DEPLOYED-AND-VERIFIED 2026-05-30-b end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 verbatim verdict: "All passed." The SECOND of the 5-session Category + Type corrective rebuild. TWO builds `5f07f24` (interactive batch: banner-row restructure + two-level @dnd-kit drag in ONE DndContext via prefixed `cat:<key>` ids + per-category nested SortableContexts + hide-with-restore + a NEW additive nullable `UserTablePreferences.categoryTableLayout` Json column threaded through the shared-types wire shape + the existing `/table-preferences` handler — REUSING the endpoint, NO new route; `prisma db push` 1.29s additive zero data loss) + `469e5c6` (FF1: `MeasuringStrategy.Always` + explicit vertical `autoScroll` to fix category drag below the fold). TWO NEW reusable PATTERNS — "Two-level nested @dnd-kit drag in ONE DndContext via prefixed sortable ids + per-group SortableContexts" + "Off-screen drop targets in an `overflowX:hidden` scroll container need `MeasuringStrategy.Always` + explicit `autoScroll`". Schema-change-in-flight YES at entry → FLIPPED YES→NO at the deploy push `5f07f24`; exit NO.**) — BUILD + DEPLOY session: TWO builds, TWO deploys, TWO Rule 9 deploy gates. One-hundred-and-eighty-first Claude Code session. Closes (a.115) RECOMMENDED-NEXT (P-49 W5 Category page "interactive batch" ✅ DEPLOYED-AND-VERIFIED 2026-05-30-b). Opens (a.116) RECOMMENDED-NEXT = **P-49 W5 Category page Session 2 — the two Category AI flows** on `workflow-2-competition-scraping` (Schema-change-in-flight NO at entry; reuses the existing `ReviewAnalysis` PER_CATEGORY storage; then Sessions 4-5 = the Type page inheriting ALL Category behaviors with its own `typeTableLayout` column). 4/4 = 100% Yes-to-Recommended pickers; running cumulative 144/147 = 98.0%. NEW baselines locked: src/lib `node:test` = **1156/1156** + `npm run build` = **69 routes** + extension = **910/910 UNCHANGED**. **ZERO DEFERRED items at entry and exit (Rule 26).** **FIFTY-NINTH end-of-session run under the Rule 30 + §4 Step 4b template.**

**Previously updated:** 2026-05-30 — Header bump only this session (`session_2026-05-30_p49-w5-category-page-session-1-scaffold-and-polish` — **W#2 polish P-49 W5 Category page (Reviews Analysis By Competitor Category Table) Session 1 scaffold + a 5-item look-and-feel polish pass + 3 follow-up fixes + a vertical-scrollbar-overlap fix — FOUR deploys, ALL ✅ DEPLOYED-AND-VERIFIED 2026-05-30 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 verbatim verdicts: all "passed." The FIRST of the 5-session Category + Type corrective rebuild. FOUR builds `f08a41f` (scaffold: NEW route `reviews-analysis-by-category` + a flat 13-column grouped table + per-review stacked Stars / Reviews Summary via real `rowSpan` sub-rows + column show/hide + drag-to-resize + NEW pure helpers `category-table-columns.ts` + `category-table-grouping.ts` +29 node:test + nav tab) / `ee56398` (5-item polish) / `90bfdf5` (3 fixes incl. the `hasLoadedPrefs` load-before-save gate) / `9b1d023` (`scrollbar-gutter: stable` + 48px trailing scroll space). All persistence reused the existing `UserTablePreferences` model (`categoryTable:` prefix) — NO schema change. TWO NEW reusable PATTERNS — "rowSpan sub-rows to align per-item data across two adjacent columns" + "Debounced merge-save must gate on initial-load-complete". Schema-change-in-flight NO entry → NO exit (NEXT session = YES at entry).**) — BUILD + DEPLOY session: FOUR builds, FOUR deploys (ONE explicit Rule 9 deploy picker + 3 in-session director directives). One-hundred-and-eightieth Claude Code session. Closes (a.114) RECOMMENDED-NEXT (Category page Block 1 planning resume RESOLVED + Session 1 scaffold + polish + fixes all deployed & verified). Opens (a.115) RECOMMENDED-NEXT = **P-49 W5 Category page "interactive batch"** on `workflow-2-competition-scraping` (drag whole categories + competitors-within-a-category + the header-row layout restructure + hide-with-restore scoped to this page only, backed by a NEW additive per-user/per-Project memory area → Schema-change-in-flight = YES at entry; then Session 2 = the two Category AI flows; the Type page inherits ALL Category behaviors). 7/7 = 100% Yes-to-Recommended pickers; running cumulative 140/143 = 97.9%. NEW baselines locked: src/lib `node:test` = **1130/1130** + `npm run build` = **69 routes** + extension = **910/910 UNCHANGED**. **ZERO DEFERRED items at entry and exit (Rule 26).** **FIFTY-EIGHTH end-of-session run under the Rule 30 + §4 Step 4b template.**

**Previously updated:** 2026-05-29-d — Header bump only this session (`session_2026-05-29-d_p49-w5-fix-session-c-deploy-2-drag-to-reorder` — **W#2 polish P-49 W5 Reviews Analysis Table Fix Session C "Deploy 2" — drag-to-reorder ✅ DEPLOYED-AND-VERIFIED 2026-05-29-d end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 verbatim verdict "Passed." This is the LAST remaining Reviews Analysis Table work — the page is now fully §1-compliant / CLOSED. Two @dnd-kit drag surfaces (both grip-handle per director Rule 14f pick, NOT whole-row grab since every cell is click-to-edit), mirroring `UrlTable.tsx`: (1) URL (competitor) rows drag to reorder with child review rows riding along, persisted via the SHARED per-user `UserTablePreferences.rowOrder` array [the SAME field the Competitor Content Table uses → order tracks across both pages; NO schema change]; (2) review rows within a URL drag to reorder, persisted via the already-shipped `CapturedReview.sortRankInReviewsTable` column [the existing `reviews-reorder.ts` handler gained a `field` discriminator — NO new route]. Single build `194e66f` (11 files +743/-29). CRITICAL Rule 3 finding: the spec-named `CompetitorUrl.sortRank` URL-row persistence column NEVER existed (the Content Table actually uses `UserTablePreferences.rowOrder`); director chose to keep "reflects everywhere" via that shared field; INFORMATIONAL. TWO NEW reusable PATTERNS — "Reorder-field discriminator on a shared reorder endpoint" + "Spec-named storage column may not exist — audit before building". Schema-change-in-flight NO entry → NO exit.**) — DEPLOY session: ONE Rule 9 deploy gate fired on the single build `194e66f`. One-hundred-and-seventy-ninth Claude Code session. Closes (a.113) RECOMMENDED-NEXT (Fix Session C "Deploy 2" drag-to-reorder ✅ DEPLOYED-AND-VERIFIED; Reviews Analysis Table page fully §1-compliant / CLOSED). Opens (a.114) RECOMMENDED-NEXT = **P-49 W5 Category page corrective rebuild — Block 1 planning resume** on `workflow-2-competition-scraping` (answer the 6 open questions in `docs/polish-item-specs/P-49-W5-S4-category-page.md` §4 before any code; Schema-change-in-flight expected NO at entry; planning/build session). 3/3 = 100% Yes-to-Recommended pickers; running cumulative 133/136 = 97.8%. NEW baselines locked: src/lib `node:test` = **1101/1101** + `npm run build` = **68 routes UNCHANGED** + extension = **910/910 UNCHANGED**. **ZERO DEFERRED items at entry and exit (Rule 26).** **FIFTY-SEVENTH end-of-session run under the Rule 30 + §4 Step 4b template.**

**Previously updated:** 2026-05-29-c — Header bump only this session (`session_2026-05-29-c_p49-w5-fix-session-c-deploy-1-nonbulleted-prose-excel-tooltips` — **W#2 polish P-49 W5 Reviews Analysis Table Fix Session C "Deploy 1" ✅ DEPLOYED-AND-VERIFIED 2026-05-29-c end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 verbatim verdict "Everything passed." "Deploy 1" = the NEW per-competitor non-bulleted (prose) AI flow [a "second-pass transform" reading the already-generated BULLETED summary (Column 9), NOT the raw review corpus, and rewriting it into theme-labeled plain-paragraph critique prose; new top + per-URL "Auto-create … (non-bulleted)" buttons + 2 NEW modals (`GlobalCompetitorNonBulletedModal.tsx` skips+flags competitors with no bullet summary; `PerCompetitorNonBulletedModal.tsx`) + Column 10 cell + banner + edit-in-place + refresh-persistence via `analysisJson.flow='per-competitor-nonbulleted'` discriminator + append-merge write-back] + the "Export Table" Excel download [xlsx; visible columns only; word-wrap on AI cells; filename `competitor-reviews-analysis-{slug}-{YYYY-MM-DD}.xlsx`] + the additive nullable `CapturedReview.sortRankInReviewsTable Int?` schema column [`prisma db push`, zero data loss; consumed by the deferred Deploy 2 drag-to-reorder]. 3 fix-forwards added fade-in hover tooltips to all six page buttons (FF1 `ecad5af` + FF2 `9da50ec` + FF3 `0631762` body-portal + viewport-fixed positioning). Deploy 1 build `a24e92c` (10 files). Q8 resolved directly = `per-competitor-nonbulleted`. Schema-change-in-flight YES entry → FLIPPED YES → NO at the Deploy 1 push; exit NO. THREE NEW reusable PATTERNS — "Body-portal + viewport-fixed positioning for tooltips/overlays..." + "Second-pass AI transform reads a prior flow's output, not raw inputs" + "analysisJson.flow discriminator lets two same-level (PER_PRODUCT) rows coexist per URL".**) — DEPLOY session: FOUR Rule 9 deploy gates fired (Deploy 1 `a24e92c` + FF1 `ecad5af` + FF2 `9da50ec` + FF3 `0631762`). One-hundred-and-seventy-eighth Claude Code session. Closes (a.112) RECOMMENDED-NEXT PARTIALLY (Fix Session C "Deploy 1" ✅ DEPLOYED-AND-VERIFIED; drag-to-reorder DEFERRED). Opens (a.113) RECOMMENDED-NEXT = **P-49 W5 Fix Session C "Deploy 2" — drag-to-reorder** on `workflow-2-competition-scraping` (URL rows via `CompetitorUrl.sortRank` + review rows via the already-shipped `CapturedReview.sortRankInReviewsTable`; @dnd-kit; Schema-change-in-flight NO at entry). 9/9 = 100% Yes-to-Recommended pickers; running cumulative 130/133 = 97.7%. NEW baselines locked: src/lib `node:test` = **1080/1080** + `npm run build` = **68 routes UNCHANGED** + extension = **910/910 UNCHANGED**. **1 NEW DEFERRED item at session end (Rule 26)** = drag-to-reorder (Deploy 2). **FIFTY-SIXTH end-of-session run under the Rule 30 + §4 Step 4b template.**

**Previously updated:** 2026-05-29-b — Header bump only this session (`session_2026-05-29-b_p52-ai-model-registry-rule-32-opus-4-8-rollout` — **W#2 + W#1 platform-wide polish P-52 — AI model registry doc + central model-selection methodology (NEW Rule 32) + Opus 4.8 rollout ✅ DEPLOYED-AND-VERIFIED 2026-05-29-b end-to-end on vklf.com on the `main` track (platform-wide; spans W#1 Keyword Clustering + W#2 Competition Scraping) — director Phase 4 verbatim verdict "passed." THREE deliverables: (1) NEW Group A doc `docs/AI_MODEL_REGISTRY.md` (central declaration-site table + consumers list + how-to-add-a-model checklist + enforcement); (2) NEW HANDOFF_PROTOCOL Rule 32 + NEW SessionStart hook `.claude/hooks/check-model-registry-drift.sh` (auto-detects unregistered model-list/pricing declaration sites; non-blocking); (3) Opus 4.8 (`claude-opus-4-8`) rollout — NEW SDK-free `models.ts` central list (`client.ts` re-exports; 3 W#2 modals now import it instead of duplicating) + `pricing.ts` placeholder + W#1 `AutoAnalyze.tsx`. Defaults UNCHANGED (W#2 Opus 4.7 / W#1 Sonnet 4.6). Single build commit `5b9784a` (14 files +343/-31). NEW reusable PATTERN — "SDK-free constants module lets client components import shared constants without bundling the server SDK". DATE-STAMP DRIFT RESOLVED: director confirmed real date 2026-05-29; prior 05-30/05-31/05-31-b stamps drifted ~1-2 days ahead; corrected to `2026-05-29-b` + logged.**) — DEPLOY session: ONE Rule 9 deploy gate fired on the single build `5b9784a`. One-hundred-and-seventy-seventh Claude Code session. Closes (a.111) RECOMMENDED-NEXT = P-52 AI model registry + Rule 32 + Opus 4.8 rollout ✅ DEPLOYED-AND-VERIFIED 2026-05-29-b. Opens (a.112) RECOMMENDED-NEXT = **P-49 W5 Reviews Analysis Table Fix Session C** on `workflow-2-competition-scraping` (NEW per-competitor non-bulleted prose AI flow + Excel export D-7 + drag-to-reorder D-6 + NEW `CapturedReview.sortRankInReviewsTable Int?` schema column; Schema-change-in-flight YES entry for that session). 6/6 = 100% Yes-to-Recommended pickers (PLUS a 7th picker with no recommended marker — the date-stamp resolution); running cumulative 121/124 = 97.6%. NEW baselines locked: src/lib `node:test` = **1059/1059** + `npm run build` = **68 routes UNCHANGED** + extension = **910/910 UNCHANGED**. **ZERO DEFERRED items at session end (Rule 26).** **NEW Rule 32 lands this session (NOT header-bump-only for HANDOFF_PROTOCOL).** **FIFTY-FIFTH end-of-session run under the Rule 30 + §4 Step 4b template.**

**Previously updated:** 2026-05-31-b — Header bump only this session (`session_2026-05-31-b_p49-w5-fu1-edit-delete-overall-analysis-box-plus-fu2-deleted-reviews-sync-bug` — **W#2 polish P-49 W5 FU-1 (EDIT + delete entries in the "Overall Analysis — Captured Reviews" traceability box, individually + in bulk) + FU-2 (deleted-reviews sync bug) ✅ DEPLOYED-AND-VERIFIED 2026-05-31-b end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — director Phase 4 verbatim verdict "Everything passed." Single build commit `7d89d75` (10 files). FU-1 reverses Fix Session D's read-only decision (delete at 3 levels + click-to-edit category names + complaint wording + always-visible bulk-select + "Delete selected" + confirm + re-run warns-then-replaces; all edits/deletes trim/modify `analysisJson.categories` on the per-competitor PER_PRODUCT ReviewAnalysis row via the extended PATCH, re-derive the flattened summary via `flattenCategoriesToSummaryString`, set `manuallyEdited:true`, NEVER touch CapturedReviews); FU-2 re-hydrates summaries + force-refetches loaded per-URL reviews on tab refocus (`visibilitychange`/`focus`). Schema-change-in-flight NO entire session. TWO NEW reusable PATTERNS — "Cross-page stale client cache → re-fetch on tab refocus" + "Structured-edit PATCH re-derives the flattened back-compat field". DATE-STAMP UNCERTAINTY: harness `currentDate` 2026-05-29 vs git-history 2026-05-31 — stamped 2026-05-31-b; flag for director correction.**) — DEPLOY session: ONE Rule 9 deploy gate fired on the single build `7d89d75`. One-hundred-and-seventy-sixth Claude Code session. Closes (a.110) RECOMMENDED-NEXT partially (FU-1 edit+delete + FU-2 sync bug ✅ DEPLOYED-AND-VERIFIED; (a.110)'s "THEN Fix Session C" remains PENDING). Opens (a.111) RECOMMENDED-NEXT = **AI model registry doc + central model-selection methodology rule + Opus 4.8 rollout** on the **`main`** track (platform-wide; spans W#1 + W#2; NEW ROADMAP entry P-52). 7/7 = 100% Yes-to-Recommended pickers; running cumulative 115/118 = 97.5%. NEW baselines locked: src/lib `node:test` = **1053/1053** + `npm run build` = **68 routes** + extension = **910/910 UNCHANGED**. **1 NEW DEFERRED item at session end (Rule 26)** = the model-registry work (destination P-52). **FIFTY-FOURTH end-of-session run under the Rule 30 + §4 Step 4b template.**

**Previously updated:** 2026-05-31 — Header bump only this session (`session_2026-05-31_p49-w5-reviews-analysis-table-fix-session-d` — **W#2 polish P-49 W5 Reviews Analysis Table Fix Session D ✅ DEPLOYED-AND-VERIFIED 2026-05-31 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — RE-SEQUENCED ahead of the planned Fix Session C per director's session-start Rule 14f pick — the "Overall Analysis — Captured Reviews" box on the URL detail page is now a read-only 3-column traceability table [Category / Complaint / source captured reviews + star counts]. Initial build `4db2b5c` (8 files +1052/-276 — STRUCTURED per-competitor analysis shape [prompt v3→v4 emits `{ categories: [{ name, bullets: [{ text, reviewRefs }] }] }` + `resolveReviewRefs` + `flattenCategoriesToSummaryString` back-compat] + NEW `ReviewsTraceabilityTable.tsx` + NEW pure helpers `reviews-traceability.ts` + `UrlDetailContent.tsx` table render + box relabel "Your notes — Captured Reviews"; removed the Fix B FF1 free-text write-back) plus ONE fix-forward commit FF1 `889667e` (4 files +318/-54 — NEW shared `src/lib/rich-text/save-with-retry.ts` transparent autosave retry on transient HTTP 5xx/network failures with exponential backoff, applied to both analysis boxes — fixes director-reported "Save failed — HTTP 500" boxes under heavy AI-run Supabase pool saturation). Director Phase 4 verdict: table feature "Both worked"; FF1 "Pass". Schema-change-in-flight NO entire session (the structured shape lives in the EXISTING `ReviewAnalysis.analysisJson` Json column). NEW reusable PATTERN — "Transient DB-auth 500 under AI-batch load → client-side autosave retry".**) — DEPLOY session: 2 Rule 9 deploy gates fired across initial build `4db2b5c` + FF1 `889667e`. One-hundred-and-seventy-fifth Claude Code session. Closes (a.109) RECOMMENDED-NEXT with a twist (was Fix Session C; RE-SEQUENCED to ship Fix Session D ✅ DEPLOYED-AND-VERIFIED 2026-05-31; Fix Session C remains pending). Opens (a.110) RECOMMENDED-NEXT = **P-49 W5 Reviews Analysis Table follow-ups** on `workflow-2-competition-scraping` (delete-from-table feature [NEEDS a design chat] + deleted-reviews sync bug + THEN Fix Session C). 7/7 = 100% Yes-to-Recommended pickers; running cumulative 108/111 = 97.3%. NEW baselines locked: src/lib `node:test` = **1038/1038** + `npm run build` = **68 routes** + extension = **910/910 UNCHANGED**. **2 NEW DEFERRED items at session end (Rule 26).** **FIFTY-THIRD end-of-session run under the Rule 30 + §4 Step 4b template.**

**Previously updated:** 2026-05-30 — Header bump only this session (`session_2026-05-30_p49-w5-reviews-analysis-table-fix-session-b` — **W#2 polish P-49 W5 Reviews Analysis Table Fix Session B ✅ DEPLOYED-AND-VERIFIED 2026-05-30 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — the SECOND of the 3-session corrective-fix plan locked 2026-05-28-b — initial build commit `351342a` (19 files +1115/-63 — Q3 NEW additive nullable `CapturedReview.title` schema column [`prisma db push` 1.50s, zero data loss] + orchestrator `saveReview` title-passthrough + title display-merge via NEW `mergeTitleAndBody` helper + D-9 per-review write-back to `CapturedReview.analysis` + D-10 bulleted-half per-competitor write-back to `CompetitorUrl.overallAnalyses["reviews"]` + D-11 PATCH PER_REVIEW edits + NEW `PerReviewSummaryCell` + D-8 persistence-on-refresh confirmed) plus ONE fix-forward commit FF1 `add00ad` (4 files +257/-55 — write-backs now fire on cache hits too via shared `writeBackPerCompetitorReviews` + content-dedup `tipTapDocContainsSummary`). Director Phase 4 verbatim PASS verdict: "Both worked now, everything passed". AUDIT FINDING (Rule 31 audit-shipped-state): the "Overall Analysis — Captured Reviews" box already rendered (shipped P-46 W2 Session 4) — D-10's "box missing" sub-claim was stale; only the write-back was the real gap. NEW reusable PATTERN memorialized — "Write-back-on-cache-hit gap".**) — DEPLOY session: 2 Rule 9 deploy gates fired across initial build `351342a` + FF1 `add00ad`. One-hundred-and-seventy-fourth Claude Code session. Closes (a.108) RECOMMENDED-NEXT = P-49 W5 Reviews Analysis Table Fix Session B ✅ DEPLOYED-AND-VERIFIED 2026-05-30 (build `351342a` + FF1 `add00ad`). Opens (a.109) RECOMMENDED-NEXT = **P-49 W5 Reviews Analysis Table Fix Session C** on `workflow-2-competition-scraping` per `docs/polish-item-specs/P-49-W5-S2-S3-competitor-reviews-analysis.md` §3 "Fix Session C" sub-section (NEW per-competitor non-bulleted AI flow + Auto-create non-bulleted button + Excel export D-7 + drag-to-reorder review rows D-6 + NEW `CapturedReview.sortRankInReviewsTable Int?` schema column + Q8 flow-naming; Schema-change-in-flight YES entry). **FIVE Rule 14f forced-pickers fired this session — 5/5 = 100% Yes-to-Recommended** (Q9 same-as-banner + Q3 add-this-session + Phase 2 scope + Phase 3 deploy gate + FF1 deploy gate); running cumulative 101/104 = 97.1% Yes-to-Recommended. **TWO Rule 9 deploy gates fired this session.** **Schema-change-in-flight flag YES entry → FLIPPED YES → NO at the initial deploy push.** Pre-deploy /scoreboard entry baselines (root tsc clean / extension tsc clean / **910 ext** / **984 src/lib** / **68 routes**); Check 6 SKIPPED per Rule 27. Post-FF1 /scoreboard 5/5 GREEN at NEW LOCKED baseline: src/lib `node:test` = **1018/1018** (+34 from 984 entry) / `npm run build` = **68 routes UNCHANGED** / extension `npm test` = **910/910 UNCHANGED**. **NEW baselines locked: src/lib `node:test` = 1018/1018 + `npm run build` = 68 routes + extension = 910/910 UNCHANGED.** ~6 pushes total this session. ZERO DEFERRED items at session end (Rule 26) — 7 in-session tasks; all 7 completed cleanly. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-30 capturing the "Write-back-on-cache-hit gap" Pattern + the audit-shipped-state positive win + P-43 cwd-leak running tally. NEW §B 2026-05-30 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (FIFTEENTH build/deploy-session §B entry per Rule 18; SEVENTH W5 entry). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED. **2 polish-item-specs MODIFIED:** `docs/polish-item-specs/P-49-W5-S2-S3-competitor-reviews-analysis.md` + `docs/polish-item-specs/P-49-W5-reviews-phase-2-master-spec.md`. No memory files added or modified this session. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.

**Previously updated:** 2026-05-29 — Header bump only this session (`session_2026-05-29_p49-w5-reviews-analysis-table-fix-session-a-shipped-with-4-bundled-ff-cycles` — **W#2 polish P-49 W5 Reviews Analysis Table Fix Session A ✅ DEPLOYED-AND-VERIFIED 2026-05-29 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — the first of the 3-session corrective-fix plan locked 2026-05-28-b — initial build commit `8708343` + FOUR bundled fix-forward commits (FF1 `0b21c09` 8 redirects + FF2 `31c54a0` 5 redirects + FF3 `12c042c` 4 redirects + FF4 `3fbe12e` 1 redirect = 18 total redirects across 4 FFs in one Phase 4 verification day — NEW RECORD for the Same-day Phase 4 multi-redirect bundling Pattern beating the prior 3-FF max). Director Phase 4 verbatim PASS verdict on FF4: "everything passed". Q3 schema gap (`CapturedReview.title` column missing — title silently dropped at orchestrator's `saveReview` adapter) DISCOVERED mid-Fix-Session-A + DEFERRED to Fix Session B per director-approved Rule 14f picker — preserves Fix Session A's NO-schema-change scope intact while still capturing the work for Fix Session B's now-YES schema-change-in-flight entry state.**) — DEPLOY session: 5 Rule 9 deploy gates fired across initial build + FF1 + FF2 + FF3 + FF4 within one Phase 4 verification day. One-hundred-and-seventy-third Claude Code session. Closes (a.107) RECOMMENDED-NEXT = P-49 W5 Reviews Analysis Table Fix Session A ✅ DEPLOYED-AND-VERIFIED 2026-05-29 with 4 bundled fix-forward cycles. Opens (a.108) RECOMMENDED-NEXT = **P-49 W5 Reviews Analysis Table Fix Session B** on `workflow-2-competition-scraping` per `docs/polish-item-specs/P-49-W5-S2-S3-competitor-reviews-analysis.md` §3 "Fix Session B" sub-section (with Q3 schema gap carry-over: `CapturedReview.title` schema column + orchestrator wire-up + PATCH endpoint extension + display-time merge). **SEVEN Rule 14f forced-pickers fired this session — 7/7 = 100% Yes-to-Recommended** (1 Q10 resolution + 1 Q3 schema-gap deferral + 5 Rule 9 deploy gates); running cumulative 96/99 = 97.0% Yes-to-Recommended. **FIVE Rule 9 deploy gates fired this session.** **Schema-change-in-flight flag STAYS NO entire session.** Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **950 src/lib** / **67 routes**); Check 6 SKIPPED per Rule 27. Post-FF4 /scoreboard 5/5 GREEN at NEW LOCKED baseline: src/lib `node:test` = **984/984** (+34 net cumulative) / `npm run build` = **68 routes** (+1 NEW GET /review-analysis route in FF2) / extension `npm test` = **910/910 UNCHANGED**. **NEW baselines locked: src/lib `node:test` = 984/984 + `npm run build` = 68 routes + extension = 910/910 UNCHANGED.** ~14-16 pushes total this session. ZERO DEFERRED items at session end (Rule 26) — Q3 carry-over captured into spec doc §3 + §4 not into TaskList per Rule 14e. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-29 capturing FOUR observations: (1) NEW RECORD for the Same-day Phase 4 multi-redirect bundling Pattern; (2) Q3 schema-gap discovery is a NEW positive pattern; (3) NEW reusable PATTERN memorialized: Cell-level click handlers + state-aware text affordance Pattern; (4) P-43 cwd-leak running tally ~31-33+. NEW §B 2026-05-29 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (FOURTEENTH build/deploy-session §B entry per Rule 18; SIXTH W5 entry). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED. **2 polish-item-specs MODIFIED:** `docs/polish-item-specs/P-49-W5-S2-S3-competitor-reviews-analysis.md` + `docs/polish-item-specs/P-49-W5-reviews-phase-2-master-spec.md`. NEW source-tree files shipped: NEW handler `src/lib/competition-scraping/handlers/review-analysis-list.ts` (FF2) + NEW shared component `src/app/projects/[projectId]/competition-scraping/components/ColumnResizeHandle.tsx` (FF1). No memory files added or modified this session. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.

**Previously updated:** 2026-05-28-b — Header bump only this session (`session_2026-05-28-b_p49-w5-reviews-phase-2-master-spec-backfill-and-page-2-divergence-fix-plan` — **W#2 polish P-49 W5 Reviews Phase 2 master-spec-backfill + page-2 (Competitor Reviews Analysis Table) divergence-discovery + 3-session corrective-fix plan locked — PURE-PLANNING + DOC-ONLY SESSION on `workflow-2-competition-scraping` — NO code, NO builds, NO deploys this session. Session opened per yesterday's NEXT_SESSION.md pointer scoping "P-49 W5 corrective rebuild Block 1 planning resume + (likely) Session 1 Category page scaffold"; director redirected immediately at session-start with a MAJOR scope correction NOT covered by the launch prompt — the previously-shipped `/competitor-reviews-analysis` page (W5 Sessions 2 + 3, deployed 2026-05-27 + 2026-05-27-c) has multiple divergences from director's original verbatim spec. Director re-pasted the FULL original instruction set covering all 3 Reviews Phase 2 pages + Claude executed code-truth audit producing 12 divergence findings (D-1 through D-12). Director picked Rule 14f Option A "Planning + spec backfill only today. NO code today." 7 clarifying questions answered Yes-to-Recommended (7/7 = 100%) via 2 AskUserQuestion batches; 3 NEW open questions emerged (Q8 flow-naming / Q9 per-review Edit UI / Q10 Column 8 display format). NEW master spec doc + NEW backfilled Reviews Analysis Table spec doc + UPDATE Category page §5 + UPDATE Type page §5 + UPDATE master spec §4 + UPDATE ROADMAP P-49 entry + NEW Rule 31 sub-sections in `docs/HANDOFF_PROTOCOL.md` ("Mechanical read-guarantee" + "Audit-shipped-state mandate for in-flight workstreams") + EXTEND `.claude/hooks/inject-next-session-pointer.sh` to auto-detect `P-NN` token references in the pointer content + emit a "🔵 RULE 31 MANDATORY READ" block listing every matching spec file. 2 NEW reusable PATTERNS memorialized.**) — PURE-PLANNING + DOC-ONLY session: ZERO Rule 9 deploy gates fired; ZERO code changes; ZERO schema changes; ZERO new routes. One-hundred-and-seventy-second Claude Code session. Closes (a.106) RECOMMENDED-NEXT = P-49 W5 corrective rebuild Block 1 planning resume — REDIRECTED to Reviews Analysis Table master-spec-backfill + page-2 divergence-discovery + 3-session corrective-fix plan locked. Opens (a.107) RECOMMENDED-NEXT = **P-49 W5 Reviews Analysis Table Fix Session A** on `workflow-2-competition-scraping`. **THREE Rule 14f forced-pickers fired this session — 8/8 = 100% Yes-to-Recommended** (1 session-shape picker + 7 clarifying questions); running cumulative 89/92 = 96.7% Yes-to-Recommended. **ZERO Rule 9 deploy gates fired this session.** **Schema-change-in-flight flag STAYS NO entire session.** /scoreboard NOT RUN this session — pure-doc work; Check 6 SKIPPED per Rule 27; baselines UNCHANGED from W5 Session 4: root tsc clean / extension tsc clean / extension `npm test` = **910/910 UNCHANGED** / src/lib `node:test` = **950/950 UNCHANGED** / `npm run build` = **67 routes UNCHANGED**. **NEW baselines locked: UNCHANGED from W5 Session 4.** 3 pushes total this session (standard 3-push pattern). ZERO DEFERRED items at session end (Rule 26) — 7 in-session tasks; all 7 completed cleanly. ONE NEW HIGH-importance CORRECTIONS_LOG §Entry 2026-05-28-b. NEW §B 2026-05-28-b entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (THIRTEENTH build/deploy-session §B entry per Rule 18). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED. **2 NEW spec docs in `docs/polish-item-specs/`** (master + Reviews Analysis Table backfilled) + MODIFIED 2 existing spec docs (Category §5 + Type §5). NEW Rule 31 sub-sections added inline to `docs/HANDOFF_PROTOCOL.md`. 1 operational-file modification: `.claude/hooks/inject-next-session-pointer.sh` extended for P-NN auto-detection. No memory files added or modified this session. ZERO new Starter-file rules drafted; no Step 7b changes (Rule 31's session-start enforcement happens via the existing Step 7b plain-terms-summary mechanism + the NEW mechanical read-guarantee via the SessionStart hook auto-detection); no resume-flow changes.

**Previously updated:** 2026-05-28 — Header bump only this session (`session_2026-05-28_p49-w5-session-4-scope-misread-rollback-and-corrective-planning` — **W#2 polish P-49 W5 Session 4 — SCOPE-MISREAD ROLLBACK + CORRECTIVE-PLANNING SESSION on `workflow-2-competition-scraping` — wrong-spec build commit `5fa1f53` (8 files +2705/-54 — single combined "By Category-Type" page + card-style grouping + browser-side execution + 1 AI flow + NEW P-51 ROADMAP skeleton) shipped to vklf.com via `workflow-2-competition-scraping` → `main` and was HARD-REVERTED mid-Phase-4 via revert commit `958ccf8` (8 files +54/-2705) — director's actual spec required TWO separate pages (one Category + one Type) + flat 13-column tables + first-row-carries-label grouping + per-batch endpoint architecture + 4 AI flows + click-to-edit + drag-to-reorder + Excel export + write-back to URL detail's "Overall Analysis — Captured Reviews" box — net live-site change = ZERO since the revert returned the site to the W5 Session 3 deploy state. Director established NEW Rule 31 mid-session (HANDOFF_PROTOCOL.md line 936 — Polish-item spec capture: verbatim director instructions checked into `docs/polish-item-specs/<item-id>-<slug>.md` per the standardized 5-section structure) + Claude backfilled THREE new spec docs in the new `docs/polish-item-specs/` directory (P-49 Category page 25 KB + P-49 Type page 15 KB + P-51 comprehensive-analysis AI summary 7 KB SKELETON PLACEHOLDER). Director approved 5-session corrective rebuild plan; planning conversation paused MID-WAY through Block 1 with 6 open questions awaiting director answers at next session start**) — DEPLOY-AND-REVERT session: 2 Rule 9 deploy gates fired (build deploy + revert deploy). One-hundred-and-seventy-first Claude Code session. Closes (a.105) RECOMMENDED-NEXT = P-49 W5 Session 4 wrong-spec build SHIPPED + HARD-REVERTED; opens (a.106) RECOMMENDED-NEXT = **P-49 W5 corrective rebuild — Block 1 planning resume (answer the 6 open questions) + (likely) Session 1 Category page scaffold per `docs/polish-item-specs/P-49-W5-S4-category-page.md` §3** on `workflow-2-competition-scraping`. **THREE Rule 14f forced-pickers fired this session — 3/3 = 100% Yes-to-Recommended** (session-start scope picker + Rule 9 deploy gate picker + post-Phase-4-discovery rollback picker); running cumulative 82/85 = 96.5% Yes-to-Recommended. **TWO Rule 9 deploy gates fired this session** (build deploy of `5fa1f53` + hard-revert deploy of `958ccf8`). **Schema-change-in-flight flag STAYS NO entire session** (entry NO + STAYS NO through build + STAYS NO through revert + exit NO; corrective rebuild plan also does NOT require schema changes). Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **950 src/lib** / **67 routes**); mid-session (post-build `5fa1f53`) **968 src/lib (+18)** / **68 routes (+1 — new by-category-type page)**; post-revert /scoreboard 5/5 GREEN at LOCKED baselines UNCHANGED from W5 Session 3 exit: root tsc clean / extension tsc clean / extension `npm test` = **910/910 UNCHANGED** / src/lib `node:test` = **950/950** (back to entry baseline) / `npm run build` = **67 routes** (back to entry baseline); Check 6 Playwright SKIPPED per Rule 27. **NEW baselines locked: UNCHANGED from W5 Session 3 exit (since the build was hard-reverted).** 8 pushes total this session. ZERO DEFERRED items at session end (Rule 26) — 7 in-session tasks; all 7 completed cleanly. ONE NEW HIGH-importance CORRECTIONS_LOG §Entry 2026-05-28 capturing the scope-misread incident + the 5-session corrective rebuild plan + the 6 open Block 1 questions. NEW §B 2026-05-28 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (TWELFTH build/deploy-session §B entry per Rule 18). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED. NEW directory `docs/polish-item-specs/` with 3 NEW spec docs. NEW Rule 31 added inline to `docs/HANDOFF_PROTOCOL.md` line 936. No memory files added or modified this session. ZERO new Starter-file rules drafted; no Step 7b changes (Rule 31's session-start enforcement happens via the existing Step 7b plain-terms-summary mechanism); no resume-flow changes.

**Previously updated:** 2026-05-27-c — Header bump only this session (`session_2026-05-27-c_p49-w5-session-3-per-competitor-deploy-and-2-ff-cycles` — **W#2 polish P-49 W5 Session 3 — Per-Competitor Comprehensive (bulleted) flow ✅ DEPLOYED-AND-VERIFIED 2026-05-27-c end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — first of 3 remaining aggregation flows from §B 2026-05-27 design lock — initial build commit `b9d232e` (10 files +1481/-45) + 2 fix-forward commits (FF#1 `1cd6e3b` bundling 4 Phase 4 redirects — v1→v2 critique-only prompt with 4 fixed theme headings + NEW global Summarize-all button + NEW `GlobalCompetitorSummarizeModal.tsx` 606 LOC + Table 2 button rename + Edit affordance with NEW PATCH endpoint + NEW `review-analysis-update.ts` handler + wire-shape extension carrying `analysisId` / FF#2 `7f19aca` bundling 2 Phase 4 redirects — v2→v3 theme-emergent prompt + 9 example emergent themes + PROMPT_VERSION bump v2→v3 + explicit "✅ AI Review Summarizing job complete" banner in both modals) all ff-merged to main under 3 Rule 9 deploy gates within ONE Phase 4 verification day**) — DEPLOY session. One-hundred-and-seventieth Claude Code session. Closes (a.104) RECOMMENDED-NEXT = P-49 W5 Session 3 Per-Competitor flow ✅ DEPLOYED-AND-VERIFIED 2026-05-27-c; opens (a.105) RECOMMENDED-NEXT = **P-49 W5 Session 4 — Per-Category Comprehensive (bulleted) flow + NEW "By Category-Type" page (Table 3 scaffold)** on `workflow-2-competition-scraping` per `docs/REVIEWS_PHASE_2_DESIGN.md` §B 2026-05-27-c — BUILD session by default; ZERO Rule 9 deploy gates planned; Schema-change-in-flight STAYS NO at session entry. **EIGHT Rule 14f forced-pickers fired this session — 8/8 = 100% Yes-to-Recommended** (variant scope + build order + which flow first + bullet structure + length range + input strategy + deploy-now + end-session); running cumulative 78/81 = 96.3% Yes-to-Recommended. **THREE Rule 9 deploy gates fired this session** (initial build deploy + FF#1 bundled-4-redirect deploy + FF#2 bundled-2-redirect deploy). **Schema-change-in-flight flag STAYS NO entire session.** Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **922 src/lib** / **66 routes**); Check 6 Playwright SKIPPED per Rule 27. Post-FF#2 /scoreboard 5/5 GREEN at NEW LOCKED baseline: src/lib `node:test` = **950/950** (+28 cumulative from 922 entry); `npm run build` = **67 routes** (+1 from 66 — new PATCH endpoint); extension `npm test` = **910/910 UNCHANGED**. **NEW baselines locked: src/lib `node:test` = 950/950 + `npm run build` = 67 routes + extension = 910/910 UNCHANGED.** **6 pushes total this session** (3 deploy + 3 ping-pong pairs). ZERO DEFERRED carry-overs at session end. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-27-c capturing THREE NEW reusable Patterns ("Same-day Phase 4 multi-redirect bundling Pattern" + "Edit affordance for cached AI output Pattern" + "Test stub level-discriminator filtering Pattern") + P-43 cwd-leak reproduction running tally ~26+. NEW §B 2026-05-27-c entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (ELEVENTH build/deploy-session §B entry per Rule 18). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.

**Previously updated:** 2026-05-27-b — Header bump only this session (`session_2026-05-27-b_p49-w5-session-2-deploy-and-catchup-doc-batch` — **RETROACTIVE catch-up doc-batch for an unrecorded same-day Claude session (the 168th session) that ran W#2 polish P-49 W5 Session 2 per-batch endpoint scaffold + Per-Review Summarize end-to-end ✅ DEPLOYED-AND-VERIFIED 2026-05-27 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` — initial build commit `60609f6` (12 files +2975/-1267) + 3 fix-forward commits (FF#1 `ecf292d` Table 2 response-shape unwrap / FF#2 `d713712` in-modal View prompts panel for AI transparency / FF#3 `cd6478b` v2 bulleted-critical prompt + cache-key version bump + Table 2 whiteSpace pre-wrap) all ff-merged to main under 4 Rule 9 deploy gates within ONE Phase 4 verification day**) — catch-up doc-batch session: the prior Claude session ended without running the canonical end-of-session §4 Step 1 doc-batch protocol; this catch-up session executes the missing doc-batch + push only; ZERO code changes this session. One-hundred-and-sixty-ninth Claude Code session. Closes (a.103) RECOMMENDED-NEXT = P-49 W5 Session 2 ✅ DEPLOYED-AND-VERIFIED 2026-05-27; opens (a.104) RECOMMENDED-NEXT = **P-49 W5 Session 3 — rewrite remaining 6 of 7 flow builders into `prompts.ts` + start Tables 3 + 4 (By Category + By Type) + add their AI run buttons + modals + browser batch loops + first live e2e runs on those 6 flows** on `workflow-2-competition-scraping` per `docs/REVIEWS_PHASE_2_DESIGN.md` §B 2026-05-27-b — BUILD session by default; ZERO Rule 9 deploy gates planned; Schema-change-in-flight STAYS NO at session entry. **TWO Rule 14f forced-pickers fired in THIS catch-up doc-batch session — 2/2 = 100% Yes-to-Recommended.** **FOUR Rule 9 deploy gates fired in the unrecorded W5 Session 2 session** (initial deploy + FF#1 deploy + FF#2 deploy + FF#3 deploy); ZERO Rule 9 deploy gates fired in this catch-up doc-batch session itself. **Schema-change-in-flight flag transitions:** entry of the unrecorded W5 Session 2 session = YES; FLIPPED YES → NO at the W5 Session 2 initial deploy push completion (canonical schema-change-ships-to-production transition); STAYS NO through the 3 FFs; final state at the catch-up doc-batch entry = NO. Just-run /scoreboard 5/5 GREEN (post-FF#3 working tree): root tsc clean / extension tsc clean / extension `npm test` = **910/910 UNCHANGED** / src/lib `node:test` = **922/922** (+16 net) / `npm run build` = **66 routes** (+1 new Table 2 page); Check 6 Playwright SKIPPED per Rule 27. **NEW baselines locked: src/lib `node:test` = 922/922 + `npm run build` = 66 routes + extension = 910/910 UNCHANGED.** ONE push planned at end of this catch-up doc-batch session — workflow-branch push carrying the doc-batch commit only; parent Claude will then run /deploy to ff-merge the doc-batch to main. ZERO DEFERRED carry-overs at session end. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-27-b capturing the process-gap meta-pattern (positive learning opportunity not a top-tier slip) + THREE NEW reusable Patterns (v1 → v2 prompt-version-bump + cache-key-versioning + in-modal View prompts panel UI transparency + Rule 14f scope-narrowing) + P-43 cwd-leak reproduction running tally ~24+ + operational notes on UNCHANGED Group B docs. NEW §B 2026-05-27-b entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (TENTH build/deploy-session §B entry per Rule 18; supersedes §A.13 prompt content + the §B 2026-05-27 line-1474 reference to "7 v1 prompts locked during planning"). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED. `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.

**Previously updated:** 2026-05-27 — Header bump only this session (`session_2026-05-27_p49-w5-session-1.5-reviews-phase-3-design-lock` — **W#2 polish P-49 Reviews Phase 3 design lock + schema enum extension + shared ExecutionModeSelect component SHIPPED at code level via build commit `252e1dd` on `workflow-2-competition-scraping` — W5 Session 1.5 director-driven planning conversation locked the 3-table + 7-flow + browser-first execution + 7 v1 prompts + 4-option toggle expansion across ~20 Rule 14f forced-pickers (19/19 = 100% Yes-to-Recommended on pickers answered + 2 free-text redirects); per-batch server endpoint scaffold + `prompts.ts` rewrite + Table 2 page + first live end-to-end run DEFERRED to next W5 session (a.103) in a deliberately narrow code-mechanics scope cut** — pure BUILD session; NO production deploy; ZERO Rule 9 deploy gates fired this session; build commit stays on workflow branch until next W5 deploy session. Build commit `252e1dd` (6 files +181/-10) carrying MODIFY `prisma/schema.prisma` (`ReviewAnalysisLevel` enum extended with PER_REVIEW + PER_CATEGORY; `npx prisma db push` 1.19s additive zero data loss) + MODIFY `src/lib/shared-types/competition-scraping.ts` (union widened to all 5 values + validator narrowed) + MODIFY `src/lib/competition-scraping/handlers/review-analysis-run.ts` (row type widened to keep tsc green) + NEW `src/lib/workflow-components/execution-mode.ts` (type + constants + validator matching W#1's verbatim labels) + NEW `src/lib/workflow-components/execution-mode-select.tsx` (controlled `<select>` React component for modal consumption) + NEW `src/lib/workflow-components/execution-mode.test.ts` (7 node:test cases). Closes (a.102) RECOMMENDED-NEXT = P-49 W5 Session 1.5 director-driven output-shape planning ✅ DONE 2026-05-27; opens (a.103) RECOMMENDED-NEXT = **P-49 W5 Session 2 — per-batch server endpoint scaffold + delete shipped per-product two-sweep handler + rewrite `prompts.ts` with 7 flow-specific builders + start Table 2 page + Per-Review Summarize button + modal + browser batch loop + first live end-to-end run** on `workflow-2-competition-scraping` per the new memory file `feedback_browser_first_ai_with_server_migration.md`) — BUILD session. **~20 Rule 14f forced-pickers fired this session — 19/19 = 100% Yes-to-Recommended on pickers answered + 2 free-text redirects** (positive calibration data points for Rule 14f's free-text escape-hatch design: Round 2 row-structure picker REDIRECTED dropping Stars + Reviews Summary columns from Tables 3 + 4 + Round 3 cost-caps picker REDIRECTED removing all cost caps for transparency-only). **ZERO Rule 9 deploy gates fired** (pure BUILD session). **Schema-change-in-flight flag FLIPPED NO → YES mid-session at `npx prisma db push` completion** (PER_REVIEW + PER_CATEGORY enum values added; additive; zero data loss); STAYS YES through end of session (until next W5 deploy session ff-merges to main + Vercel auto-redeploys). Pre-build /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **899 src/lib** / **65 routes**); Check 6 Playwright SKIPPED per Rule 27 standing non-deploy SKIP precedent. Post-build 5/5 GREEN at new src/lib baseline: src/lib `node:test` = **906/906** (+7 — exact match with 7 new cases in execution-mode.test.ts); npm run build = **65 routes UNCHANGED**; extension `npm test` = **910/910** UNCHANGED; Check 6 SKIPPED. **NEW baseline locked: src/lib `node:test` = 906/906** (+7 from 899 entry baseline). ONE push this session per `feedback_approval_scope_per_decision_unit.md` (workflow-branch push carrying both build commit `252e1dd` + end-of-session doc-batch commit; NO ff-merge to main). ZERO DEFERRED carry-overs at session end after the (a.103) capture lands. ONE NEW HIGH-importance CORRECTIONS_LOG §Entry 2026-05-27 capturing FOUR sub-observations including the **NEW reusable Pattern memorialized via NEW memory file `feedback_browser_first_ai_with_server_migration.md`** — "Default new AI batch flows to browser-side execution; add execution-mode dropdown now for seamless future server-side migration; mirror dropdown into W#1's existing modal at the same time so both workflows can migrate together" + Rule 14f free-text escape-hatch calibration data point (2 redirects this session both produced superior outcomes) + P-43 cwd-leak Pattern Class reproduced ~2 more times this session (running tally now ~22+) + Reviews Phase 3 design lock supersedes REVIEWS_PHASE_2_DESIGN.md §A.10/§A.11/§A.12 substantially with §A frozen per Rule 18. NEW §B 2026-05-27 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (NINTH build/deploy-session §B entry per Rule 18; FIRST W5 Session 1.5 entry). NEW §B 2026-05-27 cross-reference pointer entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md`. `docs/COMPETITION_DATA_V2_DESIGN.md` + `docs/COMPETITION_SCRAPING_VERIFICATION_BACKLOG.md` UNCHANGED. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.

**Previously updated:** 2026-06-02 — Header bump only this session (`session_2026-06-02_p49-w5-session-1-foundation-shipped-output-shape-deferred` — **W#2 polish P-49 Reviews Phase 2 Workstream 5 AI review analysis Session 1 — foundation primitives + plumbing handler SHIPPED at code level via build commit `04f74cf` on `workflow-2-competition-scraping`; v1 prompt content + UI placement re-confirmation + first live end-to-end run DEFERRED to next session (a.102) per director's explicit mid-session directive to plan the per-product analysis output shape FIRST before writing prompt code** — pure BUILD session; NO production deploy; ZERO Rule 9 deploy gates fired this session; build commit stays on workflow branch until W5 deploy session. Build commit `04f74cf` (19 files +2968/-1) carrying NEW `src/lib/competition-scraping/review-analysis/` directory with 7 modules (`client.ts` Anthropic SDK seam + `pricing.ts` + `cache.ts` SHA-256 reviewsHash + `cost-cap.ts` preventive caps + `token-counter.ts` pre-flight + `batch-sizer.ts` adaptive batching + `prompts.ts` PLACEHOLDER) + 6 test files (~47 new node:test cases) + NEW `src/lib/competition-scraping/handlers/review-analysis-run.ts` (~430 LOC DI-seam POST handler) + NEW `review-analysis-run.test.ts` (~14 cases) + NEW thin API route shim at `src/app/api/projects/[projectId]/competition-scraping/review-analysis/run/route.ts` + NEW `scripts/test-w5-end-to-end.mjs` (--dry-run PASSED; --live DEFERRED) + MODIFY `package.json` + `package-lock.json` for NEW dependency `@anthropic-ai/sdk ^0.98.1`. Closes (a.101) PARTIALLY; opens (a.102) RECOMMENDED-NEXT = P-49 W5 Session 1.5 — director-driven planning of the per-product analysis output shape on `workflow-2-competition-scraping` per the new memory file `feedback_plan_output_shape_before_building.md`) — BUILD session. **TWO Rule 14f forced-pickers fired** — 1 answered Yes-to-Recommended (W5 architecture-decision picker; SDK + pre-flight cost-cap + pre-flight token-counter Recommended over raw-fetch-mirror-W#1; 1/1 = 100% on the picker answered) + 1 REDIRECTED by director via free-text escape-hatch ("Fire live end-to-end run now?" → "I want to first plan out the analysis"; positive calibration data point for Rule 14f free-text escape-hatch design). **ZERO Rule 9 deploy gates fired** (pure BUILD session; build commit stays on workflow branch until W5 deploy session). **Schema-change-in-flight flag STAYS NO entire session** (entry NO + exit NO; `ReviewAnalysis` model + `ReviewAnalysisLevel` enum schema already shipped via W2 Amazon Session 1's `prisma db push` 2026-05-26 + deployed 2026-05-28). Pre-build /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **910 ext** / **838 src/lib** / **64 routes**); Check 6 Playwright SKIPPED per Rule 27 picker. Post-build 5/5 GREEN at new src/lib baseline: src/lib `npm test` = **899/899** (+61 cumulative from 838); npm run build = **65 routes** (+1 for new review-analysis/run shim); extension `npm test` = **910/910** UNCHANGED; Check 6 SKIPPED. **NEW baseline locked: src/lib `npm test` = 899/899** (+61 from 838 entry baseline). ONE push this session per `feedback_approval_scope_per_decision_unit.md` (workflow-branch push carrying both build commit `04f74cf` + end-of-session doc-batch commit; NO ff-merge to main). ZERO DEFERRED carry-overs at session end after the (a.102) capture lands. ONE NEW HIGH-importance CORRECTIONS_LOG §Entry 2026-06-02 capturing FIVE sub-observations including the **NEW reusable Pattern memorialized via NEW memory file `feedback_plan_output_shape_before_building.md`** — "Plan AI-output shape (prompts + UI placement) with director BEFORE writing prompt code, even when launch prompt says ship-and-iterate" + W5-vs-W#1 architecture divergence on AI-infrastructure axis + P-43 cwd-leak Pattern Class reproduced ~1-2 more times this session (running tally now ~20+) + Rule 14f free-text escape-hatch calibration data point + session-letter date convention plain (no letter suffix). NEW §B 2026-06-02 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (EIGHTH build/deploy-session §B entry per Rule 18; FIRST W5 entry). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED (W5 is PLOS-side AI infrastructure, not extension-side). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.

**Previously updated:** 2026-06-01 — Header bump only this session (`session_2026-06-01_p49-w2-walmart-sub-cluster-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Walmart sub-cluster ✅ DEPLOYED-AND-VERIFIED 2026-06-01 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — build + deploy + 3-fix-forward cascade + Phase-4-PASS bundled into ONE calendar day; **CLOSES the entire P-49 W2 per-platform extension extraction arc** — all 4 platforms (Amazon 2026-05-28 + eBay 2026-05-30 + Etsy 2026-05-31 + Walmart 2026-06-01) now ✅ DEPLOYED-AND-VERIFIED end-to-end on vklf.com; **UNBLOCKS P-49 W5 AI review analysis Session 1** per (a.101). Session 1 build commit `3316eaa` (3 files +1625/-5) carrying NEW `walmart-review-extractor.ts` (~530 LOC URL-construction) + NEW test file (~660 LOC; 88 new node:test cases) + MODIFY orchestrator ff-merged under Rule 9 deploy gate; Vercel auto-redeploy fired; Phase 4 surfaced `LISTING_PAGE_PATH` regex too restrictive (Walmart serves BOTH `/ip/<id>` and `/ip/<slug>/<id>`) → **FF#1 `3321690`** (2 files +60/-1; +6 tests) loosen regex → FF#1 BUSTED in row-walker phase (all 14 candidate selectors returned 0) → **Rule 14f mid-Phase-4 picker rerouted to diagnostic-instrumentation FF#2 `c953e71`** (1 file +101/-1; TEMPORARY console.log + auto-download of first star + first page HTML to Downloads; reused 2026-05-30 eBay FF#4 + 2026-05-31 Etsy FF#2 Pattern THIRD consecutive day) → **FF#3 `86cbfbd` empirical fix — the WIN** (2 files +379/-403; anchor on per-review body `data-testid="enhanced-review-content"` + `closest('.overflow-visible')` walk-up + screen-reader-only `<span class="ld_Ec">` star + `<h3>` null-when-missing title + `<p>` body + `aria-label` reviewer name + word-boundary regex filter + `.f7.gray` date + removed `findWalmartReviewsContainer` + removed FF#2 instrumentation + test file rewritten); director Phase 4 PASS verdict on FF#3 "Everything passed" (third consecutive day the Diagnostic-instrumentation FF Pattern delivered a PASS verdict on the empirical-fix FF). Closes (a.100); opens (a.101) RECOMMENDED-NEXT = P-49 W5 AI review analysis Session 1 on `workflow-2-competition-scraping` per §C.5 outline) — DEPLOY + 3-FIX-FORWARD session. **SEVEN Rule 14f forced-pickers fired** all director-Yes to Recommended (7/7 = 100% calibration data point; running cumulative 50/53 = 94.3% across recent 8 sessions). **FOUR Rule 9 deploy gates fired** (build deploy + FF#1 deploy + FF#2 diagnostic deploy + FF#3 fix deploy — same count as yesterday's Etsy 3-FF). **Schema-change-in-flight flag STAYS NO entire session** (Walmart reuses W2 Amazon schema shipped 2026-05-28). Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **818 ext** / **838 src/lib** / **64 routes**); Check 6 Playwright SKIPPED per Rule 27 picker. Post-FF#3 5/5 GREEN at new ext baseline: extension `npm test` = **910/910** (+92 cumulative from 818); src/lib **838 UNCHANGED**; routes **64 UNCHANGED**. **NEW baseline locked: extension `npm test` = 910/910** (+92 from 818 entry baseline). ~10 pushes this session per `feedback_approval_scope_per_decision_unit.md`. ZERO DEFERRED carry-overs at session end (Walmart sub-cluster fully ✅ DEPLOYED-AND-VERIFIED; §C.2 Sessions 2 + 3 CLOSED; entire Walmart arc compressed into 1 calendar day; entire P-49 W2 4-platform arc COMPLETE). ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-06-01 capturing THREE NEW reusable Patterns ("Diagnostic-instrumentation FF Pattern reusability validated across 3 consecutive days/platforms" + "Word-boundary regex for reserved-keyword filtering against natural-language strings" + "closest() walk-up + anchor-on-canonical-data-testid Pattern") + 3 LOW informational sub-observations (P-43 cwd-leak Pattern Class reproduction running tally ~19+ + calibration 7/7 = 100% Yes-to-Recommended + §C.2 estimate compression memorialized for THIRD consecutive day — per-platform sub-cluster 1-day cadence is now the empirical norm validated across 3 consecutive days/3 consecutive platforms). NEW §B 2026-06-01 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (SEVENTH build/deploy-session §B entry per Rule 18; CLOSES the entire P-49 W2 4-platform arc). NEW §B 2026-06-01 cross-reference pointer entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md`. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.

**Previously updated:** 2026-05-31 — Header bump only this session (`session_2026-05-31_p49-w2-etsy-sub-cluster-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Etsy sub-cluster ✅ DEPLOYED-AND-VERIFIED 2026-05-31 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — build + deploy + 3-fix-forward cascade + Phase-4-PASS bundled into ONE calendar day; Session 1 build commit `c572a42` (3 files +1509/-3) carrying NEW `etsy-review-extractor.ts` (~565 LOC) + NEW `etsy-review-extractor.test.ts` (~580 LOC; 72 new node:test cases) + MODIFY `orchestrator.ts` (Etsy dispatch) ff-merged to main under Rule 9 deploy gate; Vercel auto-redeploy fired; Phase 4 surfaced that URL-construction was BUSTED (Etsy's overlay loads via AJAX on the same listing URL with no separate URL for filters or pagination) → **Architecture pivot via FF#1 `67aeacd`** (2 files +1058/-705; rewrote to live-DOM driver based on director's paste of the 41 KB overlay outerHTML) → FF#1 BUSTED silently → **Rule 14f mid-Phase-4 picker rerouted to diagnostic-instrumentation FF#2 `a3107b6`** (1 file +163/-2; TEMPORARY console.log + auto-download of overlay HTML; reused yesterday's eBay FF#4 Pattern) → **FF#3 `41b03c5` empirical fix — the WIN** (2 files +73/-190; restrict `findOverlayContainer` to `.deep-dive-sheet` only + reject hidden variants + remove FF#2 instrumentation); director Phase 4 PASS verdict on FF#3 "Everything worked perfectly this time" (identical phrasing to yesterday's eBay FF#5 PASS). Closes (a.99); opens (a.100) RECOMMENDED-NEXT = P-49 W2 Walmart sub-cluster Session 1 on `workflow-2-competition-scraping` per §A.2 priority order + §C.2 implementation outline) — DEPLOY + 3-FIX-FORWARD session. **EIGHT Rule 14f forced-pickers fired** all director-Yes to Recommended (8/8 = 100% calibration data point; running cumulative 35/38 = 92.1% across recent 6 sessions). **FOUR Rule 9 deploy gates fired** (build deploy + FF#1 deploy + FF#2 diagnostic deploy + FF#3 fix deploy — one fewer than yesterday's eBay 5-FF). **Schema-change-in-flight flag STAYS NO entire session** (Etsy reuses W2 Amazon schema shipped 2026-05-28). Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **731 ext** / **838 src/lib** / **64 routes**); Check 6 Playwright SKIPPED per Rule 27 picker. Post-FF#3 5/5 GREEN at new ext baseline: extension `npm test` = **818/818** (+87 cumulative from 731); src/lib **838 UNCHANGED**; routes **64 UNCHANGED**. **NEW baseline locked: extension `npm test` = 818/818** (+87 from 731 entry baseline). ~10 pushes this session per `feedback_approval_scope_per_decision_unit.md`. ZERO DEFERRED carry-overs at session end (Etsy sub-cluster fully ✅ DEPLOYED-AND-VERIFIED; §C.2 Sessions 2 + 3 CLOSED; entire Etsy arc compressed into 1 calendar day). ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-31 capturing THREE NEW reusable Patterns ("Diagnostic-instrumentation FF Pattern reusability validated across consecutive sessions" + "Over-broad fallback selectors in platform DOM-walkers should be DELETED, not added" + "AJAX-loaded overlay scraping requires live-DOM driver, not URL-construction") + 3 LOW informational sub-observations (P-43 cwd-leak Pattern Class reproduction running tally ~16+ + calibration 8/8 = 100% Yes-to-Recommended + §C.2 estimate compression — per-platform sub-cluster 1-day cadence is now the empirical norm). NEW §B 2026-05-31 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (SIXTH build/deploy-session §B entry per Rule 18). NEW §B 2026-05-31 cross-reference pointer entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md` (extension-side architecture pointer per W2 Amazon + eBay precedent). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.

**Previously updated:** 2026-05-30 — Header bump only this session (`session_2026-05-30_p49-w2-ebay-sub-cluster-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 eBay sub-cluster ✅ DEPLOYED-AND-VERIFIED 2026-05-30 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — build + deploy + 5-fix-forward cascade + Phase-4-PASS bundled into ONE calendar day; Session 1 build commit `11e99e1` (3 files +1535/-99) carrying NEW `ebay-review-extractor.ts` (~390 LOC) + NEW `ebay-review-extractor.test.ts` (~470 LOC; 64 new node:test cases) + MODIFY `orchestrator.ts` (eBay dispatch) ff-merged to main under Rule 9 deploy gate; Vercel auto-redeploy fired; Phase 4 surfaced TWO co-occurring bugs (seller auto-detect failing + This Item filter returning All Items rows) that survived 3 speculative fix-forwards (FF#1+#2 bundled `23b6221` + FF#3 `c19f187`) before Rule 14f mid-Phase-4 picker rerouted to diagnostic-instrumentation FF#4 `6675963` (TEMPORARY HTML-dump + console.log probes; director uploaded 3 HTML files + console output) → FF#5 `23aa851` empirically-verified fix (3 files +176/-135 carrying `extractSellerFromListingHtml` regex against `"sellerUserName"` JSON data island + `[role=tabpanel]:not([hidden])` walker scope + remove FF#4 instrumentation + 8 new test cases); director Phase 4 PASS verdict on FF#5 "Everything worked perfectly". Closes (a.98); opens (a.99) RECOMMENDED-NEXT = P-49 W2 Etsy sub-cluster Session 1 on `workflow-2-competition-scraping` per §A.2 priority order + §C.2 implementation outline) — DEPLOY + 5-FIX-FORWARD session. **THREE Rule 14f forced-pickers fired** all director-Yes to Recommended (3/3 = 100% calibration data point; running cumulative 27/30 = 90% across recent 5 sessions). **FIVE Rule 9 deploy gates fired** (build deploy + FF#1+#2 bundled deploy + FF#3 deploy + FF#4 diagnostic deploy + FF#5 fix deploy — stretches 2026-05-28 Amazon DEPLOY 4-FF Pattern by one further). **Schema-change-in-flight flag STAYS NO entire session** (eBay reuses W2 Amazon schema shipped 2026-05-28). Pre-deploy /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **655 ext** / **838 src/lib** / **64 routes**); Check 6 Playwright SKIPPED per Rule 27 picker. Post-FF#5 5/5 GREEN at new ext baseline: extension `npm test` = **731/731** (+76 cumulative from 655); src/lib **838 UNCHANGED**; routes **64 UNCHANGED**. **NEW baseline locked: extension `npm test` = 731/731** (+76 from 655 entry baseline). ~12 pushes this session per `feedback_approval_scope_per_decision_unit.md`. ZERO DEFERRED carry-overs at session end (eBay sub-cluster fully ✅ DEPLOYED-AND-VERIFIED; §C.2 Sessions 2 + 3 CLOSED; entire eBay arc compressed into 1 calendar day). ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-30 capturing THREE NEW reusable Patterns ("Diagnostic-instrumentation FF as escape valve from speculative-FF antipattern" + "Tabpanel-scoped DOM walking via `[role=tabpanel]:not([hidden])`" + "JSON data-island extraction for server-rendered seller/product metadata") + 3 LOW informational sub-observations (P-43 cwd-leak Pattern Class reproduction running tally ~11+ + calibration data point 3/3 = 100% Yes-to-Recommended + §C.2 estimate compression). NEW §B 2026-05-30 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (FIFTH build/deploy-session §B entry per Rule 18). NEW §B 2026-05-30 cross-reference pointer entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md` (extension-side architecture pointer per W2 Amazon precedent). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-29 — Header bump only this session (`session_2026-05-29_p49-w4-captured-reviews-ui-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 4 Captured Reviews UI extensions ✅ DEPLOYED-AND-VERIFIED 2026-05-29 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — same session ID + same calendar day; session expanded mid-session from build-only to build + deploy + Phase-4-PASS via the end-of-session Rule 14f deploy-or-exit picker (director picked Recommended "Deploy now" over Exit/Defer-Phase-4); ff-merge `a40e4ba..1e610ce main -> main` carrying build commit `e89ae50` + build-session doc-batch `1e610ce` under ONE Rule 9 deploy gate; Vercel auto-redeploy fired; Phase 4 director real-Chrome verification 6/6 PASS on vklf.com; TWO Rule 14f forced-pickers fired this session (Session 1 scope picker 0/1 + deploy-or-exit picker 1/1 = 50% final calibration; running cumulative 24/27 = 88.9%); ONE Rule 9 deploy gate fired; Schema-change-in-flight flag STAYS NO entire session; W4 entire workstream now ✅ DEPLOYED-AND-VERIFIED with no remaining scope (§C.4 Sessions 2 + 3 CLOSED); P-43 cwd-leak Pattern Class reproduced 3 total this session (running tally ~7-8+); ONE NEW INFORMATIONAL sub-observation appended to existing CORRECTIONS_LOG §Entry 2026-05-29 capturing NEW reusable Pattern candidate "End-of-build-session deploy-or-exit Rule 14f picker"; FOUR pushes total this session. Closes (a.96) + (a.97); opens (a.98) RECOMMENDED-NEXT = P-49 W2 eBay sub-cluster Session 1 on `workflow-2-competition-scraping` per §A.2 priority order + §C.2 implementation outline. Originally landed via build commit `e89ae50` (9 files +2162/-43) — first build session of the Reviews Phase 2 W4 cluster (Captured Reviews UI extensions on vklf.com); all 3 §C.4 pieces shipped in ONE session per the Rule 14f scope picker outcome (director picked Option B "All three" over Recommended Option A "§C.4 split into Sessions 1+2") — star-count counter-bar with click-to-filter per §A.14 + drag-to-reorder via the `sortRank Int?` column shipped W2 Session 1 (reusing the P-46 W3 S3 @dnd-kit shared debounced-mutation Pattern from 2026-05-23-f) per §A.5 + bulk-delete with multi-select checkboxes + confirm modal + new batch-delete API route per §A.6; directly addresses 2026-05-28 Phase 4 verification issue #3. Closes (a.96); opens (a.97) RECOMMENDED-NEXT = P-49 W4 Captured Reviews UI extensions deploy session (bundle build commit `e89ae50` + this doc-batch under ONE Rule 9 gate; Vercel auto-redeploys vklf.com; Phase 4 director real-Chrome verification ~6-step walkthrough) on `workflow-2-competition-scraping` → `main`**) — pure CODE session. **EXACTLY ONE Rule 14f forced-picker fired this session** — Session 1 scope picker (director picked Option B "All three" over Recommended Option A "§C.4 split into Sessions 1+2"); NOT director-Yes-to-Recommended; calibration data point 0/1 = 0% this session; running cumulative across recent 4 sessions: 23/26 = 88.5%. **ZERO Rule 9 deploy gates fired** entire session (build commit stays on workflow branch until W4 deploy session per (a.97)). **Schema-change-in-flight flag STAYS NO** entire session (W4 is UI + 2 new API routes only; no schema work; underlying `sortRank Int?` column shipped W2 Session 1 + already in production via 2026-05-28 Amazon DEPLOY). Pre-build /scoreboard 5/5 GREEN at entry baselines (root tsc clean / extension tsc clean / **655 ext** / **786 src/lib** / **62 routes**); Check 6 SKIPPED per Rule 27. Post-build /scoreboard 5/5 GREEN at new baselines: extension **655/655 UNCHANGED** / **838 src/lib (+52)** / **64 routes (+2)**; Check 6 SKIPPED per Rule 27. **NEW baselines locked:** src/lib **838/838** (+52); npm run build **64 routes** (+2); extension **655/655 UNCHANGED**. ONE push only this session per `feedback_approval_scope_per_decision_unit.md` (workflow-branch push carrying build commit `e89ae50` + doc-batch commit; NO ff-merge to main per the build-session push pattern memorialized 2026-05-27). ZERO DEFERRED carry-overs at session end. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-29 capturing TWO NEW reusable Patterns ("Pure helpers extracted from .tsx component file for node:test coverage" + "Customers-say split: separate AI-summary row from main reviews via source discriminator") + LOW informational P-43 cwd-leak Pattern Class reproduction (2 more this session) + calibration data point (0/1 = 0% Yes-to-Recommended this session). NEW §B 2026-05-29 entry appended to `docs/REVIEWS_PHASE_2_DESIGN.md` (FOURTH build/deploy-session §B entry per Rule 18). `docs/COMPETITION_SCRAPING_DESIGN.md` UNCHANGED this session (W4 is purely PLOS-side UI + 2 new API routes). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-28 — Header bump only this session (`session_2026-05-28_p49-w2-amazon-deploy-and-fix-forwards` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Amazon ✅ DEPLOYED-AND-VERIFIED 2026-05-28 end-to-end on vklf.com via `workflow-2-competition-scraping` → `main`** — first production deploy of the Reviews Phase 2 implementation arc. Sessions 1 + 2 build commits + intervening doc-batches all ff-merged to main under ONE Rule 9 deploy gate as the initial deploy (`1914171..0ef8340`); Vercel auto-redeploy fired; fresh extension zip via `npm run zip`; Phase 4 director real-Chrome verification surfaced 4 distinct issues across 2 verification rounds; 3 fix-forward commits shipped under their own Rule 9 deploy gates (FF#1 `8bc2e7e` accept `/dp/<ASIN>` dispatch + FF#2+#3 `b55cdbd` bundled trigger-modal-per-star-checkboxes + progress-indicator-per-star-breakdown + FF#4 `f6944db` pageNumber-increment pagination); final director PASS verdict after FF#4. Closes (a.95); opens (a.96) RECOMMENDED-NEXT = P-49 W4 Captured Reviews UI extensions Session 1 (star-count counter-bar with click-to-filter per §A.14 + drag-to-reorder via `sortRank` column shipped in Session 1 + bulk-delete) on `workflow-2-competition-scraping`) — DEPLOY + FIX-FORWARD session. **NINE Rule 14f forced-pickers fired** all director-Yes to Recommended (9/9 = 100% calibration data point). **4 Rule 9 deploy gates fired** (initial deploy + FF#1 + FF#2+#3 + FF#4). **Schema-change-in-flight flag FLIPPED YES → NO at initial deploy push completion**; STAYED NO through 3 fix-forwards; final state NO. Pre-deploy /scoreboard 5/5 GREEN at expected baselines (root tsc clean / extension tsc clean / **633 ext matches Session 2 baseline** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27 picker. Post-FF#1 5/5 GREEN: **651/651 (+18)**. Post-FF#2+#3 5/5 GREEN: **655/655 (+4)**. Post-FF#4 5/5 GREEN: **655/655 UNCHANGED**. **NEW baseline locked: extension `npm test` = 655/655** (+22 cumulative from 633 entry baseline). ~10 pushes this session per `feedback_approval_scope_per_decision_unit.md`. ZERO DEFERRED carry-overs at session end (Sessions 1 + 2 carry-over RESOLVED via Phase 4 PASS verdict). ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-28 capturing THREE NEW reusable Patterns ("Phase 4 verification fix-forward cascade scales beyond N=5" + "FF#1 dispatch over-restriction antipattern at per-platform extractor layer" + "Findnextpage-link-selector empirically falsified → pageNumber-direct-increment more robust") + LOW informational P-43 cwd-leak Pattern Class reproduction sub-observation + calibration data point. NEW §B 2026-05-28 entries appended to BOTH `docs/REVIEWS_PHASE_2_DESIGN.md` (THIRD build/deploy-session §B entry per Rule 18) AND `docs/COMPETITION_SCRAPING_DESIGN.md` (extension-side cross-reference pointer). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-27 — Header bump only this session (`session_2026-05-27_p49-w2-amazon-session-2` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Amazon Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-27 on `workflow-2-competition-scraping` via build commit `1830074` (5 files +1054/-99) — second build session of the Reviews Phase 2 implementation arc atop the Session 1 foundation; cross-star navigation loop + helpful-count sort within star + `Customers say` AI-summary block capture + Shadow DOM trigger popup with per-URL cap override all shipped at code level under one commit. Schema-change-in-flight flag STAYS YES entire session carrying from Session 1's `npx prisma db push` — no schema work this session; STAYS YES until Amazon deploy session ships migration live on vklf.com. Major refactor of `amazon-review-extractor.ts` (+419/-93): NEW `AMAZON_STAR_FILTERS` const + `AmazonStarFilter` type + helpers; `runAmazonReviewScrape` refactored from "scrape current view" to ASIN-driven cross-star loop (extension of Session 1's fetch+DOMParser pagination Pattern); `AmazonScrapeContext.cap` → `capPerStar`; NEW `sortByHelpfulCountDesc` + `extractCustomersSayFromListing` helpers; `AmazonScrapeSaveInput.source` widened to discriminated union `'extension-scrape' | 'extension-scrape:customers-say'`. `orchestrator.ts` (+59/-37) — handler refactored to fire `openScrapeTriggerModal` BEFORE dispatching. NEW `scrape-trigger-modal.ts` (320 LOC) — Shadow DOM-mounted modal reusing P-47 mount pattern (SECOND consumer after Session 1's progress indicator) + NEW `clampCap` pure helper. NEW `scrape-trigger-modal.test.ts` (44 LOC; 5 cases). `amazon-review-extractor.test.ts` (+182; 17 new cases). TOTAL: 22 new node:test cases. Closes (a.94); opens (a.95) RECOMMENDED-NEXT = P-49 W2 Amazon DEPLOY session (bundle Sessions 1 + 2 build commits + intervening doc-batches as one ff-merge under Rule 9 gate; Vercel auto-redeploy; fresh extension zip via `npm run zip`; Phase 4 director real-Chrome verification on a real Amazon product page) on `workflow-2-competition-scraping` → `main`**) — pure CODE session. **EXACTLY ONE Rule 14f forced-picker fired this session** — the Customers-say block storage-shape picker (director picked Option A Recommended: starRating=5 sentinel + source="extension-scrape:customers-say" discriminator — no schema change, no wire-validator change, reversible). **ZERO Rule 9 deploy gates fired** entire session (build session only; build commits stay on workflow branch until Amazon deploy session per (a.95)). **Schema-change-in-flight flag STAYS YES** carrying from Session 1; STAYS YES through Session 2; FLIPS TO NO at Amazon deploy completion. Pre-build + post-build /scoreboard 5/5 GREEN at new baseline (root tsc clean / extension tsc clean / **extension `npm test` 633/633 +22 from Session 1 baseline 611** — exact match with the 22 new test cases / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27. **NEW baseline locked from this session:** extension `npm test` = **633/633**. ONE push only this session per `feedback_approval_scope_per_decision_unit.md` (workflow-branch push carrying build commit `1830074` + doc-batch commit; NO ff-merge to main this session per the drift analysis). ONE DEFERRED carry-over to the Amazon deploy session (first end-to-end real-Chrome extraction test on a real Amazon product page — carrying from Session 1; resolves AT the Amazon deploy session per (a.95) by definition). ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-27 capturing TWO NEW reusable Patterns ("Build-session end-of-session push pattern: ONE push to workflow branch, NOT two" + "Pre-emptive design choice rolled into Rule 14f picker") + LOW informational drift sub-observation (Session 1's planned 2-push pattern non-destructively recovered) + calibration data point (Session 2 within estimate; multi-session arcs compound). NEW §B 2026-05-27 entries appended to BOTH `docs/REVIEWS_PHASE_2_DESIGN.md` (SECOND build-session §B entry per Rule 18) AND `docs/COMPETITION_SCRAPING_DESIGN.md` (extension-side architecture cross-reference pointer). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-26 — Header bump only this session (`session_2026-05-26_p49-w2-amazon-session-1` — **W#2 polish P-49 Reviews Phase 2 Workstream 2 Amazon Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-26 on `workflow-2-competition-scraping` via build commit `422436f` (20 files +2069/-7) — first build session of the Reviews Phase 2 implementation arc; foundation session bundling schema + shared content-script infrastructure + Amazon DOM walker under one commit. Schema migration shipped via `npx prisma db push` (1.44s; additive only; zero data loss) per §A.13 + §A.16 — new `ReviewAnalysis` model + `ReviewAnalysisLevel` enum (PER_PRODUCT/PER_TYPE/PER_PROJECT) + `CapturedReview.sortRank Int?` + `.helpfulCount Int?` + `.platform String?` + `CompetitorUrl.reviewScrapeCap Int? @default(200)`. Shared content-script infrastructure landed: NEW `scrape-pagination.ts` (~310 LOC; conservative anti-bot defaults + AbortSignal-cancellable + structured event stream per §A.15) + NEW `scrape-progress-indicator.ts` (~230 LOC; Shadow DOM-mounted corner indicator reusing P-47 mount pattern per §A.3) + NEW `amazon-review-extractor.ts` (~300 LOC; per-row DOM walker against `[data-hook="review"]` selectors + parsers + `runAmazonReviewScrape` orchestrator using `fetch()` + `DOMParser` for pagination — avoids killing content-script via full-page navigation). Right-click context-menu wiring across `background.ts` + `messaging.ts` + `orchestrator.ts` — new `start-review-scrape` ContentScriptMessage + new `create-captured-review` BackgroundRequest + ASIN-matching dispatch. PLOS-side handlers + shared-types extended additively. 49 new extension node:test cases (`scrape-pagination.test.ts` +20 + `amazon-review-extractor.test.ts` +29). Closes (a.93); opens (a.94) RECOMMENDED-NEXT = P-49 W2 Amazon Session 2 (cross-star navigation loop + helpful-count sort within star + `Customers say` AI-summary block capture + trigger popup with per-URL cap override) on `workflow-2-competition-scraping`**) — pure CODE session. **ZERO Rule 14f forced-pickers fired this session** (design doc + launch prompt removed all ambiguity from (a.93) scope). **ZERO Rule 9 deploy gates fired** entire session (build commits stay on workflow branch; no production deploy). **Schema-change-in-flight flag flipped NO → YES at `npx prisma db push`** — stays YES until the eventual Amazon deploy session (~3-5 sessions from now). Pre-build + post-build /scoreboard 5/5 GREEN at expected new baselines (root tsc clean / extension tsc clean / **extension `npm test` 611/611 +49 from baseline 562** — exact match with the 49 new test cases / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27. **NEW baseline locked from this session:** extension `npm test` = **611/611**. TWO pushes planned per `feedback_approval_scope_per_decision_unit.md`. ONE DEFERRED carry-over to the eventual Amazon deploy session (first end-to-end real-Chrome extraction test on a real Amazon product page — deferred because PLOS-side handler changes aren't yet on production vklf.com). ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-26 capturing TWO NEW reusable Patterns ("Content-script pagination via `fetch()` + `DOMParser` avoids the full-page-navigation kill" + "Foundation session bundles schema + shared helpers + first per-platform module under one commit") + calibration data point + LOW informational sub-observation (pure-design-session investment pays off at first build session). NEW §B 2026-05-26 entries appended to BOTH `docs/REVIEWS_PHASE_2_DESIGN.md` (FIRST build-session §B entry per Rule 18 in the brand-new design doc) AND `docs/COMPETITION_SCRAPING_DESIGN.md` (extension-side architecture cross-reference pointer). `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-25-b — Header bump only this session (`session_2026-05-25-b_reviews-phase-2-design-session` — **W#2 polish P-49 Reviews Phase 2 DESIGN SESSION ✅ DONE 2026-05-25-b on `workflow-2-competition-scraping` — pure design session (NO code, NO builds, NO deploys, ZERO Rule 9 gates fired). Workflow Requirements Interview walked the 15-question scaffold preserved verbatim in yesterday's NEXT_SESSION.md; director answered all 15 forced-pickers — 14 direct-Yes-to-Recommended + 1 Q7 substantive refinement on the AI model-version selector. NEW standalone `docs/REVIEWS_PHASE_2_DESIGN.md` shipped (790 lines; §A frozen 15 decisions + §B empty + §C 4 active workstreams + W3 Crawler DROPPED placeholder per Q1 outcome). Revised P-49 scope post-design: 4 active workstreams instead of 5; revised estimate ~15-25 sessions instead of ~20-50. Closes (a.92) RECOMMENDED-NEXT = Reviews Phase 2 Design Session ✅ DONE 2026-05-25-b producing `docs/REVIEWS_PHASE_2_DESIGN.md`; opens (a.93) RECOMMENDED-NEXT = P-49 W2 Amazon Session 1 on `workflow-2-competition-scraping`. THREE Rule 14f forced-pickers fired beyond the 15 interview pickers. ZERO Rule 9 deploy gates fired. Schema-change-in-flight STAYS NO entire session; EXPECTED YES at W2 Amazon Session 1. /scoreboard NOT run (no code change to verify); baselines unchanged (562 ext / 786 src/lib / 62 routes all UNCHANGED). TWO pushes planned per `feedback_approval_scope_per_decision_unit.md`. ZERO DEFERRED items at session end. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-25-b (the FOURTH 2026-05-25-dated §Entry) capturing NEW reusable Pattern "Director-supplied per-platform DOM specs in capture-session memorialization pay off at design-session ingestion" + calibration data point (14/15 = 93.3% direct-Yes-to-Recommended rate suggests recommendation framing well-calibrated) + LOW informational sub-observation (Q1 outcome DROPPED entire W3 Crawler workstream — ~5-10 sessions saved on first interview question). NEW §B 2026-05-25-b cross-reference pointer entries appended to BOTH `docs/COMPETITION_DATA_V2_DESIGN.md` AND `docs/COMPETITION_SCRAPING_DESIGN.md` per the dual-doc P-23 + P-46 precedent. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-25 — Header bump only this session (`session_2026-05-25_reviews-phase-2-capture-session` — **W#2 polish Reviews Phase 2 scope-expansion CAPTURE SESSION ✅ DONE 2026-05-25 on `workflow-2-competition-scraping` — pure-capture session (NO code, NO builds, NO deploys, ZERO Rule 9 gates fired). Director surfaced the long-promised "next round of additions" at session-start before pre-build reads completed; Rule 14f session-direction picker chose pure capture (P-48 Session 3 deferred opportunistically). NEW `P-49 Reviews Phase 2` ROADMAP entry (hub-and-spokes mirroring P-46 — 5 internal workstreams: Design Session NEXT + per-platform extension extraction × 4 platforms + Crawler infrastructure CONDITIONAL + Captured Reviews UI extensions + AI review analysis system at 3 levels) + NEW `P-50 Condition Pathology card` ROADMAP entry (small `main`-branch placeholder card; card-only scope per Rule 14f picker — no W# renumbering). `docs/COMPETITION_DATA_V2_DESIGN.md` §A.1 updated with "RESOLVED 2026-05-25 — see ROADMAP P-49" cross-reference linking the original 2026-05-23 deferral to the now-captured P-49 hub**) — pure-capture session. **THREE Rule 14f forced-pickers fired** (session-direction Recommended: pure capture / capture-shape Recommended: hub-and-spokes for P-49 + card-only for P-50 / entry-approval + next-session Recommended: both entries approved + Reviews Phase 2 Design Session locked). **ZERO Rule 9 deploy gates fired**. **Schema-change-in-flight flag STAYS NO** entire session (pure ROADMAP + design-doc capture; no code; no schema). /scoreboard NOT run (no code change to verify); baselines unchanged from prior session (root tsc clean / extension tsc clean / **562 ext UNCHANGED** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27. Closes (a.91) RECOMMENDED-NEXT = P-48 Session 3 (Diagnostic #2) DEFERRED to opportunistic insertion via P-48 entry; opens (a.92) RECOMMENDED-NEXT = Reviews Phase 2 Design Session (Workflow Requirements Interview producing `docs/REVIEWS_PHASE_2_DESIGN.md`) on `workflow-2-competition-scraping`. ZERO Rule 9 gates fired; TWO pushes planned per `feedback_approval_scope_per_decision_unit.md` (doc-batch push + ff-merge push for doc-batch — neither invokes Rule 9 since operationally adjacent + no destructive operations); ZERO DEFERRED items at session end. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-25 (the THIRD 2026-05-25-dated §Entry — Reviews Phase 2 scope-expansion capture) capturing 4 sub-observations including NEW reusable Pattern "Mid-pre-build scope-expansion redirect — pause planned task; run Rule 24 searches; capture as ROADMAP entries; defer planned task" + LOW informational on long-deferred concerns resolving when director's mental model catches up + operational note that 15 interview questions are preserved verbatim in NEXT_SESSION.md ## Proposed interview question scaffold section for next session's Workflow Requirements Interview ingestion. NEW §B 2026-05-25 (Reviews Phase 2 capture session) entries appended to BOTH `docs/COMPETITION_DATA_V2_DESIGN.md` AND `docs/COMPETITION_SCRAPING_DESIGN.md` capturing today's outcome + P-49 + P-50 cross-reference + A.1 resolution. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-25 — Header bump only this session (`session_2026-05-25_p48-session-2-implementation-and-ff1-busted` — **W#2 polish P-48 Session 2 (Implementation + Fix-Forward #1 BUSTED) ✅ DEPLOYED-PARTIAL 2026-05-25 on vklf.com via `workflow-2-competition-scraping` → `main` — `fix-webm-duration` patch shipped end-to-end via build commit `279da34` (4 files +211/-9; DI-seam wiring + 4 new node:test cases) under Rule 9 gate #1; director-verified the seek-bar fix; Phase-4 surfaced playback stutter PERSISTS even with duration metadata correct; empirical ffprobe revealed every SCREEN_RECORDING webm captured at ~6-7 fps actual rate (not 30 fps); speculative FF-1 build commit `d8b9507` (1 file +25/-2; attach hidden video element to DOM) shipped under Rule 9 gate #2 but empirically BUSTED (post-FF-1 ffprobe 8.52 fps within noise); per director Rule 14f forced-picker outcome, remaining stutter work deferred to P-48 Session 3 (Diagnostic #2); FF-1 commit STAYS in production as calibration data point**) — BUILD + DEPLOY + FIX-FORWARD session. **ONE Rule 14f forced-picker fired** (deferred-vs-iterate after FF-1 BUSTED; director picked Defer to P-48 Session 3 Recommended). **TWO Rule 9 deploy gates fired** — Gate #1 (build commit `279da34`) + Gate #2 (FF-1 `d8b9507`); both director-Yes per `feedback_recommendation_style.md` recommended path. **Schema-change-in-flight flag STAYS NO** entire session. Pre-deploy /scoreboard 5/5 GREEN at new baselines (root tsc clean / extension tsc clean / **562 ext +4 from baseline 558** — exact match with 4 new fix-webm-duration test cases / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); post-merge partial; FF-1 post-edit /scoreboard 5/5 GREEN at unchanged baselines; Check 6 Playwright SKIPPED per Rule 27. **NEW baseline locked from this session:** extension `npm test` = **562/562**. Closes (a.90) RECOMMENDED-NEXT = P-48 Session 2 (Implementation) ✅ DEPLOYED-PARTIAL 2026-05-25; opens (a.91) RECOMMENDED-NEXT = P-48 Session 3 (Diagnostic #2) on `workflow-2-competition-scraping` — empirical instrumentation pass before any more structural fixes ship; estimated 30-60 min in-Claude. TWO Rule 9 gates fired; SIX pushes planned per `feedback_approval_scope_per_decision_unit.md` (Deploy #1 + ping-pong #1 + Deploy #2 + ping-pong #2 DONE; doc-batch push + ff-merge push pending). ZERO DEFERRED items at session end. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-25 (the SECOND 2026-05-25-dated §Entry) capturing 5 sub-observations including NEW reusable Pattern "Speculative fix-forward without empirical pre-verification = antipattern (reinforcement of Session 1's ffprobe-first Pattern at the fix-forward level)" + MEDIUM informational empirical 6-7 fps finding + calibration data point on session budget overrun + operational housekeeping note (one throwaway script deleted). NEW §B 2026-05-25 (Session 2) entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md` capturing implementation + Phase-4 PARTIAL + FF-1 BUSTED lifecycle + NEW reusable Pattern memorialization + empirical 6-7 fps source-file finding + locked Session 3 path. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session (P-48 is screen-recording capture mechanics — different design layer than W#2 Competition Data table). ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-25 — Header bump only this session (`session_2026-05-25_p48-session-1-diagnostic` — **W#2 polish P-48 Session 1 (Diagnostic) ✅ DONE 2026-05-25 on `workflow-2-competition-scraping` — pure diagnostic session on screen-recording playback stutters reported during 2026-05-24-f verification; ZERO code lands; root cause empirically identified via `ffprobe` + EBML inspection of 3 SCREEN_RECORDING webm files in production + `curl` HEAD/Range tests of Supabase Storage delivery — missing WebM duration metadata in MediaRecorder-produced container headers (every file shows `Duration: N/A` per ffprobe; EBML inspector confirms `format: duration: NOPTS`; canonical MediaRecorder limitation — EBML header written at recording-start before duration is known, never patched after `stop()`); pre-Session-1 working hypothesis "uncapped MediaRecorder bitrate" empirically FALSIFIED (`record-controller.ts:42` already exports `VIDEO_BITS_PER_SECOND_DEFAULT = 2_500_000`; actual files run 0.50 / 1.35 / 0.67 Mbps — well below the cap); pre-Session-1 file path drift corrected (`content-script/screen-recorder.ts` → actual `screen-recording/record-controller.ts`); Session 2 fix path locked = add `fix-webm-duration` npm dep (~3 KB library) + ~10 LOC in `record-controller.ts` post-stop / pre-`onStopped` injecting the JS-known duration into the EBML Segment Info Duration tag**) — pure diagnostic session ruling out capture-side bitrate (cap already shipped + files come in well under) + ruling out delivery-side (Supabase Storage + Cloudflare edge both clean) + identifying missing WebM duration metadata as empirical root cause. **ONE Rule 14f forced-picker fired** during diagnostic shaping (capture-flow scoping after my query bug filtering `sourceType: 'DIRECT_BYTES'` excluded the 3rd enum value `SCREEN_RECORDING`; re-query found 3 rows). **ZERO Rule 9 gates fired** (pure diagnostic). **Schema-change-in-flight flag STAYS NO** entire session. Pre-build + post-diagnostic /scoreboard NOT run (no code change to verify); baselines unchanged from prior session (root tsc clean / extension tsc clean / **558 ext UNCHANGED** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27. Closes (a.89) RECOMMENDED-NEXT = P-48 Session 1 (Diagnostic) ✅ DIAGNOSED 2026-05-25; opens (a.90) RECOMMENDED-NEXT = P-48 Session 2 (Implementation) on `workflow-2-competition-scraping` — small build + deploy session. **Director mid-session directive ending the active work:** *"i can't do any testing right now so add that to the next session and wrap up no"* — 15-step Chrome DevTools Network Panel walkthrough drafted in-session preserved verbatim in next NEXT_SESSION.md ## Pre-session notes section for director to run between sessions. ZERO Rule 9 gates fired; TWO pushes planned per `feedback_approval_scope_per_decision_unit.md` (doc-batch push + doc-batch ff-merge push — neither invokes Rule 9 since operationally adjacent + no destructive operations); ZERO DEFERRED items at session end. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-25 capturing 5 sub-observations including NEW reusable Pattern "ffprobe-first diagnostic reveals hypothesis-bust before any code lands — empirical inspection of artifacts in their actual production state preempts speculative implementation" + MEDIUM informational enum-coverage query-bug + calibration data point + diagnostic-script housekeeping note (6 throwaway scripts deleted at session end). NEW §B 2026-05-25 entry appended to `docs/COMPETITION_SCRAPING_DESIGN.md` capturing the diagnostic findings + file path correction + busted hypothesis lesson + locked Session 2 fix path + NEW reusable Pattern memorialization. `docs/COMPETITION_DATA_V2_DESIGN.md` UNCHANGED this session (P-48 is screen-recording playback mechanics — different design layer than W#2 Competition Data table). ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-24-f — Header bump only this session (`session_2026-05-24-f_p46-w4-phase4-fix-forward-1-then-w5-p47-phase4-pass-plus-p48-capture` — **W#2 polish bundled Phase-4 real-Chrome verification session ✅ DONE 2026-05-24-f — P-46 Workstream 4 + Workstream 5 + P-47 Shadow DOM refactor ALL ✅ DONE-AND-VERIFIED 2026-05-24-f end-to-end on vklf.com via `workflow-2-competition-scraping` → `main` fix-forward #1 ff-merge `5ed754e..d38b036` carrying 1 commit (W4 fix-forward `d38b036` — 5 files +292/-94 UI-only resolving 7 director-observed Phase-4 issues)**) — bundled Phase-4 verification session walking 3 deferred verification scripts in one sitting; Walkthrough (a) W4 Comprehensive Competitor Analysis page surfaced 7 director-observed issues; fix-forward #1 bundled all 7 fixes under ONE Rule 9 deploy gate (director picked "Deploy now — Recommended" per `feedback_recommendation_style.md` recommended path); Vercel auto-redeploy fired; director re-walked all 10 W4 Phase-4 steps post-redeploy → ALL PASS; Walkthroughs (b) W5 URL save form additions + (c) P-47 Shadow DOM mount paired on Amazon both PASS first-walk (zero fix-forwards needed for either). **TWO Rule 14f forced-pickers fired during fix-forward shaping** — Light-theme scope (sitewide editor light-theme) + Font-size feature (font-size stepper in W4 toolbar). **Schema-change-in-flight flag STAYS NO** entire session — UI-only fix-forward (no schema, no API, no shared-types). Pre-deploy /scoreboard 5/5 GREEN at unchanged baselines (root tsc clean / extension tsc clean / **558 ext UNCHANGED** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); post-merge /scoreboard partial (root tsc clean / extension tsc clean / src/lib 786/786 GREEN; Check 5 + Check 3 trusted at unchanged baselines via clean ff-merge); Check 6 Playwright SKIPPED per Rule 27. Closes (a.88) RECOMMENDED-NEXT = Bundled Phase-4 verification session ✅ DONE 2026-05-24-f; closes P-46 entire 5-workstream polish arc ✅ DONE-AND-VERIFIED end-to-end 2026-05-24-f; closes P-47 ✅ DONE-AND-VERIFIED 2026-05-24-f; opens (a.89) RECOMMENDED-NEXT = P-48 Session 1 (Diagnostic) on `workflow-2-competition-scraping`. ONE Rule 9 gate fired (fix-forward #1 deploy push); FOUR pushes planned per `feedback_approval_scope_per_decision_unit.md` (deploy push DONE; ping-pong push DONE; doc-batch push + ff-merge push pending); ZERO DEFERRED items at session end — all 3 prior standing carry-overs RESOLVED today via PASS verdicts. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-24-f capturing 6 sub-observations including 2 NEW reusable Patterns ("Bundled Phase-4 verification surfaces issues concentrated in newest surface" + "Editor save-on-Done race condition — debounced save + unmount cleanup") + NEW P-48 polish-backlog capture per Rule 24 search. NEW §B 2026-05-24-f entries appended to BOTH `docs/COMPETITION_DATA_V2_DESIGN.md` (W4 + W5 Phase-4 PASS lifecycle) AND `docs/COMPETITION_SCRAPING_DESIGN.md` (P-47 Phase-4 PASS + complete band-aid → structural-fix lifecycle). ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-24-e — Header bump only this session (`session_2026-05-24-e_bundled-w5-p47-deploy` — **W#2 polish P-46 Workstream 5 + P-47 Shadow DOM refactor BOTH ✅ DEPLOYED-PHASE-4-PENDING 2026-05-24-e on vklf.com via `workflow-2-competition-scraping` → `main` bundled ff-merge `9205340..d68885a` carrying 5 commits (W5 build `3c981be` + Reviews polish `41172f1` + W5 doc-batch `4d0f771` + P-47 build `d08f673` + P-47 doc-batch `d68885a`)**) — pure orchestration deploy session of 3 build commits + 2 doc-batch commits from `workflow-2-competition-scraping` to `main` under ONE Rule 9 gate (director picked "Deploy now — push to origin/main (Recommended)" per `feedback_recommendation_style.md`); Vercel auto-redeploy fired (~2-3 minute build + cache invalidation); fresh extension zip `plos-extension-2026-05-24-w2-deploy-34.zip` 202.98 KB dropped at repo root via `npm run zip` in `extensions/competition-scraping/`. **Headline outcome:** the extension URL save form now collects Type / Description-1 / Description-2 / Price at capture time on vklf.com end-to-end (W5); the Add Review modal duplicate-Save idempotency bug is fixed in production (Reviews polish — only ONE review row lands no matter how fast Save is clicked); the content-script video-capture form now mounts inside an open Shadow DOM root in production, structurally preventing page-level event interference WITHOUT the 80-event-listener band-aid (P-47). **Phase-4 director real-Chrome verification DEFERRED to next session at director directive *"defer to next session (per W4 deploy 2026-05-26 Pattern)"*** — 3 verification walkthroughs preserved verbatim in next NEXT_SESSION.md ## Standing carry-overs section (W4 10-step carried forward AGAIN as 4th consecutive defer + W5 4-step NEW today + P-47 2-step NEW today; the W5 + P-47 walkthroughs pair into ONE Phase-4 step at next session since both ship via the same extension form surface). **TWO §4 Step 1c forced-pickers fired** — (1) Rule 9 deploy gate picker (director picked "Deploy now — push to origin/main (Recommended)") + (2) Phase-4 in-session vs deferred picker (director picked "Defer to next session (per W4 deploy 2026-05-26 Pattern)" over the Recommended "Run in-session now"; THIRD time the bundled-Phase-4 defer Pattern fires). **Schema-change-in-flight flag STAYS NO** entire session — no schema work in any of the 3 build commits; W5 consumes already-deployed W1 schema; Reviews polish is React-component-only; P-47 is content-script DOM mount refactor. Pre-deploy + post-merge /scoreboard both 5/5 GREEN at unchanged baselines (root tsc clean / extension tsc clean / **558 ext UNCHANGED** / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per Rule 27 non-deploy-spec convention. **Closes (a.87) RECOMMENDED-NEXT = Bundled W5 + P-47 deploy session ✅ DEPLOYED-PHASE-4-PENDING 2026-05-24-e on vklf.com**; **opens (a.88) RECOMMENDED-NEXT = Bundled Phase-4 real-Chrome verification session** covering 3 walkthroughs (W4 10-step + W5 4-step + P-47 2-step) on `workflow-2-competition-scraping`. ONE Rule 9 gate fired (deploy push); THREE pushes planned per `feedback_approval_scope_per_decision_unit.md` (deploy push DONE; end-of-session doc-batch push to `origin/workflow-2-competition-scraping`; end-of-session ff-merge push to `origin/main` for doc-batch — operationally adjacent; does NOT re-invoke Rule 9). THREE DEFERRED items carry to next session as standing carry-overs (Rule 26) — P-46 W4 Phase-4 verify (4th consecutive defer) + P-46 W5 Phase-4 verify (1st defer) + P-47 Phase-4 verify (1st defer); all 3 walkthroughs preserved verbatim in next NEXT_SESSION.md ## Standing carry-overs section. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-24-e capturing (a) bundled-deploy outcome (5 commits ff'd under ONE Rule 9 gate; 5/5 GREEN pre + post); (b) NEW reusable Pattern "Bundled-build-commit deploy under ONE Rule 9 gate" (first cross-workstream bundle — P-46 W5 + P-46 Reviews polish + P-47 Shadow DOM shipped together as 3 distinct polish items behind one deploy gate); (c) LOW informational sub-observation TWO more P-43 cwd-leak reproductions (Check 5 pre-deploy + Check 3 post-merge; 6th+ reproduction class); (d) calibration data point — bundled deploy landed cleanly without fix-forwards (Phase-4 deferred so no cascade possible). ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-24-d — Header bump only this session (`session_2026-05-24-d_p47-shadow-dom-refactor-session-1` — **W#2 polish P-47 Shadow DOM refactor Session 1 (content-script video-capture-form mount strategy refactor from `document.body` + 80-event-listener band-aid to open Shadow DOM root with `FORM_CHROME_CSS` injected inside the shadow + band-aid deletion) ✅ DONE-AT-CODE-LEVEL 2026-05-24-d on `workflow-2-competition-scraping` via build commit `d08f673` (2 files +203/-220, net -17 LOC)**) — pure CODE session on `workflow-2-competition-scraping`; NOT pushed to main this session (next session is the bundled W5 + P-47 deploy session per the end-of-session §4 Step 1c picker outcome); build commit sits on workflow branch alongside the 2 prior W5 build commits (`3c981be` + `41172f1`) all awaiting the next deploy session. The content-script video-capture form now mounts inside an open Shadow DOM root attached to a fixed-positioned host `<div>` in `document.body`, rather than appending the backdrop directly into `document.body`; events fired inside the shadow root do not surface to page-level handlers by default, so the per-input event-isolation band-aid shipped in P-45 Build #2 2026-05-22-i (20 events × 4 inputs = 80 listeners on each form open) is no longer needed and has been deleted (the `applyAggressiveEventIsolation` function + its 4 call sites + its 20-line context comment, 62 LOC total). `styles.ts` extracts a new exported `FORM_CHROME_CSS` constant containing the `.plos-cs-form-*` + `.plos-cs-chip-*` rules previously inlined in `CONTENT_SCRIPT_CSS` and re-interpolates them back via `${FORM_CHROME_CSS}` so the other forms (image / text / url-add) that still mount to `document.body` continue to receive these rules via the host-page `<head>` stylesheet without drift between the two consumption sites. **W#4 Phase-4 director real-Chrome verification deferred a THIRD consecutive session at director directive** — 10-step verification walkthrough preserved verbatim AGAIN in next NEXT_SESSION.md ## Standing carry-overs section for a future session to copy + execute. **ONE §4 Step 1c forced-picker fired** — End-of-session next-session scope picker: picker offered (A) Bundled W5 + P-47 deploy to main (Recommended) / (B) P-26 below-fold scroll capture Session 1 / (C) P-43 mechanical prevention small fix / (D) question first; director picked A. **Schema-change-in-flight flag STAYS NO** — pure content-script DOM mount refactor; no schema, no API, no shared-types. Pre-build + post-refactor /scoreboard both 5/5 GREEN at unchanged baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **786 src/lib UNCHANGED** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED. Closes (a.86) RECOMMENDED-NEXT = P-47 Shadow DOM refactor Session 1; opens (a.87) RECOMMENDED-NEXT = Bundled W5 + P-47 deploy session on `workflow-2-competition-scraping` → `main`. ZERO Rule 9 gates fired; ONE DEFERRED item carries to next session as standing carry-over (Rule 26) — P-46 W4 Phase-4 real-Chrome verification (third consecutive defer); three prior standing carry-overs RESOLVE at next session's bundled deploy (P-46 W5 deploy + P-46 W5 Phase-4 verify + P-47 Session 1 Phase-4 verify); ONE push planned at end-of-session to `origin/workflow-2-competition-scraping` carrying the build commit + this doc-batch commit per `feedback_approval_scope_per_decision_unit.md`. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-24-c — Header bump only this session (`session_2026-05-24-c_p46-workstream-5-session-1-extension-url-form-additions-and-reviews-modal-polish` — **W#2 polish P-46 Workstream 5 Session 1 (Extension URL save form additions — Type/Description-1/Description-2/Price) ✅ DONE-AT-CODE-LEVEL 2026-05-24-c on `workflow-2-competition-scraping` via build commit `3c981be` (4 files +225/-0) + opportunistic Reviews modal idempotency polish ✅ DONE-AT-CODE-LEVEL 2026-05-24-c via build commit `41172f1` (1 file +9/-1) fixing the `CapturedReviewAddModal` clientId regen-on-every-Save bug from W2 Session 4 (2026-05-28) that defeated server-side P2002 dedup**) — pure CODE session on `workflow-2-competition-scraping`; NEITHER commit pushed to main (W5 deploy DEFERRED at director request). Extension URL save form now collects Type / Description-1 / Description-2 / Price at capture time via a new `makeTextareaField()` helper alongside existing `makeField()`, plus the POST urls handler allowlist + `CreateCompetitorUrlRequest` wire type extended additively. The Reviews modal duplicate-Save idempotency bug fixed in the React component layer via a `useState` clientId seeded per modal-open + reset on close. **W#4 Phase-4 director real-Chrome verification deferred a second consecutive session at director directive** — 10-step verification walkthrough preserved verbatim in next NEXT_SESSION.md ## Standing carry-overs section for a future session to copy + execute. **TWO §4 Step 1c forced-pickers fired** — (1) Session-start picker: director deferred original launch-prompt task (W4 Phase-4 verify) + chose to "work on next item on roadmap"; resolved via clarifying picker to **P-46 Workstream 5 (Recommended)** over P-47 Shadow DOM + P-43 mechanical prevention. (2) End-of-session next-session scope picker: director's free-text "Other" answer *"for the next session, defer any real world testing items that I need to do. instead work on the next item on our road map for workflow#2"* + follow-up picker resolved to **P-47 Shadow DOM refactor Session 1 (Recommended)** over P-26 + P-43. **Schema-change-in-flight flag STAYS NO** — no `prisma db push`; W5 consumes already-deployed W1 schema; Reviews modal polish is React-component-only. Pre-deploy /scoreboard after W5 build commit `3c981be` 5/5 GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **786 src/lib +3 from baseline 783** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED. Post-polish /scoreboard after Reviews polish commit `41172f1` 5/5 GREEN at unchanged baselines. Closes (a.85) RECOMMENDED-NEXT = P-46 Workstream 5 Session 1 build (opened + closed today); (a.84) RECOMMENDED-NEXT = P-46 W4 Phase-4 verification STAYS OPEN (deferred a second consecutive session — standing carry-over); opens (a.86) RECOMMENDED-NEXT = P-47 Shadow DOM refactor Session 1 on `workflow-2-competition-scraping`. ZERO Rule 9 gates fired; THREE DEFERRED items captured for next session at end-of-session sweep (Rule 26) — Task #1 W4 Phase-4 real-Chrome verification (carried from start of session) + Task #11 W5 deploy session (NEW today) + Task #12 W5 Phase-4 real-Chrome verification (NEW today); destinations all captured in next NEXT_SESSION.md ## Standing carry-overs section; ONE push planned at end-of-session to `origin/workflow-2-competition-scraping` carrying the 2 build commits + this doc-batch commit per `feedback_approval_scope_per_decision_unit.md`. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-26 — Header bump only this session (`session_2026-05-26_p46-workstream-4-deploy-session-phase-4-verification-deferred` — **W#2 polish P-46 Workstream 4 (Comprehensive Competitor Analysis page) ✅ DEPLOYED-PHASE-4-PENDING 2026-05-26 on vklf.com via `workflow-2-competition-scraping` → `main` ff-merge `cafd3ed..096a2ac` carrying 4 commits (W4 S1 build `283d4d1` + W4 S1 doc-batch `8b30ab3` + W4 S2 build `5854eff` + W4 S2 doc-batch `096a2ac`)**) — pure deploy session of the W4 build sessions that landed cleanly at code level 2026-05-24-b + 2026-05-25; ff-merge fired against `origin/main` under ONE Rule 9 gate; Vercel auto-redeploy fired. **Phase-4 director real-Chrome verification DEFERRED to next session at director request** — full 10-step verification walkthrough drafted in-session + preserved verbatim in `docs/NEXT_SESSION.md`. W4 status now `✅ DEPLOYED-PHASE-4-PENDING` on vklf.com. **TWO additional pickers fired beyond the Rule 9 gate** — (1) Truncated-picker-response handling NEW informational Pattern observation: director's first AskUserQuestion answer rendered as truncated string "deploy now but defer any real wor"; Claude fired a clarifying AskUserQuestion picker; director picked "Deploy now — but pause BEFORE the push to main" (effectively default Rule 9 behavior). **Memorialize as reusable Pattern "Truncated picker response → fire clarifying picker, don't silently interpret"**. (2) Rule 27 Check 6 Playwright SKIP vs RUN picker — director picked SKIP. **Schema-change-in-flight flag STAYS NO** — no `prisma db push`; no schema work in W4 at all. Pre-deploy + post-merge /scoreboard both 5/5 GREEN at expected W4 S2 baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 783 src/lib UNCHANGED / **62 routes UNCHANGED**; Check 6 SKIPPED per Rule 27). **TWO P-43 cwd-leak reproductions on Check 5 (LOW informational; 5th+ reproduction in Pattern class)** — caught + recovered with absolute cd retry; mechanical prevention candidate flagged (add absolute cd prefix to ALL scoreboard.md Bash commands, specifically Check 5). Closes (a.83) RECOMMENDED-NEXT = P-46 Workstream 4 deploy session ✅ DEPLOYED-PHASE-4-PENDING 2026-05-26 on vklf.com; opens (a.84) RECOMMENDED-NEXT = P-46 Workstream 4 Phase-4 verification session — director performs 10-step real-Chrome verification walkthrough drafted this session (preserved verbatim in NEXT_SESSION.md). ONE Rule 9 gate fired (initial deploy push); ZERO new DEFERRED items at session end (Rule 26) — Task #10 `DEFERRED: P-46 W4 Phase-4 real-Chrome verification` created + closed in same end-of-session pass after NEXT_SESSION.md + ROADMAP P-46 entry both received the destination capture; THREE pushes this session (1 deploy to origin/main + 1 doc-batch to origin/workflow-2-competition-scraping + 1 end-of-session ff-merge to origin/main for doc-batch) per `feedback_approval_scope_per_decision_unit.md`. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-25 — Header bump only this session (`session_2026-05-25_p46-workstream-4-session-2-internal-hyperlink-tiptap-extension` — **W#2 polish P-46 Workstream 4 (Comprehensive Competitor Analysis page) Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-25 on `workflow-2-competition-scraping`** — second build session of the P-46 Workstream 4 implementation arc (Session 2 of ~2-3 estimated per §C.4); landed the full §C.4 Session 2 scope end-to-end at code level — the internal-hyperlink TipTap extension that lets the director write `#url/<urlId>` shorthand inline and have it render as a clickable hyperlink to the corresponding URL detail page — NEW `url-reference-helpers.ts` (~115 LOC pure helpers) + NEW `url-reference-helpers.test.ts` (19 new node:test cases bringing src/lib to **783 from baseline 764**) + NEW `UrlReferenceExtension.ts` (~105 LOC TipTap Extension via `addProseMirrorPlugins`) + NEW `LinkToUrlPicker.tsx` (~304 LOC toolbar dropdown — lazy-loads URL list via `authFetch`; case-insensitive search filter) + MODIFIED `RichTextEditor.tsx` (+90/-1 LOC; accepts optional `projectId` prop; registers `UrlReferenceExtension`; extends `Link.configure`'s `isAllowedUri` to accept `#url/` hrefs; surfaces `LinkToUrlPicker` in 'full' toolbar when `projectId` provided; inline `<style>` with `InternalLinkStyles` for distinct styling — 🔗 emoji glyph prefix + same blue underlined per session-start picker outcome) + MODIFIED `AnalysisEditor.tsx` + `AnalysisReadView.tsx` + `comprehensive-analysis/page.tsx` all pass `projectId` through. Build commit `5854eff` — 8 files +786/-1 on `workflow-2-competition-scraping`; NOT pushed to main since Session 2 is a build session, not a deploy session. **TWO Rule 14f forced-pickers fired at session start** — Session 2 scope (Extension + Link-to-URL picker recommended) + Visual styling of internal hyperlinks (Distinct — small URL icon prefix + same blue recommended). **ONE new reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-25 — "Custom TipTap extension via `addProseMirrorPlugins` for click interception of shorthand hrefs without a custom Mark type"** (pairs with W4 S1's Pattern A + Pattern B as the third W4-arc Pattern). **Schema-change-in-flight flag STAYS NO** — no `prisma db push`. All 6 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **783 src/lib +19 from baseline 764** / **62 routes UNCHANGED**); Check 6 Playwright SKIPPED per non-deploy-session convention. Closes (a.82) RECOMMENDED-NEXT = P-46 Workstream 4 Session 2; opens (a.83) RECOMMENDED-NEXT = P-46 Workstream 4 deploy session — Phase-4 deploy ff-merging `workflow-2-competition-scraping` → `main` carrying W4 Sessions 1+2 build commits + doc-batch commits. ZERO Rule 9 gates fired; ZERO new DEFERRED items at session end (Rule 26) — all 7 TaskCreate tasks completed; ONE push planned at end-of-session to `origin/workflow-2-competition-scraping` per `feedback_approval_scope_per_decision_unit.md`. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-24-b — Header bump only this session (`session_2026-05-24-b_p46-workstream-4-session-1-comprehensive-analysis-page` — **W#2 polish P-46 Workstream 4 (Comprehensive Competitor Analysis page) Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-24-b on `workflow-2-competition-scraping`** — first build session of the P-46 Workstream 4 implementation arc (Session 1 of ~2-3 estimated per §C.4); landed the full §C.4 Session 1 scope end-to-end at code level — NEW `comprehensive-analysis.ts` DI-seam handler factory (~225 LOC mirroring `user-table-preferences.ts` precedent) + NEW `comprehensive-analysis.test.ts` (20 new node:test cases bringing src/lib to **764 from baseline 744**) + W1's 501-stub at `comprehensive-analysis/route.ts` replaced by thin DI-seam shim + NEW page route `comprehensive-analysis/page.tsx` (~269 LOC) + NEW `AnalysisEditor.tsx` (~151 LOC parallel-component to W2 S1's PerItemAnalysisBox) + NEW `AnalysisReadView.tsx` (~76 LOC; renders via `RichTextEditor readOnly=true` to keep typography identical across read/edit modes) + extended `RichTextEditor.tsx` variant='full' toolbar per §A.5 + standalone navigation button row above `ColumnVisibilityBar` in `CompetitionScrapingViewer.tsx` (+31 LOC) + `@tiptap/extension-underline@3.23.6` promoted to top-level dep. Build commit `283d4d1` — 9 files +1258/-32 on `workflow-2-competition-scraping`; NOT pushed to main since Session 1 is a build session, not a deploy session. **TWO Rule 14f forced-pickers fired at session start** — Session 1 scope (Full §C.4 scope recommended) + Navigation surface placement (Standalone button above the bar recommended). **FIRST APPLICATION of the 2026-05-24 Rule 14a tightening** — §C.4 vs. pointer divergence on Session 1 scope surfaced via Rule 14f rather than silently picking the pointer's recommendation. **TWO new reusable Patterns memorialized in CORRECTIONS_LOG §Entry 2026-05-24-b** — Pattern A "Per-Project edit-affordance parallel to per-row edit-affordance" + Pattern B "Editor-as-readonly substitutes for a separate static renderer". **Schema-change-in-flight flag STAYS NO** — no `prisma db push`; consumes the existing `ComprehensiveCompetitorAnalysis` model. All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **764 src/lib +20 from baseline 744** / **61 routes UNCHANGED** — W1 501-stub at existing path filled in); Check 6 Playwright SKIPPED per non-deploy-session convention. Closes (a.81) RECOMMENDED-NEXT = P-46 Workstream 4 Session 1; opens (a.82) RECOMMENDED-NEXT = P-46 Workstream 4 Session 2 — internal-hyperlink TipTap extension per §C.4 Session 2 spec. ZERO Rule 9 gates fired; ZERO new DEFERRED items at session end (Rule 26) — all 17 TaskCreate tasks completed; ONE push planned at end-of-session to `origin/workflow-2-competition-scraping` per `feedback_approval_scope_per_decision_unit.md`. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-24 — Header bump only this session (`session_2026-05-24_p46-workstream-3-deploy-and-five-fix-forwards` — **W#2 polish P-46 Workstream 3 (Competition Data table redesign) SHIPPED + DEPLOYED + REAL-CHROME-VERIFIED on vklf.com 2026-05-24 via `workflow-2-competition-scraping` → `main` initial ff-merge `51e68f8..c727da9` carrying 6 commits + 5 in-session fix-forwards `f8293f1` → `0703174` → `712efa0` → `7358963` → `ac45737` resolving 11 Phase-4 verification issues end-to-end** — pure deploy session converting Workstream 3's three build sessions into 6 production deploys to vklf.com in one calendar day; 11 distinct UI issues surfaced at Phase-4 director verification + all 11 resolved + all 11 re-verified PASS on vklf.com after fix-forward #5; director's exact end-of-session words: *"pass"*. **Headline outcome: P-46 Workstream 3 ✅ DONE-AND-VERIFIED 2026-05-24 on vklf.com end-to-end.** Combined W1+W2+W3 spend = 11 sessions vs. 7-11 estimated per §C.1/§C.2/§C.3. **Six deploy pushes to `origin/main` today — highest count in any single session to date** (initial deploy + fix-forwards #1-5); ~14 pushes total. Pre-deploy /scoreboard 5/5 GREEN at baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 744 src/lib UNCHANGED / 61 routes UNCHANGED); Check 6 Playwright SKIPPED per Rule 27. Post-merge /scoreboard 5/5 GREEN at same baselines. **NEW reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-24 — "Phase-4 verification fix-forward cascade in a single deploy session"** (pairs with the P-45 Build #2 2026-05-22-i fix-forward Pattern but extends to N≥5 fix-forwards in one session showing the pattern scales when issues are scoped + reversible + UI-only). **MEDIUM informational Rule 18 spec-capture gap surfaced + mechanical prevention added to working methodology** (any UI-shape spec given mid-build must be echoed into binding docs + read back per Rule 14a BEFORE implementation lands). **Schema-change-in-flight flag STAYS NO** entire session — ZERO `prisma db push`. Closes (a.80) RECOMMENDED-NEXT = P-46 Workstream 3 deploy session ✅ DONE-AND-VERIFIED 2026-05-24 on vklf.com; opens (a.81) RECOMMENDED-NEXT = P-46 Workstream 4 (Comprehensive Competitor Analysis page) first build session per Q10's locked sequencing — ~2-3 sessions estimated per §C.4. SIX Rule 9 gates fired (one per deploy push); ZERO new DEFERRED items at session end (Rule 26) — all 19 TaskCreate tasks completed; ~14 pushes total today per `feedback_approval_scope_per_decision_unit.md`. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-23-f — Header bump only this session (`session_2026-05-23-f_p46-workstream-3-session-3-column-resize-drag-reorder-font-size` — **W#2 polish P-46 Workstream 3 (Competition Data table redesign) Session 3 ✅ DONE-AT-CODE-LEVEL 2026-05-23-f on `workflow-2-competition-scraping`** — third + final build session of the P-46 Workstream 3 implementation arc (Session 3 of 3-4 estimated per §C.3 — landed inside the estimated window); landed the bundled-scope Session 3 deliverables — **column resize** (drag the right edge of any column header; widths persist per-user-per-project via `UserTablePreferences.columnWidths`; clamped 60-600px) + **drag-to-reorder rows** (grab the ⋮⋮ handle on the left edge of any row; row order persists via `UserTablePreferences.rowOrder`; built on `@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities`) + **table-wide font-size stepper** (− / Npt / + at the right of the horizontal bar; 10pt-24pt range per W1's `fontSize` Prisma constraint; persists via `UserTablePreferences.fontSize`). All three controls route through Session 1's `prefsTimerRef` 500 ms debounced PUT lifecycle so a burst of drag events from any control coalesces into one network write. Build commit `7ad7eff` — 6 files +781/-178 on `workflow-2-competition-scraping`; NOT pushed to main since Session 3 is a build session, not a deploy session. **Three new npm dependencies landed** (`@dnd-kit/core` + `@dnd-kit/sortable` + `@dnd-kit/utilities` — first time the Competition Data table layer takes a runtime drag-and-drop library; ~30 KB gzipped one-time cost). **ONE Rule 14f forced-picker fired this session** — session-start scope-pick: director picked "Bundle all 3 controls (recommended)" over partial-bundle alternatives per `feedback_recommendation_style.md`. **NEW reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-23-f — "Shared debounced-mutation lifecycle reused across an N-control surface"** (pairs with W2 S5's "Field-allowlist subset extraction" Pattern as another example of foundation-session primitive becoming Pattern when reused by N=2+ subsequent sessions). **Schema-change-in-flight flag STAYS NO** — no `prisma db push`; all three fields already in `UserTablePreferences` from W1's 2026-05-24 schema. All 5 /scoreboard checks GREEN at baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **744 src/lib UNCHANGED** — no new server-side code; UI-only session per pointer expectation / **61 routes UNCHANGED**); Check 6 Playwright SKIPPED per non-deploy-session convention. Closes (a.79) RECOMMENDED-NEXT = P-46 Workstream 3 Session 3; opens (a.80) RECOMMENDED-NEXT = P-46 Workstream 3 deploy session — Phase-4 deploy ff-merging `workflow-2-competition-scraping` → `main` (carries Sessions 1+2+3's build commits + the three doc-batch commits as one fast-forward, 6 commits total) → Vercel auto-redeploy → ping-pong sync → Phase-4 director real-Chrome cross-platform verify across the Competition Data table surfaces. ZERO Rule 9 gates fired; ZERO new DEFERRED items at session end (Rule 26) — all 5 TaskCreate tasks completed; ONE push planned at end-of-session to `origin/workflow-2-competition-scraping` per `feedback_approval_scope_per_decision_unit.md`. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-23-e — Header bump only this session (`session_2026-05-23-e_p46-workstream-3-session-2-click-to-edit-cell-editors-and-eight-new-data-columns` — **W#2 polish P-46 Workstream 3 (Competition Data table redesign) Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-23-e on `workflow-2-competition-scraping`** — second build session of the P-46 Workstream 3 implementation arc (Session 2 of 3-4 estimated per §C.3); landed the full §A.2 click-to-edit cell editors (every cell becomes its appropriate inline editor on click — text / multiline-text / integer / decimal / boolean / enum / date / url via new `InlineCells.tsx` ~800 LOC compact in-table editor primitives parallel-component-set to URL detail page's `EditableField.tsx`) + 8 new data columns (5 W1-additive — `type` / `description1` / `description2` / `price` / `competitionScore`; 3 pre-existing-but-unsurfaced — `resultsPageRank` / `sellerStarRating` / `numSellerReviews`) bringing the Competition Data table from 9 to 17 columns + extended the `urls/[urlId]` PATCH route allowlist for `competitionScore` via NEW `competition-score-validation.ts` trust-boundary helper (direct application of W2 S5's "Field-allowlist subset extraction" reusable Pattern; 18 new node:test cases bringing src/lib to 744) + whole-row `onClick={handleRowOpen}` removed in favor of explicit per-row "↗" Open button in row-actions column (UX behavior change mandated by §A.2 since whole-row onClick conflicted with cell-level click-to-edit). Build commit `899afd4` — 7 files +1414/-91 on `workflow-2-competition-scraping`; NOT pushed to main since Session 2 is a build session, not a deploy session. **Rule 14f forced-picker fired ONCE this session** — session-start scope-pick: director picked "Bundle: new columns + click-to-edit together (recommended)" over "Click-to-edit on existing columns only" per `feedback_recommendation_style.md`. **NEW reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-23-e — "In-table inline-cell parallel-component set to URL-detail-page EditableField primitives"** (pairs with W2 Session 3's "OverallAnalysisBox parallel to PerItemAnalysisBox" Pattern as meta-shape covering parallel-component for visual-context-divergence across both EDIT + ANALYSIS surfaces). **Schema-change-in-flight flag STAYS NO** — no `prisma db push` this session; the 8 new data columns are all already in `CompetitorUrl` from W1's 2026-05-24 schema; default expectation: stays NO through Session 3 (UI-only consuming existing schema). All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **744 src/lib node:test +18 from baseline 726** / **61 routes UNCHANGED**); Check 6 Playwright SKIPPED per non-deploy-session convention. Closes (a.78) RECOMMENDED-NEXT = P-46 Workstream 3 Session 2; opens (a.79) RECOMMENDED-NEXT = P-46 Workstream 3 Session 3 — column resize (drag column edges) + drag-to-reorder rows + adjustable font size per §C.3 Session 3 spec; consumes existing `columnWidths` / `fontSize` / `rowOrder` JSON columns on `UserTablePreferences` from W1's schema. ZERO Rule 9 gates fired; ZERO new DEFERRED items at session end (Rule 26) — all 6 TaskCreate tasks completed; ONE push planned at end-of-session to `origin/workflow-2-competition-scraping` per `feedback_approval_scope_per_decision_unit.md`. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-23-d — Header bump only this session (`session_2026-05-23-d_p46-workstream-3-session-1-table-preferences-and-column-visibility-bar` — **W#2 polish P-46 Workstream 3 (Competition Data table redesign) Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-23-d on `workflow-2-competition-scraping`** — first build session of the P-46 Workstream 3 implementation arc (Session 1 of 3-4 estimated per §C.3); landed the foundational `UserTablePreferences` plumbing + horizontal `ColumnVisibilityBar` at the top of the Competition Data table + deleted the left-side `PlatformSidebar`; column visibility persists per-user-per-project via auth-derived-userId path with cross-device sync. Build commit `d846a97` — 10 files +1369/-224. Path-convention refactor surfaced at Rule 14f session-start: W1 originally scaffolded the table-preferences route under `/api/users/[userId]/table-preferences/[projectId]` which diverged from W#2's auth-derived-userId convention (extension-state + every other W#2 project-scoped route); director picked "Refactor — match platform convention (recommended)" per `feedback_recommendation_style.md` — the W1 501-stub comment explicitly flagged this as a "surface before next workstream lands" item. NEW reusable Pattern memorialized in CORRECTIONS_LOG §Entry 2026-05-23-d — **"Foundation-workstream path-convention drift surfaced by next-workstream session-start picker"** — when a foundation workstream scaffolds a route at a path that diverges from the platform's convention, the next workstream's session-start should fire a Rule 14f picker on the path before implementation lands (pairs with the 2026-05-28 Session 4 "W1 path-divergence finding + architectural-move resolution" Pattern but for URL-shape rather than nesting depth). NEW `src/lib/competition-scraping/handlers/user-table-preferences.ts` (~340 LOC DI-seam handler factory mirroring `url-text.ts` / `reviews-by-id.ts` precedent — exports `extractTablePreferencesPatch` strict trust-boundary validator with field allowlist for the 6 editable fields per `WriteUserTablePreferencesRequest` + `toWireShape` coercing Prisma JsonValue columns to typed shapes with bad-DB-shape fallback + `makeUserTablePreferencesHandlers` factory returning GET + PUT) + 34 new node:test cases; NEW `src/app/api/projects/[projectId]/competition-scraping/table-preferences/route.ts` (~70 LOC thin shim wiring DI seam to `verifyProjectAuth` + `withRetry` + `recordFlake` + CORS); DELETED W1's `/api/users/[userId]/table-preferences/[projectId]/route.ts` 501-stub at the old path; NEW `ColumnVisibilityBar.tsx` (~180 LOC horizontal bar with two checkbox groups — Platforms All+7 single-select preserving all 7 platforms from the deleted sidebar + Columns 9 toggles from `TABLE_COLUMN_DEFS` multi-select) + `isColumnVisible(map, columnId)` exported helper with missing-key-default-true semantics matching §A.3; NEW `url-table-columns.ts` (~35 LOC canonical `TABLE_COLUMN_DEFS` registry + `TABLE_COLUMN_IDS` Set + relocated `ScopeFilter` type from PlatformSidebar); MODIFIED `CompetitionScrapingViewer.tsx` (drops 220px grid + sidebar layout for vertical stack — bar above table; adds `columnVisibility` state seeded from GET at mount with silent 404/network fallback + `handleToggleColumn` with 500ms debounced PUT via `prefsTimerRef`); MODIFIED `UrlTable.tsx` (adds optional `columnVisibility` prop + `visibleColumns` `useMemo` with missing-key-default-true semantics + extracted cell renderers into a `Record<SortKey, (row) => ReactNode>` map so the tbody iterates `visibleColumns.map((col) => cellRenderers[col.key](row))` without duplicate visibility branches per column; trash-button column stays unfiltered as row-action surface); DELETED `PlatformSidebar.tsx`. **Schema-change-in-flight flag STAYS NO** — no `prisma db push` this session; consumes existing Workstream 1 schema; default expectation: stays NO through Sessions 2-3 (UI-only consuming existing schema). All 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / **726 src/lib node:test +34 from baseline 692** — exact match with 34 new user-table-preferences.test.ts cases / **61 routes UNCHANGED** — new table-preferences route replaced deleted W1 stub, net 0); Check 6 Playwright SKIPPED per non-deploy-session convention. Closes (a.77) RECOMMENDED-NEXT = P-46 Workstream 3 Session 1; opens (a.78) RECOMMENDED-NEXT = P-46 Workstream 3 Session 2 — click-to-edit cell editors per §C.3 Session 2 spec (text/number/decimal/enum/boolean/date/tags inline editors reusing Session 5's `EditableTextField` + `EditableEnumField<T>` from `EditableField.tsx`); Session 2 may also begin landing the ~12 new data columns since the new columns + click-to-edit are mechanically coupled. ZERO Rule 9 gates fired; ZERO new DEFERRED items at session end (Rule 26) — all 6 TaskCreate tasks completed; ONE push planned at end-of-session to `origin/workflow-2-competition-scraping` per `feedback_approval_scope_per_decision_unit.md`. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-23-c — Header bump only this session (`session_2026-05-23-c_p46-workstream-2-deploy-session` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) SHIPPED + DEPLOYED + REAL-CHROME-VERIFIED across all 6 surfaces on vklf.com 2026-05-23-c via `workflow-2-competition-scraping` → `main` ff-merge `783abf4..9969427` (49 files +7504/-477 — 13 commits ff'd as one fast-forward carrying Workstream 1's 2026-05-24 schema commits + Sessions 1-5's UI/route commits from 2026-05-25 through 2026-05-23-b)** — pure orchestration deploy session, no new code, no new schema, no new dependencies; closes the P-46 Workstream 2 implementation arc end-to-end after Workstream 1 schema (2026-05-24) + Session 1 TipTap wrapper + per-item Analysis on Captured Text (2026-05-25) + Session 2 Captured Image + Captured Video card-list (2026-05-26) + Session 3 URL-level + per-category Overall Analysis boxes (2026-05-27) + Session 4 Captured Reviews UI end-to-end (2026-05-28) + Session 5 URL-level structural fields + Scraping Status toggle + Sizes/Options UI removal + Status column mirror (2026-05-23-b) + today's deploy + Phase-4 6/6 real-Chrome verification PASS on vklf.com (Captured Text + Image + Video + Reviews card-list sections all PASS / URL-level affordances Type+Description-1+Description-2+Price+Scraping Status toggle+Overall Competitor Analysis box PASS / Competition Data table Status pill column bidirectional mirror PASS). Pre-deploy /scoreboard all 5 checks GREEN at exact Session 5 baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 692 src/lib UNCHANGED / 61 routes UNCHANGED — Check 6 Playwright SKIPPED via Rule 27 picker since the lone `extensions/` file in the ff-merge bundle is `captured-text-validation.test.ts` +11 lines, test-only, doesn't ship in the .crx runtime). Rule 9 deploy gate fired ONCE via AskUserQuestion picker for `git push origin main` — director picked "Deploy now (recommended)". ff-merge `783abf4..9969427` succeeded clean; Vercel auto-redeploy fired; ping-pong sync was a NO-OP (workflow-2 already at `9969427` from the prior session's doc-batch push); Phase-4 director real-Chrome cross-platform verify ALL 6 SURFACES PASS clean with zero caveats — the cleanest end-of-workstream verification in any P-46 session. **Schema-change-in-flight flag FLIPPED YES → NO at deploy completion** — Workstream 1's schema columns (3 new tables + 8 new `CompetitorUrl` columns + 1 `analysis` JSON column on each of CapturedText/Image/Video + 1 `ScrapingStatus` enum) now have production code reading + writing them on vklf.com after being live on Supabase since 2026-05-24. **Closes (a.76) RECOMMENDED-NEXT = P-46 Workstream 2 deploy session**; closes **P-46 Workstream 2 ✅ DONE-AND-VERIFIED 2026-05-23-c** end-to-end (5 build sessions vs. 3-5 estimated per §C.2 — right on plan; combined Workstream 1+2 total spend = 7 sessions across 6 build + 1 deploy vs. 4-8 estimated). **Opens (a.77) RECOMMENDED-NEXT = P-46 Workstream 3 (Competition Data table redesign) first build session** per Q10's locked sequencing — ~3-4 sessions covering ~12 new columns + click-to-edit on every cell + resizable column widths + drag-to-reorder rows + per-column show/hide toggles + adjustable font size + horizontal checkbox bar at top + cross-device-synced UserTablePreferences. ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-23-c — the P-46 Workstream 2 DEPLOY CLOSING §Entry capturing (a) Workstream 2 done end-to-end on vklf.com via today's deploy + 6/6 Phase-4 PASS; (b) NEW reusable Pattern memorialized **"Multi-session workstream deploy gate timing"** — deploy session lands AFTER the LAST build session that contains user-visible UI, not after the schema session; pairs with the 2026-05-24 "Workstream Foundation Build Bundle" Pattern to give the full multi-session-workstream deploy-cadence shape; (c) TWO LOW informational sub-observations — P-43 cwd-leak class re-reproduced TWICE during /scoreboard Check 5 in both pre-deploy + post-merge scoreboard runs (caught + recovered both times with absolute `cd`); pointer-file off-by-one on main-tip SHA (NEXT_SESSION.md said `ee8c79d` but actual main was `783abf4` — the prior session's doc-batch SHA after the canonical 3-push pattern; ff-merge count 13 was still correct); (d) calibration data point — Workstream 2 came in at the top end of the 3-5 estimate (5 sessions); combined W1+W2 right on plan. **No top-level corrections-tier slip occurred this session.** NEW §B 2026-05-23-c entry appended to `docs/COMPETITION_DATA_V2_DESIGN.md` before END OF DOCUMENT marker capturing Workstream 2 deploy outcome + schema-change-in-flight flag YES → NO transition + the new "Multi-session workstream deploy gate timing" Pattern + the two LOW informational sub-observations; §A stays frozen per Rule 18. ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes.)

**Previously updated:** 2026-05-23-b — Header bump only this session (`session_2026-05-23-b_p46-workstream-2-session-5-url-level-structural-fields` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 5 ✅ DONE-AT-CODE-LEVEL 2026-05-23-b on `workflow-2-competition-scraping`** — fifth + final build session of the P-46 Workstream 2 implementation arc (Session 5 of 3-5 estimated per §C.2); landed the last §C.2 sub-scope for Workstream 2 — URL-level structural fields (Type / Description-1 / Description-2 / Price as 4 new free-text fields at the top of the URL box) + Scraping Status toggle (Incomplete/Complete enum write via the NEW `EditableEnumField<T>` segmented-control component) + remove Sizes/Options UI (full delete per project rules; underlying `CompetitorSize` table data stays in the database per Q6 hide-UI-keep-data; Git history preserves reversibility) + add read-only Status pill column (green Complete / gray Incomplete) second-from-left after URL in the Competition Data table bidirectionally mirroring the URL detail page toggle per Q8 §A.8; NEW `src/lib/competition-scraping/url-structural-fields-validation.ts` (~105 LOC pure trust-boundary helper `extractUrlStructuralFieldsPatch(body)` returning `{ ok, patch } | { ok, error }`; trim-or-null normalization for the 4 text fields; strict enum-acceptance for `scrapingStatus` via `isScrapingStatus`) + 22 new node:test cases; NEW generic `EditableEnumField<T extends string>` segmented-control component in `EditableField.tsx` (reusable by Workstream 3 for click-to-edit enum cells); `EditableTextField` extended with optional `multiline` + `rows` props for db.Text fields; `SizesSubsection` function + helpers fully deleted; Status pill column added to UrlTable via new `scrapingStatusBadgeStyle(status)` helper; build commit `374f1a3` 6 files +669/-109; schema-change-in-flight flag STAYS YES (carrying from Workstream 1's `prisma db push`); all 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 692 src/lib +22 from baseline 670 / 61 routes UNCHANGED); closes (a.75) RECOMMENDED-NEXT (P-46 Workstream 2 Session 5); opens (a.76) RECOMMENDED-NEXT = P-46 Workstream 2 deploy session — Phase-4 deploy ff-merging `workflow-2-competition-scraping` → `main` (carries Workstream 1's schema commits + Sessions 1-5's UI/route commits as one fast-forward) → Vercel auto-redeploy → ping-pong sync → director real-Chrome cross-platform verify across 5 surfaces; schema-change-in-flight flag flips YES → NO at deploy completion; director picked Proceed at Rule 14f session-start operational scope picker (per `feedback_default_to_recommendation.md` the sub-picker was skipped — only §C.2 sub-scope remaining); NO Rule 14f sub-pickers fired during execution; ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-23-b capturing Workstream 2 completion + the new "Field-allowlist subset extraction" reusable Pattern + the date-stamping anomaly informational observation (today's date per director confirmation is 2026-05-23 but doc-history's recent stamps run 2026-05-24 → 2026-05-28; session suffix `-b` per Rule 14 disambiguates) + P-43 cwd-leak re-reproduction informational; ZERO Rule 9 gates fired; ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-28 — Header bump only this session (`session_2026-05-28_p46-workstream-2-session-4-captured-reviews-ui` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 4 ✅ DONE-AT-CODE-LEVEL 2026-05-28 on `workflow-2-competition-scraping`** — fourth build session of the P-46 Workstream 2 implementation arc (Session 4 of 3-5 estimated per §C.2); landed the Captured Reviews UI end-to-end via a NEW `CapturedReviewsSection` + `CapturedReviewCard` + `CapturedReviewAddModal` (with a 1-5 star-rating picker widget) + per-item Analysis box below each review + Overall Reviews Analysis box at the bottom of the section (via the existing Session 3 `OverallAnalysisBox` with `category='reviews'`) + 4 fully-implemented route handlers behind the Workstream 1 501-stub surface (POST list + GET list at the collection path + PATCH + DELETE at the per-record path) + an architectural-move fix relocating the per-record review route from W1's deeper `urls/[urlId]/reviews/[reviewId]` path to the shallow `reviews/[reviewId]` path matching the text/[textId] / images/[imageId] / videos/[videoId] precedent for the other 3 capture types; NEW `src/lib/competition-scraping/handlers/url-reviews.ts` (~295 LOC DI seam for POST + GET collection handlers) + NEW `src/lib/competition-scraping/handlers/reviews-by-id.ts` (~290 LOC DI seam for per-record PATCH + DELETE — sets a NEW PRECEDENT for the per-record surface) + 32 new node:test cases; MODIFIED `urls/[urlId]/reviews/route.ts` (W1 501-stub → thin shim); NEW `reviews/[reviewId]/route.ts` at SHALLOW PRECEDENT PATH; DELETED W1's nested per-record 501-stub; NEW `CapturedReviewAddModal.tsx` (~479 LOC); MODIFIED `UrlDetailContent.tsx` (+398 LOC); build commit `82d390a` 9 files +2350/-69; schema-change-in-flight flag STAYS YES (carrying from Workstream 1's `prisma db push`); all 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 670 src/lib +32 from baseline 638 / 61 routes UNCHANGED); closes (a.74) RECOMMENDED-NEXT (P-46 Workstream 2 Session 4); opens (a.75) RECOMMENDED-NEXT = P-46 Workstream 2 Session 5 — recommended scope is the URL-level structural fields (Type / Description-1 / Description-2 / Price + Scraping Status toggle + remove Sizes/Options UI per §C.2) since that's the last §C.2 sub-scope remaining for Workstream 2; after Session 5, Workstream 2 is complete and the next session can be the Workstream 2 deploy session; director picked Option A at Rule 14f session-start scope picker between 2 §C.2-aligned candidates this session (A Captured Reviews UI recommended / B URL-level structural fields alt); NO Rule 14f sub-pickers fired during execution (the architectural move from W1's nested per-record path to the shallow precedent path was a clear "most thorough/reliable" choice per `feedback_recommendation_style.md`; star-rating widget + sort-key set were direct applications of existing precedents); ONE NEW INFORMATIONAL CORRECTIONS_LOG §Entry 2026-05-28 capturing the W1 path-divergence finding + architectural-move resolution + the new "Per-record handler DI-seam precedent extension" reusable Pattern; ZERO Rule 9 gates fired; ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-27 — Header bump only this session (`session_2026-05-27_p46-workstream-2-session-3-url-level-and-per-category-overall-analysis-boxes` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 3 ✅ DONE-AT-CODE-LEVEL 2026-05-27 on `workflow-2-competition-scraping`** — third build session of the P-46 Workstream 2 implementation arc (Session 3 of 3-5 estimated); completed the "Analysis surface" arc across all capture levels via a NEW `OverallAnalysisBox` parallel component shipping URL-level Overall Competitor Analysis + per-category Overall Analysis boxes (one per capture category — Text / Image / Video) at the bottom of each capture subsection + one URL-level box at the bottom of the URL detail page + extended the existing `urls/[urlId]` PATCH route allowlist for the two new fields with a NEW `isValidOverallAnalysesBag` strict-shape trust-boundary guard + bag-merge over replace at the PATCH so saving one category doesn't wipe sibling categories; NEW `OverallAnalysisBox.tsx` (~200 LOC) is a parallel component to Session 1's `PerItemAnalysisBox.tsx` (same save-lifecycle but different wire body shape driven by a `field` discriminator prop — `{ kind: 'overallCompetitorAnalysis' }` emits `{ overallCompetitorAnalysis: <doc> }`; `{ kind: 'overallAnalyses', category: 'text'|'image'|'video'|'reviews' }` emits `{ overallAnalyses: { [category]: <doc> } }`); NEW `isValidOverallAnalysesBag` guard in `tiptap-helpers.ts` (+37 LOC) strict-rejects unknown category keys + requires each known-category value pass `isValidAnalysisPayload`; existing `urls/[urlId]` PATCH route allowlist extended (+65 LOC) with bag-merge semantics for `overallAnalyses`; `UrlDetailContent.tsx` (+65/-1) threads `overallAnalysisInitial` prop through to 3 subsections + renders `OverallAnalysisBox` at the bottom of each; 10 new node:test cases for `isValidOverallAnalysesBag` (+88 LOC) pinning down the strict-shape guard's contract; NO new npm dependencies; NO new schema; NO new routes (only extended one existing allowlist); build commit `4773b62` 5 files +452/-1; schema-change-in-flight flag STAYS YES (carrying from Workstream 1's 2026-05-24 `prisma db push`); all 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 638 src/lib +10 from baseline 628 / 61 routes UNCHANGED); closes (a.73) RECOMMENDED-NEXT (P-46 Workstream 2 Session 3); opens (a.74) RECOMMENDED-NEXT = P-46 Workstream 2 Session 4 — multiple §C.2-aligned candidates remain (Captured Reviews UI RECOMMENDED first since §C.2 originally sequenced it as Session 3 of Workstream 2 — natural next §C.2 step after the Analysis surface arc completion; new URL-level Type/Description/Price fields + Scraping Status toggle + remove Sizes/Options UI alt); director picks at Session 4 start; director picked Option A at Rule 14f session-start scope picker between 3 §C.2-aligned candidates this session (A URL-level + per-category Overall Analysis recommended / B Captured Reviews UI alt / C URL-level structural fields alt); NO Rule 14f sub-pickers fired during execution (bag-merge over replace at PATCH was the only valid choice; strict unknown-key rejection in the guard is most-thorough; parallel `OverallAnalysisBox` component over overloading `PerItemAnalysisBox` preserves wire-contract clarity at callsites); ZERO Rule 9 gates fired; ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-26 — Header bump only this session (`session_2026-05-26_p46-workstream-2-session-2-per-item-analysis-on-captured-image-and-video` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 2 ✅ DONE-AT-CODE-LEVEL 2026-05-26 on `workflow-2-competition-scraping`** — second build session of the P-46 Workstream 2 implementation arc (Session 2 of 3-5 estimated); applied Session 1's locked card-list precedent + the `PerItemAnalysisBox` reusable component to Captured Image + Captured Video as the next two capture types after Captured Text + extended the corresponding PATCH routes (`images/[imageId]` + `videos/[videoId]`) for the `analysis` field using the same one-line fix-shape Session 1 set for `text/[textId]`; `UrlDetailContent.tsx` rewrites both `CapturedImagesGallery` (was thumbnail grid) + `CapturedVideosGallery` (was 2-col card grid) to vertical card lists with the per-item Analysis editor below each card via the `PerItemAnalysisBox` component (different `apiUrl` prop per capture type); per-row trash NOT added to video cards (matches Build #5's "renderer only" scope); removed now-unused `thumbnailTrashButtonStyle`; 6 new edge-case node:test cases for `isValidAnalysisPayload` pinning down the trust-boundary guard's contract (nested object → true; plain object with arbitrary keys → true; function → false; Object.create(null) → true; TipTap doc with empty content array → true; bigint → false); NO new npm dependencies (TipTap landed in Session 1); build commit `9747f63` 4 files +325/-102; schema-change-in-flight flag STAYS YES (carrying from Workstream 1's 2026-05-24 `prisma db push`); all 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 628 src/lib +6 from baseline 622 / 61 routes UNCHANGED); closes (a.72) RECOMMENDED-NEXT (P-46 Workstream 2 Session 2); opens (a.73) RECOMMENDED-NEXT = P-46 Workstream 2 Session 3 — multiple §C.2-aligned candidates surface (URL-level Overall Competitor Analysis + per-category Overall Analysis boxes RECOMMENDED to complete the "Analysis surface" arc before structural fields; Captured Reviews UI alt; new URL-level Type/Description/Price fields + Scraping Status toggle + remove Sizes/Options UI alt); director picks at Session 3 start; NO Rule 14f forced-picker fired this session (every layout choice was a direct application of Session 1's locked precedent per `feedback_default_to_recommendation.md`); ZERO Rule 9 gates fired; ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-25 — Header bump only this session (`session_2026-05-25_p46-workstream-2-session-1-tiptap-wrapper-and-per-item-analysis-on-captured-text` — **W#2 polish P-46 Workstream 2 (URL detail page redesign) Session 1 ✅ DONE-AT-CODE-LEVEL 2026-05-25 on `workflow-2-competition-scraping`** — first build session of the P-46 Workstream 2 implementation arc (Session 1 of 3-5 estimated); landed the TipTap shared rich-text editor wrapper component (`RichTextEditor.tsx` 317 LOC) + the per-item Analysis text box (`PerItemAnalysisBox.tsx` 174 LOC) on Captured Text as the first user-visible slice + pure helpers (`tiptap-helpers.ts` 92 LOC including the `isValidAnalysisPayload` route-trust-boundary guard) + 20 new node:test cases for the helpers + the route-handler half (`PATCH text/[textId]` allowlist extension for `analysis` field validated at the trust boundary); URL detail page's `CapturedTextSubsection` switches from a 5-column HTML table to a vertical card list per the Rule 14f forced-picker outcome (director picked Option A "Card layout — replace the table (recommended)" over 3 alternatives); layout precedent locked for the remaining 3 capture types (Image / Video / Review) in subsequent sessions; 3 new npm dependencies `@tiptap/react` + `@tiptap/starter-kit` + `@tiptap/extension-link` at 3.23.6 (50 packages added transitively; all React 19 + Next.js 16 compatible); build commit `b6e43fe` 8 files +1572/-149; schema-change-in-flight flag STAYS YES (carrying from Workstream 1's 2026-05-24 `prisma db push`); all 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext UNCHANGED / 622 src/lib +20 from baseline 602 / 61 routes UNCHANGED); closes (a.71) RECOMMENDED-NEXT (P-46 Workstream 2 Session 1); opens (a.72) RECOMMENDED-NEXT = P-46 Workstream 2 Session 2 — wire per-item Analysis on Captured Image + Captured Video using the same `PerItemAnalysisBox` component + convert image gallery + video gallery to the same card-list layout pattern; ZERO Rule 9 gates fired; ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-24 — Header bump only this session (`session_2026-05-24_p46-workstream-1-schema-first-build-session` — **W#2 polish P-46 Workstream 1 (Schema) ✅ DONE-AT-CODE-LEVEL 2026-05-24 on `workflow-2-competition-scraping`** — first build session of the P-46 5-workstream implementation arc; schema migration landed live on Supabase under Rule 9 director-Yes gate at `npx prisma db push` (1.32s; zero data loss; additive only); 4 new API route shells scaffolded as 501-stubs; shared-types extended; 12 new src/lib node:test cases; build commit `caad82a` 21 files +781/-11; live database now carries 3 new tables + 8 new `CompetitorUrl` columns + 1 new `analysis` JSON column on each of CapturedText/Image/Video + 1 new `ScrapingStatus` enum; schema-change-in-flight flag FLIPPED NO → YES at `prisma db push` completion; all 5 /scoreboard checks GREEN at new baselines (root tsc clean / extension tsc clean / 558 ext / 602 src/lib +12 / 61 routes +4); closes (a.70) RECOMMENDED-NEXT (P-46 Workstream 1); opens (a.71) RECOMMENDED-NEXT = P-46 Workstream 2 (URL detail page redesign) first build session per Q10's locked sequencing; Workstream 1 came in under estimate 1 session vs. 2-3 planned per §C.1; ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-23 — Header bump only this session (`session_2026-05-23_p46-w2-phase-2-design-session` — **W#2 polish P-46 W#2 Phase 2 design session ✅ DONE 2026-05-23 on `workflow-2-competition-scraping`** — pure DESIGN session, no code commits, no deploys, no Rule 9 gates fired, no fresh extension zip, no ping-pong sync; shipped new `docs/COMPETITION_DATA_V2_DESIGN.md` (~700 lines; §A frozen 10 questions + §B empty append-only + §C 5-workstream implementation outlines) mirroring the §A frozen-shape pattern of `docs/CAPTURED_VIDEOS_DESIGN.md` per Rule 18 + the 2026-05-20-b P-27 design-doc-split precedent; 10 DECISIONS LOCKED via Rule 14f forced-pickers (8 pickers fired + 1 Default-to-recommendation skip on Q8 + 1 director "Other" pick on Q9 DROPPING Select-preview-thumbnail from P-46 + 1 follow-up picker on Q1 DEFERRING per-platform Reviews extraction); Q1's deferral + Q9's drop shrink P-46 from 15-25 sessions to revised 11-17 sessions across 5 workstreams; Q10 locks workstream sequencing as Schema → URL detail page → Competition Data table → Comprehensive Analysis page → Extension + Reviews; closes (a.69) RECOMMENDED-NEXT (P-46 design); opens (a.70) RECOMMENDED-NEXT = P-46 Workstream 1 (Schema) first build session; schema-change-in-flight flag entered + exited NO (pure design); ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-22-i — Header bump only this session (`session_2026-05-22-i_p45-build-2-deploy-with-phase-1-fix-forward` — **W#2 polish P-45 screen recording SHIPPED + DEPLOYED + REAL-CHROME-VERIFIED across Amazon + Ebay + Walmart + Etsy 2026-05-22-i on vklf.com** via `workflow-2-competition-scraping` → `main` ff-merge `d4a2940..ee8c79d` (11 commits 39 files +5067/-200 carrying Build #1a + 1a-doc + 1b + 1b-doc + P-42 + P-42-doc + P-43 + P-43-doc + P-44 + P-44-doc + today's Build #2 fix-forward `ee8c79d` — 3 files changed +78/-30 with Issue 1 selfBrowserSurface include + Issue 2 aggressive event-isolation band-aid on all 4 text inputs + Issue 3 normalized Content-Type for Supabase strict allowedMimeTypes); pre-deploy /scoreboard all 6 checks GREEN at exact baselines (root tsc clean / extension tsc clean / 57 routes / 590 src/lib / 558 ext / 94 Playwright in 2.7m); Rule 9 deploy gate fired once via AskUserQuestion picker for `git push origin main`; ping-pong sync brought workflow-2 back even with main at `ee8c79d`; fresh extension zip `plos-extension-2026-05-22-w2-deploy-33.zip` 202.75 KB at repo root via `npm run zip` 2.2s (exits cleanly thanks to P-44's wrapper); Phase 4 director real-Chrome cross-platform verify ALL 4 PLATFORMS PASSED CLEAN with zero caveats — the cleanest cross-platform PASS in any W#2 cooperation session to date; closes P-45 ✅ DONE-AND-VERIFIED + closes P-27 Bug #11 ✅ DONE via band-aid; opens (a.69) RECOMMENDED-NEXT = P-46 W#2 Phase 2 design session + NEW P-47 polish entry (Shadow DOM refactor as proper replacement for the Issue 2 band-aid; LOW priority); schema-change-in-flight flag FLIPPED YES → NO at deploy completion; ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-22-h — Header bump only this session (`session_2026-05-22-h_p44-wxt-zip-build-parent-process-hang-fix` — closed the multi-reproduction `wxt build` + `wxt zip` parent-process hang class via NEW programmatic-API wrappers at `extensions/competition-scraping/scripts/wxt-build.mjs` + `scripts/wxt-zip.mjs` (16 LOC each — import wxt's exported `build()` + `zip()` functions, await them, then `process.exit(0)` to bypass the broken event-loop drain caused by Vite 8 → Rolldown 1.0.0-rc.18 native worker-thread retention) + `package.json` 4 script rewires + `.claude/commands/scoreboard.md` + `.claude/commands/deploy.md` KNOWN ISSUE → wrapper-note swaps; build commit `57ed383` — 5 files changed +61/-11; Rule 24 scope-expansion to cover `wxt zip` (shares the same `internalBuild()` codepath); ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-22-g — Header bump only this session (`session_2026-05-22-g_p43-absolute-paths-scoreboard-deploy-ship-polish-item` — closed the FOUR-reproduction `.claude/commands/scoreboard.md` parallel-Bash CWD-leak class via absolute-path normalization across `.claude/commands/scoreboard.md` + `.claude/commands/deploy.md` + `.claude/commands/ship-polish-item.md` + baseline refresh in all three templates; build commit `4afea35` — 3 files changed +20/-20; ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-22-f — Header bump only this session (`session_2026-05-22-f_p42-backup-memory-dir-hook-fix` — closed the three-reproduction backup-memory-dir + sibling track-edited-docs Layer-1 hook gap via empirically-confirmed `.tool` → `.tool_name` jq selector fix + added a NEW Layer-3b SessionStart staleness canary at `.claude/hooks/check-memory-mirror-staleness.sh`; build commit `2e3270d`; ZERO new Starter-file rules drafted; no Step 7b changes; no resume-flow changes).

**Previously updated:** 2026-05-21 — NEW Step 7b added to the START-OF-SESSION ROUTINE: "Plain-terms session summary" — Claude produces a plain-language summary of what this session will do BEFORE the director gives go-ahead. Companion to HANDOFF_PROTOCOL Rule 30 (Session bookends — NEW 2026-05-21) + §4 Step 4b template extension. Director's verbatim 2026-05-21 directive: *"From here on, for every next session, I want you to also tell me in simple terms what we will do in the session and summarize what was done in the session and what we will do in the next session. I also want you to check and tell me what work is pending according to the roadmap in simple terms."* Operational-memory cross-reference: `feedback_session_bookends_plain_summary.md`.

---

## 🟢 Resume-flow handling (REWRITTEN 2026-05-14 — multi-layered defense replacing the prior single-mechanism design)

**Primary mechanism — SessionStart hook injects pointer content as context (NEW 2026-05-14):**

When Claude Code launches, the `SessionStart` hook (`.claude/hooks/inject-next-session-pointer.sh`, wired in `.claude/settings.json`) reads `docs/NEXT_SESSION.md` and injects its contents into the session as a `system reminder` BEFORE the user's first prompt. The injected content includes a clear marker: *"🟢 RESUME-FLOW POINTER — docs/NEXT_SESSION.md content follows … treat this content as the session's launch prompt and proceed with the start-of-session sequence per docs/CLAUDE_CODE_STARTER.md once the user sends any first message — even a single word like 'go' or 'proceed'."*

**When this hook fires, Claude's behavior on receiving the user's first message** (which may be just "go", "proceed", or any short acknowledgment): treat the injected pointer-file content as if the director had pasted the launch prompt directly as the first message. The pointer file is the source of truth for branch + task + pre-session notes. Then continue with the standard start-of-session routine below — branch verification per Step 2, the rest of the Group A doc reads, drift check, wait for go-ahead.

**Procedural fallback — sentinel-string match (KEPT from 2026-05-13-c):**

**If the session's very first user message contains the phrase "Resume per docs/NEXT_SESSION.md"** (e.g., `"Resume per docs/NEXT_SESSION.md — read this pointer file first, then proceed with the start-of-session sequence per docs/CLAUDE_CODE_STARTER.md."`):

Same behavior as the primary mechanism — read `docs/NEXT_SESSION.md` and treat its `## Launch prompt` section as if directly pasted. This fallback covers the case where the SessionStart hook didn't fire for any reason (e.g., user invoked `claude --bare` which skips hooks; user is on a branch that doesn't have the hook yet because the fix hasn't merged in; hook script erroneously emitted empty additionalContext).

**Why two layers:** the primary mechanism (SessionStart hook) is mechanical and director-zero-effort beyond a single keystroke. The procedural fallback (sentinel-string match) is Claude-side discipline and works even when the hook layer is unavailable. The two compose: when both fire, Claude reads the pointer once and proceeds normally (idempotent).

**If the first message is anything OTHER than a wake-up keystroke or the sentinel** (e.g., the director pasted a full launch prompt directly via the 3-step ESCAPE HATCH path because `./resume` errored out), proceed as before — read `docs/CLAUDE_CODE_STARTER.md`, run the mandatory start-of-session routine, treat the director's pasted text as the task. No pointer-file read needed in that path; the ESCAPE HATCH path is fully self-contained.

**Why this matters — historical context:**

The original `./resume` script (shipped 2026-05-13-c) was designed to launch `exec claude "$SENTINEL"` so Claude Code would receive the sentinel as an auto-submitted first user message. **That design was structurally broken from day 1:** Claude Code's positional `[prompt]` argument only auto-submits in non-interactive print mode (`-p`); in interactive mode (which is what `./resume` wants) the positional pre-fills the input box silently or is ignored — director still had to copy/paste the launch prompt manually. The director hit this bug at the start of `session_2026-05-14_w2-main-deploy-session-11-region-screenshot-DEPLOYED-FULL-VERIFY` and asked for both a fix + redundancy to prevent recurrence; the multi-layered defense above is the response. Full background: `CORRECTIONS_LOG.md` 2026-05-14 entry "Resume-flow design flaw" + `HANDOFF_PROTOCOL.md` Rule 28.

---

## 🚨 NON-NEGOTIABLE RULES — CLAUDE READS AND CONFIRMS BEFORE ANY WORK 🚨

I am the director of the PLOS (Product Launch Operating System) project. **I am a NON-PROGRAMMER.** I have no formal programming background, no developer-tools experience, and no technical vocabulary. This instruction has been necessary in MULTIPLE successive chats in the predecessor (claude.ai) system — most recently flagged as Pattern 11 recurrence #4 in `docs/CORRECTIONS_LOG.md`. The rule is mechanical, not aspirational.

### Communication rules (apply to every message)

1. **Plain language only.** No "endpoint," "route," "schema," "migration," "projectId," "TypeScript error," "context," "payload," "upsert," "enum," "foreign key," "null." Substitute with user-visible terms: "the address the user types," "the behind-the-scenes record," "the thing the user sees," "the list of projects."

2. **The Read-It-Back test.** Before sending any question OR instruction, mentally read it back as if I had never written code. If any word requires domain/programming knowledge — rewrite.

3. **Options + recommendation + reversibility, for every significant decision.** Don't leave me to pick blindly — give me Option A vs. B vs. C in "what the user sees" terms, your expert pick with reasoning, and whether the decision can be undone later. **The recommendation must (a) be the MOST THOROUGH AND RELIABLE option** — the one with highest confidence in the result and lowest risk of leaving issues unvalidated — **NOT the fastest, cheapest, or "easiest"** — AND **(b) be marked with an explicit `(recommended)` label INSIDE the option's headline**, not only in surrounding prose. Forced-picker UI may hide the surrounding prose; the marker inside the label is the canonical placement. Mark exactly one option as recommended; never zero, never two. (Full mechanical test in `HANDOFF_PROTOCOL.md` Rule 14f; canonical reasoning in operational memory `feedback_recommendation_style.md`. Director's standing preference, reinforced 2026-05-02.)

3a. **Default-to-recommendation exception (NEW 2026-05-19-g-3).** Director's standing default is "yes, proceed with your recommendation." When the picker would only be re-confirming the recommended path AND the work fits pre-approved patterns (small / reversible / non-destructive), SKIP the forced-picker and proceed with the recommended option. The picker still fires for: Rule 9 destructive operations (deploys, force-pushes, rm -rf, prisma migrate reset, SQL DELETE/DROP/TRUNCATE); scope decisions with no clear "most thorough"; workflow design / Rule 18 interview clusters; anywhere director's intent is genuinely ambiguous. The TEST before firing: "is this question about clarifying the director's INTENT, OR is it asking permission to proceed on a path the director would default-approve?" If the latter — skip + proceed. Full exception in `HANDOFF_PROTOCOL.md` Rule 14f "Default-to-recommendation exception" section. Operational memory: `feedback_default_to_recommendation.md`.

4. **Equal visual weight in recaps.** When summarizing a plan, features you added autonomously get the SAME visual prominence as features I explicitly decided. Never bury an autonomous addition in a one-liner.

5. **Persistence decisions need explicit framing.** When data saves to local storage vs. database, explain in plain terms (syncs across devices? visible to other users? survives cache clears?). Never bury as parenthetical.

6. **Every imperative instruction needs a concrete method.** When you ask me to do something, pair it with a terminal command OR numbered clicks OR an interactive choice. Never "paste X" or "share Y" without telling me HOW. (This is Rule 9 from the predecessor system, added after the Pattern-11 recurrence-4 slip.)

7. **If you slip mid-session:** acknowledge openly, don't minimize. I've earned the right to flag slips. **Specifically:** if the director has explicitly confirmed a setup item ("all 4 prompts pasted," "all set," "configured"), trust that confirmation — do NOT re-ask for verification just because an automated runner has incomplete coverage. Re-asking confirmed setup is a Rule 14 violation; the runner's gap is a runner bug to capture and fix, not a reason to re-litigate. Full rule: `HANDOFF_PROTOCOL.md` Rule 14g (NEW 2026-05-02). Operational memory: `feedback_trust_director_setup_confirmation.md`.

### Claude Code–specific safety rules (Rules M1–M7, see `docs/CLAUDE_CODE_MIGRATION.md` §5)

8. **STOP before any destructive operation.** Before `rm`, `rm -rf`, `git reset --hard`, `git push --force`, `prisma migrate reset`, `prisma db push --force-reset`, SQL DELETE/DROP/TRUNCATE, or anything that deletes/overwrites data or rewrites history: DESCRIBE what will happen in plain English (what files/records/commits affected, whether recoverable), and ASK for explicit confirmation. Proceed only on clear affirmative — never silence or ambiguity.

    **Container-level destructive operations have a separate audit gate per `HANDOFF_PROTOCOL.md` Rule 29 (NEW 2026-05-20).** "Codespaces: Rebuild Container," `devcontainer.json` edits that auto-trigger rebuild, container reset/delete-and-recreate, and any operation that wipes the home directory (`/home/codespace/`) are ALL destructive — they wipe everything outside `/workspaces/` including Claude's persistent memory directory at `/home/codespace/.claude/projects/-workspaces-brand-operations-hub/memory/`, globally installed npm packages, manually apt-installed packages, and `~/.config/` customizations. Before recommending any of these, run the Pre-Container-Audit (`bash scripts/codespace-rebuild-audit.sh`) and surface the audit via Rule 14f forced-picker. Default to "back up critical state to `/workspaces/brand-operations-hub/.codespace-backup/` first, then proceed" — never recommend a rebuild without backup. Captured 2026-05-20 after a slip where a rebuild silently wiped Claude's memory directory + the Claude Code CLI binary itself; full slip narrative + 4-layer protective architecture in HANDOFF_PROTOCOL Rule 29 + CORRECTIONS_LOG.md 2026-05-20 entry.

9. **For deploys (`git push origin main` affecting the live site):** describe what commits will go live, what user-visible changes result, and ask for explicit confirmation before pushing.

10. **Visual verification after deploy is my job.** You can't open a browser. When work deploys, describe exactly what I should check on the live site, and do NOT mark work "done" until I confirm.

11. **Commit hygiene — Option A clean split.** Each session's commit contains ONLY that session's work. If pre-existing leftovers show up in `git status`, unstage them (`git reset HEAD <paths>`) before committing. See `docs/CORRECTIONS_LOG.md` 2026-04-17 entry "Pre-existing .bak/untracked files" for the canonical procedure.

12. **Escape hatch to paste-dance when in doubt.** If a single operation feels risky, fall back to the claude.ai pattern: "Here's the command I'd like to run — can you paste it yourself so we both see it before execution?" Rare but available.

### Session management

13. **Read the mandatory start-of-session sequence** in `docs/HANDOFF_PROTOCOL.md` §2 before any substantive work. It includes drift check, session identifier capture, known-unknowns check, and go-ahead wait.

14. **Session identifier format:** `session_YYYY-MM-DD_short-topic-slug`. Capture at start, use in end-of-session doc updates. Multiple sessions same day: append `-a`, `-b`.

15. **End-of-session doc update** is mandatory. Run the checklist in `docs/HANDOFF_PROTOCOL.md` §4 Step 1. Update whatever changed, commit to git, and produce a personalized handoff summary. **Per `HANDOFF_PROTOCOL.md` Rule 26 (NEW 2026-05-04-d), the end-of-session deferred-items sweep is driven by `TaskList` — Claude calls `TaskList`, reviews every `DEFERRED:`-prefixed task, migrates each one's content to its destination doc, then closes the task via `TaskUpdate → completed`. Any `DEFERRED:` task still open at end-of-session is an automatic CORRECTIONS_LOG entry. Mid-session, every defer creates a `TaskCreate` immediately — same sentence as the destination-naming per Rule 14e.** **Per `HANDOFF_PROTOCOL.md` §4 Step 1 row 12 (NEW 2026-05-13-c), every end-of-session ALSO writes `docs/NEXT_SESSION.md` — the pointer file the next session's `./resume` script reads. If today's session has no obvious continuation, Claude runs the §4 Step 1c "No obvious next task" Rule 14f forced-picker BEFORE writing the pointer — never silently guess.**

    **MANDATORY content of that handoff summary — no exceptions, applies to every session:**

    - **"What we did this session"** — 2–4 sentences in plain language, no jargon.
    - **"Files changed and committed"** — list with commit hash. If pushed, say so. If not pushed, say so and explain why.
    - **"Deferred items"** — every flagged-and-set-aside item, with the specific doc + section where it's now captured (per Rule 14e of HANDOFF_PROTOCOL).
    - **🚪 "END-OF-SESSION INSTRUCTIONS — what the user types NOW to close this session"** — step-by-step, concrete. Example: *"Type `exit` and press Enter to leave Claude Code. You can close the terminal tab or leave it open — either works."*
    - **🚪 "NEXT-SESSION INSTRUCTIONS — what the user types when they come back"** — step-by-step, concrete, with:
      - Exact terminal command to launch Claude Code (`cd /workspaces/brand-operations-hub && claude`)
      - Exact first-message text to paste (the "Read docs/CLAUDE_CODE_STARTER.md..." line with the specific next task filled in)
      - Any offline steps to do between sessions (e.g., "check Vercel env vars," "find a file on your computer") if applicable
    - **"Open questions / carry-overs"** — anything unresolved that the next session needs to know about.

    **Why this is mandatory:** the user is a non-programmer. Session-boundary moments (end of this session, start of next) are when they're most likely to feel lost without an exact-word instruction. The same discipline that applies to mid-session imperatives (Rule 6 + Rule 9 above) applies at session bookends. "Run the start-of-session sequence" is not a concrete instruction — "type this exact command in the terminal: `cd /workspaces/brand-operations-hub && claude`" is.

16. **Proactive context-degradation warning.** If the session is running long and your focus is stretched, say so proactively (HANDOFF_PROTOCOL Rule 13). I'd rather pause and resume fresh than push a tired session into a risky operation.

    **Concrete triggers — raise the pause-and-resume concern when ANY of these is true:**
    - Session has been active for ~90 minutes or longer of continuous work
    - You've made ~30+ substantive exchanges (not counting trivial y/n confirmations)
    - You notice you've had to re-read a file or re-derive a decision you already made earlier in the session
    - You're about to execute a destructive operation (per Rule 8) AND the session has been long — destructive ops at end-of-session are the highest-risk combination
    - The user notes that you seem to be slipping, bundling instructions, dropping details, or losing the thread — that's a direct signal to pause, not to push through harder
    - You find yourself about to say "let me just finish this quickly" or "one more thing and we're done" — that reflex IS the warning sign
    - Context window usage is clearly getting high (even if you can't measure it precisely, trust the subjective feeling)

    **When a trigger fires:** stop, state it plainly ("I notice X — I'd recommend we pause here and resume in a fresh session"), and let me decide. Don't unilaterally push through or unilaterally end the session. The decision is mine; your job is to surface the concern.

    **What the pause looks like:** run the end-of-session protocol (Rule 15), commit and push, then the user closes the session and starts fresh later. Resume instructions in the handoff summary tell the next session exactly where to pick up.

### Decision-framing rules

20. **Option questions (A/B/C, 1/2/3) must include per-option context, an "I have a question first" escape-hatch option, AND a closing free-text invitation.** (NEW 2026-04-18 — Pattern 14 in CORRECTIONS_LOG. User raised the initial concern at end of first Claude Code session: *"at several points you posed options to me where rather than type my response, I could only pick from 1,2,3… The problem is, in many instances, I had questions about an option and couldn't type it in."* Then refined with a second directive that solves the tool-UI constraint: *"Let's add a new rule. Always give me an additional choice to all the choices you're offering that says 'I have a question first that I need clarified'. This way, I select from a forced options list and still get to type my response."*)

    **Background — why the escape-hatch option matters.** Claude Code sometimes renders multi-option questions as an interactive picker UI in which the input box is temporarily hidden and the user can only navigate with arrow keys or number-select. In those moments, a free-text invitation in the prose of my message is inaccessible — the user can't type anything. Adding an escape-hatch option WITHIN the forced picker lets the user select their way back into normal chat mode, where the input box reappears and they can type their question.

    **For every multi-option question I give the user, each option must contain:**
    - A plain-language description of what the option actually means (not just a label like "Option A — do X")
    - The user-visible consequence of picking it ("if you pick A, you'll see X; it's reversible by doing Y" vs. "if you pick B, X is locked in permanently")
    - Enough context that a non-programmer can evaluate it without needing to ask a clarifying question — OR an explicit acknowledgment that the option has a subtlety they might want to ask about

    **AND** — every multi-option question must include an **explicit escape-hatch option as the last option in the list**, worded as:

    > *"I have a question first that I need clarified"*

    (or near-equivalent wording the user will recognize as the escape hatch — consistent phrasing is the goal). This option is NON-NEGOTIABLE regardless of how confident I am that the main options are self-explanatory. Selecting it means the user wants to ask something before picking one of the "real" options, and I should respond with a clarification-focused reply rather than executing any action.

    **AND** — every multi-option question must also close with a free-text invitation such as:
    > *"Or if you have a question about any option before picking, just ask — a clarification-first response is always valid. You're never locked into a letter/number answer."*

    This covers the case where I'm rendered as plain text (not an interactive picker), where the user's input box is already visible and they don't need the escape-hatch option to type.

    **Mechanical test before sending a multi-option question:**
    1. Scan each option: "does this option have enough context that a non-programmer can evaluate it without asking?" If no, add context.
    2. Is there an "I have a question first that I need clarified" option as the final option? If no, add it.
    3. Is there a free-text invitation at the close? If no, add it.

    If any of the three fails, rewrite before sending.

    **Scope exception:** simple yes/no/not-sure questions don't need elaborate per-option context (the options are trivially understood). But they STILL must include the escape-hatch option and the free-text invitation. "Yes / No / I have a question first / Not sure" is the shape for a simple binary with escape-hatch — never just "yes / no."

### `.claude/` tooling — PLOS-specific extensions to use (NEW 2026-05-19-g-3)

The `.claude/` folder ships PLOS-specific Claude Code extensions that mechanize recurring session work. Use them instead of issuing the underlying commands manually — they're mechanically consistent across sessions and save 5-15 min per use.

| Extension | Path | When to use |
|---|---|---|
| **`/ship-polish-item P-NN`** slash command | `.claude/commands/ship-polish-item.md` | AT SESSION START when shipping a specific polish item — orchestrates the full ship pattern (branch verify → ROADMAP entry read → Rule 3 diagnosis → Rule 14f pickers → code → /scoreboard → /deploy → /end-of-session) |
| **`/deploy`** slash command | `.claude/commands/deploy.md` | When ready to deploy a build commit — wraps pre-deploy /scoreboard → main-hasn't-moved check → Rule 9 gate (AskUserQuestion) → ff-merge → post-merge /scoreboard → push + ping-pong → fresh extension zip → director real-Chrome walkthrough |
| **`/end-of-session`** slash command | `.claude/commands/end-of-session.md` | When ready to close a session — orchestrates TaskList sweep → spawn `plos-doc-batch` agent → commit + push + ping-pong → Personalized Handoff per §4 Step 4b |
| **`/rule-24-search [keyword]`** slash command | `.claude/commands/rule-24-search.md` | BEFORE adding any new ROADMAP item — runs the canonical 7-grep pre-capture search per Rule 24 |
| **`/scoreboard`** slash command | `.claude/commands/scoreboard.md` | BEFORE every deploy (pre-deploy + post-merge) — runs the 6-check verification (tsc / ext tsc / npm run build / src/lib node:test / extension npm test / Playwright) and reports green/red with deltas |
| **`plos-doc-batch`** subagent | `.claude/agents/plos-doc-batch.md` | At end-of-session for the doc-bundle WRITING — spawn via Agent tool with `subagent_type: "plos-doc-batch"`. The parent Claude still owns the deferred-items sweep + commit + push + ping-pong + Personalized Handoff |
| **`track-edited-docs.sh`** PostToolUse hook | `.claude/hooks/track-edited-docs.sh` (auto-fires) | Auto-fires on every Edit/Write — logs `docs/` edits to `.claude/session-modified-docs.log` (session-scoped, gitignored). The `plos-doc-batch` agent consumes this log |

Also pre-existing (don't need to invoke; auto-fire): `SessionStart` hook (`inject-next-session-pointer.sh`) + `PreToolUse` Bash hook (`check-next-session-doc.sh`).

To see live state of installed extensions in a session: type `/agents` (interactive panel listing custom + built-in subagents) or `/` (slash-command autocomplete listing available commands + skills).

### Doc access

17. **Handoff docs live in `/docs/` in the repo.** Read them directly from disk at session start — no uploads. The 15 Group A docs are authoritative on platform-wide facts; Group B docs (e.g., `KEYWORD_CLUSTERING_ACTIVE.md`) are tool-specific and loaded when that tool is in scope.

18. **When docs contradict code, code wins** (HANDOFF_PROTOCOL Rule 3). Log the doc drift to `CORRECTIONS_LOG.md` and update the doc.

19. **Do NOT make changes to the handoff docs mid-session silently.** Track what needs updating, then batch all doc updates at end-of-session per the checklist.

### ROADMAP capture discipline

21. **Pre-capture search before adding ANY ROADMAP item or proposing a new architectural concern** (`HANDOFF_PROTOCOL.md` Rule 24, NEW 2026-04-27). Before reading back any proposed ROADMAP entry to me, Claude MUST first search existing docs for prior treatment of the same concern. The search must cover: (a) direct keyword grep with synonyms across `ROADMAP.md`, the relevant tool's `<TOOL>_DESIGN.md` / `_ACTIVE.md` / `_DATA_CONTRACT.md` / `_ARCHIVE.md`, `PLATFORM_ARCHITECTURE.md`, `CORRECTIONS_LOG.md`, and any architectural-pivot or design doc relevant to the concern (e.g., `PIVOT_DESIGN.md` for Auto-Analyze concerns); (b) read-through of the canonical doc's "Known limitations" / "Open questions / deferred items" / "Infrastructure TODOs" sections; (c) CORRECTIONS_LOG entries from the last 5-10 sessions; (d) verify against actual code when the concern relates to specific behavior — Read the source files, not just trust doc claims.

    **If prior treatment IS found:** surface it explicitly to me BEFORE reading back the proposed entry — *"I found this was already discussed in [doc] [section] on [date]. The prior treatment was: [summary]. Compared to my current proposal: [diff]."* I decide whether to (a) update the existing item, (b) create a new related item with cross-reference, or (c) consolidate.

    **If prior treatment is NOT found:** surface the search performed — *"I checked [list of locations: doc names + sections searched] and found no prior treatment. Proceeding with new capture."*

    **Why this rule exists:** logged in `CORRECTIONS_LOG.md` 2026-04-27 entry (HIGH severity). Claude proposed a context-scaling ROADMAP item without first searching, framing the concern as "the system was not explicitly designed to handle it" — when `PIVOT_DESIGN.md` lines 205 + 246 had explicitly acknowledged the trade-off and `ROADMAP.md` line 162 documented that V2's Mode A→B (deleted in Pivot E) had been credited with avoiding the same issue. Claude had read both pieces earlier in the same session but failed to synthesize them when writing the new ROADMAP entry. Synthesis from working memory is unreliable; the structured search forces a deliberate re-read at the moment the verification matters.

### Verification approach discipline

22. **Playwright forced-picker before manual browser walkthroughs** (`HANDOFF_PROTOCOL.md` Rule 27, NEW 2026-05-14). Whenever I'm about to propose a director manual browser walkthrough with 5+ steps, OR to verify code (new feature or bug fix) that lives in a real-browser context, I MUST first run a Rule 14f forced-picker comparing: (A) Playwright automated test [recommended for repeatable regression checks], (B) Director manual walkthrough [recommended for one-time exploratory verification or first-time-ever flows involving visual judgment, copy-correctness, or cross-physical-device concerns], (C) Hybrid [Playwright for mechanical parts + Director for judgment parts], (D) escape hatch per Rule 14f. The `(recommended)` marker goes on whichever option best catches the same bug class on regression AND fits the verification's repeatability profile. Scope exceptions where manual walkthrough is the natural choice even after the trigger fires: cross-physical-device tests, Chrome extension popup flows, visual-judgment checks, and one-off post-deploy smoke checks. Full mechanical test + concrete examples in `HANDOFF_PROTOCOL.md` Rule 27; operational memory in `feedback_playwright_for_repeatable_walkthroughs.md`. (Director's standing preference, captured 2026-05-14 after asking *"why didn't we use this until now and how can I ensure we consistently use such methods moving forward?"* in the P-17 Playwright ship session.)

---

## START-OF-SESSION ROUTINE (do these before asking me to confirm task)

1. Confirm you've read this file and will follow every rule.

2. **Branch verification (NON-NEGOTIABLE — do this BEFORE the heavy doc reads in step 4 below).** Run `git branch --show-current` + `git status`. Compare the current branch against the task's required branch per the table below. If they don't match, **STOP** — surface the mismatch to the director immediately with the exact terminal commands to switch (do NOT continue doc reads on the wrong branch; the doc state on the wrong branch may be stale relative to the task, and any in-progress edits you start will land on the wrong branch). The director may have switched branches before launching you, but verify.

   | Session task type | Required branch |
   |---|---|
   | Work on W#1 (Keyword Clustering) | `main` |
   | Work on W#k for k ≥ 2 (specific named workflow) | `workflow-N-<slug>` (e.g., W#2 → `workflow-2-competition-scraping`) |
   | Cross-workflow / platform-wide infrastructure (e.g., components library; platform refactors) | `main` |

   The full canonical "How to start a session for any workflow" procedure lives in `docs/MULTI_WORKFLOW_PROTOCOL.md` §11. This branch-check rule was added 2026-05-04 after a session started on the W#2 branch when the task required `main` — the friction surfaced after several minutes of doc reads. Project memory `project_sequential_workflow_operation.md` carries the operational context: director works workflows sequentially in one Codespace; branch state persists across sessions.

3. Read `docs/HANDOFF_PROTOCOL.md` end-to-end (Rules 1–26).

4. Read the 16 Group A docs (see `docs/DOCUMENT_MANIFEST.md` for the list). The list includes `docs/MULTI_WORKFLOW_PROTOCOL.md` — read it if today's task references any workflow with N ≥ 2 OR if the "Current Active Tools" table in `ROADMAP.md` shows more than one workflow in flight.

5. Read any Group B docs relevant to today's expected work.

6. Run `git log --oneline -10` and `git status` to understand current repo state. Run `git pull --rebase origin <current-branch>` to catch anything pushed since this branch's last activity (per HANDOFF_PROTOCOL Rule 25).

7. Produce a drift check: "Here's where we are. Here's what looks off, if anything. Here's what I understand today's task to be. Ready to proceed?" If multi-workflow state is in play, note any in-flight workflows (their branches, their schema-change-in-flight flag) in the drift check.

7b. **Plain-terms session summary (NEW 2026-05-21 per HANDOFF_PROTOCOL Rule 30).** Produce a plain-language summary covering: (a) what this session will do (1-3 sentences in sentence-form English; name the polish item / task; say what the director will / won't see at session-end; say whether a deploy is planned); (b) what's still left on the total roadmap in plain terms (3-8 bullets spanning the active workflow's remaining polish items + future workflows + any infrastructure TODOs / offline director steps). Avoid acronyms unless paired with a parenthetical paraphrase. Avoid file paths / commit hashes as primary nouns. This summary is the start-of-session bookend; the end-of-session bookend lives in HANDOFF_PROTOCOL §4 Step 4b template's 3 new mandatory plain-terms sections. The director should be able to read your summary and confirm scope without parsing technical narrative. Director's verbatim 2026-05-21 directive (the source of this rule): *"From here on, for every next session, I want you to also tell me in simple terms what we will do in the session and summarize what was done in the session and what we will do in the next session. I also want you to check and tell me what work is pending according to the roadmap in simple terms."*

8. Wait for my explicit go-ahead before executing.

---

## HOW TO START A NEW CLAUDE CODE SESSION (terminal commands + paste-message)

**You have two paths. The EASY PATH is one command. The ESCAPE HATCH is the original 3-step path — always works, always documented in every end-of-session handoff.**

---

### EASY PATH (recommended — NEW 2026-05-13-c; EXTENDED 2026-05-19-g-5 with workflow-switching)

**Continue the current workflow** (whatever the prior session was working on per `docs/NEXT_SESSION.md`):

```
cd /workspaces/brand-operations-hub && ./resume
```

The `resume` script reads `docs/NEXT_SESSION.md`, switches to the branch the pointer names, pulls the latest, prints the pointer file's contents to your terminal, then launches Claude Code with the SessionStart hook auto-injecting the launch prompt as context.

**Switch to a specific workflow** (e.g., W#1 re-entry while NEXT_SESSION.md points at W#2):

```
cd /workspaces/brand-operations-hub && ./resume-workflow <N>
```

Where `<N>` is the workflow number (1 for W#1 Keyword Clustering, 2 for W#2 Competition Scraping). The `resume-workflow` script switches to the workflow's canonical branch, pulls the latest, generates a workflow-specific launch prompt (for W#1: asks you interactively what you want to do, then fills the Rule 22 graduated-tool re-entry template; for W#2: uses NEXT_SESSION.md), writes the prompt to `.claude/active-workflow-prompt.md` (single-use; the SessionStart hook reads + deletes after), then launches Claude. Future workflows (W#3-W#14) error gracefully with the canonical "first session for a never-started workflow" procedure from `MULTI_WORKFLOW_PROTOCOL.md` §11.

**If either script fails** (pointer file missing, branch checkout fails, etc.), it aborts loudly with a clear error message + the commands you'd run for the ESCAPE HATCH. The 3-step path below is always available as a known-good fallback.

---

### ESCAPE HATCH (the original 3-step path — always works)

**Three steps — Step 1 is non-negotiable for any session that's not a same-workflow continuation.**

**Step 1 — In a Codespaces terminal, switch to the right branch for the next session's task and pull the latest.**

The branch depends on what you're working on:

| Session task type | Branch | Step-1 command |
|---|---|---|
| Work on W#1 (Keyword Clustering) | `main` | `cd /workspaces/brand-operations-hub && git fetch origin && git checkout main && git pull --rebase origin main` |
| Work on W#k for k ≥ 2 (specific workflow — replace `workflow-N-<slug>` with the workflow's branch name; for W#2 it's `workflow-2-competition-scraping`) | `workflow-N-<slug>` | `cd /workspaces/brand-operations-hub && git fetch origin && git checkout workflow-N-<slug> && git pull --rebase origin workflow-N-<slug>` |
| Cross-workflow / platform-wide infrastructure (e.g., components library; platform refactors) | `main` | `cd /workspaces/brand-operations-hub && git fetch origin && git checkout main && git pull --rebase origin main` |
| FIRST session for a never-before-started workflow | (created in this step from `main`) | `cd /workspaces/brand-operations-hub && git fetch origin && git checkout main && git pull --rebase origin main && git checkout -b workflow-N-<slug>` |

**Why Step 1 matters.** You work workflows sequentially in one Codespace. The branch from your last session is still checked out when you come back. If your last session was W#2 but today's task is W#1 or platform-wide, you'd start on the W#2 branch — and Claude would have to flag the mismatch after several minutes of doc reads. Step 1 prevents that. Captured 2026-05-04 in project memory `project_sequential_workflow_operation.md`.

The full canonical "How to start a session for any workflow" lives in `docs/MULTI_WORKFLOW_PROTOCOL.md` §11.

**Step 2 — Launch Claude Code:**

```
claude
```

This starts an interactive Claude Code session in whatever folder you're in (the `cd` in Step 1 made sure that's the repo root).

**Step 3 — As your very first message inside Claude Code, paste a launch prompt naming the specific task:**

For most sessions, use the templates in `docs/MULTI_WORKFLOW_PROTOCOL.md` Appendix A (W#2), Appendix B (W#1), or §11.1 (cross-workflow / platform-wide). For a generic launch prompt:

```
Read docs/CLAUDE_CODE_STARTER.md and follow every rule in it. Today's task:
[short description of what you want to do]. Start by running the mandatory
start-of-session sequence.
```

That's it. The starter file handles everything else. Claude Code will read the starter prompt, run the branch verification (Step 2 of the start-of-session routine above), read the handoff protocol, read the Group A docs, check git state, produce a drift-check, and wait for your go-ahead before doing any work.

---

END OF DOCUMENT
