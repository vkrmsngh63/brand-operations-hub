import AdminNotes from "@/components/AdminNotes";

export default function PLOSNotesPage() {
  return (
    <AdminNotes
      system="plos"
      systemLabel="Product Launch Operating System"
      systemIcon="🚀"
      backRoute="/plos"
    />
  );
}
